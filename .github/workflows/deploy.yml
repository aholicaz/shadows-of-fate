# ★ สร้างเกมเวอร์ชันเว็บอัตโนมัติ แล้วเอาขึ้น GitHub Pages ★
#
# ทำงานทุกครั้งที่ push ขึ้น branch main (หรือกดรันเองจากแท็บ Actions)
# ต้องตั้งค่าใน repo ก่อน 1 ครั้ง: Settings -> Pages -> Source = "GitHub Actions"
#
# ★★ รอบ 91 — เพิ่มขั้นตอน "ปรับภาพให้เหมาะกับเว็บ" ก่อน export ★★
#
# ทำไมต้องมี: ไฟล์ในโปรเจกต์ตั้งค่าไว้สำหรับ **เวอร์ชันคอม/Steam** (compress/mode=2 BC7)
# ซึ่งเป็นข้อมูลดิบของการ์ดจอ — ไฟล์เกมจะพองเป็นเกือบ 900 MB และเว็บโหลดไม่ไหว
# ขั้นตอนด้านล่างจึงแปลงเป็น **โหมด 1 Lossy** เฉพาะตอน build เว็บ
# (แก้เฉพาะสำเนาที่ runner เช็คเอาต์มา — ไฟล์ใน repo ไม่ถูกแตะ เวอร์ชันคอมยังได้ภาพเต็ม)
#
# Historical build sizes predate the current assets and are not a current budget.
# Keep image dimensions intact; tune import compression through build_web.py.

name: build-web

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GODOT_VERSION: "4.7"
    steps:
      - uses: actions/checkout@v4

      - name: ติดตั้งเครื่องมือ Python ที่สคริปต์ปรับภาพต้องใช้
        run: pip install --quiet pillow numpy

      # ---------------------------------------------------------------
      # ★ ปรับภาพให้เหมาะกับเว็บ (ทำก่อน import เสมอ) ★
      # ---------------------------------------------------------------
      - name: ตรวจไฟล์ภาพที่ถูกอ้างถึงแต่หายไป
        # ถ้าเปลี่ยนชื่อ/ย้ายไฟล์ภาพแล้วลืมแก้ที่อ้างถึง ในโปรแกรม Godot จะแค่เตือน
        # แต่ตอน export คือช่องว่างจริง (เคยทำให้ดาบหายจากมือ + ท่าตายของมอนไม่มีภาพ)
        run: python3 check_missing_assets.py --fix-obvious

      - name: ตั้งค่าภาพเป็นโหมดเว็บ (Lossy) แทนโหมดคอม (BC7)
        run: python3 build_web.py web

      # Preserve source dimensions and frame registration on desktop and mobile.
      # Blanket 42% monster resizing is disabled; use import compression only.

      - name: ติดตั้ง Godot + export templates
        run: |
          set -e
          BASE="https://github.com/godotengine/godot-builds/releases/download/${GODOT_VERSION}-stable"
          curl -fsSL -o godot.zip "$BASE/Godot_v${GODOT_VERSION}-stable_linux.x86_64.zip"
          curl -fsSL -o templates.tpz "$BASE/Godot_v${GODOT_VERSION}-stable_export_templates.tpz"
          unzip -q godot.zip
          sudo mv "Godot_v${GODOT_VERSION}-stable_linux.x86_64" /usr/local/bin/godot
          sudo chmod +x /usr/local/bin/godot
          unzip -q templates.tpz
          mkdir -p "$HOME/.local/share/godot/export_templates/${GODOT_VERSION}.stable"
          mv templates/* "$HOME/.local/share/godot/export_templates/${GODOT_VERSION}.stable/"
          godot --version

      - name: นำเข้าไฟล์ (import)
        # รอบแรกมักมี error เรื่องลำดับการโหลด class_name — รัน 2 รอบถึงจะครบ
        run: |
          godot --headless --import || true
          godot --headless --import

      - name: สร้างเวอร์ชันเว็บ
        run: |
          mkdir -p build/web
          godot --headless --export-release "Web" build/web/index.html || {
            echo "::group::export ล้มเหลว — ลองใหม่แบบโชว์ทุกข้อความ"
            godot --headless --verbose --export-release "Web" build/web/index.html
            echo "::endgroup::"
          }
          ls -lh build/web
          test -f build/web/index.html

      - name: เช็คขนาดไฟล์เกม
        # GitHub Pages มีเพดานเว็บรวม 1 GB — ถ้าเกินจะรู้ตรงนี้เลย ไม่ต้องไปงงตอนเล่นไม่ได้
        run: |
          PCK=$(stat -c%s build/web/index.pck)
          echo "index.pck = $((PCK/1024/1024)) MB"
          TOTAL=$(du -sm build/web | cut -f1)
          echo "เว็บทั้งหมด = ${TOTAL} MB (เพดาน GitHub Pages = 1024 MB)"
          if [ "$TOTAL" -gt 1000 ]; then
            echo "::error::เว็บใหญ่เกินเพดาน GitHub Pages — ต้องลดคุณภาพภาพลงอีก"
            exit 1
          fi

      - name: กัน GitHub Pages แปลงไฟล์เอง
        run: touch build/web/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
