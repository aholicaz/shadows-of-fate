# War Wraith — ชุด Spriteflow ปัจจุบัน

เปลี่ยนภาพเก่าทั้งหมดเป็นชุดจาก Spriteflow วันที่ 12 กันยายน 2026 แล้ว ใช้ PNG ต้นฉบับ 66 เฟรม: Idle 8, Walk/Run 16, Attack 18, Hit 6, Die 18; Skill ใช้ Attack เป็นภาพทดแทน

ภาพที่เกมใช้: `Sprites/monsters/spriteflow/war_wraith/` และ `data/sprites/monsters/war_wraith_frames.tres`

เปิด `war_wraith_review.tscn` แล้วกด F6 ดูชุดใหม่ทั้งซ้ายและขวา

รายละเอียดการนำเข้า จังหวะโจมตี การทดสอบ และข้อจำกัดต้นฉบับอยู่ใน [spriteflow_import.md](spriteflow_import.md)

เอกสารและภาพชุดเดิมเก็บใน `output/spriteflow/before/` และ `output/spriteflow/retired/` ซึ่งไม่ถูกนำเข้า Godot
