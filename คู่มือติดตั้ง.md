# Ragnarok Battle Offline — คู่มือระบบเกม

ระบบทั้งหมดถูกติดตั้งและ **เชื่อมเข้ากับฉาก/สไปรท์ที่คุณทำไว้แล้ว**

> ผ่านการทดสอบรันจริงบน Godot headless แล้ว 27 เคส — ไม่มี error

---

## ⚠ อ่านก่อนเป็นอันดับแรก

**ปิด Godot แล้วเปิดใหม่** ก่อนกดเล่น

เพราะ Godot อ่าน `project.godot` ตอนเปิดโปรแกรมครั้งเดียว ตัว Autoload ทั้ง 6 ตัวที่เพิ่งเพิ่มเข้าไปจะยังไม่ทำงานถ้าไม่รีสตาร์ท
และ **ห้ามกด Ctrl+S ทับฉาก `world_node_2d` ก่อนปิด** เพราะจะเขียนทับของเดิมที่ผมแก้ไว้

---

## 1. เล่นเลย

เปิด Godot ใหม่ → กด **F5** → เริ่มที่ฉากเดิมของคุณ (พื้นหลังป่า + พรอนเทรา)

| ปุ่ม | ทำอะไร |
|---|---|
| ← → | เดิน |
| Space | กระโดด |
| **J หรือ X** | โจมตี (J เหมือนเดิมที่คุณตั้งไว้) |
| 1 2 3 4 | ใช้สกิลที่ตั้งไว้ |
| Z | เก็บไอเทม |
| Q | กินยาแดงด่วน |
| **F** | คุย NPC / เข้าประตู |
| **C** | หน้าต่างสเตตัส (อัพ STR/AGI/VIT/INT/DEX/LUK) |
| **I** | กระเป๋า |
| **E** | สวมใส่อุปกรณ์ |
| **K** | สกิล (เรียน + ตั้งปุ่มลัด) |
| **V** | อัลบั้มการ์ดมอนสเตอร์ |
| Esc | ปิดหน้าต่างทั้งหมด |
| F5 / F9 | เซฟ / โหลด |

### ลองตามนี้
1. ตีโพริง 4 ตัวที่เกิดกลางแมพ → ได้ EXP, ซีนี, ไอเทมดรอป (เดินทับเก็บอัตโนมัติ)
2. กด **C** → อัพ STR ดู ATK เพิ่มทันที (มี 20 แต้มให้ตั้งแต่เริ่ม)
3. กด **K** → เรียน Bash → กดเลข 1 ตั้งเป็นปุ่มลัด → กลับไปกด 1 ใส่โพริง
4. เดินไปขวาสุดของแมพ → กดผ่านประตู **"→ เมือง"** → เจอพ่อค้า/ช่างตีเหล็ก/หมอ/จุดเซฟ
5. คุยช่างตีเหล็ก (F) → ตีบวกดาบมือใหม่

---

## 2. ผมทำอะไรกับฉากของคุณบ้าง

`Sprites/world_node_2d.tscn` ยังเป็นฉากเดิม แต่เปลี่ยนให้เป็น "แมพ" ของระบบใหม่

| ของเดิม | ตอนนี้ |
|---|---|
| TileMap + พื้นหลัง + StaticBody2D | **เก็บไว้เหมือนเดิมทุกอย่าง** |
| Node Player ที่ฝังในฉาก | ถอดออก → ระบบเกิดผู้เล่นให้เอง ใช้ `scenes/player/player.tscn` |
| Poring ที่วางไว้ 1 ตัว (ติดกลุ่ม player ผิด) | ถอดออก → ใช้ `Spawners/PoringSpawner` เกิด 4 ตัวและเกิดใหม่เอง |
| — | เพิ่ม `SpawnPoints` (จุดเกิดผู้เล่น) และ `Portals/ToTown` |
| สคริปต์ `world_node_2d.gd` | เปลี่ยนเป็น `scripts/world/map_base.gd` (กล้องตาม + จำกัดขอบแมพ) |

**สไปรท์ของคุณถูกดึงออกมาเป็นไฟล์ใช้ซ้ำได้:**
- `data/sprites/player_frames.tres` — Idle (s1-s6), Run (r1-r6), Attack (f1-f6)
- `data/sprites/poring_frames.tres` — Idle, Attack, Jump

ไฟล์เดิมที่ไม่ได้ใช้แล้วแต่ยังเก็บไว้: `Sprites/world_node_2d.gd`, `Sprites/player.gd`, `poring.gd`, `attack.gd`, `Sprites/poring.tscn`, `Sprites/attack.tscn` (ลบทิ้งได้ถ้าไม่ใช้)

> ถ้าอนิเมชันไหนยังไม่มี (เช่น Death, Hit ของผู้เล่น) ระบบจะใช้ตัวสำรองแทนอัตโนมัติ ไม่ค้างไม่หาย
> ทำเพิ่มเมื่อไหร่ก็แค่เพิ่มอนิเมชันชื่อ `Hit` / `Death` / `Jump` ลงใน `data/sprites/player_frames.tres` แล้วมันจะใช้เองทันที

---

## 3. แมพที่มีตอนนี้

| id | ไฟล์ | สภาพ |
|---|---|---|
| `prontera_field` | **`Sprites/world_node_2d.tscn` (ฉากของคุณ)** | มีพื้นหลังสวยแล้ว มีโพริง 4 ตัว + ประตูไปเมือง |
| `prontera_town` | `scenes/maps/prontera_town.tscn` | พื้นเป็นสี่เหลี่ยมสีเปล่า ๆ มี NPC ครบ 5 ตัว รอคุณเอาภาพเมืองมาทับ |
| `dark_forest` | `scenes/maps/dark_forest.tscn` | พื้นเปล่า มีมอน 7 ชนิดรวมบอสบาฟโฟเมท ยังไม่มีประตูเชื่อม |

ทะเบียนแมพอยู่ที่ `scripts/core/game.gd` (const `MAPS`) — เพิ่มแมพใหม่ = เพิ่ม 1 บรรทัด
`scenes/maps/prontera_field.tscn` เป็นแมพเปล่าที่ผมทำไว้เป็นตัวอย่าง ตอนนี้ไม่ได้ใช้แล้ว เก็บไว้ดูโครงสร้างได้

---

## 3.5 ★ วิธีทำให้แมพกว้างขึ้น

ตอนนี้แมพของคุณเปิด **Auto Fit Bounds** ไว้แล้ว — ระบบจะวัดขนาดแมพจากกระเบื้องที่วาด
กับกล่องชนของพื้นให้เอง **ไม่ต้องมานั่งแก้ Map Bounds ทุกครั้ง**

> ขนาดที่วัดได้ตอนนี้: กว้าง **1664** สูง **988** (x จาก -360 ถึง 1304)
> พื้นที่เดินได้จริงแค่ x -153 ถึง 1133 เพราะกล่องชนของพื้นสั้นกว่าภาพพื้นหลัง

### ทำให้กว้างขึ้นแค่ 2 ขั้น

**ขั้นที่ 1 — ยืดพื้นให้ยาวขึ้น**
เปิด `Sprites/world_node_2d.tscn` → เลือก `TileMap/StaticBody2D/CollisionShape2D`
ใน Inspector: `Shape > Size` ตอนนี้เป็น `(1286, 30)`
- อยากได้ยาว 2 เท่า → เปลี่ยนเป็น `(2572, 30)`
- แล้วเลื่อน `Position.x` ไปครึ่งหนึ่งของส่วนที่เพิ่ม (จาก `490` เป็น `490 + 643 = 1133`)

หรือถ้าอยากทำเป็นหลายแท่น: คลิกขวา `TileMap` → Add Child Node → `StaticBody2D`
→ ใส่ `CollisionShape2D` + `RectangleShape2D` ข้างใน แล้ววางตรงไหนก็ได้
ตั้ง `Collision Layer = 1`, `Collision Mask = 0`

**ขั้นที่ 2 — วาดพื้นหลังต่อ**
เลือก `TileMap` → เปิดแท็บ **TileMap** ด้านล่างจอ → เลือกกระเบื้องแล้ววาดต่อไปทางขวา
(ภาพพื้นหลังของคุณถูกตัดเป็นกระเบื้อง 16x16 อยู่แล้ว วาดซ้ำเป็นฉากต่อเนื่องได้เลย)

จบ — กด F5 กล้องจะตามไปได้ไกลขึ้นเองทันที ไม่ต้องแก้โค้ดหรือ Map Bounds

### ถ้าอยากกำหนดขอบเองแทน
ที่ node `WorldNode2D` ปิด `Auto Fit Bounds` แล้วใส่ `Map Bounds` เอง เช่น
`Rect2(-360, -324, 4000, 988)` = แมพกว้าง 4000 พิกเซล

### เอามอนไปเกิดในพื้นที่ใหม่
คลิกขวา `Spawners` → Add Child Node → `Node2D` → ลากสคริปต์ `scripts/world/monster_spawner.gd` ใส่
→ วางตำแหน่ง → ใส่ `Monster Types` + `Monster Scene` (`scenes/monsters/monster.tscn`)
→ ตั้ง `Max Alive` กับ `Spawn Width`

---

## 4. โครงสร้างโปรเจกต์

```
data/                  ← ★ ปรับค่าเกมทั้งหมดที่นี่ แก้ใน Inspector ได้เลย
  monsters/            มอนสเตอร์ 11 ตัว (poring ผูกสไปรท์ของคุณแล้ว)
  items/               ไอเทม 33 ชิ้น
  skills/              สกิล 7 ตัว
  jobs/                อาชีพ 2 อาชีพ
  sprites/             SpriteFrames ที่ดึงจากฉากของคุณ
scripts/
  resources/  แม่แบบข้อมูล   core/  หัวใจเกม
  entities/   ผู้เล่น+มอน     ui/    หน้าต่างทุกบาน (สร้างด้วยโค้ด)
  world/      แมพ/spawner/ประตู/NPC
scenes/
  player/player.tscn      ← ใช้สไปรท์ของคุณแล้ว
  monsters/monster.tscn   ← มอนทุกตัวใช้ Scene นี้ตัวเดียว
  items/ npc/ world/ maps/
```

Autoload 6 ตัว: `Events` → `GameData` → `Game` → `PlayerState` → `SaveManager` → `UI`

---

## 5. ★ ปรับค่าพลังมอนสเตอร์

เปิด `.tres` ใน `data/monsters/` แก้ใน Inspector ได้เลย ไม่ต้องแตะโค้ด

| กลุ่ม | ปรับอะไรได้ |
|---|---|
| Visual | SpriteFrames, ขนาด, offset, กล่องชน, ตำแหน่งหลอดเลือด |
| Core Stats | เลเวล, HP, ATK ต่ำ-สูง, DEF, MDEF, HIT, FLEE, CRIT |
| Type | ธาตุ, เผ่าพันธุ์, ขนาด (มีผลกับตารางธาตุ 10x10) |
| Movement & AI | ความเร็ว, กระโดดไหม, ระยะเห็นผู้เล่น, ระยะโจมตี, ระยะห่างจุดเกิดสูงสุด |
| **การเดินเล่น** | `wander_speed` เดินเร็วแค่ไหน · `wander_range` เดินห่างบ้านได้กี่พิกเซล · `hop_while_wandering` เด้งตอนเดินไหม · `jump_interval` เว้นกี่วินาทีถึงเด้งอีกที |
| Attack Timing | จังหวะดาเมจออก, ความยาวท่า, คูลดาวน์, แรงกระเด็น |
| Reward | EXP, ซีนี, ตารางดรอป |
| Respawn | เกิดใหม่กี่วินาที |

**HIT / FLEE เป็น "โบนัส"** ระบบบวก `100 + เลเวล` ให้เอง เพื่อเทียบกับผู้เล่น (`100 + Lv + Dex/Agi`)
เกณฑ์: `hit ≈ เลเวล × 1.5`, `flee ≈ เลเวล × 1.2`

### เพิ่มมอนใหม่ (ไม่ต้องเขียนโค้ด)
1. คลิกขวาใน `data/monsters/` → New Resource → **MonsterData**
2. ตั้ง `id` (ห้ามซ้ำ), ชื่อ, SpriteFrames, ค่าพลัง, ดรอป
3. เปิดแมพ → เลือก node ใน `Spawners` → ลากไฟล์ใหม่ใส่ช่อง **Monster Types**

### ตั้งค่าดรอป
ช่อง **Drops** กด `+` → New **DropEntry** → `item_id`, `chance` (%), `min/max_count`, `min/max_refine`

---

## 5.5 พฤติกรรมมอนสเตอร์

- **เดินเล่นอิสระ**: มอนจะสุ่มเดินซ้าย-ขวา-ยืนพัก รอบ ๆ จุดเกิดของตัวเอง
  ระยะเดินกำหนดที่ `wander_range` ของมอนแต่ละตัว
- **ไม่เดินตกเหว**: ระบบยิงเรย์เช็คพื้นข้างหน้า ถ้าเป็นขอบจะกลับตัวเอง
- **เด้งตามจังหวะ**: โพริงจะเด้งเป็นจังหวะ ไม่เด้งรัวทุกเฟรม (คุมด้วย `jump_interval`)
- **มอนใจดี (PASSIVE)** จะไม่ไล่ผู้เล่นจนกว่าจะถูกตี พอถูกตีจะจำไว้ 8 วินาที
  แล้วค่อยกลับไปเดินเล่นตามปกติ
- **มอนดุ (AGGRESSIVE)** เห็นในระยะ `detect_range` แล้วไล่ทันที
- ทั้งหมดจะไม่ไล่เลย `leash_range` จากจุดเกิด แล้วจะเดินกลับบ้านเอง

### ป็อปอัพยืนยันเข้าประตู
เดินชนประตูแล้วจะมีกล่องถามก่อน (เกมหยุดรอ) — กด **Enter/F = ไปเลย**, **Esc = ยังก่อน**
กดยกเลิกแล้วยืนตรงประตูกด **F** ถามใหม่ได้ หรือเดินออกแล้วเดินเข้าใหม่

ปิดป็อปอัพได้ที่ node `Portal` → ติ๊ก `Require Confirm` ออก
(แล้วเลือกต่อว่า `Auto Enter` เปิด = ชนแล้วไปเลย / ปิด = ต้องกด F)

---

## 6. ระบบสเตตัส (สูตร RO)

| สเตตัส | ให้อะไร |
|---|---|
| **STR** | ATK (โตแบบ `STR + (STR/10)²` ทุก 10 แต้มคุ้มขึ้น) |
| **AGI** | FLEE + ความเร็วโจมตี (ASPD) |
| **VIT** | MaxHP + DEF + ฟื้นเลือด |
| **INT** | MaxSP + MATK + ฟื้น SP + พลังสกิลฟื้นเลือด |
| **DEX** | HIT + ATK เล็กน้อย + ASPD เล็กน้อย |
| **LUK** | CRIT + ATK เล็กน้อย |

- ค่าอัพ = `⌊(ค่าปัจจุบัน−1)/10⌋ + 2`
- เลเวลขึ้น 1 ครั้ง ได้ `3 + ⌊Lv/5⌋` Stat Point + 1 Skill Point
- ตัวคูณของอาชีพแก้ที่ `data/jobs/swordsman.tres`

---

## 7. สกิล / ไอเทม / ตีบวก

**สกิล 7 ตัว**: Sword Mastery, HP Recovery, Bash, Magnum Break, Endure, Battle Cry, First Aid
มีเงื่อนไขสกิลบังคับก่อน (Magnum Break ต้องมี Bash Lv5 + ตัวละคร Lv10)

**กระเป๋า 40 ช่อง** / **สวมใส่ 8 ช่อง** (อาวุธ, มือรอง, หัว, เกราะ, ผ้าคลุม, รองเท้า, เครื่องประดับ×2)

**ตีบวก +0→+10**: อัตราสำเร็จ 100/100/100/90/80/65/50/35/25/15 %
ใช้ฟราคอน (+0→+4) และเอ็มเวเรทาร์คอน (+5 ขึ้นไป) — ตั้งแต่ +7 พลาดแล้วลด 1 ขั้น
ปรับความยากที่ `const` หัวไฟล์ `scripts/core/refine_system.gd`

---

## 7.5 ★ ระบบการ์ดมอนสเตอร์

มอนสเตอร์ **ทุกตัว** มีการ์ดของตัวเอง ดรอปที่ **5%** (ทดสอบสุ่ม 4000 ครั้งได้ 5.05% ตรงตามที่ตั้ง)

### กด V เปิดอัลบั้มการ์ด
- โชว์การ์ดทุกใบในเกม ใบที่ยังไม่ได้จะเป็น **เงาดำ + ชื่อ ???**
- ใบที่เก็บได้แล้วจะโชว์ **รูปมอนสเตอร์ตัวนั้นในกรอบการ์ด** แบบ Ragnarok
  พร้อมชื่อ คุณสมบัติ ช่องที่ใส่ได้ และระดับความหายาก
- นับความคืบหน้าให้ด้วย เช่น "เก็บได้ 3 / 11 ใบ"

### ใส่การ์ดลงอุปกรณ์
1. กด **V** → เลือกการ์ดที่มี
2. ใต้ภาพการ์ดจะมีปุ่ม **"ใส่ลงใน:"** พร้อมรายชื่ออุปกรณ์ที่ใส่ได้ → กดเลือก
3. ค่าพลังเพิ่มทันที (ดูได้ที่หน้าสเตตัส C)

ถอดออกที่แท็บ **จัดการการ์ด** → กด **ถอด** (การ์ดกลับเข้ากระเป๋า)
> อยากให้ถอดแล้วการ์ดหายแบบ RO ต้นฉบับ: แก้ `unsocket_card(..., return_to_inventory)` เป็น `false`
> ใน `scripts/core/player_state.gd`

### ช่องใส่การ์ดของอุปกรณ์
กำหนดที่ `data/items/<ชื่อ>.tres` → ช่อง **Card Slots** (0-4)

| อุปกรณ์ | ช่อง | อุปกรณ์ | ช่อง |
|---|---|---|---|
| ดาบมือใหม่ | 1 | เสื้อผ้าฝ้าย / ชุดนักผจญภัย | 1 |
| ฟัลชิออน / เบลด / เคลย์มอร์ | 2 | เกราะเพลท | 2 |
| ดาบเพลิง | 3 | การ์ด(โล่) / บัคเลอร์ | 1 |
| หมวก / ผ้าคลุม / รองเท้า / บูท | 1 | แหวน / คลิป / ถุงมือ | 1 |

### การ์ดทั้ง 11 ใบที่ใส่ไว้ให้ (แก้ได้ที่ `data/cards/`)

| การ์ด | ใส่ใน | คุณสมบัติ | ระดับ |
|---|---|---|---|
| โพริง | เครื่องประดับ | LUK +2, FLEE +5 | ธรรมดา |
| แฟเบร | ชุดเกราะ | VIT +1, MaxHP +100 | ธรรมดา |
| ลูนาติก | เครื่องประดับ | LUK +1, CRIT +3, FLEE +2 | ธรรมดา |
| ดรอปส์ | อาวุธ | DEX +1, HIT +3 | ไม่ธรรมดา |
| ชอนชอน | โล่ | AGI +1, DEF +1 | ไม่ธรรมดา |
| ฮอร์เน็ต | อาวุธ | STR +1, ATK +3 | ไม่ธรรมดา |
| หมาป่า | อาวุธ | ATK +15, HIT +5 | หายาก |
| มูนัค | ผ้าคลุม | MDEF +3, MaxHP +150 | หายาก |
| ออร์คนักรบ | อาวุธ | CRIT +5, **ATK +5%** | หายากมาก |
| บาฟโฟเมทจูเนียร์ | เครื่องประดับ | AGI +3, CRIT +7 | หายากมาก |
| บาฟโฟเมท | อาวุธ | STR +2, **ATK +10%, MaxHP +5%** | ตำนาน |

### เพิ่ม/แก้การ์ด
เปิดไฟล์ใน `data/cards/` แล้วแก้ใน Inspector ได้เลย:
- **Monster Id** — มอนเจ้าของการ์ด (ใช้ดึงรูปมาโชว์)
- **Fits Slot** — ใส่ในอุปกรณ์ช่องไหน
- **Illustration** — ★ ใส่ภาพการ์ดที่คุณวาดเองตรงนี้ ★
  (ถ้าเว้นว่าง ระบบจะใช้เฟรมแรกของอนิเมชัน Idle ของมอนตัวนั้นมาใส่ในกรอบให้เอง)
- **Rarity** 1-5 — เปลี่ยนสีกรอบการ์ด
- ค่าโบนัสตัวเลขตรง ๆ ใช้ช่องเดียวกับไอเทม (ATK, DEF, Bonus Str, Max Hp ...)
- **Percent Effects** — โบนัสแบบ % เช่น `{"atk_percent": 5.0}`
  รองรับ: `atk_percent, matk_percent, def_percent, max_hp_percent, max_sp_percent,`
  `aspd_percent, move_speed_percent, crit_damage_percent`
- **Effect Text** — เขียนคำอธิบายเอง (เว้นว่าง = ระบบสร้างให้อัตโนมัติจากค่าโบนัส)

**การ์ดมอนใหม่**: คลิกขวาใน `data/cards/` → New Resource → **CardData**
ตั้ง `id` เป็น `card_<ชื่อมอน>` แล้วเพิ่ม DropEntry 5% ในไฟล์มอนตัวนั้น

**เปลี่ยนอัตราดรอปทั้งหมด**: แก้ `chance` ของ DropEntry ที่ชี้ไปที่ `card_*` ในไฟล์มอนแต่ละตัว

---

## 7.6 ★ ตัวละครเปล่า + อาวุธโผล่ที่มือ (Paper Doll แบบ RO)

RO ทำแบบนี้: ตัวละครไม่ได้เป็นภาพเดียว แต่เป็น **หลายเลเยอร์ซ้อนกัน**
(ตัวเปล่า → เกราะ → โล่ → อาวุธ → หมวก) ทุกเลเยอร์เล่น **เฟรมเดียวกัน พร้อมกัน**
ระบบนี้ติดตั้งให้แล้วที่ `scripts/entities/character_visual.gd`
และเพิ่ม node `EquipVisual` ไว้ใน `scenes/player/player.tscn` เรียบร้อย

### ขั้นตอนทำ

**ขั้นที่ 1 — วาดตัวเปล่า**
วาดตัวละครแบบไม่มีอาวุธ/เกราะ ให้ครบทุกท่า (Idle, Run, Attack, ...)
แล้วเอาไปแทนที่ `data/sprites/player_frames.tres`
> ตอนนี้ `player_frames.tres` ยังเป็นตัวที่ **ถือดาบอยู่ในภาพ** (f1-f6)
> ต้องวาดใหม่แบบมือเปล่า ไม่งั้นจะเห็นดาบ 2 เล่ม

**ขั้นที่ 2 — วาดอาวุธเป็นเลเยอร์**
กฎเดียวที่ต้องจำ: **วาดบนผืนผ้าใบขนาดเท่ากันกับตัวเปล่า และเฟรมต้องตรงกัน**

| ตัวเปล่า | อาวุธ |
|---|---|
| Idle 6 เฟรม | Idle 6 เฟรม |
| Run 6 เฟรม | Run 6 เฟรม |
| Attack 6 เฟรม | Attack 6 เฟรม |

วิธีง่ายสุด: เปิดไฟล์ตัวเปล่าในโปรแกรมวาด → สร้างเลเยอร์ใหม่ทับ → วาดดาบให้อยู่ในมือของแต่ละเฟรม
→ ซ่อนเลเยอร์ตัวเปล่า → export เฉพาะเลเยอร์ดาบ (พื้นหลังโปร่ง ขนาดผ้าใบเท่าเดิม)
ได้ครบ 18 ภาพเหมือนกัน แค่มีแต่ดาบ

**ขั้นที่ 3 — ผูกเข้ากับไอเทม**
1. สร้าง `SpriteFrames` ใหม่จากภาพดาบ ตั้งชื่ออนิเมชันให้ตรง (Idle / Run / Attack)
   เซฟเป็น `data/sprites/sword_novice_frames.tres`
2. เปิด `data/items/novice_sword.tres` → กลุ่ม **ภาพตอนสวมใส่**
   → ลาก SpriteFrames ใส่ช่อง **Equip Sprite Frames**

จบ — ใส่ดาบเมื่อไหร่ ดาบโผล่ที่มือทันที ถอดออกก็หายไป ตอนโจมตีดาบจะฟันตามทุกเฟรม
เพราะ **ไม่ต้องตั้ง offset อะไรเลย** ถ้าผ้าใบขนาดเท่ากัน

### 📦 ไฟล์ตัวอย่างที่แถมมาให้ (ดูของจริงได้เลย)

```
Sprites/example/body/    ตัวเปล่า 18 ภาพ  (Idle/Run/Attack อย่างละ 6)
Sprites/example/sword/   ดาบ 18 ภาพ      (เฟรมตรงกันเป๊ะ)
data/sprites/example_body_frames.tres    SpriteFrames พร้อมใช้
data/sprites/example_sword_frames.tres
scenes/example/paperdoll_demo.tscn       ฉากสาธิต
ตัวอย่างสไปรท์.png                        ภาพรวมทั้ง 3 แถว
```

**ดูของจริง**: เปิด `scenes/example/paperdoll_demo.tscn` แล้วกด **F6**
จะเห็น 3 ตัวเทียบกัน — ตัวเปล่า / เลเยอร์ดาบ / ซ้อนกัน สลับท่าให้ดูอัตโนมัติ

**เอาไปลองในเกมจริง** (2 ช่อง):
1. `scenes/player/player.tscn` → `AnimatedSprite2D` → Sprite Frames = `example_body_frames.tres`
2. `data/items/novice_sword.tres` → Equip Sprite Frames = `example_sword_frames.tres`

กด F5 — เดิน วิ่ง ฟัน จะเห็นดาบอยู่ในมือตลอด ถอดดาบออกดาบก็หายไป
(ภาพตัวอย่างทั้งหมดสร้างด้วยโค้ด ใช้เป็นตัวเทียบระยะ/สัดส่วนได้ แล้วค่อยวาดของจริงทับ)

> สังเกตว่าไฟล์ดาบ **ไม่มีตัวละครอยู่ในภาพเลย** มีแต่ดาบลอย ๆ อยู่ตำแหน่งที่มือจะไปอยู่
> นั่นคือหัวใจของวิธีนี้

### ⚠ ข้อควรระวังจากสไปรท์ชุดปัจจุบันของคุณ
ตอนนี้ Idle/Run เป็น **50x90** แต่ Attack เป็น **80x90** — คนละขนาด
ระบบยังทำงานได้ แต่ต้องจำกฎนี้: **ภาพอาวุธเฟรมที่ N ต้องขนาดเท่ากับภาพตัวเฟรมที่ N**
(เพราะ Godot วางภาพโดยอิงจุดกึ่งกลางของแต่ละเฟรม)
ทางที่ปลอดภัยกว่าคือ **ทำทุกเฟรมให้ขนาดเดียวกันทั้งหมด** เหมือนไฟล์ตัวอย่าง

### ถ้ายังไม่อยากวาดครบทุกเฟรม (แบบ B)
ใส่ภาพดาบใบเดียวในช่อง **Equip Texture** แทน แล้วปรับ **Equip Offset** ให้ไปอยู่ตรงมือ
ดาบจะติดตัวไปด้วยแต่ไม่ขยับตามท่า — ใช้ดูคร่าว ๆ ก่อนได้ พอวาดครบแล้วค่อยย้ายไปช่อง A

### ช่องตั้งค่าใน ItemData (กลุ่ม "ภาพตอนสวมใส่")

| ช่อง | ทำอะไร |
|---|---|
| **Equip Sprite Frames** | ★ ภาพเคลื่อนไหวครบทุกท่า (แบบ A) |
| **Equip Texture** | ภาพนิ่งใบเดียว (แบบ B) |
| **Equip Offset** | เยื้องตำแหน่ง — ถ้าผ้าใบเท่าตัวเปล่า ปล่อย (0,0) |
| **Equip Z Index** | ลำดับวาด: บวก = อยู่หน้าตัว, ลบ = อยู่หลังตัว |
| **Use Flipped Z / Equip Z Index Flipped** | ให้ดาบอยู่หลังตัวตอนหันอีกด้าน (เหมือน RO) |

### ลำดับเลเยอร์ที่ตั้งไว้
ผ้าคลุม (หลังสุด) → รองเท้า → ชุดเกราะ → โล่ → อาวุธ → หมวก
แก้ได้ที่ `LAYER_ORDER` ใน `character_visual.gd` หรือตั้ง Equip Z Index รายชิ้น

### เกร็ด
- อุปกรณ์ที่มีแค่บางท่า (เช่นมีแต่ Attack) ระบบจะซ่อนเลเยอร์ตอนท่าอื่นให้เอง ไม่ค้าง
- เอาไปใช้กับ NPC/มอนได้: เพิ่ม node `CharacterVisual` แล้วปิด `Use Player Equipment`
  จากนั้นเรียก `set_layer(slot, item_data)` เอง
- เครื่องประดับไม่มีเลเยอร์ภาพ (ตามแบบ RO)

---

## 7.7 ★ ปรับขนาดตัวละคร

### แก้ที่ช่องเดียว
`scenes/player/player.tscn` → เลือก node **Player** → กลุ่ม **ขนาดตัวละคร** → **Auto Fit Height**

ค่านี้คือ "อยากให้ตัวละครสูงกี่พิกเซลบนจอ" ตอนนี้ตั้งไว้ **240**
- ใหญ่ไป → ลดเป็น 190-210
- เล็กไป → เพิ่มเป็น 280-320

**กล่องชนปรับตามให้อัตโนมัติแล้ว** (ช่อง Auto Fit Collision) แก้ตัวเลขเดียวจบ
อยากให้ตัวอ้วน/ผอมกว่านี้ ปรับ **Collision Width Ratio** (0.17 = ผอม, 0.25 = อ้วน)
- เทียบคร่าว ๆ: โพริงสูงประมาณ 60-70px ตัวละครควรสูงราว 2 เท่า

> ถ้าตั้ง **0** = ปิดระบบนี้ กลับไปใช้ช่อง **Scale** ของ `AnimatedSprite2D` เองเหมือนเดิม

### ทำไมต้องมีระบบนี้
ไฟล์ภาพของคุณตอนนี้ **ขนาดไม่เท่ากัน**:

| ท่า | ไฟล์ | ขนาดผ้าใบ |
|---|---|---|
| Idle | z1-z6 | **300 x 300** |
| Run | r1-r6 | 50 x 90 |
| Attack | f1-f6 | 80 x 90 |

ถ้าใช้ Scale ตัวเดียว (เดิม 1.92) จะได้ Idle สูง 576px แต่ Run สูงแค่ 173px
→ ตัวละครจะ **โตขึ้น 3 เท่าตอนยืน แล้วหดตอนวิ่ง**

ระบบใหม่จะวัด "ความสูงจริงของตัวละคร" ในแต่ละท่า (ไม่นับพื้นที่โปร่งใส)
แล้วคำนวณ Scale ให้เอง + จัดให้ **เท้าแตะพื้นระดับเดียวกัน** และ **ตัวอยู่กึ่งกลาง** ทุกท่า

ผลทดสอบจริงกับขนาดที่คุณใช้:

| ท่า | ผ้าใบ | ตัวสูงจริง | Scale ที่คำนวณได้ | สูงบนจอ |
|---|---|---|---|---|
| Idle | 300x300 | 240px | 0.583 | 140 |
| Run | 50x90 | 88px | 1.591 | 140 |
| Attack | 80x90 | 78px | 1.795 | 140 |

### กล่องชนไม่ต้องแตะแล้ว
ระบบสร้างแคปซูลใหม่ให้เองทุกครั้งที่เปลี่ยน Auto Fit Height
(สูง = Auto Fit Height, กว้าง = Auto Fit Height x Collision Width Ratio)

### ⚠ แต่ถ้าจะใช้ระบบซ้อนเลเยอร์อาวุธ (หัวข้อ 7.6)
ระบบปรับขนาดช่วยเรื่อง "ตัวใหญ่/เล็กไม่เท่ากัน" ได้ แต่ **ไม่ช่วยเรื่องอาวุธซ้อนตรง**
เพราะอาวุธต้องอิงผ้าใบเดียวกับตัว → สุดท้ายยังต้อง export ทุกท่าให้ผ้าใบขนาดเดียวกันอยู่ดี
แนะนำ: ทำใหม่ทั้งชุดที่ **300x300 เท่ากันหมด** แล้วตั้ง Auto Fit Height ให้ได้ขนาดที่ชอบ

---

## 7.8 ★ ท่าโจมตีแยกตามอาวุธ

ตอนนี้ท่า `Attack` ของคุณคือ **ต่อยมือเปล่า** ระบบเลยรองรับให้เพิ่มท่าถืออาวุธได้แล้ว
เวลาถืออาวุธ ตัวละครจะสลับไปเล่นท่าของอาวุธชนิดนั้นเอง ถอดอาวุธก็กลับมาต่อยมือเปล่า

### ชื่อท่าที่ต้องสร้าง (ตั้งใน SpriteFrames ของตัวละคร)

| ชื่อท่า | ใช้ตอนไหน | สถานะ |
|---|---|---|
| `Idle` | ยืน | ✅ มีแล้ว |
| `Run` | เดิน/วิ่ง | ✅ มีแล้ว |
| `Attack` | **โจมตีมือเปล่า** | ✅ มีแล้ว |
| **`Attack_Blade`** | **โจมตีตอนถือดาบมือใหม่/เบลด** | ⬅ สร้างอันนี้ |
| `Jump` `Hit` `Death` | กระโดด / เจ็บ / ตาย | ยังไม่มี (ระบบใช้ตัวสำรองไปก่อน) |

**ตั้งชื่อท่าให้อาวุธแต่ละชิ้นไว้แล้ว** — เปิดไฟล์ไอเทมดูได้ที่ช่อง **Attack Animation**

| อาวุธ | ไฟล์ | ชื่อท่าที่ต้องสร้าง |
|---|---|---|
| ดาบมือใหม่ | `novice_sword.tres` | **`Attack_Blade`** |
| เบลด | `blade.tres` | `Attack_Blade` (ใช้ท่าเดียวกัน) |
| ฟัลชิออน | `falchion.tres` | `Attack_Falchion` |
| เคลย์มอร์ | `claymore.tres` | `Attack_Claymore` |
| ดาบเพลิง | `flame_sword.tres` | `Attack_FlameSword` |

**อาวุธใหม่ที่จะทำเพิ่ม**: สร้างไฟล์ไอเทม → ใส่ชื่อท่าที่ช่อง **Attack Animation**
→ สร้างท่าชื่อเดียวกันใน SpriteFrames ตัวละคร → จบ ใส่อาวุธนั้นแล้วสลับท่าเอง
(ชื่อจะตั้งว่าอะไรก็ได้ ขอแค่ตรงกันทั้งสองที่)

### ลำดับการเลือกท่า
1. ชื่อในช่อง **Attack Animation** ของอาวุธที่ถือ → เช่น `Attack_Blade`
2. ถ้าไม่มีท่านั้น → `Attack_` + `weapon_type` → เช่น `Attack_sword`
3. ถ้ายังไม่มีอีก → `Attack` (มือเปล่า)

เพราะงั้นทำท่าไหนเสร็จก่อนก็ใช้ได้เลย ที่เหลือถอยไปใช้ท่าเดิมโดยไม่พัง

### สกิลก็ตั้งท่าเฉพาะได้
เปิดไฟล์สกิลใน `data/skills/` → ช่อง **Animation**
เช่นอยากให้ Magnum Break มีท่าหมุนตัวของมันเอง ก็ใส่ `Attack_magnum` แล้วสร้างท่าชื่อนั้น
เว้นว่าง = ใช้ท่าโจมตีตามอาวุธที่ถืออยู่

### ถ้ายังไม่ได้สร้างท่านั้น
ไม่พัง — ระบบจะไล่หาตามลำดับ: `ชื่อที่ขอ` → `Attack` → `Idle`
ทำสไปรท์เสร็จเมื่อไหร่ แค่เพิ่มท่าชื่อนั้นใน SpriteFrames มันจะใช้เองทันที ไม่ต้องแก้โค้ด

### ⚠ ถ้าใช้ระบบซ้อนเลเยอร์อาวุธ (หัวข้อ 7.6) ด้วย
ไฟล์ภาพอาวุธ (`Equip Sprite Frames`) ต้องมีท่า **ชื่อเดียวกัน** กับตัวละคร
คือต้องมี `Attack_Blade` ด้วย ไม่ใช่แค่ `Attack` ไม่งั้นตอนฟันดาบจะหายไป

---

## 7.9 ★ ตัวละครกับมอนสเตอร์ยืนระนาบเดียวกัน

### ปัญหาที่เจอ (แก้แล้ว)
เดิมระบบวัดระยะโจมตีจาก **จุดกำเนิด (origin)** ของแต่ละตัว
แต่จุดกำเนิดอยู่กึ่งกลางกล่องชน ซึ่งขนาดไม่เท่ากัน:

- ผู้เล่น กล่องชนสูง 240 → จุดกำเนิดอยู่เหนือเท้า **120px**
- โพริง กล่องชนสูง 50 → จุดกำเนิดอยู่เหนือเท้า **25px**

ยืนบนพื้นเดียวกันแท้ ๆ แต่จุดกำเนิดต่างกันถึง **95px**
ซึ่งเกินระยะโจมตีแนวตั้ง (`attack_range_y = 70`) → **ตีไม่โดนแม้ยืนติดกัน**

### แก้ยังไง
เปลี่ยนไปวัดจาก **ตำแหน่งเท้า** ทั้งคู่ (`foot_position()`)
เพราะ CharacterBody2D จะวางก้นกล่องชนแนบพื้นเสมอ → เท้าของทุกตัวอยู่ระนาบเดียวกันจริง ๆ

ผลทดสอบในแมพจริง:

| | จุดกำเนิด y | เท้า y | ต่างจากผู้เล่น |
|---|---|---|---|
| ผู้เล่น | 352.0 | 472.0 | – |
| โพริง | 446.9 | 471.9 | **0.1px** (เดิมต่าง 94.9px) |

ทั้งผู้เล่นตีมอน และมอนตีผู้เล่น ใช้ตำแหน่งเท้าเหมือนกันหมดแล้ว

### ภาพก็ยืนตรงระนาบด้วย
MonsterData เพิ่ม 2 ช่อง:

| ช่อง | ทำอะไร |
|---|---|
| **Display Height** | ความสูงของมอนบนจอ (พิกเซล) — 0 = ใช้ Sprite Scale เหมือนเดิม |
| **Align Feet** | จัดให้เท้าแตะพื้นเสมอ (เปิดไว้แล้ว) |

ตั้งค่าให้ทุกตัวแล้ว โดยอิงผู้เล่นสูง 240:

| มอน | สูง | มอน | สูง |
|---|---|---|---|
| แฟเบร / ชอนชอน | 72 | หมาป่า | 125 |
| ฮอร์เน็ต | 80 | มูนัค | 170 |
| ลูนาติก | 82 | ออร์คนักรบ | 205 |
| โพริง | 85 | บาฟโฟเมทจูเนียร์ | 150 |
| ดรอปส์ | 88 | **บาฟโฟเมท** | **340** |

เปลี่ยนขนาดผู้เล่นเมื่อไหร่ ปรับเลขพวกนี้ตามสัดส่วนได้เลย
(หรือใส่ 0 ถ้าอยากคุมด้วย Sprite Scale เองเหมือนเดิม)

---

## 7.10 ★ มอนเกิดกระจายทั่วแมพ (MapSpawner)

เปลี่ยนจากเดิมที่วางจุดเกิดทีละจุด มาเป็น **ตัวเดียวคุมทั้งแมพ**
node ชื่อ `Spawners/MapSpawner` ในแมพของคุณ

### ทำงานยังไง
1. อ่านขอบเขตแมพจาก `Map Bounds` เอง
2. แบ่งความกว้างแมพเป็นช่อง ๆ เท่าจำนวนมอน แล้วให้แต่ละตัวไปคนละช่อง
3. ยิงเรย์ลงหาพื้นในช่องนั้น → วางให้ **เท้าแตะพื้นพอดี**
4. ตายแล้วเกิดใหม่ **ที่จุดใหม่** ไม่ใช่ที่เดิม

ผลทดสอบในแมพจริง (โพริง 5 ตัว): กระจายกินพื้นที่ **743-928 px** ตัวที่ใกล้กันสุดห่าง **111-173 px**

### เพิ่มมอนชนิดใหม่เข้าแมพ
เลือก node **MapSpawner** → ช่อง **Monster Types** → กด `+` → ลากไฟล์ `.tres` ของมอนใส่
ใส่ได้เลย 3 ชนิดตามที่วางแผนไว้ → จะได้ 3 x 5 = **15 ตัวต่อแมพ**
(`Count Per Type` ตั้งไว้ 5 เปลี่ยนได้)

### ช่องตั้งค่าอื่น ๆ

| ช่อง | ทำอะไร |
|---|---|
| **Count Per Type** | เกิดชนิดละกี่ตัว (ตอนนี้ 5) |
| **Edge Margin** | เว้นจากขอบซ้าย-ขวาของแมพ (150) |
| **Min Spacing** | มอนต้องห่างกันอย่างน้อยกี่พิกเซล (170) |
| **Avoid Player Range** | ห้ามเกิดใกล้ผู้เล่นเกินระยะนี้ (280) |
| **Only When Player Exists** | เกิดเฉพาะตอนมีผู้เล่นในแมพ |
| **Spawn Offscreen** | ★ ให้เกิดนอกจอเท่านั้น (เปิดไว้) |
| **Offscreen Margin** | เผื่อจากขอบจอออกไปอีกกี่พิกเซล (120) |
| **Max Spawn Distance** | ไกลสุดที่ยอมให้เกิด กันไปเกิดคนละฟากแมพ (1600) |
| **Despawn Distance** | เดินห่างเกินเท่านี้ มอนจะถูกเก็บไปเกิดใหม่ข้างหน้า (2600) |
| **Spawn Interval** | ทยอยเกิดทีละตัว ห่างกันกี่วินาที (0.6) |
| **Relax After** | หาที่นอกจอไม่ได้กี่วินาทีถึงจะผ่อนเงื่อนไข (4) |

> ถ้าที่ว่างไม่พอ (แมพเล็ก/มอนเยอะ) ระบบจะค่อย ๆ ผ่อนเงื่อนไขให้เอง เพื่อให้เกิดครบจำนวน

### แมพอื่นที่ตั้งไว้ให้แล้ว
- **ป่ามืด** — ชอนชอน / ฮอร์เน็ต / หมาป่า อย่างละ 5 ตัว + บาฟโฟเมท (บอส) ยังใช้จุดเกิดตายตัวเหมือนเดิม
- **ทุ่งหญ้าตัวอย่าง** — โพริง / แฟเบร / ลูนาติก อย่างละ 5 ตัว

---

## 7.11 ★ ภาพไอเทม (ไอคอน + ตอนตกพื้น)

**ใส่ที่เดียว ใช้ได้ทุกที่** — ช่อง **Icon** ใน `ItemData` ใช้กับ:
กระเป๋า · ช่องสวมใส่ · ร้านค้า · **ของที่ตกอยู่บนพื้น** (ทั้งดรอปจากมอนและตอนผู้เล่นทิ้ง)

ดาบมือใหม่ใส่ให้แล้วที่ `Sprites/items/blade.png` (ตัดพื้นหลังดำออก ครอปชิดตัวดาบ ย่อเหลือ 141x256)

### เพิ่มไอคอนไอเทมอื่น
1. วางไฟล์ภาพใน `Sprites/items/`
2. เปิด `data/items/<ชื่อ>.tres` → ลากภาพใส่ช่อง **Icon**

**ไม่ต้องย่อภาพมาก่อน** — ระบบย่อให้เองตอนแสดงผล
ขนาดตอนตกพื้นคุมด้วยช่อง **Drop Display Size** (ตั้งไว้ 46 px ด้านที่ยาวที่สุด)
ตั้ง 0 = ใช้ขนาดจริงของไฟล์

> อยากให้ **เบลด** ใช้ภาพเดียวกันไปก่อน: เปิด `data/items/blade.tres` แล้วลาก `blade.png` ใส่ช่อง Icon

ของที่ตกพื้นจะลอยขึ้นลงเบา ๆ ให้สังเกตเห็นง่าย และมีชื่อไอเทมกำกับด้านบน

---

## 7.12 ★ แก้ 4 เรื่อง (รอบล่าสุด)

### 1) เดินทับแล้วเก็บของไม่ได้ — แก้แล้ว
คุณเดาถูกครับ **เป็นเรื่องความต่างระดับจริง ๆ**

จุดกำเนิด (origin) ของตัวละครอยู่ "กลางลำตัว" ซึ่งลอยสูงจากพื้น **120 px**
(เพราะกล่องชนสูง 240) แต่ของที่ตกอยู่วางที่ระดับ "พื้น"
โค้ดเดิมวัดระยะจากจุดกำเนิด → ของที่วางอยู่แทบเท้าจึงถูกมองว่าห่างเกิน 120 px เลยเก็บไม่ได้

แก้โดยเปลี่ยนไปวัดจาก **ปลายเท้า** ทั้งหมด (วิธีเดียวกับที่แก้เรื่องตีไม่โดน)

พร้อมกันนั้นเจออีกบั๊กที่ทำให้เก็บไม่ได้เหมือนกัน:
ของที่ดรอปจะ **ลอยค้างกลางอากาศ / ร่วงทะลุแมพ** เพราะคำนวณจังหวะหยุดผิด
ตอนนี้ของจะยิงเรย์หาพื้นระหว่างตกทุกเฟรม แล้ว **วางบนพื้นพอดี** เสมอ

ปรับระยะเก็บได้ที่ node `Player`:

| ช่อง | ค่า | ทำอะไร |
|---|---|---|
| **Pickup Range** | 90 | ระยะเก็บแนวนอน (กดปุ่ม F) |
| **Pickup Range Y** | 90 | ระยะเก็บแนวตั้ง เผื่อของอยู่ต่างระดับ |

และที่ `scenes/items/dropped_item.tscn`:
**Auto Pickup Range X** (70) / **Auto Pickup Range Y** (80) = ระยะเก็บอัตโนมัติตอนเดินผ่าน

### 2) หน้าสวมใส่ย่อภาพให้พอดี — แก้แล้ว
ช่องสวมใส่เดิมเอาไฟล์ภาพมาแปะขนาดจริง ภาพใหญ่ ๆ (ดาบ 141x256) เลยดันช่องจนบวม
ตอนนี้เปิด `expand_icon` + จำกัดความกว้างไอคอนไว้ **34 px**
→ ไฟล์ภาพจะใหญ่แค่ไหนก็ย่อลงพอดีช่องเสมอ ไม่ต้องย่อภาพมาก่อน

### 3) popup ลอยไปมุมบนซ้าย — แก้แล้ว
สาเหตุ: โค้ดเดิมสั่งจัดกึ่งกลางตอนสร้างกล่อง ซึ่งตอนนั้นกล่องยัง **กว้าง 0** อยู่
ค่าตำแหน่งเลยถูกล็อกไว้ผิดตั้งแต่แรก

เปลี่ยนโครงใหม่เป็น: กล่องกินเต็มจอ แล้วใช้ `CenterContainer` ให้เอนจินจัดกลางให้เอง
→ อยู่กลางจอเป๊ะทุกครั้ง ไม่ว่าข้อความจะสั้นยาวแค่ไหน หรือย่อ/ขยายหน้าต่างเกม
แถมใส่ **ฉากหลังมืดลง 45%** ให้กล่องเด่นขึ้นด้วย

### 4) มอนกระจุกอยู่ในจอ — แก้แล้ว
ตอนนี้มอน **เกิดนอกจอเท่านั้น** แล้วค่อย ๆ โผล่ทีละตัว (ห่างกัน 0.6 วินาที)
ตอนเดินเปิดแมพไป จะเจอมอนใหม่เรื่อย ๆ ไม่ใช่โผล่พรึบพร้อมกันต่อหน้า

- ระยะใกล้สุดคิดจาก **ครึ่งความกว้างจอ + 120 px** (จอ 1280 → ประมาณ 760 px)
- ไกลสุดไม่เกิน **1600 px** จะได้ไม่ไปเกิดคนละฟากแมพจนเดินหาไม่เจอ
- เดินห่างเกิน **2600 px** มอนตัวเก่าจะถูกเก็บไปเกิดใหม่ข้างหน้าแทน
- ระบบจะเลือก "ช่องที่ไกลผู้เล่นที่สุด" เสมอ
- ถ้าแมพแคบจนหาที่นอกจอไม่ได้ ระบบจะค่อย ๆ ผ่อนระยะลง (ภายใน 4 วินาที) เพื่อไม่ให้แมพว่างเปล่า
  แต่จะไม่มีทางเกิดใกล้กว่า 168 px จากตัวผู้เล่น

**ผลทดสอบในแมพจริงของคุณ** (โพริง 5 ตัว ผู้เล่นยืนนิ่งที่ x=60):
เกิดครบ 5 ตัวโดยใช้เวลาทยอยเกิด 9.3 วินาที · 3 ตัวแรกเกิดนอกจอ (607-873 px)
ตัวที่ใกล้ที่สุดยังห่าง 287 px · กระจายกินพื้นที่ 534 px

> **หมายเหตุ** แมพปัจจุบันของคุณกว้าง 1664 px แต่จอกว้าง 1280 px
> พื้นที่ "นอกจอ" จึงเหลือน้อยมาก ถ้าขยายแมพให้กว้างขึ้น (ดูหัวข้อ 3.5)
> ระบบนี้จะเห็นผลชัดกว่านี้เยอะ — มอนจะเกิดนอกจอได้ครบทุกตัว

---

## 7.13 ★ ปุ่ม / HUD / เซฟ / วาป (รอบล่าสุด)

### ⚠ ต้องปิด Godot แล้วเปิดใหม่ 1 ครั้ง
รอบนี้แก้ `project.godot` (เพิ่มปุ่มใหม่) — Godot จะอ่านค่าใหม่ตอนเปิดโปรเจกต์เท่านั้น
ไฟล์เดิมสำรองไว้ที่ `project_godot_ก่อนเพิ่มปุ่ม.txt`

### 1) ปุ่มเดินเป็น W A S D แล้ว

| ปุ่ม | ทำอะไร |
|---|---|
| **A / D** | เดินซ้าย-ขวา (ลูกศรซ้าย-ขวายังใช้ได้) |
| **W** | กระโดด (Space และลูกศรขึ้นก็ได้) |
| **S** | ลง (จองไว้ให้ระบบลงแท่นในอนาคต) |
| **J / X** | โจมตี |
| **F** | เก็บของ / คุยกับ NPC / เข้าประตู |
| **Q** | ยาแดง |
| **1-4** | สกิล |
| **Tab** | เมนูระบบ (เซฟ / โหลด / เริ่มใหม่) |

อยากเปลี่ยนปุ่มเอง: `Project → Project Settings → Input Map`

### 2) ปุ่มสกิล + ยา ย้ายขึ้นไปข้างหลอดเลือดแล้ว
แถวปุ่มลัดอยู่ **ขวามือของหลอด HP/SP/EXP ติดกันเลย** มุมซ้ายบน
มี 6 ปุ่ม: สกิล 1-4 · **ยาแดง (โชว์จำนวนที่เหลือ)** · **เมนู**

ปุ่มยาจะจางลงเมื่อยาหมด · ปุ่มสกิลจะจางตอนติดคูลดาวน์
แถวล่างจอเหลือแค่บรรทัดบอกปุ่ม

ปรับขนาดปุ่มได้ที่ `HOTKEY_SIZE` หัวไฟล์ `scripts/ui/hud.gd` (ตอนนี้ 54x46)

### 3) ตัวละครวิ่งไวขึ้น
`BASE_MOVE_SPEED` ใน `scripts/core/player_stats.gd`: **300 → 430**
อยากไวกว่านี้อีกก็แก้เลขนี้เลย (สกิล/ไอเทมที่เพิ่ม `move_speed_percent` จะคูณจากค่านี้)

### 4) เซฟเกม / เริ่มเกมใหม่
กด **Tab** (หรือปุ่ม "เมนู" ข้างหลอดเลือด) เปิดหน้าต่าง **เมนูระบบ**

- **3 ช่องเซฟ** แต่ละช่องมีปุ่ม บันทึก / โหลด / ลบ และโชว์เลเวลกับวันเวลาที่เซฟ
- **เริ่มเกมใหม่** — ล้างตัวละครกลับไปเป็น Lv.1 ของเริ่มต้นครบ เงิน 1000
  (ไฟล์เซฟที่บันทึกไว้ **ไม่หาย** ยังโหลดกลับมาได้)
- ปุ่มลัดเดิมยังใช้ได้: **F5** เซฟช่อง 1 · **F6** โหลดช่อง 1

ไฟล์เซฟอยู่ที่ `user://saves/slot_N.json`
(Windows: `%APPDATA%\Godot\app_userdata\<ชื่อโปรเจกต์>\saves`)

### 5) เมือง Asgard กว้างขึ้นเท่าภาพฉาก
ภาพ `Map_Asgard.png` (3212x941 ย่อ 1.1269) กินพื้นที่จริง **x -1036 ถึง 2584**
แต่พื้นเดินได้เดิมยาว x -1774..2395 (เลยขอบภาพทางซ้าย แต่ขาดไปทางขวา)

ตอนนี้ปรับให้ตรงกันเป๊ะ:

| ค่า | เดิม | ใหม่ |
|---|---|---|
| กล่องชนพื้น | 4169 x 48 | **3620 x 48** |
| ขอบเขตแมพ | Rect2(0, 0, 4000, 900) | **Rect2(-1036, -73, 3620, 1060)** |
| Auto Fit Bounds | เปิด | **ปิด** (ล็อกให้ตรงภาพ) |

ไฟล์เดิมสำรองไว้ที่ `scenes/maps/prontera_town_ก่อนขยาย.tscn.bak`

### 6) ของที่ดรอป ต้องกด F เก็บ
ไม่เก็บอัตโนมัติแล้ว — เดินเข้าไปใกล้จะมีป้าย **"กด F เก็บ"** ลอยขึ้นเหนือไอเทม

อยากให้กลับไปเก็บอัตโนมัติ: เปิด `scenes/items/dropped_item.tscn` → ติ๊ก **Auto Pickup**
ระยะที่ป้ายโผล่/เก็บได้ ปรับที่ **Auto Pickup Range X / Y** (70 / 80)

### 7) วาปไม่เด้ง popup แล้ว
เดินผ่านประตูเฉย ๆ = **ผ่านไปได้ตามปกติ ไม่เกิดอะไรขึ้น**
ยืนอยู่ในประตูจะมีป้าย **"กด F เพื่อไป ..."** ลอยอยู่ กดแล้วถึงจะเปลี่ยนแมพ

ถ้ายืนอยู่ตรงที่มีของตกอยู่ด้วย ปุ่ม F จะ **เก็บของก่อน** แล้วค่อยกดซ้ำเพื่อเข้าประตู

อยากได้กล่องยืนยันแบบเดิมเฉพาะประตูบางบาน: เลือก node ประตูนั้น → ติ๊ก **Require Confirm**
อยากให้เดินชนแล้วเข้าเลย: ติ๊ก **Auto Enter**

### 8) ท่าตายของมอนสเตอร์ — แก้แล้ว
**สาเหตุ:** โค้ดสั่งเล่นท่าชื่อ `Death` แต่ SpriteFrames ของโพริงตั้งชื่อว่า **`Die`**
ระบบหาไม่เจอเลยถอยไปเล่น `Idle` แทน — มอนจึงหายไปเฉย ๆ โดยไม่มีท่าตาย

**แก้ 3 อย่าง:**

1. **ชื่อท่าตั้งได้หลายแบบ** — `Death` / `Die` / `Dead` / `Dying` ใช้ได้หมด
   และ **ตัวพิมพ์เล็ก-ใหญ่ไม่สำคัญ** (`die` = `Die` = `DIE`)
   ท่าอื่นก็เหมือนกัน: `Run`/`Walk`/`Move` · `Attack`/`Atk` · `Hit`/`Hurt`/`Damage` · `Jump`/`Hop`
2. **เล่นจนจบจริง** — เดิมตัดทิ้งที่ 0.6 วินาทีตายตัว ตอนนี้คำนวณจากจำนวนเฟรม ÷ ความเร็ว
   (ท่า Die ของโพริง 6 เฟรม 5 fps = 1.2 วินาที) แล้วค้างที่เฟรมสุดท้าย
3. **จางหายไปนุ่ม ๆ** แทนที่จะหายวับ

ปรับได้ใน MonsterData หมวด **ท่าตาย**:

| ช่อง | ทำอะไร |
|---|---|
| **Death Time** | บังคับให้ท่าตายยาวกี่วินาที (0 = คิดจากจำนวนเฟรมเอง) |
| **Death Fade** | เล่นจบแล้วจางหายกี่วินาที (0.35) |

> มอนที่ตายแล้วจะไม่ถูกนับเป็น "ตัวเป็น" อีก ระบบเกิดมอนจึงเติมตัวใหม่ได้ทันที
> ไม่ต้องรอศพหายก่อน

---

## 7.14 ★ รูปการ์ดกึ่งกลาง + popup ตอนได้การ์ดใบใหม่

### 1) รูปในช่องไอเทม/การ์ดอยู่กึ่งกลางแล้ว
**สาเหตุเดิม:** ใช้ `Button.icon` + `expand_icon` ซึ่ง Godot จะวางรูป **ชิดมุม** ไม่ใช่กึ่งกลาง
โดยเฉพาะภาพทรงสูง (การ์ด) จะเห็นชัดว่าเบี้ยวไปทางซ้าย

**แก้:** เลิกใช้ `Button.icon` เปลี่ยนเป็น `TextureRect` ซ้อนเต็มปุ่มแทน
ตั้ง `STRETCH_KEEP_ASPECT_CENTERED` → **กึ่งกลางเป๊ะ + ไม่บิดสัดส่วน** ไม่ว่าภาพจะขนาดไหน

ใช้กับ **กระเป๋า · ช่องสวมใส่ · อัลบั้มการ์ด** ทั้งหมด
(ช่องสวมใส่จัดรูปชิดซ้าย-กึ่งกลางแนวตั้ง เพื่อเว้นที่ให้ชื่อไอเทม)

ตัวเลขที่วัดได้จริงจากการทดสอบ: กึ่งกลางปุ่มกับกึ่งกลางรูปตรงกัน **คลาดเคลื่อน 0.0 px**

มีฟังก์ชันกลางให้เรียกใช้ซ้ำแล้วที่ `UITheme.make_slot_icon(button)`
คืน `[TextureRect (รูป), Label (จำนวน มุมขวาล่าง)]`

> **การ์ดที่ยังไม่ได้ใส่ไอคอนเอง** จะดึงภาพการ์ด (กรอบ + รูปมอน) มาโชว์ในกระเป๋าให้อัตโนมัติ

### 2) ได้การ์ดใบใหม่ครั้งแรก → เด้ง popup ยินดีด้วย
เก็บการ์ดที่ **ยังไม่เคยมี** ปุ๊บ จะเด้งกล่องกลางจอ:

```
        ยินดีด้วยนักผจญภัย!
   ┌────────────────────────┐
   │   [ ภาพการ์ดใบเต็ม ]    │
   └────────────────────────┘
      คุณได้การ์ด
      [ การ์ดโพริง ]
      กดปุ่มใดก็ได้เพื่อปิด
```

- โชว์ **การ์ดใบเต็ม** (กรอบสีตามระดับความหายาก + รูปมอนสเตอร์ + คุณสมบัติ)
- ชื่อการ์ดเปลี่ยนสีตามระดับความหายากของใบนั้น
- **หยุดเกมไว้ให้ดู** ปิดด้วยการกดปุ่มใดก็ได้ / คลิก / หรือรอ 6 วินาที
- **ใบซ้ำไม่เด้งอีก** — เด้งเฉพาะครั้งแรกของการ์ดใบนั้น ๆ
- นับรวมการ์ดที่ใส่อยู่ในอุปกรณ์ด้วย (ถอดออกแล้วเก็บใหม่ก็ไม่เด้งซ้ำ)

ปรับเวลาปิดเองได้ที่ `auto_close` ในไฟล์ `scripts/ui/card_get_popup.gd` (0 = ไม่ปิดเอง)

### เบื้องหลัง
- สัญญาณใหม่ `Events.card_obtained(card_id)`
- **ของทุกชิ้นเข้ากระเป๋าผ่าน `PlayerState.gain_item(inst)` แล้ว** (แทน `inventory.add()` ตรง ๆ)
  ตัวนี้จะเช็คก่อนใส่ว่า "เป็นการ์ดที่ยังไม่เคยมีหรือเปล่า" แล้วยิงสัญญาณให้
  **ถ้าเขียนโค้ดให้ของเข้ากระเป๋าเพิ่มในอนาคต ให้เรียก `gain_item()` เสมอ** ไม่งั้น popup จะไม่เด้ง

---

## 7.15 ★ ท่าสกิลแยกตามอาวุธ + ไอคอนสกิล

### 1) ท่าสกิลแยกตามอาวุธ — เชื่อม `Attack_Blade_bash` ให้แล้ว

ระบบไล่หาชื่อท่าตามลำดับนี้ เจอชื่อไหนก่อนก็ใช้อันนั้น:

| ลำดับ | ชื่อท่า | ใช้เมื่อ |
|---|---|---|
| 1 | ช่อง **Animation** ใน SkillData | ตั้งชื่อท่าเองในไฟล์สกิล (ปกติเว้นว่าง) |
| 2 | **`Attack_<อาวุธ>_<สกิล>`** | ★ ท่าเฉพาะอาวุธนั้น + สกิลนั้น |
| 3 | `Attack_<สกิล>` | ท่าสกิลกลาง ใช้ได้กับทุกอาวุธ |
| 4 | `Attack_<อาวุธ>` | ยังไม่มีท่าสกิล ใช้ท่าโจมตีปกติของอาวุธ |
| 5 | `Attack` | มือเปล่า |

**ตอนนี้ใช้ได้แล้ว:** ถือ *ดาบมือใหม่* (ท่าโจมตี `Attack_Blade`) กดสกิล *bash*
→ เล่นท่า **`Attack_Blade_bash`** อัตโนมัติ

### ทำอาวุธชิ้นใหม่ต้องทำอะไรบ้าง
1. วาดท่าโจมตีปกติ ตั้งชื่อเช่น **`Attack_Claymore`**
2. วาดท่าสกิล ตั้งชื่อ **`Attack_Claymore_bash`** (ต่อท้ายด้วย id ของสกิล)
3. เปิด `data/items/<อาวุธ>.tres` → ช่อง **Attack Animation** ใส่ `Attack_Claymore`

**แค่นี้ ไม่ต้องแก้โค้ดเลย** — id ของสกิลดูได้จากช่อง **ID** ในไฟล์ `data/skills/*.tres`
(`bash` · `magnum_break` · `sword_mastery` · `endure` · `battle_cry` · `first_aid` · `hp_recovery`)

> **ตัวพิมพ์เล็ก-ใหญ่ไม่สำคัญ** — `attack_blade_bash` ก็หาเจอ
> อยากเปลี่ยนรูปแบบชื่อ: node `Player` → กลุ่ม **ท่าโจมตี** →
> **Skill Weapon Anim Format** (`{attack}_{skill}`) · **Skill Anim Format** (`Attack_{skill}`)

### อยากให้ท่าสกิลใช้ร่วมกันทุกอาวุธ
วาดท่าเดียวชื่อ **`Attack_bash`** — อาวุธไหนที่ยังไม่มีท่าเฉพาะจะมาใช้ท่านี้แทน

### 2) ไอคอนสกิล — โชว์แล้วทั้ง 2 ที่

ใส่ภาพในช่อง **Icon** ของ `data/skills/<สกิล>.tres` ที่เดียว ใช้ได้ทั้ง:

- **ปุ่มลัด 1-4 ข้างหลอดเลือด** — โชว์รูปเต็มปุ่ม มีเลขปุ่มลัดมุมซ้ายบน
  ชี้เมาส์ค้างจะเห็นชื่อ + คำอธิบายสกิล · ปุ่มจะจางลงตอนติดคูลดาวน์เหมือนเดิม
- **หน้าต่างสกิล (K)** — โชว์ไอคอนหน้าชื่อสกิลทุกแถว
  สกิลที่ยังไม่ได้เรียนไอคอนจะจาง ๆ

**สกิลที่ยังไม่ได้ใส่ไอคอน** ปุ่มลัดจะโชว์ชื่อสกิลย่อ ๆ แทน (เหมือนเดิม) ไม่พัง

ภาพจะถูก **ย่อให้พอดีช่องและจัดกึ่งกลางเอง** ไม่ว่าไฟล์ต้นฉบับจะขนาดไหน
ปรับขนาดไอคอนในหน้าต่างสกิลได้ที่ `ICON_SIZE` หัวไฟล์ `scripts/ui/skill_window.gd` (34 px)

> ตอนนี้ใส่ไอคอนแล้ว 1 สกิล (**bash**) เหลืออีก 6 สกิล ใส่เพิ่มได้เลย

---

## 7.16 ★ กล่องรายละเอียดไอเทม (เด้งข้างหน้าต่าง)

กดที่ไอเทม → กล่องรายละเอียดโผล่ **ข้าง ๆ หน้าต่างที่กดมา** ไม่ทับกัน แบบเกม Ragnarok

```
┌─ กระเป๋า ──────┐ ┌─ ดาบมือใหม่ ────── ✕ ─┐
│ ▣ ▣ ▣ ▣ ▣ ▣ │ │ ┌──────┐ ประเภท : อาวุธ │
│ ▣ ▣ ▣ ▣ ▣ ▣ │ │ │ รูป  │ พลังโจมตี : 25  │
│ ▣ ▣ ▣ ▣ ▣ ▣ │ │ │ ใหญ่ │ ตีบวก : +0 / +10│
│              │ │ └──────┘ ช่องการ์ด : 0/1 │
└──────────────┘ └────────────────────────┘
```

### ใช้ได้ที่ไหนบ้าง
| หน้าต่าง | กดตรงไหน | ได้อะไรเพิ่ม |
|---|---|---|
| **กระเป๋า (I)** | กดช่องไอเทม | ค่าตีบวกจริง + การ์ดที่ใส่อยู่ |
| **ร้านค้า — แท็บซื้อ** | กดชื่อของ | บอก **ราคาซื้อ** ด้านบน |
| **ร้านค้า — แท็บขาย** | กดชื่อของ | บอก **ราคาขาย** ที่จะได้จริง |
| **ช่องสวมใส่ (E)** | กดช่อง | กดครั้งแรก = ดูรายละเอียด · **กดซ้ำ = ถอดออก** |

### ในกล่องมีอะไร
รูปไอเทมใหญ่ 120x120 · ประเภท (อาวุธ/ชุดเกราะ/ของสวมศีรษะ/การ์ด/ของใช้/วัตถุดิบ) ·
พลังโจมตี-ป้องกัน · ค่าสเตตัสที่เพิ่มให้ · ระดับตีบวก · ช่องการ์ดและการ์ดที่ใส่อยู่ ·
คุณสมบัติการ์ด · คำอธิบายไอเทม · ราคา

- ของยาว ๆ จะมี **แถบเลื่อน** ให้อ่านต่อ (สูงสุด 280 px แล้วค่อยเลื่อน)
- ถ้าวางด้านขวาแล้วล้นจอ จะ **ย้ายไปด้านซ้ายเอง** และไม่หลุดขอบจอ
- **ลากหน้าต่างไปไหน กล่องตามไปด้วย** · ปิดหน้าต่างต้นทาง กล่องหายตาม
- ปิดกล่องได้ที่ปุ่ม ✕ หรือกด Esc

### ปรับแต่ง
หัวไฟล์ `scripts/ui/item_info_popup.gd`:

| ค่า | ทำอะไร |
|---|---|
| `ART_SIZE` | ขนาดกรอบรูป (120x120) |
| `BOX_WIDTH` | ความกว้างกล่อง (380) |
| `MAX_HEIGHT` | สูงเท่าไหร่ถึงจะมีแถบเลื่อน (280) |
| `GAP` | ห่างจากหน้าต่างต้นทางกี่พิกเซล (8) |

**เรียกใช้จากที่อื่น** (เผื่ออยากเพิ่มในหน้าต่างใหม่):
```gdscript
UI.show_item(inst, self)                       # ของในกระเป๋า
UI.show_item_data(item_data, self, "ข้อความ")   # แม่แบบไอเทม เช่นในร้าน
UI.hide_item_popup()
```

---

## 7.17 ★ แก้อัตรา miss (อัพ DEX แล้วคุ้มขึ้น)

### ปัญหาเดิม
สูตรเก่า: `โอกาสเข้า % = 75 + HIT ผู้เล่น − FLEE มอน` และ **DEX 1 แต้ม = HIT +1 เท่านั้น**
Lv1 DEX1 ตีโพริงเข้าแค่ **74%** (พลาด 1 ใน 4 ครั้ง) และอัพ DEX 20 แต้มได้ความแม่นเพิ่มแค่ 20%
ขณะที่ FLEE ของมอนโตเร็วกว่า (ประมาณ เลเวล x1.5) — ยิ่งไปแมพยาก ยิ่ง miss

### แก้ 3 จุด
| ค่า | เดิม | ใหม่ | อยู่ที่ |
|---|---|---|---|
| ฐานตอน **ผู้เล่นตีมอน** | 75 | **85** | `BASE_HIT_RATE` |
| ฐานตอน **มอนตีผู้เล่น** | 75 (ใช้ร่วมกัน) | **72** (แยกออกมา) | `MONSTER_BASE_HIT_RATE` |
| **DEX 1 แต้ม** | HIT +1 | **HIT +1.5** | `HIT_PER_DEX` |
| เพดานความแม่น | 95% | **98%** | `MAX_HIT_RATE` |

**แยกฐานของสองฝั่งออกจากกัน** — ผู้เล่นแม่นขึ้นโดยที่มอนไม่ได้แม่นขึ้นตาม
(โพริงตีผู้เล่น Lv10 AGI10: เดิม 62% → ใหม่ **59%** คือมอนพลาดมากขึ้นนิดหน่อยด้วย)

### ผลจริงหลังแก้ (โอกาสตีเข้า %)

| ผู้เล่น | โพริง | แฟเบร | ลูนาติก | ชอนชอน | ฮอร์เน็ต | หมาป่า | มูนัค | ออร์ค |
|---|---|---|---|---|---|---|---|---|
| Lv1 DEX1 | 84 | 81 | 71 | 60 | 52 | 54 | 47 | 43 |
| Lv5 DEX10 | 98 | 98 | 89 | 78 | 70 | 72 | 65 | 61 |
| Lv10 DEX20 | 98 | 98 | 98 | 98 | 90 | 92 | 85 | 81 |
| Lv20 DEX40 | 98 | 98 | 98 | 98 | 98 | 98 | 98 | 98 |

- Lv1 ตีโพริง: 74% → **84%**
- Lv10 ตีฮอร์เน็ต: DEX1 = 61% → **อัพ DEX เป็น 21 ได้ 91%** (อัพ 20 แต้ม = +30% ไม่ใช่ +20% แบบเดิม)
- **บาฟโฟเมท (บอส)** Lv20 DEX40 ยังเข้าแค่ 55% — บอสยังต้องเตรียมตัว

ทดสอบสุ่มตีจริง 4,000 ครั้ง: เข้า 90.7% ตรงกับที่สูตรคำนวณไว้ (90%)

### อยากปรับเอง
เปิด `scripts/core/combat.gd` หัวไฟล์ — แก้ `BASE_HIT_RATE` ตัวเดียวก็เปลี่ยนทั้งเกม
(+5 = ตีเข้ามากขึ้น 5% กับมอนทุกตัว)
อยากให้ DEX คุ้มกว่านี้อีก แก้ `HIT_PER_DEX` ใน `scripts/core/player_stats.gd`

> **ถ้ามอนตัวไหนหลบเก่งเกินไปเป็นตัว ๆ** แก้ช่อง **Flee** ในไฟล์ `data/monsters/<ตัวนั้น>.tres`
> ได้โดยตรง (ค่านี้เป็น "โบนัส" ระบบบวก 100 + เลเวล ให้เอง)

---

## 7.18 ★ เปลี่ยนอาวุธแล้วภาพตัวละครเปลี่ยนตาม "ทุกท่า"

### ทำไม falchion ใส่แล้วยังเป็นภาพเดิม
เพราะเมื่อก่อน **มีแค่ท่าโจมตี** ที่แยกตามอาวุธ
ท่ายืน (`Idle`) กับท่าวิ่ง (`Run`) มีอันเดียว — และในภาพนั้นตัวละคร **ถือดาบเล่มเดิมอยู่แล้ว**
เวลาใส่ falchion จึงเห็นดาบเก่าตลอด ยกเว้นตอนกดโจมตีแวบเดียว

### ตอนนี้ทุกท่าแยกตามอาวุธได้หมดแล้ว
ตั้งชื่อว่า **`<ท่า>_<ชื่ออาวุธ>`** ระบบจะหยิบมาใช้ก่อนเสมอ ถ้าไม่มีก็ถอยไปใช้ท่าธรรมดา

| อยากให้เปลี่ยนตอนไหน | ตั้งชื่อท่าว่า |
|---|---|
| ยืนเฉย ๆ | `Idle_falchion` |
| วิ่ง | `Run_falchion` |
| กระโดด | `Jump_falchion` |
| โจมตีปกติ | `Attack_falchion` |
| ใช้สกิล bash | `Attack_falchion_bash` |
| โดนตี | `Hit_falchion` |
| ตาย | `Death_falchion` |

**วาดเฉพาะท่าที่อยากให้เปลี่ยนก็พอ** ท่าที่ไม่ได้วาดจะใช้ภาพธรรมดาไปก่อน ไม่พัง

> ตอนนี้ falchion มีแค่ `Attack_falchion` กับ `Attack_falchion_bash`
> ถ้าอยากให้เห็นดาบใหม่ตลอดเวลา ต้องวาด **`Idle_falchion`** กับ **`Run_falchion`** เพิ่ม

### "ชื่ออาวุธ" เอามาจากไหน
1. ช่อง **Attack Animation** ของไอเทม — `Attack_Falchion` → ชื่ออาวุธคือ `Falchion`
2. ถ้าเว้นว่างไว้ → ใช้ **id ของไอเทม** เช่น `falchion`

**ตัวพิมพ์เล็ก-ใหญ่ไม่สำคัญ** — ตั้ง `Attack_Falchion` ในไอเทม แต่วาดสไปรท์ชื่อ `Attack_falchion` ก็เจอ

### เพิ่มอาวุธใหม่ = ไม่ต้องตั้งค่าอะไรเลย
ตั้งชื่อท่าให้ตรงกับ **id ของไอเทม** ก็พอ เช่นไอเทม id `claymore` วาดท่า `Attack_claymore`
ระบบเดาให้เอง ไม่ต้องแตะช่อง Attack Animation

### ⚠ ข้อสังเกตของ falchion.tres
ไฟล์นี้ตั้งช่อง **Equip Sprite Frames** ไว้ด้วย โดยข้างในมีท่าชื่อ **`Attack falchion` (เว้นวรรค)**

ช่องนี้คือระบบ **Paper Doll** (ซ้อนภาพอาวุธทับตัวเปล่า) ซึ่งต้องตั้งชื่อท่า **ให้ตรงกับท่าของตัวละคร**
ชื่อที่มีเว้นวรรคจึงไม่ตรงกับอะไรเลย → เลเยอร์นี้ถูกซ่อนอยู่ ไม่มีผลอะไรตอนนี้

**แนะนำ:** ภาพ `Attack falchion.png` ถูกใช้เป็นท่าของตัวละครโดยตรงอยู่แล้ว (ใน `player_frames.tres`)
จึง **ล้างช่อง Equip Sprite Frames ของ falchion ทิ้งได้เลย** จะได้ไม่วาดซ้อนกันสองชั้นในอนาคต
(ถ้าจะใช้ Paper Doll จริง ๆ ต้องวาดเฉพาะ "ตัวดาบ" ไม่ใช่ทั้งตัวละคร แล้วตั้งชื่อท่าให้ตรงกัน)

---

## 7.19 ★ เลเวลของสวมใส่ · ช่องการ์ดเฉพาะของดรอป · ระบบเควส

### ⚠ ต้องปิด Godot แล้วเปิดใหม่ 1 ครั้ง (เพิ่มปุ่ม U)

---

### 1) ของสวมใส่มีระดับเลเวลแล้ว

ช่อง **Required Level** ใน `data/items/<ไอเทม>.tres` — เลเวลไม่ถึงใส่ไม่ได้ มีข้อความบอก
กล่องรายละเอียดไอเทมจะโชว์ **เขียว = ใส่ได้ / แดง = ยังไม่ถึง**

| ไอเทม | Lv | | ไอเทม | Lv |
|---|---|---|---|---|
| ดาบมือใหม่ · เสื้อผ้าฝ้าย · การ์ด · หมวก · รองเท้าแตะ | 1 | | ผ้าคลุมนักผจญภัย | 8 |
| เบลด · ฮู้ด | 4 | | ถุงมือ | 10 |
| ฟัลชิออน · บักเลอร์ | 12 | | รองเท้าบู๊ต | 15 |
| แหวน | 18 | | เคลย์มอร์ | 25 |
| เกราะเพลท | 30 | | ดาบเพลิง | 40 |

**เปลี่ยนเลขได้เลย** ที่ช่อง Required Level ของไอเทมนั้น ๆ

---

### 2) ★ ช่องการ์ดติดมากับ "ของที่ดรอปจากมอน" เท่านั้น ★

| ที่มาของของ | ช่องการ์ด |
|---|---|
| **ซื้อจากร้านค้า** | ไม่มี (0 ช่อง) |
| **ของแจกตอนเริ่มเกม** | ไม่มี |
| **ดรอปจากมอนสเตอร์** | มี ตามที่ตั้งไว้ในช่อง Card Slots |

ช่อง **Card Slots** ใน ItemData ตอนนี้แปลว่า **"จำนวนช่องสูงสุดถ้าดรอปจากมอน"**
ส่วนช่องจริงของแต่ละชิ้นเก็บไว้ที่ `ItemInstance.slots` (แยกกันทีละชิ้น)

- ชื่อไอเทมจะมี `[2]` ต่อท้ายเฉพาะชิ้นที่มีช่องจริง ๆ
- กล่องรายละเอียดบอกชัดเจน: *"ช่องการ์ด: ไม่มี (ของจากร้านค้าไม่มีช่อง)"*
- ในร้านค้าจะบอกว่า *"สูงสุด 2 ช่อง ถ้าดรอปจากมอนสเตอร์"*

> **แก้บั๊กเก่าที่เจอระหว่างทำ:** ตอนเก็บของสวมใส่เข้ากระเป๋า ระบบสร้างชิ้นใหม่ทับ
> ทำให้ **การ์ดที่ใส่ไว้และค่าตีบวกหายไป** ตอนนี้เก็บ "ชิ้นเดิม" แล้ว ไม่หายอีก

---

### 3) ★ ระบบเควส ★ (กด **U** เปิดสมุดเควส)

**เควสที่มีตอนนี้**

| เควส | คนให้ | เงื่อนไข | รางวัล |
|---|---|---|---|
| กำจัดโพริงให้ฮันส์ | ช่างตีเหล็กฮันส์ | ล่าโพริง 100 ตัว | **ฟราคอน x3** + EXP 500 |
| กำจัดแฟเบรให้โทนี่ | พ่อค้าโทนี่ | ล่าแฟเบร 100 ตัว | **3,000 z** + EXP 800 |

**วิธีเล่น**
1. NPC ที่มีเควสจะมีเครื่องหมาย **`!` สีเหลือง** ลอยเหนือหัว
2. กด **F** คุย → กล่องถามว่ารับเควสไหม
3. ออกไปล่ามอน — มีข้อความบอกความคืบหน้าทุก 10 ตัว
4. ครบแล้วเครื่องหมายเปลี่ยนเป็น **`?` สีเขียว** → กลับไปคุยเพื่อรับรางวัล

**สมุดเควส (U)** โชว์เควสที่กำลังทำ แถบความคืบหน้า และเควสที่ทำเสร็จแล้ว
เควสจะถูกเซฟไปกับไฟล์เซฟด้วย

> **NPC ที่มีเควสยังใช้งานปกติ** — คุยเรื่องเควสจบแล้ว ร้านค้า/หน้าตีบวกจะเปิดตามเดิม

### เพิ่มเควสใหม่ (ไม่ต้องเขียนโค้ด)
1. ก็อปไฟล์ `data/quests/hans_poring.tres` ตั้งชื่อใหม่
2. แก้ `id`, `title`, `kill_monster_id`, `kill_count`, รางวัล
3. เอา `id` ไปใส่ช่อง **Quest Ids** ของ NPC คนที่จะให้เควส

| ช่องใน QuestData | ทำอะไร |
|---|---|
| **Kill Monster Id / Kill Count** | ต้องล่ามอนตัวไหน กี่ตัว (เว้นว่าง = แค่ไปคุย) |
| **Required Level** | ต้องเลเวลเท่าไหร่ถึงรับได้ |
| **Reward Item Id / Count** | ของรางวัล |
| **Reward Zeny / Exp** | เงินและค่าประสบการณ์ |
| **Repeatable** | ทำซ้ำได้ไหม |
| **Dialog Offer / Progress / Complete** | บทพูด 3 ช่วง |

> กระเป๋าเต็มจะส่งเควสไม่ได้ (มีข้อความเตือน) ของรางวัลจะไม่หาย

---

## 8. ยังไม่ได้ทำ

ระบบเควส, กิลด์นักผจญภัย, อาชีพที่ 2 (Knight — โครงรองรับไว้แล้ว), สกิลระยะไกล, เสียง/เพลง

---

## 7.20 ปุ่มเมาส์ — คลิกซ้ายตีธรรมดา / คลิกขวาใช้สกิลช่อง 1

### ใช้ยังไง
| ปุ่ม | ทำอะไร |
|---|---|
| **คลิกซ้าย** | โจมตีปกติ (เหมือนกด J หรือ X) |
| **คลิกขวา** | ใช้สกิลที่ตั้งไว้ใน **ช่องลัดที่ 1** (เหมือนกดเลข 1) |

- คลิกแล้ว **ตัวละครจะหันไปทางที่คลิกให้เอง** ก่อนฟัน (คลิกซ้ายของตัว = หันซ้าย, คลิกขวาของตัว = หันขวา)
- ปุ่มคีย์บอร์ดเดิมยังใช้ได้ครบเหมือนเดิม ไม่ได้ตัดอะไรทิ้ง
- **ไม่ต้องรีสตาร์ท Godot** รอบนี้ไม่ได้แตะ `project.godot` เลย

### กันคลิกโดน UI แล้วฟันลม
คลิกในหน้าต่างกระเป๋า / ร้านค้า / สมุดเควส / กล่องยืนยัน หรือคลิกบนแผงหลอดเลือด-ปุ่มลัด
**จะไม่ทำให้ตัวละครฟัน** กันไว้ 2 ชั้น:

1. คลิกรับที่ `Player._unhandled_input()` — ถ้าปุ่ม/หน้าต่างบนจอกินคลิกไปแล้ว คลิกจะไม่ไหลมาถึงตัวละคร
2. เช็คซ้ำด้วย `UI.is_point_over_ui(จุดที่คลิก)` — ทับกรอบหน้าต่างที่เปิดอยู่เมื่อไหร่ ตัดทิ้งทันที

เปิดกระเป๋าค้างไว้แล้วคลิก **นอก** หน้าต่าง ยังฟันได้ปกติ

### ปรับได้ใน Inspector (เลือกโหนด Player)
กลุ่ม **"ปุ่มเมาส์"**

| ช่อง | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| Mouse Attack | เปิด | คลิกซ้าย = โจมตีปกติ (ปิดได้ถ้าไม่ชอบ) |
| Mouse Skill Slot | 1 | คลิกขวาใช้สกิลช่องลัดที่เท่าไร (1-4) · ใส่ **0 = ปิดคลิกขวา** |
| Mouse Turns Facing | เปิด | คลิกแล้วหันหน้าไปทางที่คลิก |

> อยากให้คลิกขวาเป็นสกิลช่อง 2 ก็แค่เปลี่ยน **Mouse Skill Slot** เป็น 2 ไม่ต้องแก้โค้ด

### ไฟล์ที่แก้
- `scripts/entities/player.gd` — รับคลิกที่ `_unhandled_input()` + `_face_click()` + ตัวเลือก 3 ช่อง
- `scripts/ui/ui_manager.gd` — เพิ่ม `is_point_over_ui()`
- `scripts/ui/hud.gd` — เก็บ `top_panel` / `bottom_panel` ไว้เช็คคลิก + แถบคำใบ้บอกเรื่องเมาส์
- `scripts/core/input_setup.gd` — เลิกผูกปุ่มเมาส์ไว้ใน InputMap (ถ้าผูกไว้ คลิกโดนหน้าต่างจะกลายเป็นฟันลม)

### ข้อควรระวัง (บทเรียนจากรอบนี้)
- **ห้ามผูกปุ่มเมาส์ไว้ใน InputMap** แล้วอ่านด้วย `Input.is_action_just_pressed()`
  เพราะวิธีนั้นไม่สนใจว่า UI กินคลิกไปแล้วหรือยัง — คลิกปุ่มในกระเป๋าก็จะฟันลมไปด้วย
  ให้รับที่ `_unhandled_input()` แทนเสมอ
- ตำแหน่งที่คลิกในโลก ให้แปลงจาก `event.position` ด้วย
  `get_viewport().get_canvas_transform().affine_inverse() * event.position`
  (ใช้ `get_global_mouse_position()` ตรง ๆ จะทดสอบไม่ได้ และไม่ตรงกับคลิกที่เพิ่งเกิด)
- โหมด headless **ไม่มีระบบชี้เมาส์ของ GUI** — `gui_get_hovered_control()` คืน null เสมอ
  เทสต์จึงต้องเช็คด้วยกรอบหน้าต่าง (`get_global_rect()`) แทน
  และตอน `push_input()` ต้องส่ง `in_local_coords = true` ไม่งั้นพิกัดจะเพี้ยน

---

## 7.21 หน้าสวมใส่อุปกรณ์ — จัดใหม่ให้เหมือนหน้าต่างของ RO

### หน้าตาใหม่
```
┌ ไอเท็มที่สวมใส่ ──────────────────────────┐
│ [อุปกรณ์ (equipment)] [เงา (Shadow)]      │
│ ┌──────────────┐ ┌──────┐ ┌──────────────┐│
│ │ หมวกผ้า  [รูป]│ │      │ │ผ้าคลุม  [รูป]││
│ └──────────────┘ │ ตัว  │ └──────────────┘│
│  ศีรษะ · head    │ ละคร │  ผ้าคลุม · robe │
│ ┌──────────────┐ │      │ ┌──────────────┐│
│ │+5 ฟัลชั่อน[รูป]│ │      │ │การ์ด   [รูป]││
│ └──────────────┘ └──────┘ └──────────────┘│
│  อาวุธ · R-hand            มือรอง · L-hand │
└───────────────────────────────────────────┘
```

- ทุกช่อง = **ชื่อไอเทมอยู่ซ้าย · รูปไอเทมอยู่ขวาในกรอบสี่เหลี่ยม**
- **ป้ายชื่อช่องเล็ก ๆ อยู่ใต้กล่อง** (ศีรษะ · head / อาวุธ · R-hand / มือรอง · L-hand ...)
  เหมือนหน้าต่างของ RO ไม่ต้องเขียนชื่อช่องซ้ำในกล่อง
- ช่องที่ยังไม่ได้ใส่ของ = กล่องโล่ง ๆ (ดูชื่อช่องจากป้ายใต้กล่อง)
- ซ้าย 4 ช่อง (ศีรษะ · อาวุธ · ชุดเกราะ · ประดับ 1) · ขวา 4 ช่อง (ผ้าคลุม · มือรอง · รองเท้า · ประดับ 2)
- ตัวละครอยู่ตรงกลาง โชว์ท่ายืนของอาวุธที่ถืออยู่
- แท็บบนสุด: **อุปกรณ์ (equipment)** / **เงา (Shadow)** — แท็บเงายังไม่เปิดใช้

> อยากเปลี่ยนชื่อป้ายใต้กล่อง แก้ที่ `SLOT_CAPTION` ในหัวไฟล์ `equipment_window.gd`
> อยากได้กล่องใหญ่/เล็กลง แก้ `SLOT_SIZE` · รูปใหญ่ขึ้น แก้ `ICON_MAX`

### แถมมาด้วย: หน้าต่างไม่บวมค้างอีกแล้ว
เจอบั๊กเก่าตอนทำอันนี้ — **หน้าต่างทุกบานไม่เคยหดกลับ** เพราะไม่ได้อยู่ในคอนเทนเนอร์
(หน้าสวมใส่เคยสูงถึง 2389 px ทั้งที่เนื้อหาแค่ 436 px)

ตอนนี้ `GameWindow.show_window()` เรียก **`fit_to_content()`** ให้อัตโนมัติ
(รอ 1 เฟรมแล้ว `reset_size()`) — สำคัญมากเพราะกรอบหน้าต่างที่ใหญ่เกินจริง
จะไปกินคลิกเมาส์ในที่ที่ไม่มีอะไรอยู่ (ระบบคลิกซ้ายตีของหัวข้อ 7.20)

### ไฟล์ที่แก้
- `scripts/ui/equipment_window.gd` — จัดวางใหม่ทั้งหน้า
- `scripts/ui/game_window.gd` — เพิ่ม `fit_to_content()`

### ข้อควรระวัง
- `btn.get_node("SlotIcon")` **หาไม่เจอ** เพราะรูปอยู่ลึกเข้าไปในกล่องจัดวาง
  ต้องใช้ `btn.find_child("SlotIcon", true, false)`
- ตั้ง `size = get_combined_minimum_size()` ทันทีหลังซ่อน/โชว์หน้าย่อย **จะได้ค่าเก่า**
  ต้อง `await get_tree().process_frame` ก่อนเสมอ แล้วค่อย `reset_size()`

---

## 7.22 ตัวเลขดาเมจใหญ่ขึ้น + ไม่ทับกับ EXP

ข้อความลอยทุกอันตอนนี้ "จองที่" ก่อนโผล่ อันไหนจะทับของเดิมก็ดันขึ้นไปอีกชั้น
แล้วแยกทิศลอยตามชนิด:

| ชนิด | ลอยไปทาง | ขนาด |
|---|---|---|
| ดาเมจที่เราตีมอน | ตรงขึ้น | **34** (คริ **44**) |
| ดาเมจที่เราโดน | ตรงขึ้น | 32 |
| EXP / Job EXP | ขึ้นไปทางขวา | 18 |
| ของที่เก็บได้ / เงิน | ขึ้นไปทางซ้าย | 18 |
| MVP | ตรงขึ้น ค้างนานกว่า | 52 |

- อยากให้ตัวเลขดาเมจใหญ่กว่านี้อีก แก้ `DAMAGE_FONT_SIZE` / `DAMAGE_FONT_CRIT`
  ที่หัวไฟล์ `scripts/entities/monster_base.gd`
- อยากปรับทิศ/ระยะห่าง แก้ `START_OFFSET` และ `DRIFT` ใน `scripts/world/floating_text_layer.gd`

---

## 7.23 ★ ระบบ Base Level กับ Job Level แยกกัน ★

| หลอด | ได้จาก | ขึ้นเลเวลแล้วได้ |
|---|---|---|
| **Base EXP** (เขียว) | ฆ่ามอน / เควส | **แต้มสเตตัส** (Stat Point) + HP/SP เพิ่ม |
| **Job EXP** (ม่วง) | ฆ่ามอน / เควส | **แต้มสกิล** (Skill Point) + ATK/HIT/DEF เล็กน้อย |

- Base สูงสุด 99 · **Job สูงสุด 50** (`MAX_JOB_LEVEL` ใน `player_stats.gd`)
- บนจอโชว์ `Lv.42 / Job 18 นักดาบ` และมีหลอดม่วง JOB เพิ่มมาใต้หลอด EXP
- หน้าสเตตัส (C) โชว์ Base EXP / Job EXP เป็นตัวเลขเต็ม
- Job EXP ที่ได้จากมอน = ช่อง **Job Exp Reward** ใน MonsterData
  **ใส่ 0 = คิดให้เองเป็น 70% ของ EXP ปกติ** (มอนเก่าทุกตัวใช้ได้ทันที ไม่ต้องแก้)
- เควสก็มีช่อง **Reward Job Exp** เหมือนกัน (0 = 70% ของ Reward Exp)
- ไฟล์เซฟเก่ายังโหลดได้ จะเริ่มที่ Job Lv.1

---

## 7.24 ★ บอส King Poring + สกิลระเบิดเหมือก ★

### ต้องวาด Sprite ชื่ออะไรบ้าง
ไฟล์ `data/monsters/king_poring.tres` ช่อง **Sprite Frames** ตอนนี้ชี้ไปที่ `poring_frames.tres`
(ยืมของโพริงมาใช้ก่อนจะได้เล่นได้เลย) — พอวาดเสร็จให้สร้าง SpriteFrames ใหม่แล้วลากมาใส่ช่องนี้แทน

ชื่อท่าใน SpriteFrames:

| ชื่อท่า | ใช้ตอน |
|---|---|
| `Idle` | ยืนเฉย |
| `Run` | เดิน/ไล่ผู้เล่น (ตั้ง `Walk` หรือ `Move` ก็ได้) |
| `Attack` | ตีปกติ |
| **`Skill_SlimeBomb`** | **★ สกิลระเบิดเหมือก ★** |
| `Hit` | โดนตี (ตั้ง `Hurt` ก็ได้) |
| `Die` | ตาย (ตั้ง `Death` หรือ `Dead` ก็ได้) |

- ตัวพิมพ์เล็ก-ใหญ่ไม่สำคัญ (`skill_slimebomb` = `Skill_SlimeBomb`)
- ไม่มีท่า `Skill_SlimeBomb` ระบบจะถอยไปใช้ท่าชื่อ `Skill` แล้วถอยไป `Attack` ให้เอง
- อยากเปลี่ยนชื่อท่า แก้ช่อง **Skill Anim** ใน king_poring.tres ได้เลย

### ค่าของบอส
Lv.15 · HP 1800 · ATK 34-52 · DEF 12 · EXP 900 / Job 700 · เงิน 400-900z
เกิดใหม่ทุก 120 วินาที · ดรอปฟราคอน, น้ำยาส้ม, เจลโลปี้ และ **การ์ดคิงโพริง 5%**

### สกิลระเบิดเหมือก
ร่ายจากระยะ 380 px · รัศมีโดน 340 x 200 · ดาเมจ x2.4 · คูลดาวน์ 9 วิ · โอกาสร่าย 65%
หลบทันจะขึ้นคำว่า "หลบได้!" · ทุกค่าปรับได้ในกลุ่ม **"สกิลมอนสเตอร์"** ของ .tres

> มอนตัวอื่นก็ใส่สกิลได้เหมือนกัน แค่กรอกช่อง Skill Name + Skill Anim ใน .tres ของตัวนั้น
> ไม่ต้องแก้โค้ดเลย

### ป้าย MVP
บอสตาย = คำว่า **MVP** ตัวใหญ่สีทองเด้งเหนือหัวผู้เล่น + ข้อความแจ้งเตือน
เปลี่ยนคำได้ที่ช่อง **Boss Title** · ทำมอนตัวอื่นเป็นบอสก็แค่ติ๊ก **Is Boss**

---

## 7.25 หน้าตีบวกอุปกรณ์แบบเดียวกับร้านค้า

- ทุกแถวมี **ไอคอนไอเทมในกรอบสี่เหลี่ยม** เหมือนหน้าร้านค้า
- โชว์ระดับตีบวกปัจจุบัน (+0, +5) ไว้ขวาสุด
- กดที่ชื่อ = เลือก + เด้งกล่องรายละเอียดไอเทมข้าง ๆ · แถวที่เลือกอยู่มีกรอบสีทอง
- มุมขวาบนโชว์เงินที่มี

---

## 7.26 แมพใหม่ Asgard Forest 2

- ไฟล์: `scenes/maps/asgard_forest_2.tscn` · id = `asgard_forest_2`
- ประตูเดิมที่คุณทำไว้ชื่อ **ToTown2** (ป้าย "→ Asgard forest 2") ถูกต่อให้แล้ว
  และเพิ่มจุดเกิด **from_forest_2** ในแมพเดิม สำหรับตอนเดินกลับมา
- ในแมพใหม่มี: โพริง · แฟเบร · ลูนาติก กระจายทั่วแมพ + **จุดเกิดบอส King Poring**
  ประตูซ้ายกลับ Forest 1 · ประตูขวาเข้าเมือง
- **พื้นหลังเป็นสีเปล่า ๆ ไว้ก่อน** — วาดเสร็จแล้วเปิดแมพนี้ใน Godot
  เลือกโหนด `Background/Art` แล้วลากไฟล์ภาพใส่ช่อง **Texture** ได้เลย
  (ถ้าภาพใหญ่กว่าพื้นเดิม ปรับ `map_bounds` ของโหนด Map ตามด้วย)

### ไฟล์ที่แก้รอบนี้
`signal_bus` · `player_stats` · `player_state` · `game` · `monster_base` · `player` ·
`dropped_item` · `monster_data` · `quest_data` · `floating_text_layer` ·
`hud` · `ui_theme` · `ui_manager` · `status_window` · `refine_window`
\+ ไฟล์ใหม่ `data/monsters/king_poring.tres` · `data/cards/card_king_poring.tres` ·
`scenes/maps/asgard_forest_2.tscn`
สำรองแมพเดิมไว้ที่ `Sprites/world_node_2d_ก่อนต่อ forest2.tscn.bak`

### บั๊กเก่าที่เจอระหว่างทาง (แก้ให้แล้ว)
**แถบคำใบ้ปุ่มด้านล่างจอหลุดออกนอกจอมาตลอด** และข้อความชื่อแมพไปกองมุมซ้าย
เพราะ `HUD` กับ `UIRoot` ใช้ `set_anchors_preset()` ซึ่ง "รักษากรอบเดิม" ที่ยังกว้าง 0 อยู่
เปลี่ยนเป็น `set_anchors_and_offsets_preset()` แล้ว

---

## 7.27 ต่อสไปรท์ดาบซามู + แก้ของดรอปออร์ค

รันด้วย `python3 _patch_katana.py .` (รันซ้ำได้ ไม่ทำซ้ำซ้อน)

### 1) ออร์คนักรบดรอปของที่ไม่มีอยู่จริง
`orc_warrior.tres` ดรอป `blade` แต่ไอเทมนั้นถูกลบไปแล้ว (ของจริงคือ `novice_sword`)
ของชิ้นนี้เลยไม่เคยหล่นออกมาเลย → แก้เป็น `novice_sword` แล้ว

### 2) ท่าดาบซามู
เพิ่ม 2 ท่าลง `data/sprites/player_frames.tres`:

| ท่า | เฟรม | speed | ภาพ |
|---|---|---|---|
| `Attack_Katana` | 14 | 16 | spritesheet Attack Katana.png |
| `Attack_Katana_bash` | 15 | 13 | spritesheet Attack Katana bash.png |

สคริปต์ **อ่านจากไฟล์ภาพเอง** ว่าช่อง 512x512 ไหนมีรูปอยู่ แล้วเรียงซ้าย→ขวา บน→ล่าง
(วิธีเดียวกับที่ท่า Attack_falchion ทำไว้) — วาดใหม่แล้วลบท่าเดิมทิ้ง รันสคริปต์ซ้ำได้เลย

### 3) ไอเทมดาบซามู
- `attack_animation` → `Attack_Katana` (เดิมชี้ไปที่ `Attack_Blade` เลยใช้ท่าดาบมือใหม่)
- `id` `&"Katana"` → **`&"katana"`** ให้เป็นตัวพิมพ์เล็กเหมือนไอเทมอื่นทุกชิ้น
  (ถ้าปล่อยไว้ เวลาใส่ในรายการดรอป/ร้านค้าด้วยชื่อ `katana` จะหาไม่เจอ)

> **ดาบซามูยังไม่มีที่มาในเกม** — ไม่มีร้านไหนขาย ไม่มีมอนตัวไหนดรอป
> อยากให้ได้มา: เพิ่ม `&"katana"` ลง `shop_items` ของ NPC พ่อค้า หรือใส่ในรายการดรอปของมอน

### ข้อควรระวังตอนแก้ .tres ด้วยสคริปต์
**`sub_resource` ต้องอยู่ก่อนหัวข้อ `[resource]` เสมอ** — ถ้าแทรกไว้หลัง `[resource]`
(เช่นแทรกก่อนบรรทัด `animations = [`) Godot จะอ่านไฟล์ไม่ผ่านทั้งไฟล์
เจอจริงตอนทดสอบรอบนี้ ก่อนแตะไฟล์จริง

### สำรองไว้แล้ว
`data/sprites/player_frames_ก่อนใส่ katana.tres.bak` ·
`data/items/katana_ก่อนแก้.tres.bak` · `data/monsters/orc_warrior_ก่อนแก้.tres.bak`

---

## 7.28 สกิล Slash (พุ่งฟัน) + ผังสกิล + เอฟเฟกต์สกิล

### สกิล Slash
พุ่งตัวไปข้างหน้าเร็ว ๆ **มอนทุกตัวที่ขวางทางโดนดาเมจ** (ตัวละ 1 ครั้งต่อการพุ่ง 1 ครั้ง)
เงื่อนไข: **Bash เลเวล 5 + Base Level 15** · ไฟล์ `data/skills/slash.tres`

ค่าเริ่มต้น: พุ่งไกล 300 px (+15 ต่อเลเวลสกิล) · ความเร็ว 1400 px/วิ ·
ดาเมจ 160% (+30% ต่อเลเวล) · SP 16 · คูลดาวน์ 4 วิ · ชนกำแพงแล้วหยุด

ปรับได้ทั้งหมดในกลุ่ม **"พุ่ง (Dash)"** ของ .tres:
`dash_distance` · `dash_distance_per_level` · `dash_speed` · `dash_range_y` ·
`dash_hit_once` · `dash_stop_on_wall` · และ `max_targets` (0 = ไม่จำกัดจำนวนตัวที่โดน)

**ท่าที่ต้องวาด (ไม่บังคับ):** `Attack_<อาวุธ>_slash` เช่น `Attack_Blade_slash`
ไม่มีก็ถอยไปใช้ `Attack_slash` แล้วถอยไปท่าโจมตีปกติของอาวุธนั้น

> **ทำสกิลพุ่งตัวใหม่:** สร้าง .tres ใหม่ ตั้ง Type = **Active Dash** แล้วกรอกค่าในกลุ่ม "พุ่ง"
> ไม่ต้องแก้โค้ดเลย

### ผังสกิล (Skill Tree)
ระบบเงื่อนไขมีอยู่แล้วใน `SkillData` — รอบนี้ทำให้ **เห็นเป็นผังในหน้าต่างสกิล**:

- สกิลที่ต่อยอดจากสกิลอื่นจะเยื้องเข้าไปพร้อมเส้น `└─` อยู่ใต้สกิลแม่
- แถวที่เงื่อนไขยังไม่ครบ: ตัวหนังสือจาง · ขึ้นคำว่า **( ล็อก )** · ปุ่มเรียนกดไม่ได้ ·
  มุมขวาบอกเงื่อนไขเป็นสีแดง เช่น `Lv.15 · Bash 5`
- กดเลือกสกิลแล้วกล่องล่างบอกเงื่อนไขและเหตุผลที่ยังเรียนไม่ได้

**ตั้งเงื่อนไขสกิลใหม่** — ในไฟล์ .tres ของสกิลนั้น:

| ช่อง | ความหมาย |
|---|---|
| `required_level` | ต้อง Base Level เท่าไหร่ |
| `required_skills` | ต้องมีสกิลอะไรเลเวลเท่าไหร่ เช่น `{"bash": 5}` |

ใส่ได้หลายตัวใน `required_skills` · ตัวแรกในรายการจะถูกใช้จัดผัง (ถือเป็น "สกิลแม่")
**สกิลใหม่ต้องเพิ่ม id ลง `skill_ids` ใน `data/jobs/swordsman.tres` ด้วย** ไม่งั้นจะไม่ขึ้นในหน้าต่าง

---

## 7.29 ★ ทำเอฟเฟกต์สกิลให้ใหญ่/ไกลเกินตัวละคร ★

**ปัญหา:** ถ้าวาดเอฟเฟกต์ลงไปในสไปรท์ตัวละคร (player_frames) เอฟเฟกต์จะโตได้แค่เท่ากรอบภาพตัวละคร
และพุ่งออกไปไกลกว่านั้นไม่ได้

**วิธีแก้:** เอฟเฟกต์เป็น **โหนดแยกที่ไปเกิดในแมพ** ไม่ใช่ลูกของตัวละคร
(ไฟล์ `scripts/entities/skill_effect.gd`) เลยใหญ่แค่ไหน ไกลแค่ไหนก็ได้

### วิธีใช้ — ไม่ต้องเขียนโค้ดเลย
1. วาดเอฟเฟกต์เป็นภาพชุด แล้วสร้าง **SpriteFrames** ใหม่ 1 ไฟล์ (คนละไฟล์กับตัวละคร)
2. เปิด .tres ของสกิล → กลุ่ม **"เอฟเฟกต์สกิล"** → ลากใส่ช่อง **Effect Frames**
3. ปรับตัวเลขให้ได้ตำแหน่ง/ขนาดที่ชอบ

| ช่อง | ความหมาย |
|---|---|
| `effect_frames` | ภาพเอฟเฟกต์ (เว้นว่าง = ไม่มีเอฟเฟกต์) |
| `effect_anim` | ชื่อท่าใน SpriteFrames นั้น (เว้นว่าง = ใช้ท่าแรกที่ไม่ใช่ `default`) |
| `effect_offset` | เยื้องจากตัวละครกี่ px · **x กลับข้างให้เองตามที่ตัวละครหัน** |
| `effect_height` | อยากให้เอฟเฟกต์สูงกี่ px บนจอ (0 = ใช้ `effect_scale`) |
| `effect_speed` | พุ่งออกไปข้างหน้ากี่ px/วินาที (0 = อยู่กับที่) |
| `effect_follow` | เกาะไปกับตัวละคร (ใช้กับสกิลพุ่ง) |
| `effect_life` | อยู่กี่วินาที (0 = จนกว่าอนิเมชันจะเล่นจบ) |
| `effect_delay` | หน่วงกี่วินาทีถึงจะโผล่ |
| `effect_z` | ชั้นการวาด (60 = อยู่หน้าตัวละคร) |

### ตัวอย่างการตั้งค่า
- **ฟันแล้วมีคลื่นพุ่งออกไป:** `effect_offset (110, -60)` · `effect_speed 900` · `effect_life 0.6`
- **เอฟเฟกต์วิ่งไปกับตัวตอนพุ่ง (Slash ใช้แบบนี้):** `effect_follow` เปิด · `effect_life 0.45`
- **ระเบิดรอบตัว:** `effect_offset (0, -40)` · `effect_speed 0` · `effect_height` ใหญ่ ๆ
- **เอฟเฟกต์ใหญ่กว่าตัวละคร 2 เท่า:** ตัวละครสูง 240 px → ตั้ง `effect_height` 400-500 ได้เลย

> ใช้ได้กับสกิลทุกชนิด ไม่ใช่แค่ Slash — ใส่ Effect Frames ให้ bash / magnum_break ก็ได้เหมือนกัน

### ไฟล์ที่แก้รอบนี้
`skill_data.gd` (เพิ่ม type **Active Dash** + กลุ่มพุ่ง + กลุ่มเอฟเฟกต์) ·
`player.gd` (การพุ่ง + เรียกเอฟเฟกต์) · **ไฟล์ใหม่** `skill_effect.gd` ·
`skill_window.gd` (ผังสกิล) · **ไฟล์ใหม่** `data/skills/slash.tres` ·
`swordsman.tres` (เพิ่ม slash ลงสายอาชีพ · สำรองไว้ที่ `swordsman_ก่อนเพิ่ม slash.tres.bak`)

### ข้อควรระวัง
- **enum `SkillType` ต้องเพิ่มต่อท้ายเท่านั้น** (Active Dash = 5) เพราะไฟล์ .tres
  เก็บค่า type เป็น "ตัวเลข" — สลับลำดับเมื่อไหร่ สกิลเก่ากลายเป็นคนละชนิดทันที
- อีโมจิ (🔒) ในหน้าต่างเกมขึ้นเป็นสี่เหลี่ยม เพราะฟอนต์ไทยไม่มีตัวอีโมจิ — ใช้ข้อความธรรมดาแทน

---

## 7.30 ★ ปรับขนาดมอนสเตอร์เอง (ทำเองได้ทุกตัว) ★

เปิด `data/monsters/<ชื่อมอน>.tres` ใน Godot แล้วดูที่ Inspector — มี **4 ช่อง** ที่เกี่ยวกับขนาด

| ช่อง | ความหมาย | ควรตั้งเท่าไหร่ |
|---|---|---|
| **Display Height** | อยากให้ตัวสูงกี่พิกเซลบนจอ | **ปรับตัวนี้ตัวเดียวก็พอ** |
| Hitbox Size | กล่องชน (กว้าง, สูง) | ประมาณ 0.5 เท่าของ Display Height |
| Hp Bar Offset Y | หลอดเลือดลอยสูงจากจุดกึ่งกลางเท่าไหร่ | ติดลบ · ประมาณ −0.8 เท่าของความสูง |
| Attack Range | ระยะที่มอนตีถึง | ตัวใหญ่ควรมากกว่าตัวเล็ก |

**ทำไมแค่ Display Height ก็พอ:** ระบบวัดขนาดจริงของภาพ (ไม่นับพื้นที่โปร่งใส)
แล้วย่อ/ขยายให้สูงเท่าที่ตั้งไว้เสมอ — ไฟล์ภาพจะใหญ่แค่ไหนก็ได้ ไม่ต้องมานั่งคำนวณ Scale เอง
และจัดเท้าให้แตะพื้นให้อัตโนมัติด้วย (ช่อง Align Feet)

**ตัวอย่าง** ตอนนี้ King Poring ตั้งไว้:
```
Display Height  = 300     (โพริงธรรมดา 85 → ใหญ่กว่า 3.5 เท่า)
Hitbox Size     = 170 x 160
Hp Bar Offset Y = -250
Attack Range    = 150
```
อยากให้ใหญ่กว่านี้อีก เช่น 400 px ก็แก้ Display Height เป็น 400 แล้วขยับอีก 3 ช่องตามสัดส่วน
(หรือรัน `python3 _patch_bosssize.py .` หลังแก้ตัวเลขในไฟล์ `_patch_bosssize.py` ก็ได้)

> **ตัวละครผู้เล่นใช้กติกาเดียวกัน** — ช่อง **Auto Fit Height** ในโหนด Player (ตอนนี้ 240)

---

## 7.31 เอฟเฟกต์สกิลของมอนสเตอร์และบอส

กติกาเดียวกับเอฟเฟกต์สกิลของผู้เล่น (หัวข้อ 7.29) แต่ตั้งใน **MonsterData** กลุ่ม **"เอฟเฟกต์สกิลมอน"**

| ช่อง | ความหมาย |
|---|---|
| `skill_effect_frames` | SpriteFrames ของเอฟเฟกต์ (เว้นว่าง = ไม่มีเอฟเฟกต์) |
| `skill_effect_anim` | ชื่อท่าใน SpriteFrames นั้น |
| `skill_effect_offset` | เยื้องจากตัวมอนกี่ px (x กลับข้างตามที่มอนหันให้เอง) |
| `skill_effect_height` | อยากให้เอฟเฟกต์สูงกี่ px บนจอ |
| `skill_effect_speed` | พุ่งออกไปข้างหน้ากี่ px/วิ |
| `skill_effect_follow` | เกาะไปกับตัวมอน |
| `skill_effect_life` | อยู่กี่วินาที (0 = ตามความยาวอนิเมชัน) |
| `skill_effect_delay` | หน่วงกี่วิถึงโผล่ — **ตั้งเท่า Skill Windup จะระเบิดพร้อมดาเมจพอดี** |

**ตัวอย่างสำหรับ "ระเบิดเหมือก" ของ King Poring:**
`skill_effect_height 380` · `skill_effect_offset (0, -60)` · `skill_effect_speed 0` ·
`skill_effect_follow` ปิด · `skill_effect_delay 0.75` (= Skill Windup)

ตอนนี้ครบทั้ง 3 ฝั่งแล้ว: **ผู้เล่น · มอนสเตอร์ · บอส** ใช้โหนดเอฟเฟกต์ตัวเดียวกัน
(`scripts/entities/skill_effect.gd`) เลยใหญ่/ไกลเกินตัวได้เหมือนกันหมด

---

## 7.32 ผังสกิลแบบใหม่ (หน้าต่างสกิล กด K)

หน้าตาเป็นตารางแบบเกมจริงแล้ว — หนึ่งช่องประกอบด้วย

```
   [ 5/10 ]     <- ป้ายเลเวล
   ( ไอคอน )
   [  Bash  ]   <- ป้ายชื่อ
   [เรียน +1]
```

- สกิลลูกอยู่ **แถวถัดลงไป** พร้อม **เส้นโยง** จากสกิลแม่ลงมา
- สกิลแม่จะอยู่ **กึ่งกลางเหนือกลุ่มลูก** ของตัวเอง
- ช่องที่เงื่อนไขยังไม่ครบ = **สีเทา + เครื่องหมาย !** และปุ่มเรียนกดไม่ได้
- กดที่ไอคอนเพื่อเลือก แล้วดูรายละเอียด/เงื่อนไขในกล่องด้านล่าง

ปรับหน้าตาได้ที่หัวไฟล์ `scripts/ui/skill_window.gd`:
`TILE` (ขนาดไอคอน) · `COL_W` (ความกว้างคอลัมน์) · `GAP_X` / `GAP_Y` (ระยะห่าง)

---

## 7.33 ★ แถบปุ่มลัดใหม่ + ช่องยา HP/SP ★

### หน้าตา
แถบปุ่มลัด **แยกออกมาเป็นแผงของตัวเอง** อยู่ใต้แผงหลอดเลือด ช่องเป็น **สี่เหลี่ยมจัตุรัส 58x58**

```
[1][2][3][4]   [Q ยาเลือด][R ยามานา]   [Tab เมนู]
```

- ช่องยาโชว์ **รูปยาที่ตั้งไว้** + **จำนวนที่เหลือ** มุมขวาล่าง (เหลือ 0 = เลขเป็นสีแดง)
- ปุ่ม **Q** = ยาเลือด · ปุ่ม **R** = ยามานา (ปุ่ม R เพิ่มใหม่ ระบบสร้างให้ตอนเปิดเกม ไม่ต้องรีสตาร์ท)

### เลือกยาเองได้
เปิดกระเป๋า (I) → **กดที่ยา** → แถว "ตั้งเป็นยาด่วน" จะโผล่ขึ้นมา → กด **ช่อง Q** หรือ **ช่อง R**
ยาที่ตั้งอยู่แล้วจะมีดาว ★ นำหน้าปุ่ม · ยาชิ้นเดียวกันอยู่ได้ช่องเดียว · **เซฟไปกับไฟล์เซฟด้วย**
(เลือกได้เฉพาะของประเภทของกิน/ยา — ของสวมใส่แถวนี้จะไม่โผล่)

### ปรับเอง
`scripts/ui/hud.gd` หัวไฟล์:
`HOTKEY_SIZE` (ขนาดช่อง) · `HOTKEY_PANEL_POS` (ตำแหน่งแผง — ค่าเริ่มต้น 12, 150)

### ข้อควรระวังที่เจอตอนทำ
**ปุ่มของ Godot กันที่ให้ข้อความ 1 บรรทัดเสมอ** ต่อให้ text ว่างเปล่า —
ช่องเลยสูงเกินจนไม่เป็นจัตุรัส (58 กลายเป็น 62) แก้โดยตั้ง `font_size` ของปุ่มเป็น **1**
แล้วย้ายข้อความทั้งหมดไปไว้ที่ "ป้ายซ้อน" (Label ลูกของปุ่ม) แทน

### ไฟล์ที่แก้รอบนี้
`player_state` (ช่องยาด่วน + เซฟ) · `input_setup` (ปุ่ม R) · `player` (กด Q/R) ·
`skill_effect` (รองรับมอน) · `monster_base` · `monster_data` (ช่องเอฟเฟกต์) ·
`hud` (แถบปุ่มลัดใหม่) · `ui_manager` · `inventory_window` (ตั้งช่องยา) · `skill_window` (ผังกริด)
สำรอง: `data/monsters/king_poring_ก่อนขยาย.tres.bak`

---

## 7.34 แถบปุ่มลัด — ย้ายไปข้างขวาหลอดเลือด และลากย้ายได้ (รอบ 21)

### ตำแหน่งเริ่มต้น
แถบปุ่มลัดไม่อยู่ข้างล่างแผงหลอดเลือดแล้ว ย้ายไป **ข้างขวา แถวเดียวกัน**
เป็นคนละแผงกัน มีช่องไฟคั่นไว้ ไม่ได้เชื่อมติดกัน

ระยะห่างแก้ได้ที่ `scripts/ui/hud.gd`:

```gdscript
const HOTKEY_GAP_X := 10.0        # ห่างจากแผงหลอดเลือดกี่พิกเซล
const HOTKEY_SIZE := Vector2(58, 58)   # ขนาดช่อง (จัตุรัส)
```

ระบบคำนวณตำแหน่งเองจากความกว้างจริงของแผงหลอดเลือด (`default_hotkey_pos()`)
ถ้าเพิ่มหลอดใหม่จนแผงกว้างขึ้น แถบปุ่มลัดจะขยับตามให้เอง

### ลากย้ายได้
- **ลากพื้นที่ว่างบนแผง** หรือ **ลากที่จุด `[::]` ด้านซ้ายของแถบ** = ย้ายไปวางตรงไหนก็ได้
- ปุ่มในแถบยังกดได้ตามปกติ (ลากโดนแต่พื้นแผง ไม่โดนปุ่ม)
- ลากออกนอกจอไม่ได้ ระบบดึงกลับให้อยู่ในจอเสมอ (`_clamp_hotkey_panel()`)
- **คลิกขวาบนแผง = คืนตำแหน่งเดิม** (กลับไปอยู่ข้างขวาแผงหลอดเลือด)

### ตำแหน่งถูกจำไว้
เก็บที่ `user://ui_layout.cfg` (คนละไฟล์กับไฟล์เซฟ ใช้ร่วมกันทุกช่องเซฟ)

```
[hud]
hotkey_pos = Vector2(318, 12)
```

บน Windows ไฟล์นี้อยู่ที่ `%APPDATA%\Godot\app_userdata\<ชื่อโปรเจกต์>\ui_layout.cfg`
ถ้าอยากรีเซ็ตตำแหน่ง ลบไฟล์นี้ทิ้ง หรือคลิกขวาบนแถบก็ได้

### เบื้องหลัง (ถ้าจะทำแผงอื่นให้ลากได้บ้าง)
1. แผง (`PanelContainer`) ต้องเป็น `MOUSE_FILTER_STOP` — ถ้าเป็น PASS จะลากไม่ติด
2. แถวข้างใน (`HBoxContainer`) ต้องเป็น `MOUSE_FILTER_IGNORE` — คลิกจะได้ทะลุไปโดนแผง
   **ปุ่มที่เป็นลูกยังกดได้ตามปกติ** (IGNORE ที่พ่อไม่ได้บังลูก)
3. จับ `gui_input` ของแผงเพื่อ "เริ่มลาก" แล้วจับ `_input()` ที่ระดับ HUD เพื่อ "ลากต่อ"
   — เพราะลากเร็ว ๆ เมาส์จะหลุดออกนอกแผง แล้ว `gui_input` จะหยุดส่งเหตุการณ์
4. `UI.is_point_over_ui()` ต้องรู้จักแผงนี้ด้วย ไม่งั้นคลิกบนแถบแล้วตัวละครจะตี

### กับดักที่เจอ
- **ห้ามตั้ง `.text` ให้ปุ่มช่องลัดเด็ดขาด** — Godot จะกันที่ให้ข้อความ 1 บรรทัดเสมอ
  ช่องจะสูงเกินจนไม่เป็นจัตุรัส ข้อความทั้งหมดอยู่ที่ป้ายซ้อน `SlotName` / `SlotCount` แล้ว
  (โค้ดเก่าใน `_process()` ที่เขียน `potion_button.text` ถูกลบออกแล้ว)

---

## 7.35 ระยะการฟัน — วัดจาก "ขอบตัวมอน" ไม่ใช่กลางตัว (รอบ 22)

### ปัญหาเดิม
ระบบเก่าวัด **จุดกึ่งกลางถึงจุดกึ่งกลาง** เลยเพี้ยน 3 แบบ

1. **บอสตัวใหญ่ต้องเดินไปประชิดมาก** — กึ่งกลางบอสอยู่ลึกเข้าไป 85 px จากขอบตัว
   ยืนติดตัวบอสแล้วยังตีไม่ถึงกึ่งกลาง
2. **มอนที่ยืนทับ/เราเหยียบอยู่ไม่โดนเลย** — อยู่ข้างหลังนิดเดียวก็ถูกตัดทิ้งทั้งตัว
   และตัวที่อยู่ใต้เท้าก็หลุดเงื่อนไขแนวตั้ง
3. **โพริงที่กำลังกระโดดไม่โดน** — ปลายเท้าลอยขึ้นเกินระยะแนวตั้ง

### ระบบใหม่: กรอบชนกรอบ
- **กรอบดาบ** (`player.attack_rect()`) เป็นสี่เหลี่ยมรอบตัวเรา
- **กรอบตัวมอน** (`monster.body_rect()`) เป็นสี่เหลี่ยมเท่าขนาดตัวจริงบนจอ
- สองกรอบแตะกันเมื่อไหร่ = โดน (ขอบชนขอบก็นับ)

### ค่าที่ปรับได้ (เลือก Player ใน Scene แล้วดูใน Inspector)

| ช่อง | ค่าใหม่ | ความหมาย |
|---|---|---|
| `Attack Range X` | **150** (เดิม 110) | ดาบยื่นไปข้างหน้ากี่พิกเซล วัดถึง **ขอบ** ตัวมอน |
| `Attack Range Y` | **200** (เดิม 70) | เอื้อมขึ้นเหนือปลายเท้าเท่าไหร่ (ครอบทั้งตัวขึ้นไปบนหัว) |
| `Attack Back Reach` | **55** | เอื้อมไปข้างหลังเท่าไหร่ — ตัวที่เบียด/ทับเราอยู่จะได้โดน |
| `Attack Reach Down` | **40** | ฟันต่ำกว่าปลายเท้าลงไปเท่าไหร่ — ตัวที่เราเหยียบอยู่จะได้โดน |

**ระยะจริงที่ได้ = 150 + ครึ่งความกว้างตัวมอน**
โพริง (กว้าง 50) → 175 px · King Poring (กว้าง 170) → **235 px**

### กรอบตัวมอนคิดมาจากอะไร (`monster_base.body_size()`)
- ความสูง = `Display Height` (ถ้าไม่ได้ตั้ง จะวัดจากสไปรท์จริง)
- ความกว้าง = `Hitbox Size.x` แต่ไม่น้อยกว่า 35% ของความสูง (กันตัวสูงแต่กล่องแคบจนฟันยาก)
- **ขยายมอนให้ใหญ่ขึ้น = กรอบนี้ใหญ่ตามเอง ไม่ต้องไปแก้ระยะที่ไหนอีก**

### ระยะตีของมอนก็แก้ตามด้วย
`monster_base.attack_reach()` = `Attack Range` ที่ตั้งใน .tres **+ ครึ่งความกว้างตัวมัน**
บอสจึงตีถึงจากขอบตัวมัน ไม่ต้องเอากึ่งกลางตัวมาจ่อผู้เล่น
(King Poring: ตั้งไว้ 150 → ใช้จริง 235)

### สกิลใช้กรอบเดียวกันหมด
`range_x` / `range_y` ใน SkillData ความหมายเปลี่ยนตามด้วย —
`range_x` = ยื่นไปข้างหน้าถึง **ขอบ** ตัวมอน · `range_y` = เอื้อมขึ้นเหนือปลายเท้า
สกิลรอบตัว (Magnum Break) และสกิลพุ่ง (Slash) ใช้กรอบสมมาตรซ้าย-ขวา

### อยากดูกรอบเองตอนเล่น
ใส่โค้ดนี้ชั่วคราวใน `player.gd`:

```gdscript
func _draw() -> void:
    var r := attack_rect(attack_range_x, attack_range_y)
    draw_rect(Rect2(r.position - global_position, r.size), Color.RED, false, 3.0)

func _process(_d): queue_redraw()
```

---

## 7.36 ผังสกิลโฉมใหม่ — ปุ่ม + บนรูป · กล่องรายละเอียดเด้งข้าง ๆ (รอบ 23)

### เปลี่ยนอะไรบ้าง
| เดิม | ใหม่ |
|---|---|
| ปุ่ม **"เรียน +1"** เป็นแถบใต้ทุกช่อง (มีตลอด กดไม่ได้ก็ยังอยู่) | **ปุ่มกลม `+` สีเขียวที่มุมขวาล่างของรูป** โผล่เฉพาะตอนอัพได้จริง |
| แถบข้อความรายละเอียดอัดอยู่ท้ายหน้าต่าง | **กดที่รูป → กล่องเด้งข้าง ๆ หน้าต่าง พร้อมไอคอนใหญ่** (แบบเดียวกับดูไอเทม) |
| แถวปุ่ม "ตั้งปุ่มลัด 1-4" ค้างอยู่ท้ายหน้าต่าง | ย้ายไปอยู่ในกล่องรายละเอียด (โผล่เฉพาะสกิลที่เรียนแล้ว) |

**หลักคิด:** ในผังเหลือแค่ "ดู" กับ "กด +" · ส่วน "อ่านรายละเอียด / ตั้งช่องลัด"
ไปอยู่ในกล่องที่เปิดตอนเลือกสกิล — หน้าต่างเลยโล่งและเข้าใจง่ายกว่าเดิม

### ปุ่ม + (`skill_window.gd`)
- ขนาดแก้ที่ `const PLUS_SIZE := 24.0`
- **โผล่เฉพาะตอน `book.can_learn()` เป็นจริง** — เงื่อนไขครบ + มีแต้ม + ยังไม่เต็มเลเวล
  เห็นปุ่มเมื่อไหร่ = กดได้แน่นอน ไม่ต้องเดา
- tooltip บอกชัด: "ใช้ 1 แต้มสกิล อัพ Bash เป็นเลเวล 6"
- ★ ปุ่มต้องอยู่ **ในกรอบ** ของปุ่มไอคอน ★ เพราะปุ่มไอคอนตั้ง `clip_contents = true`
  ล้นออกไปแม้แต่นิดเดียวก็โดนตัดหายทั้งปุ่ม (เจอมาแล้ว)

### กล่องรายละเอียด
ใช้ `ItemInfoPopup` ตัวเดียวกับกล่องไอเทม เพิ่มความสามารถใหม่ 2 อย่าง:

```gdscript
UI.show_info(title, icon, body_bbcode, anchor, color, actions)
```
`actions` เป็นรายการ Dictionary:

| รูปแบบ | ความหมาย |
|---|---|
| `{"text": "...", "on": Callable, "disabled": bool}` | ปุ่มเต็มความกว้าง |
| `{"row": [ {...}, {...} ]}` | หลายปุ่มในแถวเดียว (ใช้กับช่องลัด 1-4) |
| `{"hint": "ข้อความ", "hint_color": Color}` | บรรทัดคำอธิบาย ไม่ใช่ปุ่ม |

ในกล่องสกิลจะมี: ประเภท · เลเวล x/y · เงื่อนไข (เขียว=ครบ แดง=ไม่ครบ) ·
ค่าดาเมจ/ฟื้นฟู/บัฟตามเลเวลปัจจุบัน · SP + คูลดาวน์ · คำอธิบาย ·
ปุ่ม "อัพเป็นเลเวล N (ใช้ 1 แต้ม)" · ปุ่มช่องลัด 1-4 (ช่องที่ตั้งไว้แล้วมี `*`)

### ขนาดและตำแหน่งหน้าต่าง
- หน้าต่างผังสกิลย้ายไปที่ **(60, 110)** (เดิม 240, 130) และกว้าง 700
  **เหตุผล:** กล่องรายละเอียดกว้าง 380 ต้องมีที่ยืนข้างขวา ไม่งั้นมันจะเด้งไปทับผังเอง
  ถ้าจะขยายหน้าต่างผังอีก ต้องเช็คว่า `x + กว้าง + 8 + 380 <= 1276` ไม่งั้นกล่องจะทับ
- ความกว้างผัง = `max_col * (COL_W + GAP_X) + COL_W` — **ห้ามบวกช่องไฟของช่องสุดท้าย**
  ไม่งั้นกว้างเกินหน้าต่างนิดเดียว แล้วมีแถบเลื่อนแนวนอนโผล่มาโดยไม่จำเป็น
- ชื่อสกิลยาวเกิน 11 ตัวจะถูกตัด (`_short()`) เช่น "Magnum Break" → "Magnum Brea."

### เทสต์
`hud2_test` (91 เคส) ครอบคลุมทั้งหมด — ปุ่ม + อยู่บนรูป · ช่องที่ล็อกไม่มีปุ่ม + ·
กดสกิลแล้วกล่องเปิด · กล่องมีไอคอน/เลเวล/ปุ่มอัพ/ปุ่มช่องลัด · กดปุ่มในกล่องแล้วเลเวลขึ้นจริง

**กับดักที่เจอตอนเขียนเทสต์:** lambda ใน GDScript จับตัวแปรนอกแบบ "สำเนา" —
เขียน `var found = null` แล้วให้ lambda ไปตั้งค่า จะไม่ได้ค่าออกมา ต้องใส่ผลลง `Array` แทน

---

## 7.37 กล่องสนทนา — คุยกับตัวละครแบบมีรูปครึ่งตัว (รอบ 24)

เควส/บทคุยไม่ใช้ "กล่องยืนยันกลางจอ" แล้ว เปลี่ยนเป็น **กล่องสนทนาล่างจอ + รูปตัวละครยืนข้าง ๆ**
เพื่อให้ใส่เนื้อเรื่องยาว ๆ ได้ลื่น (ไฟล์ `scripts/ui/dialogue_box.gd`)

### หน้าตา
- กล่องข้อความอยู่ล่างจอ · **รูปตัวละครยืนติดขอบล่าง ล้ำขึ้นไปเหนือกล่อง** · กล่องหลบให้รูปเอง
- ป้ายชื่อผู้พูดลอยอยู่มุมบนของกล่อง (คนพูดอยู่ฝั่งขวา ป้ายจะย้ายไปขวา)
- ข้อความ **ขึ้นทีละตัวอักษร** — กด F / Enter / คลิก 1 ครั้ง = ขึ้นครบทันที · กดอีกที = บรรทัดถัดไป
- **คนที่ไม่ได้พูดจะหรี่ลง** เหมือนเกม RPG ทั่วไป
- แถบคำใบ้ปุ่มล่างจอถูกซ่อนอัตโนมัติระหว่างคุย

### เขียนบทสนทนา

```gdscript
var pick: int = await UI.talk([
    {"name": "ช่างตีเหล็กฮันส์", "portrait": tex, "side": 0, "text": "ว่าไงเจ้าหนู"},
    {"name": "คุณ",             "side": 1,       "text": "มีงานให้ทำมั้ย"},
    {"name": "ช่างตีเหล็กฮันส์", "side": 0, "text": "มีสิ",
     "info": "เงื่อนไข: ล่าโพริง 100 ตัว\nรางวัล: ...",
     "choices": ["รับเควส", "ไว้ก่อน"]},
])
# pick = ลำดับปุ่มที่กด (0 = อันแรก) · -1 = บทนี้ไม่มีตัวเลือก · Esc = อันสุดท้าย
```

| คีย์ | ความหมาย |
|---|---|
| `name` | ชื่อผู้พูด (ไม่ใส่ = ไม่โชว์ป้ายชื่อ) |
| `text` | ข้อความ (ใส่ BBCode ได้ เช่น `[color=#ffe14a]...[/color]`) |
| `portrait` | `Texture2D` หรือ path `"res://..."` — ไม่ใส่ = ใช้รูปเดิมของฝั่งนั้น |
| `side` | 0 = รูปซ้าย · 1 = รูปขวา |
| `info` | ข้อความเสริมตัวเล็กสีทอง (เงื่อนไข/รางวัล) |
| `choices` | รายชื่อปุ่ม — มีเมื่อไหร่จะรอให้เลือกก่อนไปต่อ |

คุยประโยคเดียวจบ: `await UI.say_as("ชื่อ", "ข้อความ", รูป, ฝั่ง)`

### ใส่รูปให้ NPC
1. วาดรูป **ครึ่งท่อนบน (หัวถึงเอว) พื้นหลังโปร่งใส สูง 400-500 px**
2. เซฟไว้ที่ `Sprites/portraits/<ชื่อ>.png`
3. ใน Godot เลือก NPC คนนั้น → กลุ่ม **"รูปตัวละครในกล่องสนทนา"**
   - `Portrait` — ลากไฟล์ภาพมาใส่ (ทางที่ง่ายสุด)
   - `Portrait File` — หรือพิมพ์ path เช่น `res://Sprites/portraits/tony.png`
   - `Portrait Side` — ซ้าย / ขวา
4. ไม่ใส่รูปก็คุยได้ปกติ กล่องจะกินเต็มความกว้างแทน

**ทำตัวอย่างไว้แล้ว:** ช่างตีเหล็กฮันส์ (`Sprites/portraits/hans.png`)
— ตัดมาจากภาพเต็มตัวที่ส่งมา เอาเฉพาะหัวถึงเอว แล้วย่อให้สูง 420 px

### ตัดรูปเองด้วย python (ถ้ามีภาพเต็มตัว)
```python
from PIL import Image
im = Image.open("ภาพเต็มตัว.png").convert("RGBA")
im = im.crop(im.getbbox())                    # ตัดขอบโปร่งใสทิ้ง
w, h = im.size
bust = im.crop((0, 0, w, int(h * 0.56)))      # เอาแค่ 56% บน = หัวถึงเอว
bust = bust.crop(bust.getbbox())
k = 420 / bust.size[1]
bust.resize((int(bust.size[0] * k), 420)).save("Sprites/portraits/ชื่อ.png")
```

### NPC พูดเองอัตโนมัติ
- `npc.line("ข้อความ", "ข้อมูลเสริม", ["ปุ่ม1","ปุ่ม2"])` = สร้างบทพูด 1 บรรทัดพร้อมชื่อ+รูปของ NPC คนนั้น
- NPC แบบ **DIALOG** (ไกด์): ช่อง `Dialog` **เว้นบรรทัดว่าง 1 บรรทัด = ขึ้นหน้าใหม่** ในกล่อง
- เควส: ชวนรับ / บอกความคืบหน้า / ส่งเควส ใช้กล่องนี้หมดแล้ว

### ปรับขนาด
`dialogue_box.gd`: `BOX_HEIGHT 150` (ส่วนข้อความ) · `INFO_H 44` · `CHOICE_H 44` ·
`PORTRAIT_HEIGHT 330` (รูปสูงบนจอ) · `PORTRAIT_SLOT 300` (กล่องหลบให้กี่ px) ·
`TEXT_SPEED 55` (ตัวอักษรต่อวินาที) · `BOX_MARGIN 28`

### กับดักที่เจอ
- **ป้ายที่ตัดบรรทัดเอง (autowrap) ตอนความกว้างยังเป็น 0 จะคิดความสูงขั้นต่ำเป็นพันพิกเซล**
  (เพราะมันตัดบรรทัดทีละตัวอักษร) แล้วดันกล่องยาวทะลุจอ —
  แก้ด้วยการตั้ง `custom_minimum_size.x` ให้ป้ายก่อน แล้วจัดกล่องซ้ำอีกรอบหลังผ่านไป 1 เฟรม
- **กล่องที่สูงเปลี่ยนไปตามเนื้อหา ต้องยึด "ขอบล่าง"** ไม่งั้นพอมีปุ่มตัวเลือกกล่องจะงอกลงนอกจอ

### หน้าตากล่อง (ปรับรอบ 25)
กล่องเปลี่ยนเป็น **พื้นขาวนวล ตัวหนังสือเข้ม** ไม่มีขอบทองแล้ว อ่านง่ายขึ้นเยอะ

| ค่าคงที่ใน `dialogue_box.gd` | ค่า | ความหมาย |
|---|---|---|
| `BOX_BG` | `#f6f3ebfa` | พื้นกล่อง (ขาวนวล) |
| `BOX_EDGE` | `#2b3346` | เส้นขอบบาง 2 px มุมมน 14 + เงาใต้กล่อง |
| `NAME_BG` | `#232c42fa` | ป้ายชื่อ พื้นเข้ม ตัวหนังสือทอง ตัดกับกล่องขาว |
| `TEXT_DARK` | `#1c2231` | ตัวหนังสือหลัก |
| `INFO_DARK` | `#8a5a12` | ข้อความเสริม (เงื่อนไข/รางวัล) |
| `FONT_TEXT` | **22** | ขนาดตัวหนังสือหลัก (เดิม 17) |
| `FONT_NAME` / `FONT_INFO` / `FONT_HINT` | 19 / 15 / 13 | ป้ายชื่อ / ข้อความเสริม / คำใบ้ |
| `PAD_X` `PAD_TOP` `PAD_BOTTOM` | 22 / 16 / 12 | ขอบในกล่อง (ใช้ `MarginContainer` ชื่อ `Pad`) |

**ที่แก้เพิ่ม**
- **ป้ายชื่อวางเหนือกล่องทั้งป้าย** (เดิมคร่อมขอบกล่องอยู่ครึ่งหนึ่ง เลยบังบรรทัดแรก)
- **กล่องไม่ทับรูปตัวละครแล้ว** — กล่องทึบ ทับเมื่อไหร่ตัวละครโดนบังครึ่งตัว
  (`box_left = BOX_MARGIN + PORTRAIT_SLOT` ไม่ลบ 40 เหมือนเดิม)
- **รูปตัวละครวาดทีหลังกล่อง** (`move_child`) ตัวละครจะได้ยืน "หน้า" กล่องเสมอ
- มีเส้นคั่นบาง ๆ เหนือข้อความเสริม · ปุ่มตัวเลือกใหญ่ขึ้น (สูง 38 ฟอนต์ 16)
- ฉากหลังมืดลงนิด (0.42) ให้กล่องขาวเด่นขึ้น

---

## 7.38 ปุ่มจอสัมผัส (มือถือ) + เอาขึ้นเว็บ (รอบ 26)

### ปุ่มจอสัมผัส — `scripts/ui/touch_controls.gd`

**ทำไมต้องเขียนเอง ไม่ใช้ Button ธรรมดา**
Godot แปลง "นิ้วแรก" เป็นเมาส์ให้เท่านั้น ถ้าใช้ Button ปกติจะกดได้ทีละปุ่ม
เดินไปตีไปไม่ได้ ไฟล์นี้เลยอ่าน `InputEventScreenTouch` เองแยกตามหมายเลขนิ้ว (multi-touch)

**ปุ่มส่งออกเป็น "action" ทั้งหมด** เกมส่วนอื่นเลยไม่ต้องแก้อะไรเลย
ส่ง 2 ทางพร้อมกันเพื่อให้ครบทุกแบบที่โค้ดเช็ค:
- `Input.action_press(action)` — สำหรับโค้ดที่เช็คแบบ polling (`Input.is_action_pressed`)
- `Input.parse_input_event(InputEventAction)` — สำหรับโค้ดที่เช็คใน `_input`/`_unhandled_input`
  (เช่น NPC กด F คุย · กล่องสนทนากด F ไปต่อ) — **สองอย่างนี้ขาดอันใดอันหนึ่งไม่ได้**

**ปุ่มที่มี** (13 ปุ่ม)
- ซ้ายล่าง: ◀ ▶ (เดิน) · ▼ (ลงจากแท่น)
- ขวาล่าง: โจมตี · กระโดด · คุย/เก็บ
- แถวถัดขึ้นไป: สกิล 1 2 3 4
- แถวบนสุด: ยา · มานา · เมนู

**ขนาด/ตำแหน่ง** แก้ได้ที่ค่าคงที่ด้านบนไฟล์:
`PAD_BIG 112` (เดิน/โจมตี) · `PAD_MID 96` (กระโดด/คุย) · `PAD_SMALL 78` (สกิล) ·
`PAD_TINY 68` (ยา/เมนู) · `EDGE 22` (ห่างขอบจอ) · `GAP 10` (ห่างกัน) ·
`IDLE_ALPHA 0.55` / `HOLD_ALPHA 1.0` (ความจางตอนไม่กด/กด)

**โหมด** `UI.touch.set_mode(TouchControls.Mode.AUTO / ON / OFF)`
- **AUTO** = โผล่เองเมื่อ `DisplayServer.is_touchscreen_available()` หรือเมื่อมีการแตะจอครั้งแรก
- เปลี่ยนได้ในเกมที่ **เมนูระบบ (Tab) → ปุ่มจอสัมผัส** (กดวนไป อัตโนมัติ → เปิดตลอด → ปิด)
- จำไว้ที่ `user://ui_layout.cfg` คีย์ `[touch] mode`

**พฤติกรรมอื่น**
- เปิดหน้าต่าง / คุยกับ NPC อยู่ = ซ่อนปุ่มให้อัตโนมัติ (กันบังปุ่มในหน้าต่าง)
- ซ่อนแถบคำใบ้ปุ่มคีย์บอร์ดล่างจอตอนเปิดปุ่มจอสัมผัส
- ลากนิ้วจากปุ่มหนึ่งไปอีกปุ่ม = สลับปุ่มให้เลย (สไลด์ ◀ ↔ ▶ ได้ลื่น)
- ปิดโหมดตอนกดค้างอยู่ = ปล่อย action ให้ครบ ไม่มีค้าง
- `UI.is_point_over_ui()` นับปุ่มพวกนี้ด้วย → แตะปุ่มแล้วตัวละครไม่ฟันมั่ว

### เอาขึ้นเว็บ / GitHub
ดูขั้นตอนละเอียดที่ **`คู่มืออัพขึ้น GitHub.md`** (ไฟล์แยก)

ไฟล์ที่เตรียมไว้แล้ว:
| ไฟล์ | หน้าที่ |
|---|---|
| `export_presets.cfg` | preset ชื่อ **`Web`** · **`variant/thread_support=false`** (สำคัญ! GitHub Pages ตั้ง COOP/COEP header ไม่ได้) · ออกที่ `build/web/index.html` |
| `.github/workflows/deploy.yml` | GitHub Actions: โหลด Godot ตาม `GODOT_VERSION` → `--import` 2 รอบ → `--export-release "Web"` → ขึ้น GitHub Pages |
| `.gitignore` | ตัด `.godot/` · `build/` · `_update*.zip` · `*.bak` |
| `README.md` | หน้าแรกของ repo |

**จุดที่พลาดง่าย**
- `--headless --import` **ต้องรัน 2 รอบ** รอบแรก error เรื่องลำดับ `class_name` ได้
- ชื่อ preset ต้องเป็น `Web` เป๊ะ ๆ ตรงกับคำสั่ง `--export-release "Web"`
- โปรเจกต์ตั้ง `stretch/mode=canvas_items` + `aspect=expand` อยู่แล้ว → จอมือถือยืดได้เลย
- เปิดไฟล์ `index.html` ตรง ๆ ไม่ได้ ต้องเสิร์ฟผ่าน http (เช่น `python -m http.server`)

---

## 7.39 กระโดด/ท่าโดนตี/หน้าจอตาย + มินิแมพ + แถบปุ่มไอคอน (รอบ 27)

### ก) การกระโดด — เฟรมเชื่อมกันแล้ว

**ปัญหาเดิม** ท่า `Jump` ถูกเล่นแบบ "วนตาม FPS ที่ตั้งไว้" ไม่เกี่ยวกับว่าตัวละคร
กำลังพุ่งขึ้นหรือกำลังตกอยู่ ของคุณตั้ง Jump ไว้ 5 เฟรม 5 FPS = เล่นจบใน 1 วินาที
แต่กระโดดจริงใช้เวลาไม่เท่ากัน ภาพเลย "ค้างเฟรมสุดท้าย" ตั้งแต่ยังลอยอยู่
แล้วพอแตะพื้นก็ตัดไป Idle ทันที = เฟรมไม่เชื่อมกัน

**ของใหม่** เฟรมท่ากระโดดเดินตาม **ความเร็วแนวตั้งจริง**

| ช่วง | ความเร็ว | เฟรมที่ใช้ |
|---|---|---|
| พุ่งขึ้นเต็มแรง | `vy = -jump_power` | เฟรมแรก (0) |
| จุดสูงสุด (ลอยนิ่ง) | `vy = 0` | เฟรมกลาง |
| ตกเต็มที่ | `vy = +jump_power` | เฟรมสุดท้าย |

แตะพื้นแล้วจะ **ค้างเฟรมสุดท้ายไว้ 0.1 วินาที** (หรือเล่นท่า `Land` ถ้ามี) ก่อนกลับไป Idle/Run
ภาพเลยต่อกันเป็นชุดเดียว

**ค่าปรับได้ใน Inspector ของ Player → กลุ่ม "การกระโดด"**

| ช่อง | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| Jump Power | 420 | แรงกระโดด ยิ่งมากยิ่งสูง |
| Fall Gravity Mult | 1.35 | ★ ตกเร็วกว่าตอนขึ้น 1.35 เท่า = กระโดดหนึบ ไม่ลอยค้าง (1.0 = แบบเดิม) |
| Jump Cut Mult | 0.45 | ★ ปล่อยปุ่มกลางอากาศ = ตัดแรงขึ้นเหลือ 45% → คุมความสูงได้ แตะเบา ๆ = กระโดดเตี้ย |
| Coyote Time | 0.10 | ★ เดินตกขอบไปแล้ว ยังกดกระโดดทันได้อีก 0.1 วิ (เกมแพลตฟอร์มดี ๆ ทำกันหมด) |
| Jump Buffer Time | 0.12 | ★ กดกระโดดก่อนแตะพื้น 0.12 วิ ระบบจำไว้ให้ กระโดดต่อเนื่องได้ลื่น |
| Jump Anim Follow Physics | เปิด | ★ ให้เฟรมเดินตามฟิสิกส์ (ปิด = กลับไปเล่นวนแบบเดิม) |
| Land Time | 0.10 | ค้างท่าลงพื้นกี่วินาที (0 = ตัดทันทีแบบเดิม) |

> **ถ้าสไปรท์กระโดดของคุณเรียงคนละแบบ** (เช่น เฟรมสุดท้ายเป็นท่าย่อตัวก่อนกระโดด)
> ให้ **ปิด Jump Anim Follow Physics** แล้วมันจะเล่นวนตามปกติ
> หรือจะเรียงเฟรมใหม่เป็น *ย่อ → พุ่งขึ้น → ลอย → ตก → ลงพื้น* ก็จะได้ผลดีที่สุด

> **อยากมีท่าลงพื้นแยก** ตั้งชื่อท่าว่า `Land` ในไฟล์ SpriteFrames ระบบจะใช้ให้เอง

### ข) ท่าโดนตี (Hit) — ไม่โดน Idle ทับแล้ว

เดิม `take_damage()` สั่งเล่นท่า Hit แต่ `_update_animation()` ที่รันทุกเฟรม
สั่ง Idle/Run ทับทันทีในเฟรมถัดไป ท่า Hit เลยแทบไม่ทันโผล่

ตอนนี้เล่นท่า Hit แล้ว **ล็อกไว้จนเล่นจบ** (คิดเวลาจากจำนวนเฟรม ÷ FPS ของท่านั้นเอง)

| ช่อง (กลุ่ม "ท่าโดนตี") | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| Hit Anim Time | 0 | ล็อกกี่วินาที · 0 = คิดจากจำนวนเฟรมให้อัตโนมัติ |
| Hit Anim Max | 0.6 | ล็อกได้นานสุดเท่าไหร่ (กันสไปรท์ FPS ช้ามากจนค้าง) |

- ระหว่างล็อกยัง **เดินได้ตามปกติ** ล็อกแค่ "ภาพ" ไม่ล็อกการควบคุม
- กดโจมตีตอนกำลังเจ็บ = ยกเลิกท่าเจ็บ ฟันได้เลย ไม่ต้องรอ
- ชุดภาพไหนไม่มีท่า Hit ก็ไม่ล็อกอะไร (ทำงานเหมือนเดิมทุกอย่าง)

### ค) หน้าจอตอนตาย + คำอวยพรจากธอร์

`scripts/ui/death_popup.gd` — ตายแล้ว **ไม่เด้งกลับเมืองเองอีกต่อไป**

1. ท่าตาย (`Die` / `Death` / `Dead` ก็ได้) เล่นจนจบแล้ว **ค้างเฟรมสุดท้ายไว้** ไม่วนลูป
2. จอมืดลง ขึ้นกล่องกลางจอ: สายฟ้า → หัวข้อ → **คำอวยพรจากธอร์** (สุ่ม 1 จาก 10 ประโยค)
3. ปุ่ม **เกิดใหม่ที่เมือง (Enter)** และ **โหลดเซฟล่าสุด (F9)**
4. ระหว่างเปิดกล่อง เกม `paused` ไว้ · กด Enter / Space / F / แตะจอ = เกิดใหม่

**เพิ่ม/แก้คำอวยพรเอง** แก้ลิสต์ `THOR_BLESSINGS` ที่หัวไฟล์ `scripts/ui/death_popup.gd`
ใส่กี่ข้อก็ได้ ระบบสุ่มให้เอง

**เรียกเองจากโค้ด** `UI.death_popup.open()` / `.close()` / `.respawn()`

### ง) มินิแมพ (มุมขวาบน) — กด **M** เปิด/ปิด

`scripts/ui/minimap.gd` · เข้าถึงได้ที่ `UI.minimap`

| สัญลักษณ์ | คืออะไร |
|---|---|
| จุดเหลือง + ขีด | ตัวเรา (ขีดบอกทิศที่หันอยู่) |
| จุดแดงเล็ก | มอนสเตอร์ |
| จุดส้ม + วงแหวน | บอส (อ่านจาก `is_boss` ในไฟล์มอน) |
| จุดเขียว | NPC |
| เพชรฟ้า | ประตูวาป |
| จุดเหลืองจิ๋ว | ของที่ตกอยู่ |
| กรอบขาวจาง | ขอบเขตที่กล้องเห็นอยู่ตอนนี้ |
| แท่งเทา | พื้น/แท่นที่ยืนได้ (อ่านจากกล่องชน `StaticBody2D` ในแมพเอง) |

- ขนาดแมพอ่านจาก `map_bounds` ของ MapBase — **เปิด Auto Fit Bounds ไว้ มินิแมพก็อัพเดตตามเอง**
- ย่อแบบ **รักษาสัดส่วน** แมพยาว ๆ เลยไม่บี้จนดูตำแหน่งไม่ออก
- ชื่อแมพขึ้นบนหัวกล่อง (`display_name` ของแมพ)
- อยากเปลี่ยนขนาด/ระยะขอบ แก้ `VIEW_SIZE` กับ `MARGIN` ที่หัวไฟล์
- จำสถานะเปิด/ปิดไว้ที่ `user://ui_layout.cfg` คีย์ `[hud] minimap`

### จ) แถบปุ่มไอคอน (ใต้มินิแมพ)

`scripts/ui/icon_menu_bar.gd` · เข้าถึงได้ที่ `UI.menu_bar`

ปุ่ม 8 อัน เรียง 4 คอลัมน์ 2 แถว — **สเตตัส (C) · กระเป๋า (I) · ของสวมใส่ (E) · สกิล (K)**
/ **เควส (U) · สมุดการ์ด (V) · เมนูระบบ (Tab) · แผนที่ย่อ (M)**

- ปุ่มของหน้าต่างที่เปิดอยู่จะ **สว่างขึ้น**
- แตะบนมือถือได้เลย (ปุ่ม 48×48 px)
- คลิกบนแถบปุ่ม/มินิแมพ **ไม่นับเป็นการสั่งฟันดาบ** (`UI.is_point_over_ui()` นับให้แล้ว)

**★ อยากใส่ไอคอนที่วาดเอง ★**
เอาไฟล์ `.png` ไปวางที่โฟลเดอร์ **`Sprites/ui_icons/`** ตั้งชื่อให้ตรงกับ id:

```
Sprites/ui_icons/status.png      ← สเตตัส
Sprites/ui_icons/inventory.png   ← กระเป๋า
Sprites/ui_icons/equipment.png   ← ของสวมใส่
Sprites/ui_icons/skills.png      ← สกิล
Sprites/ui_icons/quests.png      ← เควส
Sprites/ui_icons/cards.png       ← สมุดการ์ด
Sprites/ui_icons/system.png      ← เมนูระบบ
Sprites/ui_icons/map.png         ← แผนที่ย่อ
```

มีไฟล์ไหน ระบบจะหยิบไปใช้แทนไอคอนที่วาดด้วยโค้ดให้เอง **ไม่ต้องแก้โค้ดเลย**
(ไม่มีไฟล์ก็ใช้ไอคอนที่วาดด้วยโค้ดไปก่อน สวยพอใช้งานได้)

**อยากเพิ่ม/ลด/สลับลำดับปุ่ม** แก้ลิสต์ `ITEMS` ที่หัวไฟล์ `icon_menu_bar.gd`

### ฉ) จุดที่พลาดง่าย (บันทึกไว้กันลืม)

- **ห้ามตั้ง `class_name MenuBar`** — Godot 4 มีคลาส `MenuBar` ของตัวเองอยู่แล้ว
  จะฟ้อง *"Class MenuBar hides a native class"* แล้วทั้งไฟล์พังเงียบ ๆ
  (เลยตั้งเป็น `IconMenuBar`)
- ตอนสั่ง `sprite.pause()` ค้างเฟรมไว้ ถ้าโค้ดเล่นท่าเช็คแค่ "ชื่อท่าเปลี่ยนไหม"
  ภาพจะค้างตลอด — ต้องเช็ค `is_playing()` ด้วย
- Label ที่เปิดตัดบรรทัดอัตโนมัติ **ต้องกำหนด `custom_minimum_size.x` ก่อนเสมอ**
  ไม่งั้นมันคิดความสูงจากความกว้าง 0 (ตัดทีละตัวอักษร) แล้วกล่องสูงเป็นพันพิกเซล
- **ห้ามใส่อีโมจิในข้อความ** ฟอนต์ไทยที่ฝังไว้ (Noto Sans Thai) ไม่มีตัวอีโมจิ
  บนเว็บ/มือถือจะกลายเป็นสี่เหลี่ยม — ถ้าอยากได้รูปให้วาดด้วย `_draw()` แทน

---

## 7.40 บทเรียนจากการเอาขึ้นเว็บ (เก็บไว้กันพลาดซ้ำ)

รอบที่เอาขึ้น GitHub Pages เจอ 2 อย่างนี้ กว่าจะรู้สาเหตุใช้เวลาพอควร

### 1) build ไม่ผ่านเพราะ VRAM compression

`export_presets.cfg` ตั้ง `vram_texture_compression/for_desktop=true` /
`for_mobile=true` ไว้ ระบบจะบังคับให้เปิด
`rendering/textures/vram_compression/import_etc2_astc` ในโปรเจกต์ด้วย
ไม่งั้น export ตายที่ขั้น "สร้างเวอร์ชันเว็บ" (exit 1)

**แก้แล้ว** ตั้งทั้งสองค่าเป็น `false` (เว็บใช้ WebGL2/Compatibility อยู่แล้ว ไม่ต้องบีบอัด)

### 2) ตัวหนังสือไทยเป็นสี่เหลี่ยม (tofu) บนเว็บ

เบราว์เซอร์ไม่มีฟอนต์ระบบให้ Godot ใช้ ต้อง **ฝังฟอนต์ไปกับเกม**

- ใส่ไฟล์ `fonts/NotoSansThai-Regular.ttf` (สัญญาอนุญาต SIL OFL 1.1 · ดู `fonts/NOTICE.txt`)
- ตั้งใน `project.godot`:
  ```
  [gui]

  theme/custom_font="res://fonts/NotoSansThai-Regular.ttf"
  ```

**★ กับดักตัวจริง ★** `project.godot` ใช้ **`;`** เป็นเครื่องหมายคอมเมนต์ **ไม่ใช่ `#`**
ตอนแรกใส่คอมเมนต์ด้วย `#` ไว้ใต้ `[gui]` → หัวข้อ `[gui]` พังทั้งบล็อก
คีย์เลยถูกเก็บเป็น `theme/custom_font` (ไม่มี `gui/` นำหน้า) = Godot ไม่เห็น ฟอนต์เลยไม่ติด

> เวลาแก้ `project.godot` เอง **อย่าใส่บรรทัด `#`** เด็ดขาด
> ถ้าจะคอมเมนต์ให้ใช้ `;` หรือแก้ผ่านหน้า Project Settings ของ Godot แทน

### 3) ขนาดไฟล์

`index.pck` ตอนนี้ประมาณ **96 MB** ใกล้ลิมิตไฟล์ละ 100 MB ของ GitHub แล้ว
สาเหตุหลักคือสไปรท์ชีท 4096×2048 หลายไฟล์
ถ้าจะเล่นบนมือถือจริงจัง (หรือจะใส่แมพ/มอนเพิ่มอีก) ควรย่อภาพลงก่อน
เช่นย่อเหลือครึ่งหนึ่ง หรือแยกไฟล์ที่ไม่ได้ใช้ออกด้วย `exclude_filter`

---

## 7.41 ★ ท่ากระโดด — แยกเป็น 3 ช่วง (รอบ 28) ★

### อาการ
กระโดดขึ้นแล้ว **ตอนกำลังตกลงมา ภาพเหมือนถอยกลับไปท่ายืน** (ท่า Idle)
แล้วตัดเข้า Idle จริง ๆ อีกที = ภาพไม่เชื่อมกัน

### สาเหตุ
ท่า `Jump` ของโปรเจกต์นี้มี **6 เฟรม** แต่มันไม่ได้เป็น "ท่าลอย" ทั้ง 6 เฟรม
มันคือ 3 ช่วงคนละเรื่องอยู่ในไฟล์เดียว

| เฟรม | เป็นท่าอะไร | ควรเล่นตอนไหน |
|---|---|---|
| 0 · 1 · 2 | ยืน → ย่อตัว → ถีบพื้น | **ตอนออกตัว** (ยังอยู่ที่พื้น) |
| 3 · 4 | ตัวลอย → เริ่มตก | **ตอนลอยกลางอากาศ** |
| 5 | ยืนรับน้ำหนัก | **ตอนแตะพื้น** |

รอบ 27 เอาทั้ง 6 เฟรมไปผูกกับความเร็วแนวตั้งรวดเดียว (เร็วขึ้นสุด = เฟรม 0,
ตกเต็มที่ = เฟรม 5) ผลคือ

- ตอนพุ่งขึ้น → ไปโชว์เฟรม "ย่อตัว" ทั้งที่ลอยอยู่แล้ว (เหมือนภาพเดินถอยหลัง)
- **ตอนตก ความเร็วตกเกิน `jump_power` เร็วมาก → ค่าไปสุดที่เฟรม 5 ตั้งแต่ยังลอยอยู่กลางอากาศ**
  เฟรม 5 คือท่ายืน เลยเห็นเป็น "ตกลงมาแล้วกลับไปท่ายืน" ก่อนถึงพื้นด้วยซ้ำ

### วิธีแก้ (แก้ให้แล้วใน `player.gd`)
แยกเป็น 3 ช่วง แต่ละช่วงใช้เฟรมของตัวเอง เฟรมเลยเดินหน้าอย่างเดียว ไม่ถอยหลัง

```
กดกระโดด → [เฟรม 0-2 ย่อตัวถีบพื้น รวด 0.12 วิ]
          → [เฟรม 3-4 ลอย · เดินตามความเร็วจริง]
          → แตะพื้น → [เฟรม 5 ลงพื้น ค้าง 0.14 วิ] → Idle/Run
```

**ช่องใหม่ใน Inspector ของ Player → กลุ่ม "การกระโดด"**

| ช่อง | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| Jump Takeoff Frames | **3** | เฟรมแรก ๆ กี่เฟรมที่เป็นช่วง "ย่อตัวถีบพื้น" |
| Jump Takeoff Time | **0.12** | เล่นช่วงย่อตัวให้จบภายในกี่วินาที |
| Jump Land Frames | **1** | เฟรมท้าย ๆ กี่เฟรมที่เป็นช่วง "ลงพื้น" |
| Land Time | **0.14** | ค้างช่วงลงพื้นกี่วินาทีก่อนกลับไป Idle/Run |

### ★ ถ้าวาดท่ากระโดดใหม่ ต้องตั้งเลข 2 ช่องนี้ให้ตรง ★

นับเฟรมในไฟล์ SpriteFrames ของท่า `Jump` แล้วใส่ตามนี้

| แบบสไปรท์ | Takeoff Frames | Land Frames |
|---|---|---|
| มีครบ 3 ช่วง (แบบตอนนี้ 6 เฟรม) | 3 | 1 |
| มีแต่ท่าลอยอย่างเดียว | **0** | **0** |
| มีย่อตัวแต่ไม่มีลงพื้น | จำนวนเฟรมย่อตัว | 0 |
| ไม่อยากให้ระบบยุ่งกับเฟรมเลย | ปิด **Jump Anim Follow Physics** | — |

> **เช็คง่าย ๆ ว่าตั้งถูกไหม** กระโดดแล้วดูว่า
> ตอนพุ่งขึ้นเห็นท่าย่อตัวแวบเดียวตอนแรก · ระหว่างลอยเป็นท่าลอย ·
> ตอนตกไม่กลับไปเป็นท่ายืน · แตะพื้นแล้วค่อยเห็นท่ายืนรับน้ำหนัก

> **ถ้าไม่อยากเห็นท่าย่อตัวเลย** (เพราะจริง ๆ มันควรเล่นก่อนตัวลอย)
> ตั้ง **Jump Takeoff Frames = 0** ระบบจะเริ่มที่เฟรมลอยทันที
> เราไม่หน่วงการกระโดดให้เล่นย่อตัวก่อน เพราะจะทำให้กดแล้วรู้สึกหน่วง (input lag)

### เกร็ด
- **เดินตกขอบเฉย ๆ (ไม่ได้กดกระโดด) จะข้ามช่วงย่อตัวไปเลย** — เพราะไม่ได้ถีบพื้น
  (ระบบดูจากตัวแปร `_jump_kick`)
- ท่ากระโดดถูกสั่ง `pause()` แล้วตั้ง `frame` เอง — โค้ดที่สั่งเล่นท่าใหม่จึงต้อง
  เช็ค `is_playing()` ด้วย ไม่งั้นภาพค้าง (เขียนกันไว้ใน `_play()` แล้ว)
- อยากให้กระโดด "หนึบ" กว่านี้ เพิ่ม **Fall Gravity Mult** (1.35 → 1.6-1.8)
  ตอนนี้ลอยอยู่กลางอากาศประมาณ 0.9 วินาที ถ้ารู้สึกลอยนานไปให้ปรับช่องนี้

---

## 7.42 ★ กดเล่นแล้วร่วงตกแมพ / ฉากกับพื้นหลังหายหมด ★

### อาการ
กด **Run (F5)** แล้ว
- ไม่มีฉาก ไม่มีพื้นหลัง มีแต่ตัวละครลอยอยู่กลางที่ว่าง
- ตัวละครร่วงลงเรื่อย ๆ ไม่มีพื้นให้ยืน
- HUD/หลอดเลือด/ปุ่มยังขึ้นปกติ (เพราะ Autoload ทำงานอยู่)

### สาเหตุ
**Main Scene ถูกเปลี่ยนไปเป็น `player.tscn` แทนที่จะเป็นแมพ**

เกิดง่ายมาก: ตอนเปิด `scenes/player/player.tscn` อยู่แล้วกด **F5** ครั้งแรก
Godot จะถามว่า "ยังไม่ได้ตั้ง Main Scene จะใช้ฉากที่เปิดอยู่ไหม" ถ้าเผลอกด **Select Current**
มันจะตั้ง Main Scene = ฉากตัวละคร ตั้งแต่นั้นกดเล่นทีไรก็ได้แต่ตัวละครเปล่า ๆ
(อีกทางคือคลิกขวาไฟล์ผิดตัวใน FileSystem แล้วเลือก *Set as Main Scene*)

ในไฟล์ `project.godot` จะเห็นเป็น
```
run/main_scene="uid://bhp7c8ti14pnn"      <- อันนี้คือ player.tscn (ผิด)
```

### วิธีแก้ (ทำได้เองใน Godot)
**วิธีที่ 1 — เร็วสุด**
FileSystem → คลิกขวาที่ **`Sprites/world_node_2d.tscn`** → **Set as Main Scene**

**วิธีที่ 2 — ผ่านหน้าตั้งค่า**
เมนู **Project → Project Settings → Application → Run** → ช่อง **Main Scene**
เลือก `res://Sprites/world_node_2d.tscn`

**วิธีที่ 3 — แก้ไฟล์ตรง ๆ** (ปิด Godot ก่อน) แก้บรรทัดใน `project.godot` เป็น
```
run/main_scene="res://Sprites/world_node_2d.tscn"
```

> สำรองไฟล์เดิมไว้ให้แล้วที่ **`project_ก่อนแก้ main_scene.godot.bak`**

### ★ วิธีเช็คว่า Main Scene ถูกไหม ★
มุมขวาบนของ Godot ปุ่ม ▶ ตัวแรก = "Run Project" (เล่น Main Scene) ·
ปุ่ม ▶ ตัวที่สอง = "Run Current Scene" (เล่นฉากที่เปิดอยู่)
**ถ้าอยากลองฉากที่เปิดอยู่เฉย ๆ ให้กด F6 ไม่ใช่ F5** จะได้ไม่ไปแตะ Main Scene

### เกร็ดอื่นที่เจอตอนตรวจ
- มีไฟล์ฉากเปล่าหลงอยู่ใน `data/monsters/` คือ **`drops.tscn`** กับ **`drps.tscn`**
  (Node2D เปล่า ๆ ไม่มีอะไรข้างใน — น่าจะกด New Scene แล้วเผลอเซฟลงโฟลเดอร์นี้)
  **ลบทิ้งได้เลย ไม่กระทบอะไร** — `GameData` โหลดเฉพาะไฟล์ `.tres`/`.res` เท่านั้น
- ไฟล์ `.bak` ในโฟลเดอร์ข้อมูลไม่ถูกโหลด เพราะนามสกุลไม่ลงท้ายด้วย `.tres`

---

## 7.43 ★ ฉากหลังของแมพ — Sky / FarTrees / Art ต้องวาดขนาดเท่าไร (Asgard Forest 2) ★

### ก) โครงสร้างชั้นฉากในแมพนี้

```
Map                         <- map_base.gd · เก็บ map_bounds
├── Background              <- Node2D เปล่า ๆ ใช้จัดกลุ่มเฉย ๆ
│   ├── Sky        z -100   <- ไกลสุด (ท้องฟ้า)
│   ├── FarTrees   z -90    <- ต้นไม้ไกล
│   ├── Art        z -80    <- ต้นไม้/พุ่มใกล้  ★ ยังว่างอยู่ รอใส่ภาพ ★
│   └── AsgardForest1       <- Sprite2D ที่หลงมา (อยู่ y=2281 นอกแมพ มองไม่เห็น)
├── Terrain
│   └── Ground              <- StaticBody2D = พื้นที่ยืนได้จริง
│       ├── Shape           <- RectangleShape2D 3600 x 240  ← ตัวนี้แหละที่ทำให้ยืนได้
│       └── Visual          <- Polygon2D = ภาพพื้น (ไม่เกี่ยวกับการชน)
├── SpawnPoints · Spawners · Portals
```

**★ Z Index คือลำดับหน้า-หลัง ★** ตัวเลขน้อย = อยู่หลัง
ฉากหลังทุกชั้น**ต้องติดลบ** ไม่งั้นจะไปบังตัวละคร (ตัวละคร/มอนอยู่ที่ z 0)
ตั้งที่ Inspector → **CanvasItem → Ordering → Z Index**

### ข) ★★ ตัวเลขที่ต้องรู้ (แมพนี้) ★★

| อย่าง | ค่า |
|---|---|
| ขอบเขตแมพ (`map_bounds` ที่โหนด Map) | `Rect2(-100, -200, 3800, 1200)` → x **-100 ถึง 3700** · y **-200 ถึง 1000** |
| **ระดับพื้นที่ยืนได้** | **y = 880** (กว้าง x -100 ถึง 3500) |
| กล้องเห็นทีละครั้ง | 1280 x 720 |
| แถบที่ตาเห็นจริงตอนยืน | y **280 ถึง 1000** |
| ความสูงตัวละคร | 240 px |
| จุดเกิดผู้เล่น | (180, 820) · จุดเกิดบอส (2900, 820) |
| ประตู | ไป Forest 1 ที่ x=60 · ไปเมือง ที่ x=3380 |

### ค) ★★★ ต้องวาดภาพขนาดเท่าไร ★★★

**ตอบสั้น ๆ: วาด `3800 x 1200 px` แล้ววางที่ตำแหน่ง `(1800, 400)` จบ**

เพราะ Sprite2D นับตำแหน่งจาก **กึ่งกลางภาพ** และกึ่งกลางของ map_bounds คือ
`(-100 + 3800/2, -200 + 1200/2)` = **(1800, 400)**

แบบละเอียดขึ้น ถ้าอยากแยกเป็นชั้น ๆ (สวยกว่า ไฟล์เล็กกว่า)

| ชั้น | ขนาดที่วาด | ตำแหน่งที่วาง | หมายเหตุ |
|---|---|---|---|
| **Sky** | 3800 x 1200 | (1800, 400) | ท้องฟ้า + ภูเขาไกล · ทึบทั้งภาพ |
| **FarTrees** | 3800 x 700 | (1800, 650) | ต้นไม้ไกล · **พื้นหลังโปร่งใส** ข้างบน |
| **Art** | 3800 x 500 | (1800, 750) | พุ่มไม้/ก้อนหินใกล้ · โปร่งใสข้างบน |
| **พื้น (Ground/Visual)** | 3600 x 240 | ผูกกับ Shape อยู่แล้ว | ผิวบนอยู่ที่ y=880 |

> **อยากประหยัดขนาดไฟล์** วาดครึ่งเดียว (1900 x 600) แล้วตั้ง **Scale = (2, 2)**
> ได้พื้นที่เท่ากันแต่ภาพจะเบลอลงนิดหน่อย
>
> **ภาพที่ต้องซ้อนทับกัน (FarTrees / Art) ต้องเซฟเป็น PNG พื้นหลังโปร่งใส**
> ไม่งั้นชั้นบนจะบังชั้นล่างหมด

### ง) วิธีใส่ภาพ (แบบง่ายสุด — แนะนำ)

1. เอาไฟล์ `.png` ไปวางในโฟลเดอร์ `Sprites/map/`
2. ใน Godot คลิกโหนด **Sky** (หรือชั้นที่ต้องการ)
3. ถ้ามันเป็น **Polygon2D** อยู่ → ทำง่ายกว่าโดยเปลี่ยนเป็น Sprite2D:
   คลิกขวาที่โหนด → **Change Type** → เลือก **Sprite2D**
4. ลากไฟล์ `.png` จาก FileSystem ไปหย่อนใส่ช่อง **Texture** ใน Inspector
5. ตั้ง **Transform → Position** ตามตารางข้อ ค)
6. ตั้ง **CanvasItem → Ordering → Z Index** ตามตารางข้อ ก)
7. กด **F6** (เล่นเฉพาะฉากนี้) ดูผล — **อย่ากด F5** ถ้าไม่อยากเปลี่ยน Main Scene

### จ) ถ้าอยากใช้ Polygon2D ต่อ (แบบที่ตั้งไว้ตอนนี้)

Polygon2D เหมาะกับ **ภาพที่ต่อกันเป็นลายซ้ำ ๆ ยาวไปเรื่อย ๆ** (เช่นแนวต้นไม้)
แต่มีกฎ 3 ข้อ ถ้าผิดจะได้ภาพยืด ๆ เบลอ ๆ

1. **จำนวนจุดของ `polygon` ต้องเท่ากับ `uv` เป๊ะ ๆ**
   ถ้าไม่เท่ากัน Godot จะทิ้ง uv แล้วแมพภาพมั่ว
2. **`uv` คือพิกัด "พิกเซลในไฟล์ภาพ"** ไม่ใช่พิกัดในโลก
   ไฟล์ `asgard forest2 sky.png` = 1700 x 400 → uv ที่ถูกคือ (0,0) ถึง (1700,400)
   ถ้าใส่ uv กว้าง 3800 ทั้งที่ภาพกว้าง 1700 ภาพจะ**ยืดขอบสุดท้ายลากยาว**
   (คือเส้นแนวตั้งเลอะ ๆ ที่เห็นในโปรแกรมตอนนี้)
3. **อยากให้ภาพต่อกันซ้ำ ๆ** ต้องตั้ง Inspector → **CanvasItem → Texture → Repeat = Enabled**
   แล้วค่อยใส่ uv กว้างเป็น 2-3 เท่าของภาพได้ (ภาพต้องวาดให้ต่อกันได้แบบ seamless ด้วย)

**แถบเครื่องมือล่างจอตอนเลือก Polygon2D**
`จุด` = เพิ่ม/ลบมุม (คลิกขวาที่จุด = ลบ) · `โพลีกอน` = ลากย้ายรูปทรง ·
`UV` = ลากตำแหน่งภาพบนรูปทรง · `โครง` = โครงกระดูก (ไม่ได้ใช้)

### ฉ) วิธี "เพิ่มฉาก" ชั้นใหม่

1. คลิกโหนด **Background** → กด **Ctrl+A** (Add Child Node) → เลือก **Sprite2D**
2. ตั้งชื่อให้รู้เรื่อง เช่น `MidTrees`
3. ลากภาพใส่ช่อง Texture → ตั้ง Position → ตั้ง Z Index
   ให้เลขอยู่ระหว่างชั้นหน้ากับชั้นหลัง เช่น Sky -100 · **MidTrees -85** · Art -80
4. อยากให้ชั้นไหนอยู่หน้ากว่า = เลข Z Index มากกว่า (แต่ยังต้องติดลบ)

### ช) วิธีทำให้แมพยาวขึ้น (เดินได้ไกลขึ้น)

แก้ 4 จุดนี้ให้ตรงกันเสมอ ไม่งั้นจะเดินตกแมพหรือชนกำแพงอากาศ

1. โหนด **Map** → `map_bounds` เช่นจะยาว 6000 → `Rect2(-100, -200, 6100, 1200)`
2. **Terrain/Ground/Shape** → `size.x` = ความยาวพื้น · **Ground → Position.x** = กึ่งกลางพื้น
   (ตอนนี้ Shape 3600 กว้าง · Ground อยู่ x=1700 → พื้นจึงกิน x -100 ถึง 3500)
3. **Terrain/Ground/Visual** → ยืด `polygon` ให้ยาวเท่าพื้นใหม่
4. ย้าย **Portals** กับ **SpawnPoints** ไปที่ปลายใหม่ · แล้ววาดฉากหลังให้ยาวตาม

> **ทางลัด** ที่โหนด Map เปิด **`Auto Fit Bounds`** แล้วระบบจะวัดขนาดแมพจาก
> TileMap + กล่องชนของพื้นให้เอง ไม่ต้องมาแก้ `map_bounds` เองทุกครั้ง
> (มินิแมพก็อัปเดตตามด้วย)

### ซ) จุดที่ต้องแก้ในไฟล์ตอนนี้

| อาการ | สาเหตุ | แก้ยังไง |
|---|---|---|
| ท้องฟ้ามีเส้นแนวตั้งเลอะทางขวา | `Sky` มี polygon **5 จุด** แต่ uv มี **4 จุด** (มีจุดเกินที่ 897.7, 650) | โหมด `จุด` → คลิกขวาที่จุดเกิน → ลบ · แล้วตั้ง uv ให้ตรง (ดูข้อ จ) |
| ภาพท้องฟ้ายืด | ภาพ 1700x400 แต่ uv กว้าง 3800x1300 | เปลี่ยนเป็น Sprite2D (ข้อ ง) หรือเปิด Texture Repeat |
| `FarTrees` เป็นแผ่นเขียวทึบ | ยังไม่ได้ใส่ Texture เลย มีแต่สี | ใส่ภาพต้นไม้ไกล (3800x700 พื้นหลังโปร่งใส) |
| `Art` มองไม่เห็น | เป็น Sprite2D ที่ยังไม่มี Texture | ใส่ภาพพุ่มไม้ใกล้ |
| `AsgardForest1` มองไม่เห็น | Sprite2D อยู่ที่ y = **2281** ซึ่งต่ำกว่าพื้นแมพ (ขอบล่าง y=1000) | ย้ายมาที่ (1800, 400) หรือลบทิ้งถ้าไม่ใช้ |

---

## 7.44 พุ่งหลบ (Dash) แทนกระโดด + เอฟเฟกต์สกิลที่ทำดาเมจเอง + ค่าหลบมอน (รอบ 29)

### ก) ★ พุ่งหลบ (Dash) ★ — ปุ่มกระโดดเดิมกลายเป็นพุ่งไปข้างหน้า

**ปุ่มเดิมทุกปุ่มยังใช้ได้เหมือนเดิม** — W / Space / ลูกศรขึ้น / ปุ่มบนจอมือถือ
(ปุ่มบนจอเปลี่ยนชื่อเป็น "พุ่งหลบ" ให้แล้ว)

- พุ่งไป **ทางที่กดค้างอยู่** ถ้าไม่ได้กดทิศไหน = พุ่งไปทางที่หันหน้า
- ★ ระหว่างพุ่งเป็น **อมตะ** ★ มอนตีมาไม่เข้า ขึ้นคำว่า **"หลบ!"** สีฟ้าแทนเลขดาเมจ
- ระหว่างพุ่ง **ไม่ตกลงพื้น** (ลอยตรง) เลยพุ่งข้ามหลุมได้
- มีคูลดาวน์กันกดรัว

**ค่าใน Inspector ของ Player → กลุ่ม "พุ่งหลบ (Dash)"**

| ช่อง | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| Dodge Enabled | เปิด | ปิด = กลับไปใช้ปุ่มนี้กระโดด (ถ้าเปิด Can Jump ไว้) |
| Dodge Distance | 260 | พุ่งไปไกลกี่พิกเซล (ตัวละครสูง 240 → พุ่งได้ราวตัวเดียว) |
| Dodge Speed | 1100 | ความเร็ว · เวลาพุ่ง = ระยะ ÷ ความเร็ว = 0.24 วิ |
| **Dodge Invincible** | **0.28** | ★ อมตะกี่วินาที ★ ยาวกว่าเวลาพุ่งนิดหน่อย เผื่อจังหวะลงตัว |
| Dodge Cooldown | 0.55 | รอกี่วินาทีถึงพุ่งได้อีก (นับหลังพุ่งจบ) |
| Dodge Sp Cost | 0 | อยากให้เปลืองมานาก็ใส่เลข เช่น 5 |
| Dodge Stop On Wall | เปิด | ชนกำแพงแล้วหยุดทันที |
| Dodge Anim | `Dash` | ชื่อท่าตอนพุ่ง |

**ท่าตอนพุ่ง** — ตอนนี้ยัง**ไม่มีท่า `Dash`** ในชุดภาพ ระบบเลย **ยืมเฟรม "ลอยกลางอากาศ"
ของท่ากระโดด** (เฟรม 3) มาค้างไว้แทน ซึ่งเป็นท่าตัวพุ่งไปข้างหน้าพอดี ดูใช้ได้เลย

> **อยากได้ท่าพุ่งของตัวเอง** วาดแล้วตั้งชื่อท่าใน SpriteFrames ว่า **`Dash`**
> ระบบจะหยิบไปใช้เองทันที ไม่ต้องแก้โค้ด
> (3-5 เฟรม พอ · ท่าเอนตัวไปข้างหน้า + เส้นความเร็วด้านหลัง)

**อยากได้กระโดดกลับมาด้วย** เปิดช่อง **Can Jump** ในกลุ่ม "การกระโดด"
ระบบกระโดดเดิม (coyote / buffer / เฟรม 3 ช่วง) ยังอยู่ครบ ไม่ได้ลบทิ้ง
แต่ใช้ปุ่มเดียวกัน — ถ้าเปิดทั้งคู่ **พุ่งหลบจะมาก่อน**
อยากแยกปุ่ม ต้องเพิ่ม action ใหม่ใน `InputSetup.DEFAULT_KEYS` แล้วแก้ `_tick_jump_timers()`

### ข) ★★ เอฟเฟกต์สกิลที่ "ทำดาเมจเอง" ★★

**ตอบคำถาม: ได้ครับ** ตอนนี้ทำให้แล้ว — เปิดช่องเดียวจบ

**แบบเดิม** ดาเมจออกตอนถึงจังหวะ `Cast Windup` แล้วเช็คกรอบสี่เหลี่ยมรอบตัวละคร
เอฟเฟกต์เป็นแค่ "ภาพประกอบ" ไม่เกี่ยวกับดาเมจเลย

**แบบใหม่** เปิด `Effect Damage` แล้ว **มอนตัวไหนโดนภาพเอฟเฟกต์ ตัวนั้นกินดาเมจ**
เอฟเฟกต์วิ่งออกไปไกลแค่ไหน ดาเมจก็ตามไปด้วย = ทำสกิลยิงได้เลย

**ช่องใหม่ใน SkillData → กลุ่ม "เอฟเฟกต์ทำดาเมจเอง"**

| ช่อง | ความหมาย |
|---|---|
| **Effect Damage** | เปิด = ให้เอฟเฟกต์เป็นคนทำดาเมจ |
| **Effect Hit Size** | ขนาดกล่องชน (0,0 = ใช้ขนาดภาพจริง) — ★ ตั้งเองดีกว่า ★ ภาพมักมีขอบโปร่งใสเยอะ |
| **Effect Max Targets** | ตีได้กี่ตัวต่อ 1 เอฟเฟกต์ (0 = ไม่จำกัด) |
| **Effect Hit Once** | มอนตัวเดิมโดนครั้งเดียว (ปิด = โดนทุกเฟรม แรงมาก ระวัง) |
| **Effect Pierce** | ทะลุไปเรื่อย ๆ (ปิด = ชนตัวแรกแล้วหายไป เหมือนกระสุน) |

> **★ เปิด Effect Damage แล้วระบบจะไม่คิดดาเมจแบบเดิมให้อีก ★**
> ไม่งั้นมอนจะโดน 2 เด้ง — โค้ดกันไว้ให้แล้ว

**สูตรทำสกิลยิง 3 แบบ**

| อยากได้ | Effect Speed | Effect Pierce | Effect Max Targets |
|---|---|---|---|
| คลื่นดาบยิงออกไปโดนตัวแรก | 400-600 | **ปิด** | 1 |
| ลำแสงทะลุทุกตัวในแนว | 700-1000 | เปิด | 0 |
| ระเบิดอยู่กับที่ (AOE) | **0** | เปิด | 0 |
| เอฟเฟกต์เกาะไปกับตัวตอนพุ่ง | 0 + เปิด `Effect Follow` | เปิด | 0 |

### ค) ★ เอฟเฟกต์ของสกิล Bash ที่ร่างไว้ให้ ★

ทำไฟล์ให้แล้ว 2 ไฟล์ ใช้ได้ทันทีไม่ต้องทำอะไรเพิ่ม

| ไฟล์ | คืออะไร |
|---|---|
| `Sprites/effects/bash_slash.png` | สไปรท์ชีท **3072 x 512** = 6 เฟรม เฟรมละ **512 x 512** เรียงแนวนอน |
| `data/sprites/fx_bash.tres` | SpriteFrames สำเร็จรูป ท่าชื่อ **`slash`** 6 เฟรม 18 fps ไม่วนลูป |

ค่าที่ตั้งให้ใน `bash.tres` แล้ว

```
effect_anim      = "slash"
effect_offset    = (95, -5)      # ออกจากตัวไปข้างหน้า 95 px ระดับอก
effect_height    = 300           # สูง 300 px บนจอ (ตัวละครสูง 240)
effect_speed     = 460           # วิ่งไปข้างหน้า
effect_delay     = 0.12          # รอให้ดาบเหวี่ยงก่อนค่อยโผล่
effect_life      = 0.5
effect_damage    = true          # ★ เอฟเฟกต์ทำดาเมจเอง ★
effect_hit_size  = (200, 260)    # กล่องชน — สูงพอโดนมอนเตี้ย ๆ อย่างโพริง
effect_max_targets = 1           # Bash ตีตัวเดียวตามดีไซน์เดิม
```

**★ สเปกสำหรับวาดเอฟเฟกต์เอง ★** (เอาไปวาดทับของที่ร่างไว้ได้เลย)

- ผ้าใบ **512 x 512 ต่อเฟรม** · 6 เฟรม เรียงแนวนอนเป็นไฟล์เดียว **3072 x 512**
- **พื้นหลังโปร่งใส** (PNG 32 bit)
- วาดให้ **หันไปทางขวา** เสมอ — ระบบพลิกภาพให้เองตอนตัวละครหันซ้าย
- วางกลางเฟรมไว้ ระบบวางเอฟเฟกต์โดยยึด **กึ่งกลางภาพ**
- ลำดับเฟรมที่ร่างไว้: เส้นบาง → โค้งเต็ม สว่างสุด → แผ่กว้างขึ้น → บางลง → จาง → หายไป
- โทนสี: ไส้ในขาว → ทอง → ขอบส้ม (แก้ได้ตามธาตุของสกิล)

### ง) ★ วิธีใส่เอฟเฟกต์ให้สกิลอื่น (ทำเองได้) ★

**ขั้นที่ 1 — เตรียมภาพ** ทำสไปรท์ชีทตามสเปกข้อ ค) เอาไปวางที่ `Sprites/effects/`

**ขั้นที่ 2 — สร้าง SpriteFrames**
1. คลิกขวาในโฟลเดอร์ `data/sprites/` → **New Resource** → พิมพ์หา **SpriteFrames** → Create
2. ตั้งชื่อเช่น `fx_magnum.tres` แล้วดับเบิลคลิกเปิด
3. แถบล่างจอจะขึ้นหน้าต่าง SpriteFrames → กด **New Animation** ตั้งชื่อท่า เช่น `boom`
4. กดปุ่ม **Add frames from sprite sheet** (ไอคอนตาราง) → เลือกไฟล์ png →
   ตั้ง **Horizontal = จำนวนเฟรม** · **Vertical = 1** → เลือกทุกช่อง → **Add frames**
5. ตั้ง **FPS** (15-20 กำลังดี) และ **ปิด Loop**

**ขั้นที่ 3 — ผูกเข้ากับสกิล**
1. เปิด `data/skills/<ชื่อสกิล>.tres`
2. Inspector → กลุ่ม **เอฟเฟกต์สกิล** → ลาก `fx_xxx.tres` ใส่ช่อง **Effect Frames**
3. ใส่ชื่อท่าในช่อง **Effect Anim** (เช่น `boom`)
4. ปรับ **Effect Offset / Height / Speed / Delay / Life** ตามที่อยากได้
5. อยากให้เอฟเฟกต์ทำดาเมจเอง → เปิด **Effect Damage** ในกลุ่ม "เอฟเฟกต์ทำดาเมจเอง"

**ขั้นที่ 4 — ลอง** กด **F6** ในแมพ → เรียนสกิล → ตั้งช่องลัด → กดใช้

> **เอฟเฟกต์ของมอนสเตอร์** ทำเหมือนกันเป๊ะ แต่ไปใส่ที่ `MonsterData` กลุ่ม
> **Skill Effect Frames** แทน (ตอนนี้ยังไม่รองรับ Effect Damage — มอนใช้วิธีคิดดาเมจแบบเดิม)

### จ) ★ กับดักที่เจอตอนทำ ★

**`Effect Delay` เคยพังเงียบ ๆ** — โค้ดเดิมสร้างตัวจับเวลาใน `_setup()` ซึ่งถูกเรียก
**ก่อน** `add_child()` ตอนนั้น `get_tree()` ยังเป็น `null` ตัวจับเวลาเลยไม่เกิด
เอฟเฟกต์ที่ตั้ง Delay ไว้จะ **ซ่อนค้างตลอดไป** ไม่โผล่เลย
ย้ายไปทำใน `_ready()` แล้ว — **บทเรียน: อย่าเรียก `get_tree()` ในโค้ดที่รันก่อนโหนดเข้า tree**

### ฉ) ลดอัตราหลบของมอนเลเวลต่ำ 20%

มอนที่ **เลเวลต่ำกว่า 15** ค่า FLEE ถูกลดลง **20%** (แก้ที่ `scripts/core/combat.gd`)

```gdscript
const LOW_LEVEL_FLEE_CAP := 15        ## มอนเลเวลต่ำกว่านี้โดนลด
const LOW_LEVEL_FLEE_REDUCTION := 0.20 ## ลดลงกี่ส่วน
```

**โอกาสตีโดนก่อน/หลัง** (ผู้เล่น Lv.1 DEX 1 · HIT 102)

| มอน | เลเวล | เดิม | ตอนนี้ |
|---|---|---|---|
| โพริง | 1 | 84% | **98%** (ชนเพดาน) |
| แฟเบร | 2 | 81% | **98%** |
| ลูนาติก | 4 | 71% | **94%** |
| ดรอปส์ | 5 | 75% | **98%** |
| ชอนชอน | 7 | 60% | **85%** |
| ฮอร์เน็ต | 9 | 52% | **79%** |
| หมาป่า | 13 | 54% | **81%** |
| **คิงโพริง** | **15** | 58% | **58% (ไม่โดนลด)** |

- ★ คิงโพริงเลเวล 15 พอดี เลย **ไม่เข้าเงื่อนไข "ต่ำกว่า 15"** ★
  อยากให้บอสตัวนี้ตีโดนง่ายขึ้นด้วย เปลี่ยน `LOW_LEVEL_FLEE_CAP` เป็น **16**
- เพดานความแม่นยังเป็น **98%** (`MAX_HIT_RATE`) จะไม่มีทางเข้า 100%
- มอนเลเวล 15 ขึ้นไปทุกตัวไม่ได้รับผลกระทบ — ความยากช่วงกลาง-ปลายเกมเท่าเดิม

---

## 7.45 รอบ 30 — เนื้อเรื่อง Shadows of Fate ลงเกมจริง

รอบนี้เอาเอกสารเนื้อเรื่องที่พี่เขียนเองมาใส่ในเกม และ **รื้อระบบเควสใหม่ทั้งระบบ**
เพราะของเดิมทำได้อย่างเดียวคือ "ฆ่ามอนกี่ตัว" ซึ่งเขียนเควสเนื้อเรื่องไม่ได้เลย

### ก) ชื่อแมพใหม่ (เปลี่ยนแค่ชื่อที่โชว์ · id ภายในเหมือนเดิม ไม่กระทบเซฟ/ประตู)

| แมพ | ชื่อเดิม | ชื่อใหม่ |
|---|---|---|
| `prontera_town` | เมืองพรอนเทรา | **พรอนเทรา นครแห่งสายฟ้า** |
| `prontera_field` | ทุ่งหญ้าพรอนเทรา | **ทุ่งวิหาร** |
| `asgard_forest_2` | Asgard Forest 2 | **ป่าสนธยา** |
| `dark_forest` | ป่ามืด | **ป่าเงาลึก** |

★ **ทำไมต้องเลิกใช้คำว่า Asgard** ★ ในเนื้อเรื่องของพี่ Asgard คือดินแดนเทพของ **บทที่ 9**
ถ้าเอามาตั้งชื่อป่าข้างเมืองมนุษย์ตั้งแต่ชั่วโมงแรก ไพ่ใบสุดท้ายของเกมจะหมดความหมาย

### ข) NPC ในเมือง — เปลี่ยนชื่อ 3 · เพิ่มใหม่ 4

| โหนด | ชื่อ | บทบาทในเนื้อเรื่อง |
|---|---|---|
| `Healer` | ~~ซิสเตอร์มาเรีย~~ → **นักบวชหญิงมาเรีย** | จบประโยคด้วย "จงขอบคุณธอร์" |
| `SavePoint` | ~~ศิลาบันทึก~~ → **ศิลาสลักแห่งธอร์** | ใต้ตราค้อนมีรูนเก่ากว่าถูกสกัดออก (เบาะแสลับ) |
| `Guide` | ~~ไกด์นำทาง~~ → **ทหารยามเอริค** | สอนปุ่ม + "อย่าเข้าป่าลึกตอนค่ำ" |
| `HighPriest` ★ใหม่ | **นักบวชสูงสุดวาลเดอร์** | ผู้ให้เควสหลักของบท 1 |
| `OldMan` ★ใหม่ | **ตาแก่กุนนาร์** | ชายชราปริศนา — ต้องมีบทพูด 4 ชุด |
| `Ingrid` ★ใหม่ | **อิงกริด** | ลูกสาวนักล่าที่หายไปในป่า |
| `GuildMaster` ★ใหม่ | **หัวหน้ากิลด์บียอร์น** | กิลด์นักผจญภัย |

พ่อค้าโทนี่กับช่างตีเหล็กฮันส์ **คงชื่อเดิม** แต่เพิ่มบทพูดที่ปลูกเมล็ดของบทที่ 2 ไว้

### ค) ★★ เควสมีได้หลายเงื่อนไข 6 ชนิด ★★ (ของใหม่ที่สำคัญที่สุดของรอบนี้)

ไฟล์ใหม่ `scripts/resources/objective_data.gd` — เควส 1 อันใส่เงื่อนไขได้ไม่จำกัดข้อ

| ชนิด | ความหมาย | ช่อง Target ใส่อะไร |
|---|---|---|
| `KILL` | ฆ่ามอนสเตอร์ | id ของมอน เช่น `poring` |
| `COLLECT` | มีไอเทมในกระเป๋า | id ของไอเทม เช่น `red_potion` |
| `TALK` | คุยกับ NPC | **ชื่อที่โชว์บนหัว** เช่น `ตาแก่กุนนาร์` |
| `VISIT` | ไปให้ถึงแมพ | map_id เช่น `dark_forest` |
| `READ` | อ่าน/ตรวจของในแมพ | id ของจุดนั้น เช่น `shrine_bookshelf` |
| `FLAG` | ธงเนื้อเรื่องถูกตั้งแล้ว | ชื่อธง เช่น `saw_ceremony` |

**วิธีสร้างเควสใหม่** — เปิดไฟล์ `.tres` ในโฟลเดอร์ `data/quests/`
ช่อง **"เงื่อนไข (หลายข้อ)"** → กด **+** → เลือก **New ObjectiveData** → กรอก Kind / Target / Count

- `COLLECT` กับ `FLAG` **นับสด** จากกระเป๋า/ธงจริง ๆ ของหายก็ลดลงตาม ไม่ต้องเก็บตัวเลข
- `COLLECT` มีช่อง **Consume** — ปิดไว้ = ส่งเควสแล้วผู้เล่นเก็บของไว้ได้
- ช่อง **Text** เว้นว่าง = ระบบเขียนบรรทัดในสมุดเควสให้เอง
- สมุดเควสโชว์ทีละข้อ เช่น `ล่า โพริง  3/3` · `คุยกับ ตาแก่กุนนาร์  [x]`

★ **เควสเก่ายังใช้ได้ทั้งหมด** ★ อันที่กรอก Kill Monster Id / Kill Count ไว้แบบเดิม
ระบบแปลงให้เป็นเงื่อนไข `KILL` 1 ข้ออัตโนมัติ ไม่ต้องแก้ไฟล์เก่าเลย

**ช่องใหม่ในไฟล์เควส** สำหรับร้อยเควสเป็นสายเนื้อเรื่อง

| ช่อง | ใช้ทำอะไร |
|---|---|
| `Required Quests` | ต้องทำเควสไหนจบก่อนถึงจะรับได้ (ใส่ได้หลายอัน) |
| `Required Flag` | ต้องมีธงเนื้อเรื่องนี้ก่อน |
| `Set Flag On Complete` | ส่งเควสแล้วตั้งธงนี้ — ใช้ปลดล็อกเควสถัดไป |

### ง) ธงเนื้อเรื่อง (Story Flags) — เก็บลงเซฟแล้ว

```gdscript
PlayerState.set_flag(&"swore_oath")      # ตั้งธง
PlayerState.has_flag(&"swore_oath")      # เช็ค
PlayerState.set_flag(&"trust_valder", 3) # เก็บตัวเลขก็ได้
PlayerState.get_flag(&"trust_valder")    # อ่านตัวเลข
```

ใช้จำว่า **สาบานหรือเงียบ** ตอน M2 · เคยเจอคนแปลกหน้าหรือยัง · เห็นพิธีแล้วหรือยัง
ธงถูกเซฟและโหลดกลับมาครบ (ทดสอบการเซฟ-โหลดผ่านแล้ว)

### จ) NPC พูดหลายชุดตามความคืบหน้า

โหนด NPC มีช่องใหม่ **`Dialog By Flag`** (Dictionary)

- **key** = ชื่อธง · **value** = บทพูดชุดนั้น
- ระบบไล่จาก **ล่างขึ้นบน** — ธงที่ตรงและอยู่ล่างสุดชนะ
- ไม่มีธงไหนตรงเลย = ใช้ช่อง `Dialog` เดิม

ตัวอย่างตาแก่กุนนาร์ที่ต้องเปลี่ยน 4 ครั้ง — ใส่ 3 คีย์เรียงตามลำดับเนื้อเรื่อง
`met_stranger` → `saw_ceremony` → `read_hunter_journal` แล้วชุดล่างสุดที่ตรงจะถูกเลือก

### ฉ) ของในแมพที่กด F อ่านได้ (LoreObject)

ไฟล์ใหม่ `scenes/world/lore_object.tscn` — ใช้ทำชั้นหนังสือ · หลุมศพ · รอยไหม้บนเปลือกไม้ ·
ป้ายบอกทาง · เป้ของนักล่าที่หายไป

**วิธีใช้**

1. ลาก `scenes/world/lore_object.tscn` เข้าไปวางในแมพ
2. ตั้งช่อง **Lore Id** (ห้ามซ้ำ เช่น `shrine_bookshelf`)
3. พิมพ์ข้อความลงช่อง **Text** — **เว้นบรรทัดว่าง = ขึ้นหน้าใหม่** ในกล่องสนทนา
4. อยากให้เห็นรูป — เพิ่ม `Sprite2D` เป็นลูกของโหนดนี้
5. อยากให้มันเดินเควส — ใส่เงื่อนไขชนิด `READ` แล้ว Target = Lore Id เดียวกัน

ช่องเสริม: `Text Again` (ข้อความตอนกลับมาอ่านซ้ำ) · `Required Flag` (ต้องมีธงก่อนถึงอ่านได้) ·
`Set Flag` (อ่านแล้วตั้งธง) · `Give Item` (อ่านครั้งแรกได้ของ) · `Label Text` (ป้ายลอยเหนือหัว)

อ่านแล้วระบบตั้งธง `read_<lore_id>` ให้อัตโนมัติ เอาไปใช้เป็นเงื่อนไขเควสต่อได้เลย

### ช) ★ บั๊กที่เจอตอนเทสต์รอบนี้ ★ `bash.tres` พัง

ตอนใส่เอฟเฟกต์สกิลรอบ 29 สคริปต์เขียนบรรทัด `effect_frames = ExtResource("2_fx")`
ลงไฟล์ **แต่ลืมประกาศ `2_fx` ไว้ข้างบน** ผลคือ Godot โหลด `bash.tres` ไม่ได้เลย
→ สกิล Bash หายจากระบบ → Slash ที่ต้องใช้ Bash เลเวล 5 เป็นเงื่อนไข **เรียนไม่ได้**

แก้แล้วในรอบนี้ (เพิ่มบรรทัด `[ext_resource ... path="res://data/sprites/fx_bash.tres" id="2_fx"]`)

**บทเรียน** — เวลาสคริปต์เติมบรรทัด `ExtResource("xxx")` ลงไฟล์ `.tres`
ต้องเติม `[ext_resource ...]` ที่ประกาศ `xxx` ข้างบนด้วยเสมอ ไม่งั้นทั้งไฟล์พังเงียบ ๆ

### ซ) เทสต์

ชุดเทสต์ตอนนี้ **971 ข้อ / 31 ชุด ผ่านหมด** (เพิ่มชุดใหม่ `r30_test` 66 ข้อ)
ครอบคลุมเงื่อนไขเควสทั้ง 6 ชนิด · การเซฟ-โหลดธง · เควสเก่าแบบเดิม · เซฟรุ่นเก่า ·
การล็อกเควสด้วย Required Quests/Flag · LoreObject

### ★ ต่อไปทำอะไร ★

1. เขียนเควสจริง M1–M6 ของบทที่ 1 เป็นไฟล์ `.tres` ด้วยระบบใหม่นี้
2. ทำ cutscene (ล็อกการควบคุม + แพนกล้อง) สำหรับ **M6 พิธีฉลองชัยชนะ**
3. แมพกลางคืนสำหรับเควสลับ S1 — ใช้ธงสลับสีแมพ ไม่ต้องทำนาฬิกา
4. NPC **คนแปลกหน้า** (= โลกิ) + ไอเทมใหม่ 5 ชิ้น
5. บอส **อสูรสายฟ้า** — งานภาพ

---

## 7.46 รอบ 31 — ฉากหลังป่าสนธยา · ไอเทมขยะเฉพาะตัว · ของสวมใส่ 29 ชิ้น · มอนบท 2 · แมพบท 2

### ก) ★ ฉากหลังมืดดำ = ช่อง Color ของ Polygon2D ★

อาการ: วางภาพลง Polygon2D แล้วภาพกลายเป็นดำมืด/เขียวเข้ม ทั้งที่ไฟล์ภาพสว่างปกติ

**สาเหตุ** — Polygon2D มีช่อง **Color** ที่ "คูณทับ" สีของ Texture
ถ้าตั้ง Color เป็นเขียวเข้ม (0.09, 0.16, 0.13) ภาพจะถูกคูณให้มืดตาม
(ช่องนี้เอาไว้ใช้ตอน **ไม่มี** Texture — เป็นสีทึบของรูปหลายเหลี่ยม)

**กฎ:** ชั้นไหนใส่ Texture → ตั้ง Color เป็น **ขาว (1, 1, 1, 1)** เสมอ

ค่าที่แก้ให้ในป่าสนธยา (`asgard_forest_2.tscn`)

| ชั้น | Texture | position | polygon | uv |
|---|---|---|---|---|
| Sky | `map/asgard forest2 sky.png` (3800x1200) | (-100, -200) | (0,0)-(3800,1200) | เท่ากับ polygon |
| FarTrees | ยังไม่มีภาพ → **ซ่อนไว้** (`visible = false`) | — | — | — |
| Ground/Visual | `map/test.png` (3500x880) | (-1800, -120) จากโหนด Ground | (0,0)-(3600,880) | (0,0)-(3500,880) |

พื้นยืนอยู่ที่ y = 880 (Ground ที่ (1700,1000) กล่องชน 3600x240 → ขอบบน 880)
ภาพพื้นเลยวางให้ขอบบนตรง 880 พอดี ส่วนล่างของภาพอยู่นอกกล้อง ไม่เป็นไร

**อีกอย่างที่แก้** — ภาพ Sky/พื้นถูกอ้างเป็น `SubResource CompressedTexture2D` ชี้ไปที่
`.godot/imported/...ctex` ตรง ๆ ซึ่งจะพังทันทีที่ Godot reimport (ชื่อไฟล์ hash เปลี่ยน)
เปลี่ยนเป็น `ext_resource path="res://Sprites/map/..."` ให้แล้ว
→ **ตอนใส่ภาพใน Inspector ให้ลากจากช่อง FileSystem เสมอ** อย่ากด "Load" แล้วไปเลือกในโฟลเดอร์ .godot

### ข) ไอเทมขยะเฉพาะตัว — มอนทุกตัวมีของตัวเอง 1-2 ชิ้น (38 ชิ้นใหม่)

ของเดิมมอนหลายตัวดรอปของซ้ำกัน (fluff 3 ตัว · jellopy 3 ตัว) ตอนนี้ **ทุกตัวมีของที่ดรอปได้แค่ตัวเดียว**

| มอน | ของขยะใหม่ (โอกาส) |
|---|---|
| โพริง | แกนวุ้นโพริง 8% |
| แฟเบร | ใยแฟเบร 35% · หนวดแมลง 15% |
| ลูนาติก | หางกระต่ายขาว 30% · แครอทกัดครึ่ง 20% |
| ดรอปส์ | วุ้นสีส้ม 45% · หยดน้ำค้างแสง 10% |
| ชอนชอน | ปีกแมลงใส 40% · ตาโปนชอนชอน 8% |
| ฮอร์เน็ต | เหล็กใน 35% · หยดน้ำผึ้ง 15% |
| หมาป่า | เขี้ยวหมาป่า 30% · กรงเล็บหมาป่า 15% |
| คิงโพริง | วุ้นราชา 80% · มงกุฎจิ๋ว 25% |
| มูนัค | ปิ่นปักผมเก่า 25% · ผ้าห่อศพเก่า 30% |
| ออร์คนักรบ | ใบขวานหัก 30% · ผ้าโพกหัวออร์ค 12% |
| บาฟโฟเมท จูเนียร์ | เขาเล็กปีศาจ 40% · หางปีศาจน้อย 15% |
| บาฟโฟเมท | กีบเท้ายักษ์ 100% · เศษเคียวต้องสาป 50% |

ทั้งหมดเป็น type **MATERIAL** ขายได้ (ราคาขายไล่ตามเลเวลมอน 8 → 6,000 z) — เอาไปใช้เป็นวัตถุดิบเควส/ตีเหล็กบท 2 ต่อได้
ของบท 2 อีก 15 ชิ้นอยู่ในมอนบท 2 (ดูข้อ ง)

### ค) ของสวมใส่ใหม่ 29 ชิ้น — ไล่เลเวล 1 → 36

**อาวุธ** (ดาบทั้งหมด · ท่าโจมตี `Attack_Blade`)

| id | ชื่อ | Lv | ATK | พิเศษ | ได้จาก |
|---|---|---|---|---|---|
| `wooden_sword` | ดาบไม้ | 1 | 14 | | ร้านโทนี่ 120z · โพริง 1% |
| `short_sword` | ดาบสั้น | 4 | 30 | | ร้านโทนี่ |
| `rapier` | เรเปียร์ | 8 | 34 | HIT+8 CRI+3 | ร้านโทนี่ |
| `bastard_sword` | ดาบบาสตาร์ด | 18 | 46 | ASPD-4% | ออร์ค 1.5% · ร้านเฮลกา |
| `iron_blade` | ใบมีดเหล็กคนแคระ | 22 | 50 | DEF+3 | ร้านเฮลกา |
| `forge_saber` | ดาบเตาหลอม | 30 | 72 | STR+2 | ร้านเฮลกา · บอสบท 2 12% |
| `runic_blade` | ดาบรูน | 36 | 94 | MATK+20 INT+2 | ผู้เฝ้ารูน 1% |

ไล่ให้เข้ากับของเดิม: ดาบมือใหม่ 25 → ฟัลชิออน 39 (12) → **คาตานะ 53 (25)** → เคลย์มอร์ 92 (35) → ดาบเพลิง 105 (50)

**โล่** โล่ไม้ Lv1 DEF4 · โล่เหล็ก Lv20 DEF22 · โล่กระจก Lv32 DEF26 MDEF8
**หมวก** ริบบิ้น Lv1 · หมวกหนัง Lv6 · หมวกเหล็ก Lv18 · หมวกนักขุด Lv24
**เกราะ** เสื้อหนัง Lv8 · เกราะโซ่ Lv18 · เกราะเกล็ด Lv26 · เกราะคนแคระ Lv34 DEF36
**ผ้าคลุม** ผ้าพันคอ Lv10 · ผ้าคลุมหนังหมาป่า Lv15 (หมาป่า 2%) · ผ้าคลุมถ่านไฟ Lv28
**รองเท้า** รองเท้าหนัง Lv6 · สนับแข้งเหล็ก Lv22 · รองเท้านักขุด Lv26
**เครื่องประดับ** สร้อยคอ/ต่างหู/เข็มกลัด Lv12 (VIT/INT/AGI+2) · ลูกประคำ Lv20 · เข็มขัดหนัง Lv24

ร้านโทนี่ขายของบท 1 ทั้งหมด (25 รายการ) · ร้านเฮลกาในเมืองคนแคระขายของบท 2 (19 รายการ)

### ง) มอนบท 2 — 7 ตัว + บอส

| id | ชื่อ | Lv | HP | แมพ | จุดเด่น |
|---|---|---|---|---|---|
| `pitman` | พิตแมน | 19 | 700 | ทางเหล็ก | ตัวเล็ก ไม่โจมตีก่อน |
| `steel_beetle` | ด้วงเหล็ก | 22 | 900 | ทางเหล็ก | **DEF 30** ช้า |
| `ember_bat` | ค้างคาวถ่านไฟ | 25 | 820 | เหมืองถ่านไฟ | เร็ว 170 · FLEE 38 · กระโดดได้ |
| `magma_slug` | ทากลาวา | 27 | 1300 | เหมืองถ่านไฟ | ธาตุไฟ ช้ามาก |
| `forge_golem` | โกเลมเตาหลอม | 30 | 2400 | ห้องโถงเงียบ | ตัวใหญ่ DEF 40 knockback แรง |
| `silent_wraith` | ภูตไร้เสียง | 33 | 1600 | ห้องโถงเงียบ | เงา · FLEE 48 MDEF 35 |
| `rune_watcher` | ผู้เฝ้ารูน | 35 | 3200 | ห้องโถงเงียบ (มินิบอส) | สกิล "รูนระเบิด" |
| `forge_guardian` | ผู้พิทักษ์เตาหลอม | 38 | 9000 | เตาหลอมร้าง (บอส) | สกิล "ค้อนเตาหลอม" · MVP |

ทุกตัวมี **การ์ด** ของตัวเอง (`card_<id>`) + ของขยะ 2 ชิ้น + ดรอปของสวมใส่บางชิ้น

★ **สไปรท์ชั่วคราว** ★ ตอนนี้ทุกตัวใช้รูปวงกลมสีมีตัวอักษรที่ `Sprites/monsters/placeholder/<id>.png`
ผ่าน `data/sprites/placeholder/<id>_frames.tres` (Idle/Attack/Hit/Die)
**วาดเสร็จแล้ว:** สร้าง SpriteFrames ใหม่ → ลากใส่ช่อง `Sprite Frames` ในไฟล์ `data/monsters/<id>.tres`
แล้วปรับ `Display Height` / `Hitbox Size` ตามรูปจริง — ไม่ต้องยุ่งกับไฟล์ placeholder

ไอคอนไอเทมก็เหมือนกัน — อยู่ที่ `Sprites/items/placeholder/<id>.png` (สี่เหลี่ยมสี + ตัวย่อ)
วาดเสร็จลากใส่ช่อง `Icon` ของไอเทมได้เลย

### จ) แมพบท 2 — 5 แมพ (ยังเป็นสีพื้น รอฉากหลัง)

```
ป่าเงาลึก ──(ล็อกด้วยธง chapter2_open)──▶ ทางเหล็ก ──▶ นิดาเวลลิร์ ──▶ เหมืองถ่านไฟ ──▶ ห้องโถงเงียบ ──▶ เตาหลอมร้าง
```

| id | ชื่อ | กว้าง | มอน | พิเศษ |
|---|---|---|---|---|
| `iron_road` | ทางเหล็ก | 4800 | พิตแมน ด้วงเหล็ก ×5 | แท่นกระโดด 4 |
| `nidavellir_town` | นิดาเวลลิร์ นครเตาหลอม | 3400 | — | NPC 6 คน + หลุมศพซินดริ |
| `ember_mine` | เหมืองถ่านไฟ | 5000 | ค้างคาว ทาก ×5 | แท่น 5 |
| `hall_of_silence` | ห้องโถงเงียบ | 4600 | โกเลม ภูต ×4 + ผู้เฝ้ารูน | มินิบอส x=4100 |
| `cold_forge` | เตาหลอมร้าง | 3000 | บอส | แบบร่างค้อนบนกำแพง (LoreObject ล็อกด้วยธง `killed_forge_guardian`) |

NPC เมืองคนแคระ: **นายหน้าเฮลกา** (ร้าน) · **ช่างเอกดวาลิน** (ตีบวก) · **บรอกก์** (มีบทพูดพิเศษหลังอ่านหลุมศพ)
· ศิลาสลักโบราณ (เซฟ) · หมอคนแคระเฮดิน (รักษา) · ฮันส์ (ตามมาจากบท 1)

ทุกแมพมี `chapter = 2` · `region = "สวาร์ทัลฟ์เฮม"` (ช่องใหม่ใน MapBase)
โหนด **Art** (Sprite2D ว่าง) เตรียมไว้ให้ลากภาพฉากหลังใส่ · **ทุกแมพใช้พื้นที่ y = 880-900**

### ฉ) ★ ประตูล็อกด้วยธงเนื้อเรื่อง ★ (ช่องใหม่ใน Portal)

| ช่อง | ความหมาย |
|---|---|
| `Required Flag` | ต้องมีธงนี้ก่อนถึงจะผ่านได้ (ว่าง = ผ่านได้เสมอ) |
| `Locked Text` | ข้อความตอนกดแล้วยังผ่านไม่ได้ |

ประตูป่าเงาลึก → ทางเหล็ก ตั้ง `chapter2_open` ไว้แล้ว → **ให้เควส M13 (บอสอสูรสายฟ้า) ใส่ `Set Flag On Complete = chapter2_open`**
ตอนนี้ยังไม่มีเควสนั้น อยากทดสอบบท 2 ให้พิมพ์ในคอนโซล/เทสต์: `PlayerState.set_flag(&"chapter2_open")`

### ช) ต่อสายแมพบท 1 ให้ถูก (ของเดิมป่าเงาลึกไม่มีทางเข้า!)

เดิม: ทุ่งวิหาร → ป่าสนธยา → **กลับเมือง** · ป่าเงาลึกไม่มีประตูจากที่ไหนเลย
ตอนนี้: ทุ่งวิหาร ⇄ ป่าสนธยา ⇄ **ป่าเงาลึก** ⇄ (บท 2) ตามลำดับเลเวล 1-6 / 6-12 / 12-18

### ซ) เทสต์

**32 ชุด / 1,012 ข้อ ผ่านหมด** (ชุดใหม่ `r31_test` 41 ข้อ)
มีเทสต์ใหม่ที่เดินเข้า **ทุกแมพ 9 แมพ** แล้วเช็คว่า **ประตูทุกบาน (15 บาน) ชี้ไปแมพที่มีจริงและจุดเกิดมีจริง**
— เทสต์นี้แหละที่จับได้ว่าป่าเงาลึกไม่มีทางเข้า

### ★ บทเรียนรอบนี้ ★

1. **สคริปต์ patch ที่รัน 2 ครั้ง ต้องกันแทรกซ้ำด้วย "marker"** ไม่ใช่แค่เช็คว่า "ข้อความใหม่" อยู่ในไฟล์ไหม
   เพราะถ้ามี patch อีกตัวมาแก้ตรงกลาง ข้อความใหม่จะไม่ตรงเป๊ะแล้ว → แทรกซ้ำ (`from_iron_road` โผล่ 2 อัน)
2. **Godot ที่เปิดอยู่จะเซฟทับไฟล์ที่แก้จากข้างนอก** (เติม `unique_id` ให้) — ปิด Godot ก่อนรันสคริปต์เสมอ
3. ทุกครั้งที่เพิ่มแมพ ต้องเพิ่มใน `Game.MAPS` + สร้าง Marker2D ชื่อตรงกับ `target_spawn_point` ของประตูที่ชี้มา

---

## 7.47 รอบ 32 — หน้าหลัก (Title Screen) · เอฟเฟกต์ Bash แดง 2 จังหวะ · เอฟเฟกต์ไม่หายก่อนเล่นจบ

### ก) หน้าหลัก `scenes/ui/title_screen.tscn` + `scripts/ui/title_screen.gd`

**ตั้งเป็น Main Scene แล้ว** (`project.godot` → `run/main_scene`) กด F5 จะเข้าหน้านี้ก่อน

- ภาพหลัก `Sprites/ui/title_key_art.jpg` (1920x1080 · JPG 500 KB เพื่อไม่ให้ไฟล์เกมบวม) ซูมช้า ๆ ไป-กลับ
- เมนูมุมล่างซ้าย: **เริ่มเกมใหม่ · โหลดช่อง 1-3 · ออกจากเกม** (ปุ่มออกซ่อนอัตโนมัติบนเว็บ)
  ช่องที่มีเซฟโชว์ `Lv.7 นักดาบ · ป่าสนธยา` · ช่องว่างกดไม่ได้และปุ่มลูกศรจะข้ามให้
- คุมได้ทั้ง ↑↓ + Enter/Space/F/X · เมาส์ · แตะจอ
- กดเริ่ม → หน้าโหลดสีดำ "กำลังเริ่มการเดินทาง..." + **เคล็ดลับสุ่ม** (แก้ลิสต์ `TIPS` ในสคริปต์) → เข้าเมืองพรอนเทรา
- **เปลี่ยนภาพ:** วางไฟล์ทับ `title_key_art.jpg` (16:9) · เปลี่ยนเวอร์ชันที่ `const VERSION`

**กลับหน้าหลักจากในเกม** — Tab → เมนูระบบ → ปุ่ม **"กลับหน้าหลัก"** (มีกล่องยืนยัน) หรือเรียก `Game.go_title()`

**ของใหม่ในระบบ**
- `UI.set_in_game(false)` ซ่อน HUD/มินิแมพ/แถบปุ่ม/จอสัมผัสทั้งหมด + ปิดปุ่มลัด C/I/K... (`UI.in_game`)
  หน้าหลักเรียกเองตอนเปิด · เข้าเกมแล้วเรียก `set_in_game(true)` ให้เอง
- เซฟเก็บ **ชื่อแมพที่โชว์** (`map_name`) เพิ่ม — เซฟเก่าที่ไม่มีจะโชว์ id แทน เซฟใหม่ทีเดียวก็หาย

★ **กับดักที่เจอ** ★ `Control.position` นับจาก **มุมซ้ายบนของแม่เสมอ** ไม่ใช่จาก anchor
ตั้ง `set_anchors_preset(BOTTOM_LEFT)` แล้ว `position.y = -300` = อยู่เหนือจอ (เมนูหายทั้งแถบ)
ต้องคำนวณเอง `position.y = size.y - เมนู.size.y - 34` หลังรอ 1 เฟรม และทำซ้ำใน `resized`

### ข) ★ เอฟเฟกต์หายก่อนเล่นจบ — สาเหตุ + วิธีแก้ ★

**สาเหตุ:** `bash.tres` ตั้ง `Effect Max Targets = 1` → พอเอฟเฟกต์ชนมอนตัวแรก โค้ด `_damage_step()`
เรียก `_expire()` ทันที (จางหายใน 0.12 วิ) ภาพเลยเล่นไปได้แค่ 2-3 เฟรมแล้วหาย
(เดิมออกแบบไว้แบบ "กระสุน" ชนแล้วหาย ซึ่งไม่เหมาะกับท่าฟัน)

**แก้ใน `skill_effect.gd`:** ชนครบโควตาแล้ว **หยุดอยู่กับที่** (`_speed = 0`) แล้วเล่นภาพต่อจนจบ
(ยังคงแบบกระสุนไว้ให้เฉพาะ `Effect Pierce = ปิด`)

**ช่องใหม่ใน SkillData** (กลุ่ม "เอฟเฟกต์ทำดาเมจเอง")

| ช่อง | ความหมาย | Bash ตั้งไว้ |
|---|---|---|
| `Effect Hit Count` | ตีกี่ครั้งต่อมอน 1 ตัว (0 = ใช้ Hit Count ของสกิล) | 2 |
| `Effect Hit Interval` | เว้นช่วงระหว่างครั้ง (วิ) | 0.2 |
| `Effect Stick On Hit` | ชนครบโควตาแล้วหยุดอยู่กับที่ | เปิด |

ตอนนี้ Bash **ตี 2 ครั้ง** (ตามสไปรท์ 2 จังหวะ) ครั้งละ `damage_mult` เต็ม ห่างกัน 0.2 วิ · ตัวเลขดาเมจเด้ง 2 ครั้ง
เทสต์ `r32_test` ยืนยัน: โดน 2 ครั้ง · อยู่ถึงเฟรมสุดท้าย · หยุดหลังชน · ไม่มีมอน = วิ่งไปจนหมดอายุแล้วหายเอง

**`Effect Life = 0` = อายุอัตโนมัติเท่าความยาวอนิเมชัน** — แนะนำใช้ 0 เสมอ ถ้าตั้งเลขเองแล้วสั้นกว่าอนิเมชัน ภาพจะโดนตัด

### ค) เอฟเฟกต์ Bash ใหม่ — แดง 2 จังหวะ (ให้เข้ากับสไปรท์ `Attack_Blade_bash`)

`Sprites/effects/bash_slash.png` **6144x512 = 12 เฟรม 512x512** · `fx_bash.tres` 24fps (0.5 วิ)

| เฟรม | ภาพ |
|---|---|
| 0-2 | จังหวะ 1 โค้งแดงกวาดบนซ้าย → ล่างขวา (โค้งคว่ำ เอียง 14°) มีเงาตามหลัง |
| 3 | กระแทกครั้งที่ 1 (แสงแฉกที่ปลาย) ← **ดาเมจครั้งที่ 1** |
| 4-7 | จังหวะ 2 โค้งหงายกวาดล่างซ้าย → บนขวา ไขว้กับอันแรกเป็นรูปตา |
| 8 | กระแทกครั้งที่ 2 + ประกายไฟกระเด็น ← **ดาเมจครั้งที่ 2** |
| 9-11 | จางหาย |

จังหวะเทียบกับสไปรท์ (13 เฟรม 13fps): ดาบฟันครั้งแรกเฟรม 6 (0.38 วิ) ครั้งสองเฟรม 8 (0.54 วิ)
→ ตั้ง `Effect Delay = 0.34` เอฟเฟกต์เลยโผล่พร้อมดาบพอดี · `Effect Speed` ลดเหลือ 380

**สร้างใหม่/ปรับสี:** `python3 make_bash_fx.py` (ต้องมี PIL) — แก้ `CORE/LIGHT/MAIN/DEEP` เปลี่ยนสี ·
`a0, a1, tilt` มุมกวาด/ความเอียง · `thick = C * 0.10` ความหนา · `N`/`FPS` จำนวนเฟรม
วาดเองก็ได้: เฟรม 512x512 เรียงแนวนอน แล้วแก้จำนวน `AtlasTexture` ใน `fx_bash.tres` ให้ตรง

### ง) เทสต์ — 34 ชุด / 1,039 ข้อ ผ่านหมด
ใหม่: `r32_test` (เอฟเฟกต์ 2 จังหวะ 11 ข้อ) · `r32b_test` (หน้าหลัก 16 ข้อ: ซ่อน HUD · ข้ามช่องว่าง · โหลดไปแมพที่เซฟ · กลับหน้าหลัก · เกมใหม่)

## 7.48 รอบ 33-37 — มอนพักเดิน · เลขดาเมจแบบ Maple · กำแพงล่องหน · กระสุนมอน · ฉากหลังป่าเงาลึก · เอฟเฟกต์ Magnum Break

### ก) รอบ 33 — มอนหยุดพัก idle 3-5 วิ ระหว่างเดิน
เดิม `_pick_new_wander()` สุ่มแค่ "เดินซ้าย/ขวา/หยุด" เท่า ๆ กัน มอนเลยดูเดินตลอด
ใหม่ MonsterData มีช่องกลุ่ม **Wander** ใน Inspector:

| ช่อง | ค่าเริ่ม | ความหมาย |
|---|---|---|
| Wander Pause Chance | 0.65 | โอกาสที่รอบต่อไปจะ "หยุดพัก" แทนเดิน |
| Wander Pause Min / Max | 3.0 / 5.0 | พักนานกี่วิ (สุ่มระหว่างนี้) |
| Wander Walk Min / Max | 1.6 / 3.2 | เดินนานกี่วิต่อรอบ |
| Wander Look Chance | 0.7 | ตอนพัก มีโอกาสหันซ้าย-ขวามองรอบ ๆ (ไม่ยืนนิ่งทื่อ) |

`patch_wander_idle.py` ตั้งค่าให้มอน 20 ตัวตามนิสัย (โพริงพักบ่อย · ค้างคาว/หมาป่าพักน้อย · บอสแทบไม่พัก)
`set_home()` สุ่มเวลาเริ่มของแต่ละตัว → มอนที่เกิดพร้อมกันไม่เดิน-หยุดพร้อมกันเป็นแถว

### ข) รอบ 34 — ตัวเลขดาเมจใหญ่ เด้งทีละหลัก เหมือน MapleStory
- `Sprites/ui/damage_digits.png` — ชีทตัวเลข 0-9 × 4 สไตล์ ช่องละ 56x72 (สร้างจาก `make_damage_digits.py`)
  แถว 0 ปกติ (ม่วง) · 1 คริ (ส้มทอง) · 2 โดนตี (แดง) · 3 ฮีล (เขียว)
- `scripts/world/damage_number.gd` (DamageNumber) — ตัวเลขเป็น Sprite2D ทีละหลัก เด้งโผล่จากซ้ายไปขวา (ใหญ่ 1.9 เท่าแล้วหดกลับ TRANS_BACK) ลอยขึ้น 78 px แล้วจาง
- `floating_text_layer.gd` ดัก DAMAGE / HURT / ฮีล "+N" ให้ไปใช้ DamageNumber อัตโนมัติ ข้อความอื่น (MISS, ชื่อสกิล) ยังเป็นฟอนต์เดิม
- ★ อยากเปลี่ยนหน้าตาตัวเลข ★ แก้สีใน `make_damage_digits.py` แล้วรันใหม่ หรือวาดชีทเองช่องละ 56x72 ทับไฟล์เดิม

### ค) รอบ 35 — มอนติดกำแพงล่องหนในทุ่งวิหาร
สาเหตุ 2 อย่างซ้อนกัน:
1. ใน `Sprites/world_node_2d.tscn` มีกล่องชนบาง ๆ 8x777 ที่ x=109 (ขอบซ้ายแมพ) และ `auto_fit_bounds` คำนวณขอบแมพจากรูปพื้นหลัง (-781..5969) → จุดเกิดมอนไปอยู่ **หลัง** กำแพงนั้น มอนเลยเดินชนกำแพงตลอด
   แก้: `map_bounds = Rect2(120, -320, 4400, 940)` · `auto_fit_bounds = false`
2. มอนไม่มีระบบ "รู้ตัวว่าติด" → เพิ่มใน `monster_base.gd`: จำความเร็วที่ตั้งใจ (`_want_vx`) ก่อน `move_and_slide()` (เพราะ move_and_slide จะเขียน velocity.x เป็น 0 ตอนชน จับไม่ได้)
   ถ้าตั้งใจเดินแต่ตัวขยับ < 8 px/วิ นาน 0.7 วิ → `_free_from_wall()` กลับหลังหัน · กระโดดถ้ากระโดดได้ · ย้าย "บ้าน" (spawn_position) มาฝั่งที่ว่าง
- `map_spawner.gd` — จุดเกิดต้องยิงเรย์จากผู้เล่นถึงได้ (`_reachable_from`) ไม่งั้นสุ่มใหม่ (ไม่เกิดหลังกำแพง)

### ง) รอบ 36 — ลูนาติกยิงบอล · คิงโพริงขว้างบอลระเบิด
`scripts/entities/monster_projectile.gd` (MonsterProjectile) — ไม่ต้องสร้างโหนดเอง แค่ใส่รูปใน MonsterData:

| กลุ่ม | ช่อง | ใช้ทำอะไร |
|---|---|---|
| Projectile | Projectile Texture | ใส่รูป = โจมตีปกติจะกลายเป็น **ยิงตรง** จากท่า Attack (ลูนาติก: `Sprites/effects/lunatic_ball.png`) |
| | Speed / Range / Height / Offset / Hit Size / Faces Left / Spin | ความเร็ว · ระยะไกลสุด · ขนาดบนจอ · จุดปล่อย (จากเท้า) · กล่องชน · รูปหันซ้าย? · หมุน |
| Skill Projectile | Skill Projectile Texture | ใส่รูป = สกิลจะ **ขว้างโค้ง** ไปตกที่เท้าผู้เล่นแล้วระเบิด (คิงโพริง: `king_poring_ball.png`) |
| | Arc / Time / Spin / Height / Offset | ความสูงโค้ง · เวลาบิน · หมุน · ขนาด · จุดปล่อย |
| | Skill Explosion Frames / Anim / Height | เอฟเฟกต์ระเบิด (ว่าง = ใช้ `Sprites/effects/slime_burst.png` ที่ทำให้) |

ระเบิดทำดาเมจถ้าผู้เล่นอยู่ในรัศมี `skill_radius_x/y` ของมอน · บอลมีเงาบนพื้นตอนลอย · `patch_projectiles.py` ตั้งค่าลูนาติก/คิงโพริงให้แล้ว

### จ) ฉากหลังป่าเงาลึก (dark_forest) เล็กผิดปกติ
`Sprites/map/dark forest.png` 2800x650 แต่ Polygon2D ไม่ได้ตั้ง uv → Godot วาดรูปขนาดจริง (เล็ก) ที่มุมซ้ายบน
แก้ใน `scenes/maps/dark_forest.tscn`: polygon (0,0)-(5600,1300) · uv (0,0)-(2800,650) (= ขยาย 2 เท่า) · ซ่อน FarLayer/Ground Visual เดิม
ระดับพื้น: ทางเดินหินในรูปอยู่ที่ y≈480 ของรูป ×2 = 960 → `Ground0` ย้ายไป y=1070 (ขอบบน 960) · ประตู/จุดเกิดบอส y=960
สำรองไว้ที่ `dark_forest_ก่อนใส่ฉากหลัง.tscn.bak`

### ฉ) รอบ 37 — ★ เอฟเฟกต์ Magnum Break: ระเบิดไฟรอบตัวตอนแทงดาบลงพื้น ★
- `Sprites/effects/magnum_break.png` — 14 เฟรม 768x384 (10752x384) @24fps ≈ 0.58 วิ สร้างจาก `make_magnum_fx.py`
  เฟรม 0-2 แฟลช+ลำแสงที่จุดแทง → 2-8 วงไฟขยายออกสองข้าง เปลวลุกตามวง ถ่าน/หินกระเด็น → 8-13 มอดเป็นควันแดง
- `data/sprites/fx_magnum.tres` — SpriteFrames ท่า `burst`
- `patch_magnum.py` ใส่ให้ `magnum_break.tres`:

| ช่อง | ค่า | เหตุผล |
|---|---|---|
| Effect Height | 340 | ชีท 384 สูง → มาตราส่วน 0.885 → กว้าง 680 · วงไฟ ±270 = **Range X** · เปลวสูง 220 = **Range Y** พอดี |
| Effect Offset | (0, 28) | กลางตัว · เส้นพื้นในชีทอยู่ y=296/384 → +28 ทำให้เส้นพื้นตรงเท้า (เท้าอยู่ +120 จากกลางตัว) |
| Effect Delay | 0.2 | ท่า `Attack_Blade_magnum_break` 6fps → เฟรมแทงดาบ (เฟรม 1) เริ่ม 0.167 วิ |
| Effect Follow / Speed | ปิด / 0 | ระเบิดอยู่กับที่ที่แทง |
| Effect Damage | ปิด | ดาเมจยังใช้กรอบรอบตัว Range X/Y + Max Targets ของสกิลตามเดิม (โดนซ้าย-ขวาพร้อมกัน สูงสุด 6 ตัว) |

★ ถ้าเปลี่ยน Range X/Y ★ ปรับ Effect Height ตาม: `height = range_x / 305 * 384` (เช่น Range X 300 → 378) แล้วเช็ก offset.y = 120 − 104 × (height/384)
★ ถ้าอยากให้ไฟสีอื่น ★ แก้ `CORE/GOLD/ORANGE/DEEP` ใน `make_magnum_fx.py` แล้วรันใหม่

### ช) เทสต์ — 38 ชุด ผ่านหมด
ใหม่: `r33_test` (พักเดิน 12) · `r35_test` (ติดกำแพง/จุดเกิด 7) · `r36_test` (กระสุน 12) · `r37_test` (Magnum Break 22: ขนาดตรง Range X/Y · เส้นพื้นตรงเท้า · ซ่อนช่วงยกดาบ · โดน 6 ตัวทั้งสองข้าง · นอกระยะไม่โดน · หายเองหลังเล่นจบ)

## 7.49 รอบ 38 — เควสเนื้อเรื่อง M1-M13 + บท 2 · บอสอสูรสายฟ้า · แมพรอยสายฟ้า · ฉากหลังบท 2 · UI กระเป๋าใหม่

### ก) เควสเนื้อเรื่อง 21 อัน (สร้างด้วย `gen_round38.py` — รันซ้ำได้)
สายบท 1: **M1 ตรานักผจญภัย** (เอริค) → **M2 คำสาบานใต้ค้อน** (วาลเดอร์ · มีตัวเลือก) → **M3 โพริงล้นทุ่ง** (= hans_poring เดิม) →
**M4 กระสอบที่ถูกกัด** (= tony_fabre เดิม) → **M5 เหล็กจากเหมืองเก่า** (ฮันส์ · เก็บแร่เหล็ก 5) → **M6 พิธีฉลองชัยชนะ** (มีฉากแพนกล้อง) →
**M7 ป่าที่เงียบเกินไป** → **M8 รอยไหม้บนเปลือกไม้** → **M9 คนที่ไม่กลับมา** (อิงกริด) → **M10 สมุดของนักล่า** →
**M11 คิงโพริง** (บียอร์น · ดรอปเศษแก้วเรืองแสง) → **M12 สิ่งที่หลบอยู่ในเงา** (วาลเดอร์ยึดเศษแก้ว!) → **M13 รอยสายฟ้า** (บอส → ตั้งธง `chapter2_open`)
สายบท 2: **C2-1 ประตูที่ไม่ต้อนรับ** → **C2-2 ค่าผ่านทาง** → **C2-3 ชื่อที่ห้ามเอ่ย** → **C2-4 หลุมศพที่ไม่มีชื่อ** →
**C2-5 ห้องโถงเงียบ** → **C2-6 สู่เตาหลอมร้าง** → **C2-7 ผู้พิทักษ์เตาหลอม** → **C2-8 ความจริงของค้อน** (จบบท 2 · ธง `chapter2_done`)

- เควสต่อกันเป็นสายด้วยช่อง `Required Quests` — NPC จะโชว์ **!** เมื่อถึงคิวเท่านั้น
- ★ อยากแก้บทพูด/รางวัล ★ เปิดไฟล์ `data/quests/<id>.tres` ใน Inspector ได้เลย หรือแก้ใน `gen_round38.py` แล้วลบไฟล์เควสนั้นทิ้งก่อนรันใหม่
- ไอเทมเควสใหม่ 4 ชิ้น (type QUEST ขาย/ทิ้งไม่ได้): `adventurer_badge` `burnt_bark_piece` `hunter_journal` `glow_shard`
- LoreObject ใหม่ 3 จุด: ซากมอน + ต้นไม้ไหม้ (ป่าสนธยา) · แคมป์ร้าง (ป่าเงาลึก) — กด F อ่านแล้วเดินเควส M7/M8/M10

### ข) ★ ฟีเจอร์ใหม่ของระบบเควส ★ (แก้ `quest_data.gd` + `npc.gd`)
| ช่องใหม่ใน QuestData | ใช้ทำอะไร | ตัวอย่าง |
|---|---|---|
| Choice Prompt / Options / Flags | ส่งเควสแล้วให้ผู้เล่น **เลือก** แล้วจำเป็นธง | M2: "ข้าขอสาบาน" → `swore_oath` · "ยืนเงียบ" → `stayed_silent` (เก็บไว้ใช้บท 9) |
| Cutscene Pan Npc / Text | ส่งเควสแล้ว **กล้องแพนไปหา NPC** โชว์ข้อความ แล้วแพนกลับ | M6: กล้องแพนไปหยุดที่กุนนาร์ คนเดียวที่ไม่ยกมือสวด |

- ★ ล้มบอสตัวไหนก็ได้ = ตั้งธง `killed_<id>` อัตโนมัติ ★ (แก้ `monster_base.gd`) เช่น `killed_forge_guardian` ปลดล็อกแบบร่างค้อน · `killed_stormscar` ปลดล็อกรอยไหม้กลางลานบอส

### ค) บอสอสูรสายฟ้า + แมพรอยสายฟ้า
- `data/monsters/stormscar.tres` — Lv.20 HP 4500 ธาตุลม บอส MVP · สกิล "สายฟ้าคำราม" · การ์ด + ของขยะ 2 ชิ้น (`storm_scale` `charged_horn`)
- **สไปรท์ชั่วคราว** `Sprites/monsters/placeholder/stormscar.png` — วาดจริงแล้วสร้าง SpriteFrames ใส่ช่อง Sprite Frames ใน stormscar.tres (สูง Display Height 300)
- แมพ `thunder_scar` (รอยสายฟ้า) — ลานบอสบทที่ 1 · ประตูอยู่กลางป่าเงาลึก **ล็อกธง `thunder_scar_open`** (ได้จากจบ M12)
- ตายแล้วเกิดใหม่ทุก 60 วิ (respawn_time ของบอส)

### ง) ฉากหลังแมพบท 2 + รอยสายฟ้า (6 แมพ) — `make_ch2_bg.py`
ภาพ generate ชั่วคราวสวย ๆ (เขียนด้วย PIL): ทางเหล็ก (หุบเขามืด) · นิดาเวลลิร์ (นครในถ้ำ ไฟเตาอุ่น) · เหมืองถ่านไฟ (ลาวา+ค้ำยัน) ·
ห้องโถงเงียบ (เสาหิน+ลำแสงเย็น) · เตาหลอมร้าง (เตายักษ์พัง) · รอยสายฟ้า (ฟ้าผ่า+ต้นไม้ไหม้)
- ไฟล์อยู่ `Sprites/map/generated/<map>_bg.png` (ครึ่งสเกล ผูกเข้าโหนด Sky ด้วย uv ×2)
- ★ วาดจริงแล้ว ★ วางรูปทับไฟล์เดิมชื่อเดิมได้เลย (ขนาดเท่าไฟล์เดิม เช่น iron_road 2500x650) หรือแก้โหนด Sky เอง
- FarLayer กับ Visual ของพื้นถูกซ่อน (`visible = false`) เพราะภาพวาดรวมพื้นไว้แล้ว

### จ) ★ UI กระเป๋าโฉมใหม่ (ตามภาพตัวอย่างของผู้ใช้) ★ — `inventory_window.gd`
- แท็บ 3 หมวด: **ของใช้** (ยา/ของกิน) · **สวมใส่** (อาวุธ/เกราะ) · **อื่น ๆ** (วัตถุดิบ/เควส/การ์ด)
- ช่องค้นหา + ปุ่มค้นหา + ปุ่ม ⟳ ล้างคำค้น (กรองสด ๆ ขณะพิมพ์)
- ปุ่ม "เรียง" มุมขวาบน · เลขจำนวน **สีแดงมุมขวาล่าง** ของช่อง · แถบเงินล่างสุด (ช่องที่ใช้ + ซีนี)
- โทนสีครีม-ชมพูแบบ RO — แก้ได้ที่ค่าคงที่ `C_XXX` หัวไฟล์ (เฉพาะหน้าต่างนี้ หน้าต่างอื่นธีมเดิม)
- กดช่องว่าง = เลิกเลือก · ของเควส (type QUEST) ทิ้งไม่ได้ ขายไม่ได้

### ฉ) กับดักที่เจอรอบนี้
50. **★ patch ฟิลด์หลายบรรทัด (เช่น dialog_by_flag = {...}) ต้อง match ทั้งก้อนถึง `}` ★**
    match แค่บรรทัดแรกแล้วแทนที่ = รันซ้ำแล้วเนื้อเก่าค้างเป็นขยะต่อท้าย (เจอบนเครื่องจริง — แก้แล้ว + มีตัวลบขยะให้)
51. เควสที่ต่อสายด้วย Required Quests ทำให้ **เทสต์เก่าที่รับเควสกลางสายพัง** — เทสต์ต้อง `quests.completed.append(...)` สายก่อนหน้า
52. UI กระเป๋ามีแท็บแล้ว **ตำแหน่งช่องบนจอ ≠ ช่องจริงในกระเป๋า** — โค้ด/เทสต์ต้องผ่าน `_display_to_slot`

### ช) เทสต์ — 39 ชุด / 1,137 ข้อ ผ่านหมด
ใหม่: `r38_test` 42 ข้อ (สายเควสล็อกตามลำดับ · ยึด glow_shard · ธงบอส · แมพรอยสายฟ้า · ประตูล็อกธง · แท็บ/ค้นหากระเป๋า)
แก้เทสต์เก่า 7 ไฟล์ตามกับดักข้อ 51-52

## 7.50 รอบ 39 — วิดีโอหน้าเข้าเกม · แก้ idle ตัวเด้ง · ปรับหน้ากระเป๋า

### ก) วิดีโอเปิดเกมแทนภาพนิ่ง
- ไฟล์ `Sprites/opening game.mp4` ของคุณถูกแปลงเป็น **`Sprites/opening_game.ogv`** แล้ว (Godot เล่นได้เฉพาะ Ogg Theora)
  แปลงบนเครื่องคุณด้วย ffmpeg: `ffmpeg -i "opening game.mp4" -vf scale=1280:720 -c:v libtheora -q:v 7 -c:a libvorbis -q:a 4 opening_game.ogv`
- `title_screen.gd` สร้าง `VideoStreamPlayer` เต็มจอ เล่นวนอัตโนมัติ (ต่อสัญญาณ `finished` → `play()` เพราะโหนดนี้ไม่มีช่อง loop)
- **ภาพนิ่ง `title_key_art.jpg` กลายเป็นตัวสำรอง** — ลบ/ไม่มีไฟล์ .ogv เมื่อไหร่ ระบบกลับไปใช้ภาพนิ่ง+ซูมช้าแบบเดิมเอง
- ★ เปลี่ยนวิดีโอ ★ วางไฟล์ .ogv ทับชื่อเดิม (แนะนำ 1280x720 — 1920 เต็มอาจหน่วงบนเว็บ)
- `export_presets.cfg` เพิ่ม `*.mp4` ใน exclude_filter แล้ว — ไฟล์ mp4 ต้นฉบับไม่ถูกยัดเข้าเกมเว็บ (ใช้ .ogv 5.8 MB แทน)

### ข) ★ ตัวละครยืน idle แล้วเด้งขึ้นลงเป็นจังหวะ ★ (กับดักข้อ 53)
สาเหตุ: ระบบ auto-fit จัด "ปลายเท้า" ให้แตะพื้น **รายเฟรม** โดยวัดขอบล่างของพิกเซลที่ไม่โปร่งใส
แต่ภาพ Idle ของจริง (frame_01..10.png) มี **พิกเซลจาง ๆ ทำให้ขอบล่างคลาดกันถึง 5 px ระหว่างเฟรม**
→ ทั้งตัวถูกดันขึ้น-ลงตามจังหวะเฟรม (8fps) = อาการ "ตัวเด้ง" ที่เห็น
แก้ใน `player.gd` (`_fit_info`): ใช้ **ค่ากลาง (median) ของทั้งท่า** แทนค่าดิบรายเฟรม ทั้งแนวตั้ง (`bottom_use`) และแนวนอน (`dx_use`)
เฟรมที่ต่างจากค่ากลางเกิน **12 px** (ท่าก้ม/เหวี่ยงดาบจริง ๆ) ยังใช้ค่าของเฟรมตัวเองตามเดิม
★ ถ้าท่าอื่นมีอาการคล้ายกัน ★ ไม่ต้องแก้ภาพ — ระบบนี้จัดการให้ทุกท่าแล้ว (ปรับเกณฑ์ที่ `const SNAP := 12.0`)

### ค) หน้ากระเป๋า (ตามที่สั่ง 4 จุด)
1. **ขอบชิดกริดแล้ว** — เอา `custom_minimum_size` กว้าง 478 ออก หน้าต่างหดพอดีเนื้อหา ไม่มีแถบว่างขวา
2. **เอากล่องชื่อ+คำอธิบายในหน้าต่างออก** — รายละเอียดโชว์ที่กล่องลอยฝั่งซ้าย (ItemInfoPopup) ที่เดียว ปุ่ม ใช้/ทิ้ง/อัลบั้ม ยังอยู่ครบ
3. **แถบหัวเรื่องเป็นสีขาว** (เดิมชมพู) — `C_BAR`
4. **โปร่งแสงเล็กน้อยมองทะลุได้** — พื้นหน้าต่าง a=0.88 · กริด a=0.55 · ช่อง a=0.72 (ปรับได้ที่ `C_BG` `C_SLOT` และสีใน `grid_panel`)

### ง) เทสต์ — 40 ชุด / 1,154 ข้อ ผ่านหมด
ใหม่: `r39_test` 17 ข้อ (มีโหนดวิดีโอ+วนซ้ำ+ภาพนิ่งถูกซ่อน · ขอบล่าง Idle นิ่งหลังปรับ · offset.y ค่าเดียวตอนยืน · หัวขาว/โปร่งแสง/ขอบชิด · กล่องลอยซ้ายยังทำงาน)

## 7.51 รอบ 40 — โหลดฉากช้า/กระตุก + ย้าย world_node_2d เป็นชื่อแมพที่ถูกต้อง

### ก) ทำไมโหลดเข้าฉากแล้วกระตุก
แมพทุ่งวิหารประกอบด้วย `Asgard forest 1.png` **11.8 MB** (6128x941) + ตัวไฟล์ฉาก **767 KB** (ข้อมูล TileMap 485 KB)
เดิมเกมโหลดทั้งหมดนี้ "บนเธรดหลัก" ตอนเปลี่ยนแมพ → จอค้างช่วงสั้น ๆ ทุกครั้งที่เข้า

### ข) แก้ 2 ชั้นใน `game.gd`
1. **โหลดเบื้องหลัง (threaded)** — เปลี่ยนแมพ = จอมืด + ป้าย "กำลังโหลด..." ระหว่างที่ไฟล์ถูกอ่านในเธรดแยก เกมไม่ค้าง
   ★ ก่อนเริ่มโหลดต้องสลับไป "ฉากเปล่า" ก่อน ★ ปล่อยแมพเก่าไว้เฉย ๆ ระหว่างโหลดมีโอกาสแย่งรีซอร์สกันจนพัง
2. **แคชแมพล่าสุด 4 แมพ** (`MAX_CACHED`) — เข้าแมพเดิมซ้ำไม่ต้องอ่านไฟล์ใหม่เลย
   วัดจริง: โหลดครั้งแรกช้าเท่าเดิม (แต่ไม่ค้างแล้ว) · **ครั้งถัดไปเร็วขึ้น ~20 เท่า**

### ค) ★ ย้ายไฟล์แมพ ★ `Sprites/world_node_2d.tscn` → **`scenes/maps/prontera_field.tscn`**
- ตัวโครงเก่าที่ไม่ได้ใช้ย้ายไป `_to_delete/prontera_field_โครงเก่าไม่ได้ใช้.tscn`
- โหนดราก `WorldNode2D` → `PronteraField` + ใส่ `map_id = &"prontera_field"` ชัด ๆ
- `Game.MAPS` ชี้ path ใหม่แล้ว · ตั้งแต่นี้แมพทุ่งวิหาร **ถูก sync มาเทสต์อัตโนมัติ** เหมือนแมพอื่น (เดิมอยู่ใน Sprites/ เลยหลุดจากการ sync)
- ★ ถ้า Godot เปิดอยู่ตอนย้าย ★ ปิดโดยไม่บันทึกแล้วเปิดใหม่ — แท็บเก่าอาจชี้ไฟล์ที่หายไปแล้ว

### ง) บั๊กที่โผล่ตามมา (เพราะแมพจริงถูกแก้ใน editor จนค่ากันบั๊กหาย)
- **map_bounds / auto_fit_bounds หาย** (ของรอบ 35) → ใส่กลับตามผังใหม่: `Rect2(120, -320, 5750, 950)` + `auto_fit_bounds = false`
  ★ อย่าลบสองช่องนี้ ★ ไม่งั้นมอนกลับไปเกิดหลังกำแพง/นอกแมพอีก (ขยายแมพเมื่อไหร่ แก้ตัวเลขเอาแทน)
- **มอนในแมพโหรงเหรง ไม่ยอมเพิ่ม** — บั๊กใน `map_spawner.gd` ตอน "ผ่อนเงื่อนไข":
  ระบบเลือกจุดเกิด "ไกลสุดแบบไม่จำกัด" → เกิดไกลกว่าโซนเก็บกลับ (despawn 2600) → โดนเก็บทันที วนลูปเกิด-หายตลอด
  แก้: เพดานระยะตอนผ่อน = `despawn_distance × 0.9` (`_relaxed_far()`) · และเช็ค "เดินถึงกันได้" ยกเส้นจาก 40 → **110 px** เหนือพื้น (เนิน TileMap เตี้ย ๆ ไม่ถือว่าขวางทาง — กำแพงสูงยังขวางเหมือนเดิม) · `relax_after` 4 → 2 วิ ให้เติมมอนเร็วขึ้น

### จ) กับดักข้อ 54
**★ แมพที่ผู้ใช้แก้ใน editor ทำค่าที่ patch ไว้หายได้เสมอ ★** (map_bounds ของรอบ 35 หายตอนขยายแมพ)
เทสต์ที่ยึดตัวเลขของแมพ (ระดับพื้น y=504 · จำนวนมอน · ลำดับประตู) ต้องเขียนแบบไม่ยึดค่าตายตัว — รอบนี้แก้เทสต์ 6 ไฟล์

### ฉ) เทสต์ — 41 ชุด / 1,166 ข้อ ผ่านหมด
ใหม่: `r40_test` 12 ข้อ (path ใหม่ · ไฟล์เก่าหาย · เข้าแมพ+ชื่อ/ไอดีถูก · แคชเร็วกว่ารอบแรก · แคชไม่เกินเพดาน · ไปแมพอื่นต่อได้)

## 7.52 รอบ 41 — วิดีโอเปิดตัวบอส (เจอครั้งแรกเล่นครั้งเดียว)

### ก) ใช้ยังไง
วิดีโอของคุณใน `Sprites/video/` ถูกแปลงเป็น .ogv แล้ว (Godot เล่น mp4 ตรง ๆ ไม่ได้):
`boss_intro_king_poring.ogv` · `boss_intro_baphomet.ogv` (1280x720 · ~6 MB/ไฟล์ · ไฟล์ mp4 ต้นฉบับไม่เข้าเกมเว็บอยู่แล้ว)

MonsterData มีช่องใหม่กลุ่ม **"วิดีโอเปิดตัวบอส"**:

| ช่อง | ค่า | ความหมาย |
|---|---|---|
| Intro Video | `res://Sprites/video/xxx.ogv` | วิดีโอที่เล่นตอนเจอครั้งแรก (เว้นว่าง = ไม่มี) |
| Intro Range | 700 | ผู้เล่นเข้าใกล้กว่านี้ = เริ่มเล่น |

ผูกให้แล้ว: **คิงโพริง + บาฟโฟเมท** (`patch_boss_intro.py` — รันบนเครื่องแล้ว)
★ บอสใหม่ในอนาคต (อสูรสายฟ้า ฯลฯ) แค่วาง .ogv แล้วกรอกช่อง Intro Video ใน .tres ★

### ข) พฤติกรรม
- เดินเข้าระยะครั้งแรก → เกมหยุด → วิดีโอเต็มจอ (จอดำ+เฟดเข้าออก) → จบ/กดข้าม (Enter·Esc·คลิก) → สู้ต่อ
- ★ เล่นครั้งเดียวต่อเซฟ ★ จำด้วยธง `seen_intro_<id>` (เก็บลงเซฟ — โหลดเซฟ/ตายกลับมา ไม่เล่นซ้ำ · เริ่มเกมใหม่เล่นอีกครั้ง)
- เจอบอสตัวอื่นที่มีวิดีโอของตัวเอง ก็เล่นของตัวนั้นครั้งแรกเช่นกัน

### ค) โค้ดที่เพิ่ม
- `ui_manager.gd` → `UI.play_video(path)` — เล่นวิดีโอเต็มจอแบบ await ได้ ใช้กับคัทซีนอื่นในอนาคตได้เลย
  ★ CanvasLayer ไม่มี modulate — เฟดที่ Control ที่ห่อข้างในแทน ★
  ★ เช็คปุ่มข้ามด้วย `is_action_pressed` ไม่ใช่ just_pressed — ตอนเกม pause เฟรม input กับเฟรม await ไม่ตรงกัน (กับดักข้อ 15) ★
- `monster_base.gd` → `_check_boss_intro()` เช็คระยะทุกเฟรมฟิสิกส์ (ถูกมาก — ตัดจบทันทีถ้าไม่ใช่บอส/เคยเจอแล้ว)

### ง) กับดักข้อ 55
**★ เทสต์ที่เอาผู้เล่นไปใกล้บอสที่มีวิดีโอ ต้องตั้งธง `seen_intro_<id>` ก่อน ★**
ไม่งั้นวิดีโอเด้ง → เกม pause → สกิล/ตัวจับเวลาของมอนหยุดหมด เทสต์พังแบบงง ๆ (r36 โดนไปรอบนึง)

### จ) เทสต์ — 42 ชุด / 1,182 ข้อ ผ่านหมด
ใหม่: `r41_test` 16 ข้อ (ไกลไม่ทริกเกอร์ · ใกล้แล้วเล่น+ตั้งธง+เกมหยุด · มีเลเยอร์เต็มจอ · กดข้ามแล้วปิด+เกมเดินต่อ · เจอซ้ำไม่เล่น · ธงติดไปกับเซฟ)

## 7.53 รอบ 42 — เอกสารข้อมูลเกมอัตโนมัติ + แก้บาฟโฟเมทไม่ใช่บอส

### ก) `dump_gamedata.py` — อ่านไฟล์เกมทั้งหมดแล้วเขียนเอกสารเอง
รัน `python3 dump_gamedata.py` ในโฟลเดอร์โปรเจกต์ → ได้ 2 ไฟล์:
- **`ข้อมูลเกมทั้งหมด.md`** — ไอเทมทุกชิ้น (ค่าพลัง ราคา **ที่มา**) · มอนทุกตัว (สเตตัส สกิล ตารางดรอป แมพ) · สกิล · แมพ (ขนาด พิกัดทุกอย่าง) · เควส · เทมเพลตพรอมพ์
- **`gamedata.json`** — ข้อมูลชุดเดียวกันแบบ JSON (ใช้ทำหน้าเว็บ/เครื่องมืออื่น)

อ่านอย่างเดียว ไม่แก้ไฟล์เกม · รันซ้ำได้ทุกเมื่อ ข้อมูลจะตรงกับเกมเสมอ

★ จุดสำคัญที่ทำให้ตัวเลขถูก ★ `.tres` **ไม่เก็บช่องที่ยังเป็นค่า default** สคริปต์เลยอ่านค่า default
จาก `@export` ในไฟล์ `.gd` จริง (รวมถึง enum เช่น `Element.WATER` → 2) แล้วเติมให้
ไม่งั้นตารางจะโชว์ 0 ผิด ๆ (เช่น HIT/FLEE ของแฟเบร) และชื่อจะหาย (poring.tres ไม่มีบรรทัด `id`/`display_name`)

### ข) ส่วน "ตรวจสุขภาพข้อมูล" (สร้างเองทุกครั้งที่รัน)
- ไอเทมที่ยังไม่มีทางได้มา (ไม่มีมอนดรอป/ร้านขาย/รางวัลเควส)
- มอนที่ยังไม่ได้วางในแมพไหนเลย
- กฎบังคับ: มอนมีการ์ดครบไหม · มีของขยะเฉพาะตัวไหม · ประตูชี้ถูกไหม · ไอเทมที่อ้างถึงมีไฟล์จริงไหม

### ค) ★ บั๊กที่เจอตอนตรวจ ★ บาฟโฟเมทไม่ได้ติ๊ก `is_boss`
บาฟโฟเมท Lv50 HP 14,000 เป็นบอสประจำป่าเงาลึก แต่ไฟล์ไม่มี `is_boss = true` ทำให้
**ไม่มีป้าย MVP · ไม่ตั้งธง `killed_baphomet` · และวิดีโอเปิดตัวที่เพิ่งทำไม่เล่น**
แก้ 2 ทาง: `patch_baphomet_boss.py` ตั้ง `is_boss` ให้ · และ `_check_boss_intro()` เลิกบังคับว่าต้องเป็นบอส
(ขอแค่ตั้งช่อง Intro Video ก็ใช้ได้ — มอนธรรมดาก็มีฉากเปิดตัวได้แล้ว)

## 7.54 รอบ 44 — แก้กระตุก · ดรอป/ราคาใหม่ · AI ไล่ไม่หยุด · ป่าเงาลึกชั้นใน · เอฟเฟกต์ฟัน

### ก) ★ ทำไมเกมกระตุก — และใช่ เป็นเพราะวิธีสร้างแมพทุ่งวิหาร (แต่ไม่ใช่ความผิดคุณ — แก้ให้แล้ว) ★
เจอ 2 สาเหตุใหญ่ แก้ครบทั้งคู่:

**1. แมพทุ่งวิหารทำด้วย TileMap ผิดวิธี** — ภาพ `Asgard forest 1.png` (6128x941) ถูกหั่นเป็นกระเบื้อง 16x16
ทั้งรูป แล้ว "วาดคืน" ลง TileMap ทีละช่องจนครบ **22,039 ช่อง** ผลบนจอ = รูปเดียวเป๊ะ ๆ
แต่ Godot ต้องสร้างกระเบื้อง 22,000 ชิ้นตอนเข้าแมพ (ไฟล์ฉาก 769 KB) และจัดการมันทุกเฟรม → เข้าแมพหน่วง + เล่นกระตุก
- `fix_field_tilemap.py` ถอดรหัส tile_data ตรวจแล้วว่าทุกช่องชี้ตำแหน่งเดิมของรูป → แทนด้วย **Sprite2D รูปเดียว**
  ที่พิกัด (-480, -208) + แถบเสริม 2 ชิ้นที่คุณก็อปวางไว้แถวล่าง (region) · กล่องชนพื้น/กำแพงย้ายไปใต้โหนด `Terrain`
- ไฟล์ฉาก 769 KB → **6 KB** · เทสต์เรนเดอร์จริงเทียบภาพเก่า-ใหม่ **ตรงกันทุกพิกเซล** (ใช้ภาพสุ่มเป็นพื้นเช็ค)
- สำรองของเดิมไว้ `scenes/maps/prontera_field_ก่อนถอด_TileMap.tscn.bak`
- ★ ต่อไปวางฉากหลังแบบไหน ★ ลาก PNG มาเป็น **Sprite2D** (ติ๊ก Centered ออก) ใต้ `Terrain` แล้ววาง StaticBody2D เป็นพื้นเอง
  TileMap เหมาะกับ "กระเบื้องซ้ำ ๆ" (พื้นหญ้า/อิฐ) ไม่ใช่รูปวาดทั้งใบ · ถ้าอยากใช้ TileMap จริง ๆ ให้ใช้ tile ใหญ่ (128-256) และวาดเฉพาะพื้นที่ที่จำเป็น

**2. ระบบ auto-fit "ดึงภาพจากการ์ดจอ" ทุกครั้งที่มอนเกิด / ผู้เล่นถูกสร้างใหม่** (`Texture2D.get_image()`)
เป็นคำสั่งช้ามาก (การ์ดจอต้องหยุดรอ) × ทุกเฟรมภาพ × ทุกท่า — มอนเกิดทีไรกระตุกทีนั้น เปลี่ยนแมพก็วัดผู้เล่นใหม่หมด
- ใหม่: `scripts/core/sprite_fit.gd` (คลาส `SpriteFit`) วัดครั้งเดียวจำไว้ **ทั้งเกม** (static) ผู้เล่นและมอนใช้ร่วมกัน
- `map_base._warm_sprite_fit()` วัดผู้เล่น + มอนทุกชนิดในแมพ **ตอนโหลดแมพ (จอยังมืด)** → ระหว่างเล่นไม่ต้องวัดอีก
  (ใน log จะเห็น `[Map] xxx อุ่นเครื่อง auto-fit N ท่า` — ครั้งต่อ ๆ ไป N จะเหลือแค่มอนชนิดใหม่)
- ★ กับดักข้อ 56 ★ `player_frames.tres` ตั้ง `resource_local_to_scene` → ผู้เล่นทุกตัวได้สำเนา SpriteFrames ใหม่ (path ว่าง)
  คีย์แคชจึงใช้ "ภาพเฟรมแรก + จำนวนเฟรม" แทน instance id ไม่งั้นเปลี่ยนแมพก็วัดใหม่ทุกครั้งอยู่ดี
- ค่ากลางกันตัวเด้ง (รอบ 39) ย้ายมาอยู่ใน SpriteFit → **มอนก็ได้ผลด้วย** (`bottom_use`/`dx_use`)
- ของแถม: ฉากของตก `dropped_item.tscn` เปลี่ยนเป็น preload (เดิม load() ตอนมอนตาย)

ถ้ายังกระตุกอยู่บ้าง ให้ดูขนาดภาพ: `Asgard forest 1.png` 12 MB / `asgard forest2 sky.png` 9 MB เป็นภาพ lossless
ขนาด 6128 px กว้างเกิน 4096 ที่การ์ดจอบางรุ่น (โดยเฉพาะเว็บ/มือถือ) รับได้ — ถ้าจะทำเกมเว็บ แนะนำย่อให้กว้าง ≤ 4096

### ข) อัตราดรอปใหม่ (`rebalance_r44.py` — รันบนเครื่องแล้ว · รันซ้ำได้)
ไล่ตามเลเวลมอน: `t = (Lv-1)/49` แล้ว lerp · **ปรับลงอย่างเดียว** (ของที่เดิมหายากกว่าสูตร คงเดิม)

| ชนิด | Lv1 | Lv50 | ตัวอย่าง |
|---|---|---|---|
| การ์ดมอนธรรมดา | 0.9% | 0.5% | โพริง 0.90 · หมาป่า 0.80 · ผู้เฝ้ารูน 0.62 |
| การ์ดบอส | 2.5% | 2.0% | คิงโพริง 2.36 · อสูรสายฟ้า 2.31 · บาฟโฟเมท 2.00 |
| ของสวมใส่ (อาวุธ/เกราะ) | 3% | 1% | แฟเบร→เสื้อผ้าฝ้าย 2.96 · หมาป่า→การ์ด(โล่) 2.51 · บาฟโฟเมท→ดาบเพลิง 1.00 |
| หินตีบวก (phracon/emveretarcon) | 5% | 1% | ฮอร์เน็ต 4.35 · หมาป่า 4.02 · บาฟโฟเมท 1.00 |

★ ผลข้างเคียงที่ควรรู้ ★ เดิมบอส (ผู้พิทักษ์เตาหลอม/บาฟโฟเมท) ดรอปหิน **100% ×3-6** เป็นรางวัลการันตี — ตามกติกาใหม่เหลือ 1-2%
ถ้าอยากให้บอสยังแจกหินการันตี บอกได้ จะยกเว้นบอสให้ (แก้บรรทัดเดียวในสคริปต์)

ราคาขาย: **การ์ด 500 · การ์ดบอส 1,000** (เดิม 5,000-500,000) · **ของสวมใส่ ×4 (+300%)**
★ แต่มีเพดาน 80% ของราคาซื้อ ★ — ไม่งั้นซื้อดาบ 500 ขายคืน 800 กลายเป็นเครื่องปั๊มเงิน
ของร้าน (ราคาขายเดิม = 40% ของราคาซื้อ) จึงกลายเป็น 80% ทุกชิ้น · ของที่ดรอปอย่างเดียวได้ ×4 เต็ม ๆ เว้นแต่ชนเพดาน
(ราคาฐานเดิมเก็บไว้ในไฟล์เป็น `metadata/sell_price_base_r44` — รันสคริปต์ซ้ำจะไม่คูณทับ)

### ค) AI มอน — ล็อกเป้าแล้วไล่ไม่หยุด (`patch_r44_ai.py`)
- **มอนดุ**: เห็นผู้เล่นในระยะ Detect ครั้งแรก = ล็อกเป้า · **ทุกตัว**: โดนตี = ล็อกเป้า
- ล็อกเป้าแล้ว: ไม่มี "กำแพงระยะ" อีก (ไม่สน leash_range / detect_range) ไล่ไปได้ทั่วแมพ
- **ไม่เลิกโกรธตามเวลา** (เดิม `AGGRO_MEMORY` 8 วิ = อาการ "ตีคืน 3 ทีแล้วเลิก") — เลิกเมื่อ **ผู้เล่นตาย** หรือ **มันตาย** เท่านั้น
- ตัวนิ่ง (STATIONARY) ไม่ไล่อยู่แล้ว · มอนที่กระโดดไม่ได้ยังไม่เดินตกเหว · `is_aggro_locked()` ไว้ให้ดู/เทสต์
- ★ ผลข้างเคียง ★ ลากมอนข้ามแมพไม่ได้อยู่แล้ว (เปลี่ยนแมพ = มอนหาย) · มอนที่ไล่ตามอยู่จะไม่โดนเก็บ (despawn) เพราะอยู่ใกล้ผู้เล่นเสมอ

### ง) แมพ (`gen_round44_maps.py`)
| แมพ | มอนเกิด | หมายเหตุ |
|---|---|---|
| ป่าสนธยา (asgard_forest_2) | **ดรอปส์ ชอนชอน หมาป่า** | คิงโพริงยังอยู่ |
| ป่าเงาลึก (dark_forest) | **ฮอร์เน็ต หมาป่า ลูนาติก** | เอาบาฟโฟเมทออก · ประตูขวา → ป่าเงาลึกชั้นใน (ไม่ล็อก) |
| ★ ป่าเงาลึกชั้นใน (dark_forest_2) ใหม่ · บท 1 | **มูนัค ออร์คนักรบ บาฟโฟเมทจูเนียร์** (4 ตัว/ชนิด) | กว้าง 4200 · แท่น 3 · **บาฟโฟเมทเฝ้าหน้าประตูไปทางเหล็ก** (x 3880 / ประตู 4140) · ประตูล็อกธง `chapter2_open` · LoreObject "แท่นบูชาหินดำ" |
| ทางเหล็ก (iron_road) | เดิม | ประตูซ้าย → ป่าเงาลึกชั้นใน |

- ฉากหลังชั่วคราว `Sprites/map/generated/dark_forest_2_bg.png` (ป่าตายม่วงดำ + ตาแดงในความมืด) รอคุณวาดจริง
- เควส M9/M12 (ล่าหมาป่า/ฮอร์เน็ต) ยังทำในป่าเงาลึกได้ตามเดิม · ลูนาติกย้ายไปป่าเงาลึก (ไม่มีเควสอ้างถึง)
- ★ กับดักข้อ 57 ★ เทสต์/สคริปต์ที่จำว่า "ประตูบท 2 อยู่ป่าเงาลึก" ต้องแก้เป็น `dark_forest_2` (r31 แก้แล้ว)

### จ) เอฟเฟกต์ฟันธรรมดา (`make_attack_fx.py` + `patch_r44_attackfx.py`)
- `Sprites/effects/attack_slash.png` (7 เฟรม 384x384 · 30fps) + `data/sprites/fx_attack.tres` ท่า `slash` / `slash2`
- ตีทุกครั้งมีเสี้ยวแสงขาว-ฟ้าโผล่ข้างหน้า สลับ "ฟันลง" / "ฟันสวนขึ้น" (พลิกแนวตั้ง — `flip_v` ใหม่ใน SkillEffect)
- ตั้งค่าใน Inspector ของ Player กลุ่ม **"เอฟเฟกต์โจมตีปกติ"**: Enabled · Frames (ว่าง = ใช้ไฟล์ข้างบน) · Anims · Offset (78,-28) · Height 230 · Delay 0.06 · Z 40
- ★ ไม่ทำดาเมจเอง ★ (ดาเมจยังคิดจากกรอบ Attack Range เหมือนเดิม) — อยากเปลี่ยนรูป: วางชีทใหม่ทับชื่อเดิม หรือชี้ Frames ไปไฟล์อื่น
- แมพทุ่งวิหารมี AnimatedSprite2D ประตู (ภาพ `รูปภาพ Codex ...png`) ที่คุณวางไว้ — ยังอยู่ ไม่ได้แตะ

### ฉ) เทสต์ — 43 ชุด / 1,241 ข้อ ผ่านหมด
ใหม่: `r44_test` 59 ข้อ (ไม่มี TileMap+ภาพตรงตำแหน่ง+ไฟล์เล็ก · อุ่นเครื่องครบ/ไม่วัดซ้ำ/มอนเกิดไม่วัดเพิ่ม · idle ไม่เด้ง ·
ดรอปทุกตัวอยู่ในช่วง+ไล่ระดับ+ราคา · โดนตีล็อกเป้า/ไล่เกิน leash/ไม่จางตามเวลา/ผู้เล่นตายปลด ·
มอนแต่ละแมพ/เดินผ่านประตูจริง/บอสเฝ้าประตู/ประตูล็อก/เดินกลับ · เอฟเฟกต์ฟันเกิด-ตาม-สลับ-หายเอง-ปิดได้)
แก้เก่า: `r31` (ประตูบท 2 ย้ายไป dark_forest_2 + รายชื่อแมพ)

## 7.55 รอบ 45 — รีสกิล/รีสเตตัส/พรสายฟ้า · ตารางสเตตัส · % ของสวมใส่ · หน้าตัวละครรวม + ลากวาง · เมนู NPC · ปุ่มมือถือใหม่

### ก) ไอเทมใหม่ 3 ชิ้น (`gen_round45.py` — รันบนเครื่องแล้ว) ขายที่ **นักบวชสูงสุดวาลเดอร์** (เมืองพรอนเทรา)
| id | ชื่อ | ราคา | ผล |
|---|---|---|---|
| `reset_skill` | สมุดลืมสกิล | 100,000 | ใช้แล้วรีเซ็ตสกิลทั้งหมด คืนแต้มสกิลครบ (`special_effect = reset_skills`) |
| `reset_stat` | น้ำแห่งการเกิดใหม่ | 100,000 | ใช้แล้วรีเซ็ตสเตตัสทั้งหมด คืนแต้มตามที่จ่ายไปจริง (`reset_stats`) |
| `thunder_blessing` | พรแห่งสายฟ้า | 1,500 | ความเร็วโจมตี +10% นาน 3 นาที (`buff_values = {"aspd_percent": 10}` · `buff_duration = 180`) |

- ItemData มีช่องใหม่ **`special_effect`** (reset_skills / reset_stats) และ **`buff_values` + `buff_duration`** → ทำไอเทมบัฟชนิดอื่นได้เองใน Inspector (คีย์เดียวกับบัฟสกิล เช่น `atk_percent` `def` `move_speed_percent`)
- บัฟไอเทมเก็บใน `active_buffs` คีย์ `item_<id>` ใช้ซ้ำ = ต่ออายุ · HUD โชว์ชื่อ+เวลา เหมือนบัฟสกิล
- วาลเดอร์ (type DIALOG) มีร้านได้เพราะช่องใหม่ **`Has Shop`** ใน NPC (ติ๊ก + ใส่ Shop Items) — NPC คนไหนก็ทำได้
- ไอคอน placeholder `Sprites/items/placeholder/{reset_skill,reset_stat,thunder_blessing}.png` (256px) รอคุณวาด

### ข) ★ ตารางผลของสเตตัส ★ (`player_stats.gd` — บล็อก "ตารางผลของสเตตัส" แก้ที่เดียว)
| สเตตัส | ให้อะไร (ต่อ 1 แต้ม) | ค่าคงที่ |
|---|---|---|
| STR | ATK +1 · โบนัสทุก 10 แต้ม (STR/10)² (10→+1 · 20→+4 · 30→+9) | `STR_ATK 1.0` |
| AGI | FLEE +1 · ความเร็วโจมตี +1.2% (นักดาบ — `aspd_agi_percent` ในไฟล์อาชีพ) | `AGI_FLEE 1.0` |
| VIT | **DEF +0.5** (2 แต้ม = 1) · **HP +6** (ใหม่) · HP +1.2% ของ HP พื้นฐาน (`hp_vit_percent` อาชีพ) · ฟื้น HP +0.05/วิ | `VIT_DEF 0.5` `VIT_HP_FLAT 6` `VIT_HP_REGEN 0.05` |
| INT | MATK +1 (+ (INT/7)²) · MDEF +0.5 · **SP +4** (ใหม่) · SP +1% ของ SP พื้นฐาน (`sp_int_percent`) · **ฟื้น SP +0.12/วิ** (เดิม 0.03) | `INT_MDEF 0.5` `INT_SP_FLAT 4` `INT_SP_REGEN 0.12` |
| DEX | HIT +1.5 · ATK +0.2 · ความเร็วโจมตี +0.4% | `HIT_PER_DEX 1.5` `DEX_ATK 0.2` `DEX_ASPD 0.004` |
| LUK | CRIT +0.3% · ATK +0.33 | `LUK_CRIT 0.3` `LUK_ATK 1/3` |

ค่าที่ขึ้นกับ "อาชีพ" (`hp_vit_percent` `sp_int_percent` `aspd_agi_percent` `hp_per_level` ฯลฯ) อยู่ใน `data/jobs/*.tres` · ปุ่ม + ในหน้าตัวละครมี tooltip บอกผลเหมือนตารางนี้ (`STAT_TIPS` ใน equipment_window.gd — แก้ตัวเลขแล้วอย่าลืมแก้ข้อความด้วย)

### ค) ของสวมใส่: โบนัส % 6 ช่องใหม่ (กลุ่ม "Percent Bonus" ใน ItemData — การ์ดใส่ได้ด้วยเพราะสืบทอด)
`damage_percent` ดาเมจสุดท้าย +% (ตีธรรมดา+สกิล คูณท้ายสุดใน `Combat.player_hits_monster`) · `defense_percent` DEF +% · `hp_percent` HP สูงสุด +% · `sp_percent` SP สูงสุด +% ·
`hp_drain_percent` ดูดเลือด = % ของดาเมจที่ทำ · `sp_drain_percent` ดูดมานา (คิดใน `monster_base._drain_to_player()` หลังตีโดน · MISS ไม่ดูด)
- รวมค่าใน `Equipment.collect_percent_bonus()` → `PlayerStats.damage_percent / hp_drain_percent / sp_drain_percent` (ค่าใหม่) + `def_percent / max_hp_percent / max_sp_percent` (คีย์เดิม)
- กล่องรายละเอียดไอเทม + หน้าตัวละคร โชว์ค่าพวกนี้แล้ว · การ์ดใส่ `percent_effects` คีย์ `damage_percent` `hp_drain_percent` `sp_drain_percent` ได้เช่นกัน
- ★ ยังไม่มีไอเทมชิ้นไหนใช้ค่าพวกนี้ — กรอกใน Inspector ของไอเทม/การ์ดที่อยากให้มีได้เลย ★

### ง) ★ หน้าตัวละครรวม (สวมใส่ + สเตตัส) ★ — `equipment_window.gd` เขียนใหม่ · `status_window.gd` เลิกใช้ (ย้ายไป _to_delete)
- กด **C หรือ E** เปิดหน้าเดียวกัน (`UI.windows["status"]` = `["equipment"]`) · โทนครีมเหมือนกระเป๋า
- ซ้าย: ช่องอุปกรณ์ 8 ช่อง + ตัวละคร + "จากของสวมใส่ + การ์ด" (รวมโบนัสทุกอย่าง) · ขวา: สเตตัส 6 ค่า (ปุ่ม + tooltip) + ค่าคำนวณ 16 ค่า
  **ตัวเลขสีเขียว = มีผลจากของสวมใส่/การ์ด** — ถอด/ใส่แล้วเห็นเปลี่ยนทันที
- ★★ ลาก-วาง (`scripts/ui/drag_slot.gd` — `class_name DragSlot`) ★★ ใช้ระบบ Drag & Drop ในตัว Godot (เมาส์/นิ้วใช้ได้เหมือนกัน)
  กดค้างแล้วขยับ = ลาก · ปล่อยลงช่อง = วาง · ถ้าไม่ได้ขยับ = คลิกปกติเหมือนเดิม
  - กระเป๋า → ช่องอุปกรณ์ที่ตรงชนิด = ใส่ (ผิดช่องขึ้นข้อความ · เครื่องประดับเลือกช่อง 1/2 ได้เอง) · กระเป๋า → รูปตัวละคร = ใส่ช่องที่เหมาะเอง
  - ช่องอุปกรณ์ → กระเป๋า = ถอด (ลงช่องที่วาง ถ้าว่าง) · กระเป๋า → กระเป๋า = สลับช่อง
  - โค้ด: ปุ่มช่องเป็น `DragSlot` ตั้ง `kind` ("inventory"/"equip"/"any") + `drag_icon_func` / `can_drop_func` / `drop_func` · เทสต์จำลองด้วย `target.simulate_drop_from(source)`
- ★ กับดักข้อ 63 ★ `Array[Button]` รับ DragSlot ได้ (สืบทอด) แต่โค้ดที่เช็ค `btn is DragSlot` ต้องทำก่อนใช้ `slot_index` · ช่องกระเป๋าตั้ง `slot_index` ใหม่ทุกครั้งใน `refresh()` (ช่องโชว์ ≠ ช่องจริง — `_display_to_slot`)

### จ) เมนูก่อนคุย NPC (`npc.gd`)
คุยกับ NPC ทุกคน (ยกเว้นศิลาเซฟ) → กล่องสนทนาถาม **พูดคุย / ซื้อขาย / ไม่คุย** (ประโยคทักตั้งได้ที่ช่อง `Greeting`)
- พูดคุย = เควสก่อน (ส่ง/คืบหน้า/รับ) แล้วค่อยบริการ (ตีบวก/รักษา) หรือบทพูดปกติ · ซื้อขาย = เปิดร้าน (โผล่เฉพาะร้านค้า/ติ๊ก Has Shop) · ไม่คุย = ปิด
- ★ กับดักข้อ 64 ★ เทสต์ที่ `interact()` แล้ว `_skip_talk()` เลือก "ปุ่มสุดท้าย" จะไปกด "ไม่คุย" — แก้ helper ใน r30/r31/portal ให้เลือก "พูดคุย" ถ้าปุ่มแรกคือพูดคุย · ร้านค้าเปิดผ่านเมนูซื้อขายเท่านั้น (portal_test แก้แล้ว)

### ฉ) ปุ่มจอสัมผัส (`touch_controls.gd`)
- ยกสูงจากขอบล่าง 22 → **64 px** (`EDGE_BOTTOM`) · ปุ่มใหญ่ขึ้น: เดิน 128 · **โจมตี 150** · สกิล 86 · คุย 100 · ยา/เมนู 70
- **ตัดปุ่มพุ่งหลบ** ออกตามที่สั่ง (คีย์บอร์ด W/Space ยังใช้ได้)
- **ปุ่มโจมตีอยู่กลาง** สกิล 1-4 **ล้อมรอบจาก 8 นาฬิกา → 1 นาฬิกา** (`SKILL_CLOCK_START 8` / `SKILL_CLOCK_END 13` · รัศมี = ครึ่งโจมตี + ช่องว่าง + ครึ่งสกิล + 16)
- **ไอคอนสกิลบนปุ่ม** ตามช่องลัด 1-4 (`refresh_skill_icons()` ต่อสัญญาณ `skills_changed`) — ไม่มีสกิล = เลขช่อง
- คุย/เก็บของอยู่ซ้ายของวง · ยา/มานา/เมนู แถวเหนือปุ่มคุย
- ★ กับดักข้อ 65 ★ ปุ่มกลม ๆ ล้อมวง แต่ระบบชนใช้ **กรอบสี่เหลี่ยม** — รัศมีต้องเผื่อให้กรอบไม่เกยกัน (เทสต์ "ปุ่มไม่ทับกัน" จับได้) เพิ่มสกิลเป็น 5-6 ปุ่มเมื่อไหร่ต้องขยาย +16 นั้น

### ช) เทสต์ — 44 ชุด / 1,299 ข้อ ผ่านหมด
ใหม่ `r45_test` 59 ข้อ (ไอเทม+ร้านวาลเดอร์ · เมนู 3 ทาง/2 ทาง · รีสเตตัสคืนแต้มตรง · รีสกิล · บัฟ 10% ติด/หมด/HUD · VIT→HP+DEF · INT→SP+ฟื้น · % ทั้ง 6 + ดาเมจเฉลี่ย + ดูดเลือดจริง · หน้าตัวละครรวม/สีเขียว/DragSlot · ลากใส่-ผิดช่อง-วางตัวละคร-ถอด-สลับ · ปุ่มมือถือ: ไม่มีกระโดด/โจมตีใหญ่/สูง 60+/สกิลระยะเท่ากัน 8→1 นาฬิกา/ไอคอน/ไม่ทับกัน)
แก้เก่า: talk · portal · r30/r31 (`_skip_talk`) · touch (ไม่มี jump) · equipui (กว้าง ≤ 860)

## 7.56 รอบ 46 — NPC idle เด้งไปเด้งมา (ชีท 6 เฟรมวางไม่ตรงกริด)

### สาเหตุ (ไม่ใช่โค้ด — เป็นตัวไฟล์ภาพ)
ชีท `Sprites/npc/*-idle-6frames.png` (1540x1540 · 3x2 · Godot ตัดช่องละ 513x770) **ตัวละครไม่ได้อยู่ตำแหน่งเดียวกันในทุกช่อง**:
- แถวล่างอยู่ต่ำกว่าแถวบน **9 px** (ปลายเท้าแถวบน y=737 · แถวล่าง y=746) · คอลัมน์ขวาสุดเยื้องขวา **6-7 px** · บางตัว (บียอร์น) เท้าเยื้องซ้าย-ขวาถึง 14 px
- "ปลายเท้าเท่ากันหมด" จริงเฉพาะ **ภายในแถวเดียวกัน** — พอ Godot ตัดเป็นกริดเป๊ะ ๆ เฟรม 4-6 เลยตกลง 9 px → เห็นเป็นเด้ง (สเกล 0.35 = ~3 px บนจอ ที่ 8 fps ชัดมาก)

### แก้ — `fix_npc_sheets.py` (รันบนเครื่องแล้ว · รันซ้ำได้)
- วัดกรอบพิกเซลจริงของทั้ง 6 ช่อง แล้ว "เลื่อนภาพในช่อง" ให้ **ขอบล่างสุด** เท่ากัน และ **กึ่งกลางของแถบเท้า (40 px ล่างสุด)** ตรงกัน
  ★ ใช้แถบเท้า ไม่ใช่กึ่งกลางทั้งตัว ★ — ผ้าคลุม/แขนที่กางออกทำให้กรอบทั้งตัวเบี้ยว แต่เท้าที่ยืนอยู่กับที่ต้องนิ่ง (ลำตัวโยกได้เป็นธรรมชาติ)
- ต้นฉบับอยู่ `_to_delete/originals_npc/` — สคริปต์คำนวณจากต้นฉบับเสมอ (รันกี่รอบผลเหมือนเดิม)
- ข้ามรูปเดี่ยว (Healer.png / HighPriest.png 1024x1536) อัตโนมัติ · แก้ 9 ชีท (bishop · blacksmith · elder-traveler · ingrid · priestess · teleport-shrine · traveler · เอริค · บียอร์น)
- ★ ชีท NPC/สิ่งของใหม่ในอนาคต ★ วางใน `Sprites/npc/` แล้วรัน `python3 fix_npc_sheets.py` ก่อนใช้ (ชีทที่ AI สร้างมักวางเฟรมไม่ตรงกริด)
- ถ้าวันหน้าอยากให้ระบบจัดเองตอนรัน (เหมือน auto-fit ของผู้เล่น/มอน) ก็ทำได้ — แต่แก้ที่ไฟล์ภาพตรงกว่า ไม่ต้องเสียเวลาวัดภาพทุกครั้งที่โหลด

### กับดักข้อ 67
**★ ชีทที่ AI สร้าง "ดูเหมือน" เป็นกริด แต่ระยะจริงไม่เท่า ★** — ก่อนโทษโค้ด ให้วัด bbox ของแต่ละช่องด้วย PIL (`getchannel("A").getbbox()`) จะเห็นทันทีว่าแถว/คอลัมน์ไหนเยื้อง

## 7.57 รอบ 46 — Codex เพิ่มแท็บ NPC + "งานภาพที่ยังไม่ทำ" · จัดชีท NPC (idle เด้ง)

### ก) ชีท NPC เด้งไปเด้งมา (`fix_npc_sheets.py` — รันบนเครื่องแล้ว)
สาเหตุ: ชีท 3×2 ที่วาดมา ตัวละคร "ไม่ได้อยู่ตำแหน่งเดียวกันทุกช่อง" (แถวล่างต่ำกว่า ~9 px · คอลัมน์ขวาเยื้อง ~7 px) — Godot ตัดกริดเป๊ะ เลยเห็นกระตุก
- สคริปต์วัดขอบพิกเซลจริงทุกช่อง แล้วเลื่อนให้ **ขอบล่างตรงกัน** + **กึ่งกลางแถบเท้า 40 px ตรงกัน** (ไม่ใช้กึ่งกลางทั้งตัว เพราะผ้าคลุม/แขนทำให้เบี้ยว)
- ต้นฉบับอยู่ `_to_delete/originals_npc/` · รันซ้ำได้ (คำนวณจากต้นฉบับเสมอ) · ชีทใหม่ในอนาคต: วางใน `Sprites/npc` แล้ว `python3 fix_npc_sheets.py`
- ★ กับดักข้อ 66 ★ ชีท 1540 px หาร 3 ไม่ลงตัว — ใช้ `W//3` เหมือน Godot (เศษ 1 px ทิ้ง) · รูปเดี่ยว/รูปคุย (ไม่ใช่สี่เหลี่ยมจัตุรัสใหญ่) จะถูกข้าม

### ข) Codex (artifact) — แท็บใหม่ 2 แท็บ
- **NPC** — ทุกคนทุกแมพ จัดกลุ่มตามแมพ: ตำแหน่ง x/y · ชนิด · ร้าน (รายการแปลชื่อไทย) · เควสที่ให้ · ประโยคทัก/บทพูด · บทพูดตามธง · รักษาราคา · กล่อง 3 ช่อง "สไปรท์ / ท่า / รูปคุย" (แดง = ไม่มี · เหลือง = ชั่วคราว) · ไฟล์ที่ใช้
- **งานภาพที่ยังไม่ทำ** — การ์ดสรุป 9 หมวด (คลิกกระโดด · แถบเขียว = ทำไปแล้วกี่ %) + ตารางแต่ละหมวดพร้อม "สเปก" ที่ต้องวาด:
  มอน (placeholder / ท่าที่ขาดจาก Idle·Run·Attack·Hit·Die / เอฟเฟกต์สกิล / วิดีโอ) · ไอคอนไอเทม (placeholder / ไม่มี) · ฉากหลังแมพ (generated / ไม่มี + ขนาดที่ต้องวาด) · NPC · สกิล (ไอคอน/เอฟเฟกต์) · ท่าผู้เล่น `Attack_<อาวุธ>_<สกิล>` ที่ยังไม่มี · ไอคอน UI · วิดีโอบอส · ไฟล์ใหญ่เกิน (ไอคอน > 600 KB / รูปคุย > 2 MB)
- ค้นหาใช้ได้ทั้ง 2 แท็บ · แถบหัวโชว์ "NPC 15 · งานภาพค้าง N"

### ค) วิธีอัปเดตเอง (ไฟล์บนเครื่อง 3 ตัว: `dump_gamedata.py` `dump_assets_ext.py` `codex_template.html`)
```
python3 dump_gamedata.py
```
→ ได้ `gamedata.json` + `ข้อมูลเกมทั้งหมด.md` + **`codex.html`** (เปิดในเบราว์เซอร์ได้เลย) · ส่ง codex.html ให้ Claude = อัปเดต artifact ลิงก์เดิม
- `dump_assets_ext.py` ถูก exec จาก dump_gamedata.py ก่อนเขียน json · บนเครื่องมันเดินโฟลเดอร์ `Sprites/` จริง (ฝั่ง Claude ใช้ `_sprites_list.txt` แทน) — ตัวเลข "ค้าง" บนเครื่องจึงถูกที่สุด
- ทำงานภาพเสร็จ → วางไฟล์ + ชี้ใน Inspector → รันใหม่ รายการหายเอง · ไฟล์ในโฟลเดอร์ชื่อ `placeholder/` หรือ `generated/` นับว่า "ยังไม่ทำ" เสมอ
- แก้หน้าตา/คอลัมน์: แก้ `codex_template.html` (ฟังก์ชัน `renderNpcs()` / `renderAssets()`) · แก้เกณฑ์ตรวจ: `dump_assets_ext.py`

ตัวเลขตอนนี้ (จากเครื่อง): มอน 19/21 · ไอคอนไอเทม 105/130 · ฉากหลัง 7/11 · NPC 6/15 (บทที่ 2 ทั้งหมด) · สกิล 1/8 · ท่าผู้เล่น 4 · วิดีโอบอส 2 · ไฟล์ใหญ่ 23 — รวม 167

## 7.58 รอบ 46 (ต่อ) — ทหารยามเอริคเด้ง (รูปใหม่) · วิธีสร้างภาพ NPC ให้ตรงตั้งแต่ต้น (`make_npc_sheet.py`)

### ก) ทำไมเอริคยังเด้งทั้งที่แก้ไปแล้ว
คุณวางไฟล์ `ทหารยามเอริค.png` **ใหม่** ทับ (10:02) หลังสคริปต์แก้รอบแรก (09:08) — รูปใหม่แถวบนสูงกว่าแถวล่าง 10-13 px และเยื้องซ้ายขวา 6-12 px
`fix_npc_sheets.py` รุ่นเก่า "คำนวณจากไฟล์สำรอง" จึงจะเอารูปเก่ากลับมา → รุ่นใหม่เทียบ md5 กับผลลัพธ์ที่มันเขียนไว้ (`_to_delete/originals_npc/<ชื่อ>.png.fixed.md5`):
ไฟล์ปัจจุบัน ≠ ผลลัพธ์เดิม และ ≠ ต้นฉบับเดิม ⇒ ถือว่าเป็นรูปใหม่ สำรองใหม่แล้วค่อยจัด · รันซ้ำกี่ครั้งก็ได้ · รันเฉพาะไฟล์: `python3 fix_npc_sheets.py ทหารยามเอริค.png`
ผล: เลื่อน (6,12) (0,12) (-6,13) (12,2) (5,2) (-2,0) → ปลายเท้า y=757 ทุกช่อง · กลางเท้า x≈253 ทุกช่อง

### ข) ★ วิธีสร้างภาพให้ตรงตั้งแต่ต้น ★ — อย่าให้ AI วาด "ทั้งกริดในรูปเดียว"
กริด 3×2 ในรูปเดียว AI จะวางตัวละครไม่ตรงช่องเสมอ (เลื่อน 5-20 px ทุกครั้ง) และช่องขนาดไม่เท่า (1540/3 ไม่ลงตัว) → ต้องพึ่งสคริปต์จัดทีหลัง
วิธีที่ตรงกว่า = **วาดทีละเฟรม บนผืนผ้าใบขนาดเดียวกัน** (ที่คุณเริ่มทำแล้ว: `frame_01.png … frame_08.png` 632×848) แล้วให้ `make_npc_sheet.py` ประกอบ:
```
python3 make_npc_sheet.py erik_idle Sprites/npc/frame_*.png            # → Sprites/npc/erik_idle.png (4x2 · ช่อง 547x733) + data/sprites/npc_erik_idle.tres
python3 make_npc_sheet.py ชื่อ โฟลเดอร์/ --cols 3 --fps 6 --anim Idle --cell 513x770 --pad 12
```
- สคริปต์วัด "ปลายเท้า" + "กึ่งกลางแถบเท้า 40 px" ของทุกเฟรม แล้ววางให้ตรงกันเป๊ะ (ทำแล้ว: erik_idle ปลายเท้า y=721 ทุกช่อง กลางเท้า x=273±1)
- ได้ `.tres` SpriteFrames พร้อมใช้ → ใน Godot เลือก AnimatedSprite2D ของ NPC → ช่อง **Sprite Frames** → โหลด `data/sprites/npc_<ชื่อ>.tres` → Animation = Idle (scale แนะนำ ≈ 250 / ความสูงตัวละคร เช่น 0.353)
- `--cell 513x770` ถ้าอยากให้ช่องเท่าชีทเก่า (scale เดิมใช้ต่อได้)

**ตอนสั่ง AI วาดเฟรม** (ใช้ได้กับทุกตัว):
1. วาดเฟรมแรกให้พอใจก่อน แล้ว "ใช้เฟรมแรกเป็นรูปอ้างอิง" สั่งวาดเฟรมถัดไป — ขนาดผืนผ้าใบเท่ากันทุกเฟรม พื้นหลังโปร่งใส (หรือสีเดียวล้วนที่ไม่มีในตัวละคร)
2. บอกให้ชัดว่า **"same character, same camera, same scale, feet planted on the same spot, only subtle breathing/cape motion, full body visible, nothing cropped"** — idle ที่ดีขยับแค่หัว/ไหล่/ผ้าคลุม 2-3% ไม่ใช่เปลี่ยนท่ายืน/ย้ายหอก (เฟรม 4 ของเอริคย้ายเท้ากางออก → ต่อให้จัดตรงก็ยังดู "กระตุก" เพราะท่าเปลี่ยน)
3. ห้ามให้ตัด/ครอปตัวละครชิดขอบ — ปลายเท้าต้องมีขอบว่างข้างล่าง ไม่งั้นสคริปต์วัดขอบล่างผิด
4. 6-8 เฟรม พอ (ที่ 6 fps = 1-1.3 วิ ต่อรอบ) · เฟรมสุดท้ายควรใกล้เฟรมแรกเพื่อให้วนเนียน
5. เสร็จแล้วรัน `make_npc_sheet.py` → เช็คแถบสรุป "ปลายเท้า y=…" ทุกเฟรมต้องเท่ากันหลังประกอบ (สคริปต์พิมพ์ให้)
ถ้าจำเป็นต้องได้ "ทั้งกริดในรูปเดียว" จริง ๆ → วางแล้วรัน `fix_npc_sheets.py` เสมอ ก่อนเปิด Godot

### ค) ไฟล์ที่เพิ่ม/แก้
`fix_npc_sheets.py` (md5 กันรูปใหม่ · รับชื่อไฟล์) · `make_npc_sheet.py` (ใหม่) · `Sprites/npc/erik_idle.png` + `data/sprites/npc_erik_idle.tres` (ตัวอย่างจาก frame_01-08 — ยังไม่ได้เปลี่ยนในฉาก เอริคยังใช้ชีท 3×2 เดิมที่จัดแล้ว)
`frame_*.png` `x1-3.png` ในชีท `Sprites/npc` ถูก Godot import เป็น texture ด้วย (ไม่พัง แต่กินเวลา import) — ย้ายเฟรมดิบไปโฟลเดอร์ย่อย `Sprites/npc/_src/` หรือ `_to_delete/` เมื่อประกอบเสร็จ

## 7.59 รอบ 47 — ราคาของสวมใส่ · ปุ่มพุ่งหลบมือถือ · ป้ายเควส ! ? · กำแพงล่องหนขอบแมพ · ไอคอนสกิล/ยาบนปุ่ม

### ก) ราคาของสวมใส่: ซื้อ ×5 · ขาย ÷2 (`rebalance_r47_prices.py` — รันบนเครื่องแล้ว)
ใช้กับ **อาวุธ (type 1) + ของสวมใส่ (type 2) ทั้ง 46 ชิ้น** ในr `data/items/` · ยา/วัตถุดิบ/การ์ด/ของเควส ไม่แตะ
| ตัวอย่าง | ซื้อเดิม | ซื้อใหม่ | ขายเดิม | ขายใหม่ |
|---|---|---|---|---|
| ดาบมือใหม่ | 500 | **2,500** | 400 | **200** |
| ฟัลชิออน | 2,200 | **11,000** | 1,760 | **880** |
| เกราะเพลท | 18,000 | **90,000** | 14,400 | **7,200** |
| ดาบเพลิง | 45,000 | **225,000** | 36,000 | **18,000** |

- ★ รันซ้ำได้ ★ ครั้งแรกจำราคาเดิมลง `metadata/price_base_r47_buy` / `_sell` ในไฟล์ แล้วคำนวณจากฐานนั้นตลอด — รัน 10 ครั้งราคาก็เท่าเดิม (ไม่ทวีคูณ)
- อยากปรับตัวคูณใหม่: แก้ `BUY_MULT` / `SELL_DIV` หัวไฟล์แล้วรันซ้ำ · อยากย้อนกลับ: เอาค่าใน metadata ใส่คืนแล้วลบ 2 บรรทัดนั้น
- ผลข้างเคียงที่ต้องการ: ขายคืนเหลือ **≤ 10% ของราคาซื้อ** ทุกชิ้น (เดิม ~80%) — ซื้อมาขายกลับขาดทุนหนัก เก็บของจากมอนมาขายคุ้มกว่าซื้อ
- ★ ราคาซื้อสูงขึ้น 5 เท่าแล้ว เควสช่วงต้นอาจให้ซีนีน้อยเกินซื้อของ — ถ้าเล่นแล้วรู้สึกฝืด บอกได้ ปรับ drop zeny หรือรางวัลเควสให้ ★

### ข) ปุ่มจอสัมผัส (`touch_controls.gd`)
- **คืนปุ่มพุ่งหลบ** (`dash` ส่ง action `jump`) วางแทนปุ่มเมนูเดิม อยู่ขวาสุดของแถว ชิดวงสกิล
  เมนูระบบยังเปิดได้จาก **แถบไอคอนมุมขวาบน** (ปุ่ม "เมนูระบบ" — แตะได้บนมือถืออยู่แล้ว)
- **แถวยา/มานา/พุ่งหลบใหญ่ขึ้น 70 → 100 px** เท่าปุ่มคุย (`PAD_MID`) · เรียงจากซ้าย: ยา · มานา · พุ่งหลบ
- ★★ **ไอคอนไม่ซีดแล้ว** ★★ เดิมตั้ง `node.modulate.a = 0.55` ซึ่งจางลงไปถึงไอคอนข้างในด้วย → ไอคอนสกิลดูเป็นสีเทา
  ตอนนี้ใส่ alpha ที่ **สีของ StyleBox เอง** (`PLATE_ALPHA 0.55` พื้น+ขอบ · กด = 0.9) แผ่นปุ่มยังโปร่งแสง แต่ไอคอน/ตัวหนังสือคมและสีเต็มที่
- **คูลดาวน์สกิล**: เพิ่งใช้ = ไอคอนเทา (`CD_GRAY 0.34`) + **ม่านพัดสีเข้มบังทั้งวง** แล้วหุบตามเข็มพร้อมสีที่ไล่กลับมาสดจนครบ 100%
  (`_tick_cooldowns()` เรียกทุกเฟรมใน `_process` เฉพาะตอนปุ่มโชว์ · ม่านคือคลาส `_CooldownVeil` วาดด้วย `draw_colored_polygon` เป็นพัด)
- **ปุ่มยา/มานาโชว์ไอคอนยาที่จะถูกใช้จริง** + จำนวนที่เหลือมุมขวาล่าง (`refresh_item_icons()` ต่อสัญญาณ `inventory_changed`)
  ยาที่โชว์ = ช่องด่วน `PlayerState.item_hotkeys` (เปลี่ยนยาในกระเป๋าแล้วไอคอนเปลี่ยนตามทันที) · **ยาหมด = ไอคอนหมอง + เลข 0 สีแดง**

### ค) ป้ายเควส ! ? เหนือหัว NPC (`npc.gd`)
- เดิมเป็นตัวหนังสือ font 30 อยู่ที่ y=-116 ซึ่ง **ต่ำกว่าป้ายชื่อ** (ป้ายชื่ออยู่ -174) เลยเบียดกับตัวละคร
- ตอนนี้เป็น **วงป้าย 74 px + ตัวอักษร font 50** วางไว้ **เหนือขอบบนของป้ายชื่อ 16 px** — คำนวณจาก `Label.offset_top` ของ NPC คนนั้นเอง ตัวสูง/เตี้ยก็ไม่ทับชื่อ
- **วงรองวาดเอง** (คลาส `_MarkDisc`): แสงเรืองจาง + วงพื้นเข้มทึบ + ขอบสี → อ่านออกทั้งบนฉากหลังสว่างและมืด
- **ลอยขึ้นลงเบา ๆ** ±7 px (`MARK_BOB` · `_process` ทำงานเฉพาะตอนป้ายโชว์)
- สีเหมือนเดิม: **!** เหลือง = มีเควสให้รับ · **?** เขียว = เอาไปส่งได้แล้ว · แก้ขนาด/ระยะที่ค่าคงที่ `MARK_SIZE` `MARK_FONT` `MARK_GAP` `MARK_BOB` หัวไฟล์

### ง) ★ กำแพงล่องหนขอบแมพ ★ (`map_base.gd`) — ทุกแมพได้เลย ไม่ต้องวางเอง
- `_build_edge_walls()` สร้าง `StaticBody2D` ชื่อ **EdgeWalls** ตอนโหลดแมพ · กล่องชน 2 อัน (Left/Right) วางอยู่ **นอก Map Bounds พอดี** (ไม่กินพื้นที่เดิน)
- `collision_layer = 1` **ชั้นเดียวกับพื้น** → ผู้เล่น (mask 1) และมอน (mask 1) ชนด้วย `move_and_slide` เหมือนกำแพงปกติ **ไม่ต้องแก้ AI หรือโค้ดเดินเลย**
  พุ่งหลบมี `dodge_stop_on_wall` อยู่แล้วจึงหยุดที่ขอบพอดี · `is_on_wall()` ทำงานถูกต้อง
- หนา **120 px** (กันตัวพุ่ง 1,100 px/s ทะลุในเฟรมเดียว) · สูงเลยขอบบน **2,000 px** (กระโดด/พุ่งข้ามไม่ได้) · ลึกเลยขอบล่าง 600 px (ไม่มีช่องว่างกับพื้น)
- ปรับ/ปิดได้ต่อแมพใน Inspector กลุ่ม **"กำแพงล่องหนขอบแมพ"**: `Edge Walls` (ติ๊กออก = ไม่สร้าง) · `Edge Wall Thickness` · `Edge Wall Extra Top` / `Bottom`
- ★ เช็คแล้วว่าประตูกับจุดเกิดทุกแมพห่างจากขอบ ≥ 40 px จึงไม่มีบานไหนถูกกำแพงบัง ★ (ถ้าวางประตูใหม่ชิดขอบมาก ๆ ให้ขยับเข้ามาหรือขยาย Map Bounds)

### จ) เทสต์ — `r47_test` 73 ข้อ (ชุดเต็ม 45 ฉาก)
ราคา ×5/÷2 ทุกชิ้น + ฐานถูกจำไว้ + การ์ด/ยาไม่ถูกแตะ + ขายคืน ≤ 10% · กำแพง (ตำแหน่ง/ความหนา/ความสูง/ชั้นชน/ประตูไม่ถูกบัง/**เดินชนจริงแล้วไม่หลุดแมพทั้ง 2 ข้าง**) · ปุ่มมือถือ (มี dash/ไม่มี menu/ใหญ่เท่าปุ่มคุย/แถวเดียวกัน/ไม่ทับกัน/อยู่ในจอ/**กดแล้วพุ่งจริง**) · ไอคอนยา (ตรงกับช่องด่วน/จำนวน/เปลี่ยนยาแล้วเปลี่ยนตาม/ยาหมดหมอง) · คูลดาวน์ (พร้อมใช้=สีปกติ → เพิ่งใช้=เทา+ม่านเต็ม → ครึ่งทาง=สดขึ้น+ม่านครึ่ง → ครบ=ปกติ+ม่านหาย) · ป้ายเควส (ขนาด/วงรอง/**อยู่สูงกว่าป้ายชื่อทั้งอัน**/กลางตัว/สี/ลอยขึ้นลง/ซ่อนตอนไม่มีเควส)
แก้เก่า: `touch_runner` — ปุ่ม `menu` ถูกแทนด้วย `dash` แล้ว (เปิดเมนูระบบผ่าน `Events.toggle_window` ตรง ๆ)
★ กับดัก 73 ★ ไฟล์ภาพที่ผู้ใช้เพิ่มบนเครื่อง (`SlashFX bash v2.png` · ชีท NPC · รูปคุย) ไม่มีในโปรเจกต์ทดสอบ → `.tres`/`.tscn` พาร์สไม่ผ่าน แมพโหลดไม่ขึ้น (NPC = 0 คน) ต้องสร้าง placeholder ขนาดเท่าจริง (ชีท NPC ต้อง **1540×1540** ไม่งั้น region 513×770 หลุดกรอบ) แล้ว `--import`

## 7.60 รอบ 48 — กระเป๋า + หน้าตัวละคร เปลี่ยนเป็นโทนสีเดียวกับ HUD
- เดิม (รอบ 39) เป็นโทนครีม/น้ำตาล ตอนนี้ใช้ **สีชุดเดียวกับแผงเลือดและแถบช่องลัดสกิล** — พื้น `#161b28cc` · ช่องว่าง `UITheme.PANEL_LIGHT` · ขอบ `UITheme.BORDER` · ช่อง/แท็บ/ปุ่มที่เลือก = ขอบเหลืองทอง `UITheme.ACCENT` · ตัวหนังสือ `UITheme.TEXT` · ซีนีสี `#ffe9a0` เหมือน HUD
- ★ สีทั้งหมดอยู่ในบล็อก `const C_*` หัวไฟล์ `inventory_window.gd` ที่เดียว (`equipment_window.gd` อ้างต่อ) — อยากปรับโทนแก้ตรงนั้น หรือแก้ `ui_theme.gd` ให้เปลี่ยนทั้งเกม ★
- โทนครีมเดิมสำรองไว้ `scripts/ui/inventory_window_ก่อนรอบ48.gd.bak` / `equipment_window_ก่อนรอบ48.gd.bak` (cat ทับกลับได้ถ้าอยากคืน)
- ค่าใหม่: `C_SEL_EDGE` (ขอบตอนเลือก) · `C_BTN` / `C_BTN_HOVER` / `C_BTN_DISABLED` · `C_FIELD` / `C_FIELD_FOCUS` (ช่องค้นหา) · `C_ZENY_BG` / `C_ZENY_TEXT` · `C_FRAME` (กรอบรูปตัวละคร) · `C_INNER` (แผงย่อยในหน้าต่าง) · `C_COUNT` แดงอ่อน + ขอบดำ · `C_GOOD` = `UITheme.GOOD`
- เทสต์ที่แตะ 2 หน้าต่างนี้ผ่านหมด: equipui 27 · iteminfo 27 · r45 59 · pickup 25 · cardui 29 · hud2 91 · ภาพจริง `/tmp/shot48_windows.png`
- **ยังไม่ได้ push GitHub** (รอบ 47 push แล้ว)

## 7.61 รอบ 48 (ต่อ) — เอฟเฟกต์เลเวลอัพแบบ Ragnarok Online
- ไฟล์ใหม่ `scripts/entities/level_up_effect.gd` (`class_name LevelUpEffect`) — วาดด้วยโค้ดทั้งหมด ไม่ต้องมีไฟล์ภาพ:
  วงแสงทองแผ่ที่เท้า → เสาแสงพุ่งขึ้น (หลังตัว) → รัศมีหมุนรอบเท้า → ประกายดาวลอยขึ้น (หน้าตัว) → **LEVEL UP!** เด้งใหญ่ค้าง 1 วิแล้วลอยจาง + บรรทัด "Base Lv. N" → จอวาบสีทอง 0.5 วิ → ตัวละครเรืองทอง 1.1 วิ · อายุ 2.3 วิ ลบตัวเอง
- จ๊อบเลเวลอัพ = ชุดเดียวกันแต่ **สีม่วง** (`UITheme.JOB`) ข้อความ JOB LEVEL UP! · ขึ้นพร้อมกัน → เล่นทีละอัน ห่าง 1 วิ (ตัวหนังสือไม่ทับ)
- `player.gd`: `_on_level_up` / `_on_job_level_up` → `_play_level_up()` เรียก `LevelUpEffect.spawn(self, Vector2(0, _feet_y()), kind, level)` · เลิกใช้ floating text "LEVEL UP!" + `_hurt_flash` เดิม
- ปรับได้ที่ค่าคงที่หัวไฟล์: `DURATION` `PILLAR_H/W` `RING_R` `RAYS` `SPARKS` `TEXT_Y` `FLASH_ALPHA` · สี/ข้อความใน `COLORS`
- ★ กับดักข้อ 76 ★ เอฟเฟกต์ที่อยากให้ "อยู่หลังตัวละคร" **ห้ามใช้ z_index ติดลบ** — จะจมลงใต้ฉากหลังของแมพ (z 0) แล้วหายไปทั้งอัน → ใช้ลำดับในต้นไม้แทน (`parent.move_child(fx, 0)` = ลูกคนแรก วาดก่อน AnimatedSprite2D)
- ยังไม่มีเสียง (โปรเจกต์ยังไม่มีระบบเสียงเลย) — ถ้าอยากได้เสียง "ติ๊งงง" แบบ RO บอกได้
- เทสต์ `r48_test` 27 ข้อ · bossjob 63 · hud2 91 · r27 90 · death 13 ผ่านหมด · ภาพ `/tmp/shot48_lvup_grid.png`
- **ยังไม่ได้ push GitHub** (รอบ 48 ทั้ง 2 ส่วน: สีหน้าต่าง + เลเวลอัพ)

## 7.62 รอบ 49 — แท็บ "สเตตัส" ใน Codex (ตารางผลของสเตตัสที่อัปเดตเอง)
ไฟล์ใหม่ `dump_stats_ext.py` (ถูก exec จาก `dump_gamedata.py` ต่อจาก dump_assets_ext) — **อ่านตัวเลขจากโค้ดจริง** แล้วสร้างตาราง
  `scripts/core/player_stats.gd` ค่าคงที่ต่อแต้ม · `scripts/core/combat.gd` สูตรความแม่น/ดาเมจ · `data/jobs/*.tres` ตัวคูณอาชีพ
→ **แก้ตัวเลขแล้วรัน `python3 dump_gamedata.py` ตารางอัปเดตตามเองทันที** (ไม่ต้องมาแก้เอกสารซ้ำ)

แท็บโชว์: การ์ด 6 สเตตัส (ได้อะไรต่อ 1 แต้ม + ค่าคงที่ที่คุม + หมายเหตุ) · สูตรที่เกี่ยวข้อง · ค่าใช้จ่ายในการอัพ · สิ่งที่ได้จาก Base/Job Level · ตัวคูณของทุกอาชีพ · ค่าคงที่ทั้งหมด 26 ตัว

**สรุปตัวเลขปัจจุบัน (นักดาบ · ต่อ 1 แต้ม)**
| | ได้อะไร |
|---|---|
| STR | ATK +1.15 · ทุก 10 แต้มได้ก้อนพิเศษ (STR/10)² → 10=+1 · 20=+4 · 30=+9 |
| AGI | FLEE +1 (= มอนพลาดเพิ่ม 1%) · ASPD +0.013 ครั้ง/วิ (+1.2% ของฐาน) |
| VIT | HP +6.54 (Lv.1) / +15.9 (Lv.50) / +25.4 (Lv.99) · DEF +0.6 · ฟื้น HP +0.05/วิ |
| INT | SP +4.12 (Lv.1) / +5.35 (Lv.50) · ฟื้น SP +0.12/วิ · MATK +0.5 · MDEF +0.5 |
| DEX | HIT +1.5 (= ตีเข้าเพิ่ม 1.5% ตรง ๆ) · ATK +0.23 · ASPD +0.004 ครั้ง/วิ |
| LUK | CRIT +0.3% (คริข้าม DEF ทั้งหมด ×1.5) · ATK +0.383 |

★ ข้อสังเกตสำหรับปรับบาลานซ์ ★
- **VIT โตตามเลเวล** (ส่วน % คิดจาก HP พื้นฐาน) — แต้มเดียวกันที่ Lv.99 ให้ HP เกือบ 4 เท่าของ Lv.1
- **DEX 1 แต้ม = ความแม่น 1.5%** เป็นสเตตัสที่ "รู้สึกได้" ที่สุดต่อแต้มในช่วงต้นเกม
- **DEF ผลตอบแทนลดหลั่น** (100/(100+DEF)) — DEF 100 ลดครึ่ง · DEF 200 ลด 67%
- **LUK คุ้มขึ้นเรื่อย ๆ กับมอนเกราะหนา** เพราะคริข้าม DEF
- ค่าใช้จ่ายอัพ: ((แต้มปัจจุบัน−1)÷10)+2 → 1-10 จ่าย 2 · 11-20 จ่าย 3 · 21-30 จ่าย 4 …
- ★ กับดักข้อ 77 ★ สตริง Python ที่ใช้ `%` จัดรูปแบบ ห้ามมี `%` เดี่ยว ๆ ในข้อความ ต้องเขียน `%%` (เจอตอนเขียน "เพิ่ม 1% ตรง ๆ" แล้วพัง)

## 7.63 รอบ 50 — STR ให้ช่องกระเป๋า · DEX ลดคูลดาวน์สกิล

### ก) STR ทุก 5 แต้ม = ช่องกระเป๋า +1
- `player_stats.gd`: `const STR_PER_BAG_SLOT := 5` → คำนวณลง `bag_bonus_slots = floor(total_str / 5)` (ใช้ **total** จึงนับโบนัส STR จากของสวมใส่/บัฟด้วย)
- `player_state.refresh()` เรียก `inventory.set_size(INVENTORY_SIZE + stats.bag_bonus_slots)` ทุกครั้งที่สเตตัสเปลี่ยน
- ฐาน 40 ช่อง → STR 50 = 50 ช่อง · STR 99 = 59 ช่อง
- ★★ `Inventory.set_size()` ออกแบบให้ **ของไม่มีทางหาย** ★★ ตอนย่อ (รีสเตตัส/ถอดของที่ให้ STR):
  1. ย้ายของที่อยู่เกินช่องใหม่ ลงมาช่องว่างข้างล่างก่อน
  2. ย่อได้แค่ถึง "ช่องสุดท้ายที่ยังมีของ" — ถ้ากระเป๋าเต็ม ช่องจะค้างไว้จนผู้เล่นใช้/ทิ้งของออก แล้วค่อยหดเอง
- ★ `Inventory.from_array()` ขยายรับตามจำนวนที่เซฟมาก่อนเสมอ ★ (เซฟเก่ามี 50 ช่อง แต่ `Inventory.new(40)` ตอนโหลด → ถ้าไม่ขยายก่อน ของช่อง 41-50 จะหาย) แล้ว `refresh()` ค่อยย่อกลับให้พอดี

### ข) DEX ทุก 5 แต้ม = ลดคูลดาวน์สกิล 1%
- `const DEX_PER_COOLDOWN := 5` · `const MAX_COOLDOWN_REDUCTION := 50.0` (เพดาน กันสกิลไม่มีคูลดาวน์)
- `cooldown_reduction = clamp(floor(total_dex/5) + ของสวมใส่, 0, 50)` — ★ ของสวมใส่/การ์ดเพิ่มได้ด้วยคีย์ใหม่ **`cooldown_reduction_percent`** (ยังไม่มีไอเทมชิ้นไหนใช้) ★
- `player_state.commit_skill_use()`: `cooldowns[id] = s.cooldown * (1 - cooldown_reduction/100)`
- DEX 50 = ลด 10% · DEX 99 = ลด 19%

### ค) หน้าตัวละคร + Codex
- หน้าตัวละครเพิ่ม 2 ช่องในตารางค่าคำนวณ: **ช่องกระเป๋า** (`40 (+10)`) และ **ลดคูลดาวน์** (`10%`) · tooltip ของ STR/DEX อัปเดตแล้ว
- แท็บ "สเตตัส" ใน Codex เพิ่ม 2 แถวนี้ให้อัตโนมัติ (`dump_stats_ext.py` อ่าน `STR_PER_BAG_SLOT` / `DEX_PER_COOLDOWN` / `INVENTORY_SIZE` จากโค้ดจริง)
- **ปรับอัตราส่วน**: แก้ `STR_PER_BAG_SLOT` / `DEX_PER_COOLDOWN` ที่ `player_stats.gd` (ใส่ 0 = ปิดผลนั้นไปเลย) แล้วรัน `dump_gamedata.py` เอกสารตามเอง

### ง) เทสต์ — `r50_test` 38 ข้อ
STR 1/5/9/50/99 · โบนัส STR จากของสวมใส่นับด้วย · ย่อกระเป๋าแล้วของครบ (ทั้งของช่องต้น ช่องท้าย และตอนเต็ม) · เซฟ/โหลดกระเป๋าใหญ่ · DEX 1/5/9/50/99 · เพดาน 50% · ของสวมใส่บวกรวมกับ DEX · **ใช้สกิลจริงแล้วคูลดาวน์สั้นลง 10% จริง** · หน้าตัวละครโชว์ถูก
ตรวจซ้ำ 10 ฉาก 305 ข้อผ่านหมด (r45 · equipui · pickup · cardui · iteminfo · card · bossjob · hud2 · r48 · slash)
★ กับดักข้อ 78 ★ `ItemInstance` สร้างด้วย `ItemInstance.create(id, count)` ไม่ใช่ `.new(...)` (constructor ไม่รับ argument)

## 7.64 รอบ 51 — ของสวมใส่/การ์ด เพิ่ม "ลดคูลดาวน์สกิล" ได้
เพิ่มช่อง **`Cooldown Reduction Percent`** ในกลุ่ม **Percent Bonus** ของ `ItemData` — ใช้ได้ทั้งของสวมใส่และการ์ด (CardData สืบทอด ItemData)

**วิธีใช้ (ไม่ต้องแตะโค้ด)**
1. เปิดไฟล์ไอเทม/การ์ดใน `data/items/` หรือ `data/cards/` ที่ Inspector
2. กลุ่ม **Percent Bonus** → ช่อง **Cooldown Reduction Percent** ใส่ตัวเลข เช่น `5` = ลดคูลดาวน์ 5%
3. เซฟ — จบ ไม่ต้องรันสคริปต์อะไร

**ทางเลือกสำหรับการ์ด** ใส่ใน `percent_effects` ก็ได้: `{"cooldown_reduction_percent": 5.0}` (ทำงานเหมือนกัน · ถ้าใส่ทั้ง 2 ที่จะบวกกัน)

**รวมค่ายังไง** ของสวมใส่ทุกชิ้น + การ์ดทุกใบ + DEX (ทุก 5 แต้ม = 1%) แล้วตัดที่เพดาน `MAX_COOLDOWN_REDUCTION` (50%)
ตัวอย่าง: อาวุธ 7% + DEX 50 (10%) = ลดคูลดาวน์ 17% → สกิลคูลดาวน์ 1.5 วิ เหลือ 1.245 วิ

**โชว์ที่ไหนบ้าง**
- กล่องรายละเอียดไอเทม → บรรทัด "ลดคูลดาวน์ +7.0%"
- หน้าตัวละคร → ช่อง "ลดคูลดาวน์" (ค่ารวม) + สรุป "จากของสวมใส่ + การ์ด"
- ตารางไอเทมใน Codex → คอลัมน์ค่าพลัง (`dump_gamedata.py` โชว์ช่อง % ทั้ง 7 ตัวแล้ว — เดิมโชว์แค่ ASPD)

★ ยังไม่มีไอเทมชิ้นไหนตั้งค่าไว้ ★ — ระบบพร้อมแล้ว รอกรอกใน Inspector (บอกได้ถ้าอยากให้ผมใส่ให้ชิ้นไหน)

★ กับดักข้อ 79 ★ **`ItemData.Slot` กับ `Equipment.EquipSlot` เป็นคนละ enum และเลขไม่ตรงกัน**
`ItemData.Slot.WEAPON = 1` แต่ `Equipment.EquipSlot.WEAPON = 0` — เรียก `PlayerState.unequip()` ต้องแปลงด้วย `Equipment.slot_for(item_data)` ก่อนเสมอ (ส่ง `d.slot` ตรง ๆ จะถอดผิดช่องแล้วคืน false เงียบ ๆ)

เทสต์ `r50_test` ขยายเป็น **50 ข้อ** (เพิ่มส่วนที่ 6: ใส่อาวุธได้ 7% · รวมกับ DEX = 17% · คูลดาวน์จริงสั้นลง 17% · กล่องรายละเอียดโชว์ · ถอดแล้วหาย · การ์ดทั้ง 2 ทาง · PCT_NAMES) · ตรวจซ้ำ iteminfo 27 · equipui 27 · r45 59 · card 32 · cardui 29 ผ่านหมด

## 7.65 รอบ 52 — เพลงประจำแมพ (ตั้งชื่อไฟล์ให้ตรงกับแมพ แล้วเล่นเอง)

### ★★ วิธีเพิ่มเพลงใหม่ — ทำแค่ขั้นเดียว ★★
วางไฟล์ที่ **`Sprites/music/<map_id>.mp3`** — จบ ไม่ต้องแก้โค้ด ไม่ต้องตั้งค่าในฉาก ไม่ต้องบอกผม
รองรับ `.mp3` `.ogg` `.wav` · ชื่อพิเศษ: **`title.mp3`** = เพลงหน้าหลัก · `boss.mp3` = เพลงบอส (เรียกเองด้วย `Game.music.play_key("boss")`)

**เพลงที่ใส่แล้ว (รอบนี้)** — asgard_forest_2 · dark_forest · prontera_field · prontera_town
**แมพที่ยังว่าง 7 แมพ** — `cold_forge` `dark_forest_2` `ember_mine` `hall_of_silence` `iron_road` `nidavellir_town` `thunder_scar`

★ เช็คชื่อก่อนเล่นได้ด้วย **`python3 check_music.py`** ★ — บอกว่าไฟล์ไหนใช้ได้ ไฟล์ไหนชื่อผิด (พร้อมเดาว่าน่าจะหมายถึงแมพไหน) และแมพไหนยังไม่มีเพลง
**ชื่อผิดแม้ตัวเดียว = เกมหาไม่เจอแล้วเงียบไปเฉย ๆ ไม่มี error เตือน** จึงควรรันสคริปต์เช็คทุกครั้งที่เพิ่มเพลง

### ระบบทำงานยังไง (`scripts/core/music_player.gd` — `class_name MusicPlayer`)
- **ไม่ใช่ Autoload** — `Game` สร้างเป็นลูกตอนเปิดเกม เรียกใช้ผ่าน **`Game.music`**
  (ทำแบบนี้เพราะไม่ต้องแก้ `project.godot` ซึ่งต้องปิด-เปิด Godot ใหม่ทุกครั้ง)
- `map_base._ready()` เรียก `Game.music.play_for_map(map_id)` — ทุกแมพได้เลยไม่ต้องแก้ฉากไหน
- **ไล่เสียงข้ามเพลง** มีเครื่องเล่น 2 ตัว: เพลงใหม่ค่อย ๆ ดังขึ้น (1.1 วิ) พร้อมเพลงเก่าค่อย ๆ เบาลง (0.9 วิ) ไม่ตัดห้วน
- **เข้าแมพเดิมซ้ำ / แมพที่ใช้เพลงเดียวกัน = เล่นต่อ ไม่เริ่มใหม่** (เดินเข้า-ออกประตูเพลงไม่กระตุก)
- **แมพที่ยังไม่มีไฟล์ = ไล่เสียงออกแล้วเงียบ** ไม่ค้างเพลงแมพก่อนหน้า และไม่ error
- **วนซ้ำอัตโนมัติ** ตั้ง `loop = true` ที่ตัว resource ตอนโหลด → ไม่ต้องไปติ๊ก Loop ในหน้า Import ของทุกไฟล์เอง
- **เล่นต่อตอนเกมหยุด** (`PROCESS_MODE_ALWAYS`) เปิดเมนู/คุย NPC เพลงไม่หยุด
- จำ path ที่หาเจอไว้ (`_path_cache`) ไม่ต้องไล่หาไฟล์ซ้ำทุกครั้งที่เปลี่ยนแมพ

### ปรับเสียงในเกม
เมนูระบบ (Tab) → แถว **"เพลง"** มีแถบเลื่อน 0-100% + ปุ่มเปิด/ปิด · ป้ายโชว์ชื่อเพลงที่เล่นอยู่
ค่าที่ตั้งถูกจำไว้ใน `user://ui_layout.cfg` (ไฟล์เดียวกับโหมดปุ่มจอสัมผัส) · ค่าเริ่มต้น 60%

### แก้ค่าได้ที่หัวไฟล์ `music_player.gd`
`DIRS` (โฟลเดอร์ที่ไปหาไฟล์) · `EXTS` (นามสกุล) · `FADE_IN` / `FADE_OUT` (เวลาไล่เสียง) · `DEFAULT_VOLUME`

### เทสต์ — `r52_test` 36 ข้อ
หาไฟล์ตามชื่อแมพ · แมพไม่มีเพลงไม่พัง · เข้าแมพแล้วเล่นเอง · ย้ายแมพเพลงเปลี่ยน · แมพไม่มีเพลงแล้วเงียบ (ไม่ค้างเพลงเก่า) · เรียกซ้ำไม่เริ่มใหม่ · ตั้ง loop ให้ mp3 · ระดับเสียง/ปิดเปิด/จำในไฟล์ · เมนูระบบเลื่อนแล้วเสียงเปลี่ยนจริง
ตรวจซ้ำ 5 ฉาก 155 ข้อผ่านหมด (r40 · r32 · portal · r47 · touch)
★ เทสต์ในคลาวด์ใช้ไฟล์ mp3 ปลอม 2 วิ (สร้างด้วย ffmpeg) เพราะไฟล์จริง 11 MB ไม่ต้องยกขึ้นมา ★

---

### 7.66 รอบ 53 — ดาบขึ้นที่มือตัวละครท่า Idle (วาดบนผืนเดียวกับตัวละคร แบบเดียวกับเอฟเฟกต์สกิล)

**แนวคิด:** ภาพดาบ = ไฟล์ PNG ผืนเดียวกับภาพท่า Idle ของตัวละคร (**1112×834 เท่า `Sprites/frame_01.png`**) พื้นโปร่ง
วาดดาบไว้ตรงมือ แล้วระบบ Paper Doll (`CharacterVisual` แบบ A) ซ้อนทับตัวละครให้ตรงเป๊ะ — ไม่ต้องตั้งค่าตำแหน่ง/ขนาด/มุมเลย
ท่าอื่น (Run/Jump/Dash/Hit/Attack) ไม่มีภาพ → **ซ่อนอัตโนมัติ** · หันซ้ายกลับด้านให้เอง

(รอบนี้เคยทำระบบ "ถือในมือ" ที่คำนวณตำแหน่งมือทุกท่าไปก่อน แล้ว **ยกเลิก** ตามที่ผู้ใช้ต้องการ — ของที่ยกเลิกอยู่ `_to_delete/r53_ยกเลิก/` · ItemData/CharacterVisual กลับเป็นของเดิม)

#### ไฟล์

| ไฟล์ | หน้าที่ |
|---|---|
| `Sprites/equip/sword_idle.png` | ★ ภาพดาบบนผืน 1112×834 — **วาดทับไฟล์นี้ได้เลย** (ตอนนี้เป็นตัวอย่างที่ประกอบจากภาพดาบที่ส่งมา) |
| `data/sprites/equip_sword_idle.tres` | SpriteFrames ท่า `Idle` 1 เฟรม → ชี้ไปที่ png ข้างบน (สคริปต์สร้างให้) |
| `make_equip_frames.py` | สร้าง .tres + ผูกกับไอเทม (`equip_sprite_frames`) |
| `_to_delete/held_preview/แม่แบบวาดดาบ_Idle.png` | แม่แบบ: ตัวละคร + กากบาทมือ (440, 415) + ตาราง 100 px — เปิดเป็นเลเยอร์อ้างอิงตอนวาด |

ผูกให้แล้ว: ดาบ 8 เล่มที่ใช้ท่า `Attack_Blade` (ดาบมือใหม่ ดาบไม้ ดาบสั้น เรเปียร์ บาสตาร์ด ใบมีดเหล็ก ดาบเตาหลอม ดาบรูน) · สำรอง .tres เดิมที่ `_to_delete/originals_items_equipframes/`

#### วิธีวาดดาบเอง
1. เปิด `Sprites/frame_01.png` (หรือแม่แบบข้างบน) เป็นเลเยอร์อ้างอิง · สร้างเลเยอร์ใหม่ วาดดาบตรงมือ (กำปั้นซ้ายของภาพ ≈ px 440, 415)
2. ซ่อนเลเยอร์ตัวละคร → export เฉพาะดาบ พื้นโปร่ง **ขนาดผืนต้อง 1112×834 เท่าเดิม** → ทับ `Sprites/equip/sword_idle.png`
3. เปิด Godot — ไม่ต้องรันอะไรเพิ่ม (ชื่อไฟล์เดิม .tres ชี้อยู่แล้ว)

มือท่า Idle ขยับแค่ ±3 px ทั้ง 9 เฟรม → ภาพเดียวพอ (ระบบใช้เฟรมสุดท้ายเมื่อเฟรมตัวละครเกินจำนวนเฟรมภาพดาบ)

#### สคริปต์
```
python3 make_equip_frames.py sword_idle Sprites/equip/sword_idle.png --items @Attack_Blade   # ที่รันไปแล้ว
python3 make_equip_frames.py katana_idle Sprites/equip/katana_idle.png --items katana          # ดาบเล่มอื่นมีภาพของตัวเอง
python3 make_equip_frames.py sword_idle f1.png f2.png ... --anim Idle --fps 5                 # วาดหลายเฟรม (เพิ่มท่าเข้าไฟล์เดิมได้ด้วย --anim Run)
```
สคริปต์เตือนถ้าขนาดผืนไม่เท่ากับภาพตัวละครของท่านั้น · รันซ้ำได้

#### กับดัก 80-81
- **80** `.tres` ที่ไม่มี `load_steps` แล้วไปเติม → ต้องเติม "ก่อน" หาตำแหน่งแทรก ext_resource (ตำแหน่งเลื่อน)
- **81** เทสต์ที่ตั้ง `flip_h`/`play()` ให้ตัวละครเอง ต้อง `player.set_physics_process(false)` ก่อน ไม่งั้น `_update_facing/_update_animation` ทับค่าทุกเฟรม

เทสต์: `r53_runner.gd` 34 ข้อ (ข้อมูล · Idle โชว์/offset ตรงตัว/กลับด้าน · Run/Jump/Dash/Hit/Attack ซ่อน · ถอดแล้วซ่อน) + เรนเดอร์จริง shot53

---

### 7.67 รอบ 54 — ฮอร์เน็ตไม่เล่นท่า Idle · มอนบิน/ลอยเหนือพื้น · ฉากดูเฟรมมอนแต่ละตัว

#### ก) ทำไมฮอร์เน็ตไม่ขยับ (กับดัก 82)
ฮอร์เน็ตมี SpriteFrames **ท่าเดียว** (Idle 8 เฟรม) → ตอน `_apply_visual()` ใส่ `sprite.sprite_frames` Godot จะตั้ง `sprite.animation = "Idle"` ให้เอง **แต่ไม่สั่งเล่น**
`_play()` เดิมเช็คแค่ `if sprite.animation != real: sprite.play()` → ชื่อตรงอยู่แล้ว เลยไม่เคยเรียก `play()` = ค้างเฟรม 0 ตลอด
มอนตัวอื่นมีหลายท่า (ท่าแรกไม่ใช่ Idle) เลยไม่เจอ
**แก้** (`monster_base.gd _play`): `if sprite.animation != real or (not sprite.is_playing() and loop): sprite.play(real)`
(ท่าไม่วนซ้ำที่เล่นจบแล้ว เช่น Attack/Death ไม่เริ่มใหม่ ไม่งั้นกระตุก)

#### ข) มอนบิน (`MonsterData` กลุ่ม "บิน (ลอยเหนือพื้น)")
| ช่อง | ฮอร์เน็ต | ความหมาย |
|---|---|---|
| `flying` | true | เปิดโหมดลอย |
| `hover_height` | 110 | ลอยสูงจากพื้นกี่ px (จุดต่ำสุดของภาพ) — ผู้เล่นสูง 240 ระดับสายตา ≈ 190 → กลางตัวฮอร์เน็ต (110 + 150/2 = 185) |
| `hover_bob` | 8 | โยกขึ้นลง ± px |
| `hover_bob_speed` | 1.4 | รอบ/วินาที |
| `flying_no_hop` | true | ไม่กระโดด |

หลักการ: **ตัวมอน (กล่องชน/foot_position) ยังเดินบนพื้นเหมือนเดิม** → AI/ระนาบ/ระยะตี/spawner ไม่เปลี่ยน
ยกเฉพาะ `sprite.position.y = -lift` + หลอดเลือด + ตัวเลขดาเมจ + **`body_rect()` (กรอบโดนฟัน) ลอยตาม** → ต้องฟันที่ตัวมัน ฟันพื้นไม่โดน
ตาย → ร่วงลงพื้น 320 px/วิ (`HOVER_FALL_SPEED`) ไม่วูบ · เฟสโยกสุ่มต่อตัว (ฝูงไม่โยกพร้อมกัน)
สคริปต์: `python3 set_monster_flying.py <id> [--height 110 --bob 8 --speed 1.4] [--off]` (ตั้งใน Inspector ก็ได้)
★ หลอดเลือดฮอร์เน็ต `hp_bar_offset_y = -42` ยังอยู่ระดับท้อง — อยากให้อยู่เหนือหัวตั้ง ≈ -165 ★

#### ค) ★ ฉากดูมอน — เห็น "เฟรมแอนิเมชัน" แถบล่างเหมือนหน้า player ★
```
python3 make_monster_preview.py hornet        # (ปิด Godot ก่อน) · --all = ทุกตัว
```
1. แยก SpriteFrames ที่ฝังในไฟล์มอน → **`data/sprites/monsters/<id>_frames.tres`** แล้วให้ `data/monsters/<id>.tres` ชี้ไปแทน (ข้อมูลเฟรมเหมือนเดิม · สำรอง `_to_delete/originals_monster_preview/`)
2. สร้าง **`scenes/monsters/preview/<id>.tscn`** = AnimatedSprite2D ที่ใช้ไฟล์เฟรมเดียวกัน + สคริปต์ `scripts/tools/monster_preview.gd`

ใช้: FileSystem → `scenes/monsters/preview/hornet.tscn` → คลิก AnimatedSprite2D → แถบล่าง "เฟรมแอนิเมชัน" ครบทุกท่า
**แก้ตรงนั้น = แก้ไฟล์เฟรมที่มอนตัวจริงใช้** (เพิ่มท่า Run/Attack/Hit/Death ก็ทำที่นี่)
กด **F6** (เล่นฉากปัจจุบัน) → ฮอร์เน็ตตัวจริงเกิดบนพื้นจำลอง + เงาผู้เล่น 240 px + เส้นระดับสายตา 190 · ป้ายบนซ้ายบอกท่า/เฟรม/▶ กำลังเล่น/ลอยกี่ px · R เกิดใหม่ · Space โดนตี · K ฆ่า (ดูร่วง) · Esc ปิด
(ซ่อน HUD ของเกมให้ตอนรัน)

#### เทสต์
`r54_runner.gd` 29 ข้อ (ไฟล์เฟรมแยก · Idle เล่นจริง/เฟรมขยับ · ลอย 102-118 · ภาพ/หลอด/กรอบฟันยกตาม · เท้าอยู่พื้น · โยกจริง · ไม่กระโดด · ระดับสายตา · ตายร่วง · โพริงไม่กระทบ · ฉากดูมอนเกิดตัวจริง) + เรนเดอร์ shot54
กับดัก **83**: `.tres` มี `uid="..."` — regex หา `id="` ต้องกัน `uid=` (ใช้ `(?<![A-Za-z_])id=`) · **84**: ไฟล์ภาพใหม่ที่ผู้ใช้เพิ่ม (`Sprites/skill/blade_slash.png` 5584×768) ต้องมี placeholder ในโปรเจกต์ทดสอบ ไม่งั้น player.tscn โหลดไม่ขึ้น

---

### 7.68 รอบ 55 — เดินเลยขอบภาพฉากไม่ได้อีกแล้ว

**อาการ:** เดินไปขวาสุดของทุ่งวิหารแล้ว "หลุดออกไปยืนบนที่ว่างสีดำ" ทั้งที่รอบ 47 ทำกำแพงล่องหนไว้แล้ว

**สาเหตุ:** กำแพงรอบ 47 อยู่ที่ขอบ **Map Bounds** — แต่ Map Bounds ของหลายแมพ "กว้างกว่าภาพฉากจริง"

| แมพ | Map Bounds เดิม | ขอบภาพฉากจริง | เกิน |
|---|---|---|---|
| ทุ่งวิหาร | 120 .. **5870** | -480 .. **5648** | 222 px |
| ป่าเงาลึก | 0 .. **5600** | -88 .. **3916** | **1,684 px** (พื้นก็จบที่ 3901 — เดินต่อไปคือตกเหว) |
| ป่าแอสการ์ด 2 | -100 .. **3700** | -136 .. **3443** | 257 px |
| แมพที่เหลือ 8 แมพ | — | เท่ากับขอบเขตพอดี | 0 |

**วิธีแก้ (`map_base.gd`):** ตอนโหลดแมพ ระบบวัด "ขอบภาพฉาก" เองแล้ว **หุบ Map Bounds เข้ามา** ก่อนสร้างกำแพง
กำแพง + กล้อง เลื่อนตามอัตโนมัติ ไม่ต้องไปแก้ทีละแมพ

- `art_span()` = ขอบซ้าย-ขวาของภาพฉากในพิกัดโลก · นับเฉพาะ `Sprite2D` / `TextureRect` / `Polygon2D` ที่ **มองเห็นอยู่** และ **กว้าง ≥ 25% ของแมพ** (ก้อนหิน ป้าย มอน ของตก ประตู ไม่ถูกนับ — `_is_actor()` ตัดกลุ่ม player/enemy/npc/dropped_item/portal ออก)
- **หุบเข้าอย่างเดียว ไม่ขยายออก** — แมพที่ภาพกว้างกว่าขอบเขตอยู่แล้ว ไม่มีอะไรเปลี่ยน
- **ไม่หุบจนบังประตู/จุดเกิด** — ถ้าหุบแล้วประตูหรือ Marker ใน `SpawnPoints` อยู่นอกกำแพง ระบบจะดันขอบกลับออกไปให้ (`KEEP_INSIDE_MARGIN` 90 px)
- หุบเกิน 300 px จะขึ้น warning ใน Output (= ภาพฉากสั้นกว่าแมพจริง ๆ อยากได้กว้างเท่าเดิมต้องขยายภาพ ไม่ใช่ปล่อยให้เดินไปที่ว่าง)

**ช่องใหม่ใน Inspector (กลุ่ม "กำแพงล่องหนขอบแมพ")**

| ช่อง | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| `clamp_bounds_to_art` | เปิด | หุบขอบแมพตามภาพฉาก (ปิดได้ถ้าแมพไหนตั้งใจให้เดินเลยภาพ) |
| `art_edge_margin` | 40 | เว้นจากขอบภาพเข้ามาอีกกี่ px — กันตัวละครยื่นพ้นภาพครึ่งตัว |

**ผลหลังแก้:** ทุ่งวิหาร 120..5870 → **120..5608** · ป่าเงาลึก 0..5600 → **0..3876** · แมพอื่นหุบข้างละ 40 px (ตามระยะเว้นขอบ)
ตอนโหลดแมพจะพิมพ์ใน Output ว่า `[Map] <id> หุบขอบตามภาพฉาก: x ... → ...`

เทสต์ `r55_runner.gd` **59 ข้อ** (วัดขอบภาพ · หุบถูกตำแหน่ง · กำแพง+กล้องตามขอบใหม่ · เดินขวา/ซ้ายไม่หลุด · พุ่งหลบไม่ทะลุ · ประตูอยู่ในขอบ · ครบทั้ง 11 แมพ) + regression 9 ฉาก 307 ข้อ ผ่านหมด

---

### 7.69 รอบ 56 — ตกแมพ · slash โดนกี่ตัว · เจาะรูการ์ด · คูลดาวน์บอสข้ามแมพ

#### ก) ★ ตาข่ายกันตกแมพ ★ (`player.gd`)
ใช้ slash พุ่งแล้วหลุดลงไปใต้แมพ = ร่วงไปเรื่อย ๆ เล่นต่อไม่ได้ (ไม่ตาย ไม่หยุด)
ตอนนี้ตัวละครจำ **"ที่ยืนล่าสุดที่ปลอดภัย"** ไว้ทุก 0.35 วิ (ตอนเท้าติดพื้น ไม่ได้พุ่ง และอยู่ในขอบแมพ)
ถ้าร่วงต่ำกว่าขอบล่างของแมพเกิน `FALL_RESCUE_MARGIN` (260 px) → หยุดพุ่ง ล้างความเร็ว แล้ว **พากลับที่จุดนั้น** ทันที
(ไม่มีที่จำไว้ → กลับจุดเกิดของแมพ) · ขึ้นข้อความ "พากลับขึ้นมาแล้ว" + warning ใน Output
ค่าคงที่ทั้งหมดอยู่หัวหัวข้อ `★★ ตาข่ายกันตกแมพ ★★` ใน `player.gd`
★ ใช้ได้กับทุกกรณี ไม่ใช่แค่ slash (ตกร่อง · ทะลุพื้น · ตำแหน่งเพี้ยน) ★

#### ข) slash โดนมอนกี่ตัว — ตามเลเวลสกิล
`SkillData` เพิ่มช่อง **`max_targets_by_level`** = `{ เลเวลสกิล: จำนวนตัว }`
slash ตั้งไว้ `{1: 3, 5: 4, 8: 5}` → **เลเวล 1-4 โดน 3 ตัว · เลเวล 5-7 โดน 4 ตัว · เลเวล 8+ โดน 5 ตัว**
- ใช้ทั้งสกิลพุ่ง (นับตัวที่ฟันโดนระหว่างพุ่ง ครบแล้วหยุดพุ่ง) และสกิลตีปกติ
- หน้าสกิลโชว์ "โดนสูงสุด N ตัว (เลเวล 5 → 4 ตัว)" ให้เอง
- เว้นว่าง = ใช้ `max_targets` ค่าเดียวทุกเลเวลเหมือนเดิม (สกิลอื่นไม่กระทบ)
- แก้ด้วยสคริปต์: `python3 set_skill_targets.py slash 1:3,5:4,8:5` (`--off` = เลิกใช้)

#### ค) ★★ ระบบเจาะรูการ์ด ★★ (`socket_system.gd` + `socket_window.gd`)
ของที่ซื้อจากร้าน **ไม่มีรู** — เอามาเจาะเองได้ที่ **ช่างตีเหล็ก** (เมนูใหม่ "เจาะรูการ์ด" อยู่ในเมนูคุย NPC ประเภท REFINER หรือ NPC ที่ติ๊ก `has_socket`)

| ระดับไอเทม | วัตถุดิบ | ค่าเจาะ | โอกาส | ของชิ้นที่ 2 | เจาะไม่ติด |
|---|---|---|---|---|---|
| เลเวล 1-30 | phracon ×10 | 15,000 z | **70%** | ต้องมี (ชนิดเดียวกัน) | ของหาย |
| เลเวล 40-70 | emveretarcon ×10 | 50,000 z | **60%** | ไม่ต้อง | ของหาย |

- **ไอเทมที่เจาะได้ 2 รู ใช้วัตถุดิบและเงินเท่าตัว** (phracon ×20 / 30,000z · emveretarcon ×20 / 100,000z) แล้วได้ 2 รูรวดเดียว
- จำนวนรูที่ได้ = `card_slots` ของไอเทมชนิดนั้น
- เจาะได้เฉพาะของสวมใส่ที่ **ยังไม่มีรู** (ของดรอปจากมอนมีรูติดมาแล้ว เจาะซ้ำไม่ได้)
- **เลเวล 31-39 เจาะไม่ได้** (ตามที่กำหนด) — หน้าต่างจะบอกเหตุผลให้
- วัตถุดิบ/เงิน/ชิ้นที่ 2 **เสียทุกครั้งไม่ว่าสำเร็จหรือไม่**
- ★ แก้เงื่อนไขทั้งหมดที่ตาราง `TIERS` หัวไฟล์ `scripts/core/socket_system.gd` ที่เดียว ★ (ช่วงเลเวล · แร่ · จำนวน · ราคา · โอกาส · ต้องมีชิ้นที่ 2 ไหม · พลาดแล้วของหายไหม)

#### ง) ★ คูลดาวน์บอสข้ามแมพ ★ (กันวนออก-เข้าแมพเพื่อฟาร์มบอส)
เดิมเวลานับถอยหลังอยู่ในฉากแมพ — ออกแมพแล้วเข้าใหม่ ฉากถูกสร้างใหม่ = บอสเกิดทันที
ตอนนี้ตอนบอสตาย ระบบจะจำ "เวลาที่เกิดใหม่ได้" ลง **เซฟ** (`PlayerState.respawn_locks`)
ออกแมพ · ปิดเกม · โหลดเซฟ ก็ยังต้องรอจนครบ (บาฟโฟเมท 120 วิ · คิงโพริง 300 วิ ตาม `respawn_time`)
- `MonsterData` ช่องใหม่: `respawn_persistent` (มอนธรรมดาติ๊กเองได้) · `respawn_time_persistent` (0 = ใช้ `respawn_time`)
- **บอส (`is_boss`) เปิดให้อัตโนมัติ** ไม่ต้องติ๊ก
- สปอว์นเนอร์ทั้ง 2 แบบ (`monster_spawner` · `map_spawner`) เช็คก่อนเกิดทุกครั้ง
- ★ กับดัก 85 ★ `_fill()` ของ monster_spawner เดิมเป็น `while ... : _spawn_one()` — พอ `_spawn_one` ไม่เกิด (ติดคูลดาวน์) จะ **วนไม่รู้จบจนเกมค้าง** → เปลี่ยนให้ `_spawn_one()` คืน bool แล้ว `return` เมื่อเกิดไม่ได้

เทสต์ `r56_runner.gd` **67 ข้อ** (เป้าหมายตามเลเวล · ตกแมพแล้วถูกพากลับ · พุ่งชนกำแพงขอบแมพ · เงื่อนไขเจาะรูครบทุกกรณี + วัดอัตราสำเร็จจริง 300 ครั้ง · คูลดาวน์บอสข้ามเซฟ/สปอว์นเนอร์ใหม่ · _fill ไม่ค้าง)

---

### 7.70 รอบ 57 — ของดรอปแรงกว่า · เมนูช่างตีเหล็ก · เสาวาป · เสียงฟันดาบ · หน้าตีบวกใหม่

#### ก) ★ ของสวมใส่ที่ดรอปจากมอน/บอส ดีกว่าของร้าน 5-30% ★
`ItemInstance` เพิ่มช่อง **`bonus_percent`** (ติดกับ "ชิ้นนั้น" ไม่ใช่ชนิดไอเทม)
- `create_drop()` สุ่มให้เอง **5.0-30.0%** ทุกครั้งที่ของสวมใส่ดรอป (ยา/วัตถุดิบ/การ์ด ไม่ได้)
- ค่าที่ถูกคูณ: ATK · DEF · MATK · MDEF · HIT · FLEE · CRIT · MaxHP · MaxSP · STR-LUK ของ **ตัวไอเทม**
  (ไม่คูณโบนัสจากตีบวกและการ์ด — ของพวกนั้นคิดแยกเหมือนเดิม)
- **ปัดขึ้นเสมอ** (`ceil`) → ของดรอปดีกว่าของร้านแน่นอน แม้ค่าจะน้อย
- ชื่อไอเทมต่อท้าย `(+18%)` · กล่องข้อมูลโชว์ค่าที่คูณแล้ว + บรรทัด "★ ของดรอป : ค่าพลังดีกว่าของร้าน +18%"
- ของคนละโบนัสไม่กองรวมกัน (`same_kind_as` เทียบ bonus_percent ด้วย) · เซฟ/โหลดจำค่าไว้
- ★ อยากปรับช่วง แก้ `DROP_BONUS_MIN` / `DROP_BONUS_MAX` หัวไฟล์ `scripts/resources/item_instance.gd` ★

#### ข) เมนูช่างตีเหล็ก — เพิ่มปุ่ม "ตีบวก"
เดิมกด "พูดคุย" แล้วเด้งหน้าตีบวกทันที (ดูแปลก) → ตอนนี้เมนูเป็น
**พูดคุย · ซื้อขาย · ตีบวก · เจาะรูการ์ด · ไม่คุย** (ตัวที่มีจริงเท่านั้น)
"พูดคุย" กับช่างตีเหล็กจะได้บทสนทนาปกติแล้ว · NPC อื่นเปิดเมนูนี้ได้ด้วยการติ๊ก `has_refine` / `has_socket`

#### ค) ★ ศิลาเซฟ → เสาวาป ★
NPC ชนิด `SAVE_POINT` กลายเป็น **เสาวาป** — กดแล้วมีเมนูเลือกปลายทาง
| ช่องใน Inspector (กลุ่ม "เสาวาป") | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| `warp_targets` | `[asgard_forest_2]` | แมพปลายทาง (เพิ่มทีหลังแค่เติม id ในลิสต์) |
| `warp_flags` | ว่าง | `{ map_id: ธงเนื้อเรื่อง }` — ปลายทางที่ต้องปลดล็อกก่อน |
| `warp_spawn_point` | `default` | ไปโผล่จุดไหนในแมพปลายทาง |
| `warp_saves_game` | เปิด | มีเมนู "บันทึกเกม" ด้วย (ของเดิมของศิลาเซฟ) |
- แมพที่ยืนอยู่ตอนนี้จะไม่โผล่ในลิสต์ · ปลายทางที่ยังไม่ปลดล็อกก็ไม่โผล่
- ชื่อแมพภาษาไทยอยู่ที่ `Game.MAP_NAMES` (เพิ่มแมพใหม่แล้วเติมชื่อตรงนี้)
- สคริปต์แปลงเสาในแมพ: `python3 set_warp_pillar.py [--targets a,b] [--list]` (แปลงให้แล้ว 2 เสา: พรอนเทรา · นิดาเวลลีร์)

#### ง) ★ เสียงเอฟเฟกต์ (ระบบใหม่) ★
`scripts/core/sfx_player.gd` → `Game.sfx` (แบบเดียวกับ `Game.music`)
- **วางไฟล์ที่ `Sprites/sfx/<ชื่อ>.ogg` (หรือ .wav/.mp3) แล้วจบ** ไม่ต้องแก้โค้ด · ไม่มีไฟล์ = เงียบ ไม่ error
- เล่นพร้อมกันได้ 8 เสียง · สุ่ม pitch ±6% ไม่ให้จำเจ · กันเสียงซ้ำถี่เกิน 0.05 วิ
- **ไล่หาไฟล์จากเฉพาะเจาะจง → ทั่วไป**: ท่า `Attack_Katana` → `attack_katana` → `attack_blade` → `attack`
  ★ เลยมีไฟล์ `attack_blade.ogg` ไฟล์เดียว **ดาบทุกเล่มมีเสียงครบ** ★ (สกิลลองหา `skill_<id>` ก่อน)
- ใส่ไฟล์ตัวอย่างให้แล้ว `Sprites/sfx/attack_blade.ogg` (เสียงหวือ 0.38 วิ) — **วาดทับ/เปลี่ยนไฟล์เองได้เลย ชื่อเดิม**
- เมนูระบบมีสไลเดอร์ "เสียงเอฟเฟกต์" + ปุ่มเปิด/ปิด (ลากแล้วได้ยินตัวอย่างทันที) · จำค่าไว้ใน `user://ui_layout.cfg`

#### จ) หน้าตีบวกใหม่ (ตามตัวอย่างที่ส่งมา)
`refine_window.gd` เขียนใหม่: **ซ้าย = รายการอุปกรณ์ · ขวา = การ์ด**
- ไอคอนใหญ่ในกรอบวงกลมขอบทอง + ป้าย **+2 → +3**
- ตาราง **ค่าพลัง: ตอนนี้ → หลังตีบวก** (ATK/DEF)
- **แถบโอกาสสำเร็จ** เปลี่ยนสีตามความเสี่ยง (เขียว ≥70% · เหลือง ≥40% · แดง)
- วัตถุดิบเป็นแถวไอคอน "ใช้ 1 (มี 12)" — สีเขียว/แดงบอกว่าพอไหม (แร่ + ซีนี)
- ปุ่มบอกระดับ "ตีบวก +2 → +3" · คำเตือนตอนถึงระดับที่พลาดแล้วลด · ของดรอปโชว์ "ของดรอป +18%"

เทสต์ `r57_runner.gd` **63 ข้อ** + regression 10 ฉาก · กับดัก **86**: เทสต์ที่เล่นเสียงซ้ำ ๆ ในเฟรมเดียวจะโดน `REPEAT_GUARD` กันไว้ ต้อง await ก่อน

---

### 7.71 รอบ 58 — เสียงแยกตามสกิล (ทุกสกิลที่ต้องกดใช้)

**เดิม:** สกิลทุกอันถอยไปใช้เสียงฟันดาบ `attack_blade` ตัวเดียว
**ตอนนี้:** แต่ละสกิลมีเสียงของตัวเอง — ลำดับหาไฟล์ (`player.skill_sfx_keys()`):

1. ช่อง **Sound** ในไฟล์สกิล (`data/skills/<id>.tres` · ตั้งใน Inspector ได้) — ถ้าตั้งไว้ใช้อันนี้ก่อน
2. **`attack_<อาวุธ>_<สกิล>`** เช่น `attack_blade_slash` — เสียงเฉพาะ "อาวุธนี้ + สกิลนี้" (ตรงกับชื่อท่าในเกม)
3. **`skill_<สกิล>`** เช่น `skill_slash` — เสียงของสกิลนี้ ใช้ได้ทุกอาวุธ ★ ใส่ให้แล้วครบทุกสกิล ★
4. เสียงฟันของอาวุธ (`attack_blade` → `attack`) — สำรองสุดท้าย ไม่ให้เงียบ

บัฟ/ฮีล (battle_cry · endure · first_aid): ช่อง Sound → `skill_<id>` → `skill_buff` / `skill_heal` — ไม่มีไฟล์ = เงียบ (ไม่ถอยไปใช้เสียงดาบ)

| ไฟล์ที่ใส่ให้ (สังเคราะห์ · เปลี่ยนทับได้ชื่อเดิม) | ใช้ตอน |
|---|---|
| `skill_bash.ogg` | Bash — ตุบหนัก + หวือสั้น |
| `skill_slash.ogg` | Slash — หวือยาวแบบพุ่ง + ชิ้ง 2 จังหวะ |
| `skill_magnum_break.ogg` | Magnum Break — บูม + ประกาย |
| `skill_battle_cry.ogg` | Battle Cry — เสียงตะโกน |
| `skill_first_aid.ogg` | First Aid — ระฆัง 3 โน้ต |
| `skill_endure.ogg` | Endure — ฮัมต่ำ + เกราะกริ๊ง |
| `attack_blade.ogg` (รอบ 57) | ฟันธรรมดาทุกดาบ |

- อยากให้ slash ตอนถือดาบซามูเสียงต่างจากดาบทั่วไป → วาง `attack_katana_slash.ogg` เพิ่ม (ระบบหยิบเองก่อน `skill_slash`)
- `python3 check_sfx.py` = ดูว่าไฟล์ไหนถูกใช้/ยังขาด/ชื่อผิด

**บอสดรอปของสวมใส่:** ผู้ใช้ตั้งใจให้ง่ายกว่ามอนธรรมดา (บอสมีคูลดาวน์เกิด) → กติกาในเทสต์ `r44` เป็น **ของสวมใส่ ≤3% · บอส ≤8%**

เทสต์ `r58_runner.gd` 22 ข้อ (ไฟล์ครบ · ลำดับหา · slash/bash/magnum ได้คนละไฟล์ · ช่อง Sound มาก่อน · ดาบซามูยังได้ skill_slash · เล่นจริง)

---

### 7.72 รอบ 59 — ศพบอสนับถอยหลัง · หน้าตีบวกแบบลุ้น · เสียงพากย์ NPC · ช่องใส่วิดีโอคัทซีน

#### 1) บอสตายแล้ว "ค้างเฟรมสุดท้าย" + ป้ายนับถอยหลังเกิดใหม่
- บอส/มอนที่ใช้คูลดาวน์ข้ามแมพ (`is_boss` หรือ `respawn_persistent`) ตายแล้ว **ไม่หายไป** — ค้างที่เฟรมสุดท้ายของท่า Death (คิงโพริง = มงกุฎบนพื้น) พร้อมป้ายเหลือง `คิงโพริง เกิดใหม่ใน 2:59` เหนือหัว นับถอยหลังจากเวลาเดียวกับสปอว์นเนอร์ (`PlayerState.respawn_locks`)
- ศพไม่อยู่ในกลุ่ม `enemy` (ดาบ/สกิลไม่โดน ไม่มี AI ไม่ชน) · ครบเวลา → จางหาย 0.8 วิ → สปอว์นเนอร์เกิดตัวจริงให้เอง
- **ออกแมพแล้วเข้าใหม่ตอนบอสยังตาย** → สปอว์นเนอร์เกิด "ศพ" ให้เลย (ไม่ให้ EXP/ของ) ผู้เล่นเห็นว่าอีกกี่วิจะเกิด — ปิดได้ที่ช่อง `Spawn Corpse While Locked` ของ MonsterSpawner / MapSpawner (ปิด = ไม่เกิดอะไรจนครบเวลา แบบรอบ 56)
- ปรับ: `monster_base.gd` ค่าคงที่ `CORPSE_LABEL_LIFT` (ป้ายสูงกว่าหลอดเลือดเท่าไหร่) `CORPSE_FADE` (จางกี่วิ) · ป้ายชื่อโหนด `RespawnLabel`
- กับดัก 87: MapSpawner เดิมกรอง "ตัวที่ตายแล้ว" ออกจาก `_alive` ทุกเฟรม → ศพจะถูกมองว่าไม่มีตัว แล้วเกิดศพซ้อนศพไม่รู้จบ → ต้องนับศพเป็นตัวที่อยู่ (`_is_corpse()`) และไม่เก็บศพกลับตอนผู้เล่นเดินไกล

#### 2) หน้าตีบวก — สลับฝั่ง + หลอดลุ้น + SUCCESS/FAIL + เสียงแยก
- **การ์ดตีบวกอยู่ซ้าย · รายการไอเทมอยู่ขวา** (โหนด `Card` / `ItemList`)
- กดตีบวก → หลอดส้มวิ่ง `ROLL_TIME` 1.6 วิ (เสียง `refine_roll`) → รู้ผล → คำ **SUCCESS!** (เขียว เด้งจากใหญ่→ปกติ) หรือ **FAIL** (แดง สั่น · ของแตกจะขึ้น "FAIL ของแตก!") ขนาด 52 px ทับกลางหน้าต่างพร้อมฉากมืดบาง ๆ ค้าง `RESULT_SHOW` 1.3 วิ แล้วจาง
- ระหว่างลุ้น: ปุ่มกดซ้ำไม่ได้ · เลือกชิ้นอื่นไม่ได้ · ถ้าของถูกย้าย/ขายไประหว่างลุ้นจะไม่ตี
- เสียง 3 ไฟล์ `Sprites/sfx/refine_roll.ogg` · `refine_success.ogg` · `refine_fail.ogg` (สังเคราะห์ไว้ให้ — เอาเสียงจริงทับชื่อเดิมได้ · สร้างใหม่ `python3 make_refine_sfx.py --force`)
- ค่าคงที่ปรับได้ที่หัวไฟล์ `refine_window.gd`: `ROLL_TIME` `RESULT_SHOW` `RESULT_FONT` `SFX_*`
- โค้ด: `_do_refine()` เป็น coroutine แล้ว (เทสต์ต้อง `await win._do_refine()` หรือรอสัญญาณ `refine_finished(out)`) · `is_rolling()` · `last_result`

#### 3) เสียงพากย์ NPC (VoicePlayer)
- วางไฟล์ **`Sprites/voice/<voice_id>/<ประโยค>.ogg`** (หรือ .wav/.mp3) แล้วจบ — กล่องสนทนาขึ้นบรรทัดไหนเล่นไฟล์นั้น · ขึ้นบรรทัดใหม่ = ตัดเสียงเก่า · ปิดกล่อง = หยุด · ไม่มีไฟล์ = เงียบ
- `voice_id` = ช่องใหม่ในกลุ่ม "เสียงพากย์" ของ NPC — ใส่ให้ครบทุกตัวแล้วด้วย `python3 set_npc_voice.py` (ตาราง `VOICE_IDS` ในสคริปต์: tony hans maria erik valder gunnar ingrid bjorn helga dvalin brokk hedin · ฮันส์ในเมืองคนแคระ = `hans_c2` เพราะบทคุยคนละชุด ชื่อไฟล์จะชนกัน)
- ชื่อประโยค: `greeting` · `dialog_1..` · `<ธง>_1..` · `<เควส>_offer_1..` · `<เควส>_offer_ask` · `<เควส>_progress` · `<เควส>_complete` · `<เควส>_choice` · `<เควส>_cutscene_1..` · `heal_full/heal_poor/heal_done`
- **รายการคำพูดทุกประโยค + พรอมพ์สไตล์เสียงแต่ละตัว:** `_to_delete/เสียงพากย์NPC_รอบ59.md` (สร้างรายการใหม่ได้ด้วย `python3 dump_npc_lines.py` → `_to_delete/npc_lines.md/.csv`)
- หน้า System มีแถว "เสียงพากย์ NPC" (ความดัง/เปิดปิด · เก็บใน `ui_layout.cfg` [voice])
- โค้ด: `scripts/core/voice_player.gd` (`Game.voice.play("hans","greeting")` · `play_path("hans/greeting")` · `stop()`) · บรรทัดสนทนาคีย์ใหม่ `"voice"` ใน `UI.talk()` · `npc.line(text, info, choices, voice_key)` · `npc.current_dialog_key()`

#### 4) ช่องใส่วิดีโอคัทซีน (ใหม่ 3 ช่อง)
| ช่อง | ที่ไหน |
|---|---|
| `Video On Accept` / `Video On Complete` | `data/quests/<id>.tres` กลุ่ม "วิดีโอคัทซีน" — เล่นหลังกดรับเควส / หลังกดรับรางวัล (ก่อนตัวเลือกและฉากแพนกล้อง) |
| `Intro Video` + `Intro Video Once` + `Intro Video Flag` | โหนดรากแมพ (MapBase) กลุ่ม "วิดีโอตอนเข้าแมพ" — เล่นครั้งแรกที่เข้า (ธง `seen_map_<id>` ในเซฟ) · ตั้ง Flag ให้เล่นเฉพาะเมื่อมีธง เช่น `thunder_scar_open` |
- ไฟล์ .ogv เหมือนเดิม (`ffmpeg -i in.mp4 -c:v libtheora -q:v 7 -c:a libvorbis out.ogv`)
- **แผนว่าเหตุการณ์ไหนควรมีคลิป (16 จุด เรียงความสำคัญ) + พรอมพ์เจนคลิป:** `_to_delete/แผนวิดีโอคัทซีน_รอบ59.md`

#### ข้อมูลที่พบระหว่างเทสต์ (ไม่ได้แก้ — เป็นค่าที่ผู้ใช้ตั้งเอง)
- `katana` required_level = 33 · `claymore` = 35 → อยู่นอกช่วงเจาะรู (1-30 / 40-70) เจาะไม่ได้ · ถ้าอยากให้เจาะได้ให้ขยาย `TIERS` ใน `socket_system.gd` เป็น 1-39
- `tony_fabre` ไม่ต้องทำ `hans_poring` ก่อนแล้ว (เทสต์ r38 ข้อ "M4 ต่อจาก M3" จึงแดง — ข้อมูลไม่ใช่โค้ด)

เทสต์ `r59_runner.gd` 102 ข้อ · ถดถอย r56 66 · r57 63 · r58 22 · r54 29 · r44 59 · r30 66 · talk 50 · r45 59 · r41 16 ผ่านหมด

---

### 7.73 รอบ 60 — ปีกแห่งวาลคีรี (วาปกลับเมือง) · ย่อไอคอนไอเทมให้ได้ไซส์

#### 1) ไอเทมใหม่ — ปีกแห่งวาลคีรี
| ช่อง | ค่า |
|---|---|
| id | `wing_of_valkyrie` (ไฟล์ `data/items/wing_of_valkyrie.tres`) |
| ชนิด | ของกิน (CONSUMABLE) → กดใช้จากกระเป๋าได้ · ตั้งเป็นช่องยาด่วนได้ |
| ราคา | ซื้อ **500z** · ขายคืน 50z · ซ้อนได้ 99 ชิ้น |
| ผลพิเศษ | `special_effect = &"warp_town"` |
| ขายที่ | **พ่อค้าโทนี่** (พรอนเทรา) — อยู่ก่อนฟราคอนในรายการ |
| ไอคอน | `Sprites/items/placeholder/wing_of_valkyrie.png` (256×256 ชั่วคราว · วาดจริงแล้วเอาทับชื่อเดิมได้เลย · สร้างใหม่ด้วย `python3 make_wing_icon.py`) |

**วาปกลับไปไหน:** "เมืองล่าสุดที่ผู้เล่นเคยเดินเข้าไป" — เล่นบท 1 อยู่ก็กลับพรอนเทรา · เข้านิดาเวลลิร์แล้วก็กลับเมืองคนแคระ
- `Game.TOWNS` = รายชื่อแมพที่นับเป็นเมือง (ตอนนี้ `prontera_town`, `nidavellir_town`) — **เพิ่มเมืองใหม่ = เพิ่ม id บรรทัดเดียว**
- `MapBase._ready()` เรียก `PlayerState.set_last_town(map_id)` ให้เอง · เก็บลงเซฟช่อง `last_town`
- `PlayerState.home_town()` = เมืองที่จะกลับ (ค่าเพี้ยน/แมพหาย → ถอยไปพรอนเทรา)
- `Game.warp_to_town()` = ตัววาปจริง (คืน false ถ้าอยู่ในเมืองอยู่แล้ว หรือกำลังเปลี่ยนแมพ)
- ใช้ในเมือง → ขึ้น "อยู่ในเมืองอยู่แล้ว" **ไม่เสียของ**
- หน้ารายละเอียดไอเทมโชว์ปลายทางให้ด้วย: "ใช้แล้ววาปกลับเมืองพรอนเทรา (ใช้ในเมืองไม่ได้)"

**อยากให้ขายที่ร้านอื่นด้วย** (เช่นเฮลกาในเมืองคนแคระ):
```
python3 add_shop_item.py --item wing_of_valkyrie --npc "นายหน้าเฮลกา"
```
(สคริปต์นี้ใช้เพิ่มไอเทมเข้าร้าน NPC คนไหนก็ได้ ไม่ต้องเปิด Godot · ใส่ซ้ำไม่พัง)

**อยากให้ตอนตายเกิดที่เมืองล่าสุดด้วย** (ตอนนี้ตายแล้วกลับพรอนเทราเสมอ): แก้ `game.gd` บรรทัดใน `respawn_in_town()`
จาก `change_map(&"prontera_town", ...)` → `change_map(PlayerState.home_town(), ...)`

**ทำไอเทมวาปแบบอื่นต่อ:** ก็อป .tres แล้วเปลี่ยน `special_effect` — ตัวจัดการอยู่ใน `player_state.use_item()`
(ตอนนี้มี `reset_skills` · `reset_stats` · `warp_town`)

#### 2) ย่อไอคอนไอเทม/ปุ่มเมนูให้เป็นไซส์ที่ถูกต้อง
`shrink_icons.py` เปลี่ยนเกณฑ์เป็น **"ด้านยาวสุดเกิน 256 px → ย่อเหลือ 256"** (เดิมดูแค่ไฟล์ใหญ่กว่า 600 KB)

```
python3 shrink_icons.py            # ดูรายการเฉย ๆ
python3 shrink_icons.py --apply    # ย่อจริง (ต้นฉบับสำรองที่ _to_delete/originals_icons/)
```
- ทำกับ `Sprites/items` + `Sprites/ui_icons` เท่านั้น · **ไม่แตะ `Sprites/portraits`** (รูปคุยโชว์สูง 330 px ต้องคมกว่านี้) และไม่แตะสไปรท์ตัวละคร/มอน
- ทำไม 256: ในเกมไอคอนโชว์ใหญ่สุด 120×120 (หน้ารายละเอียดไอเทม) · การ์ด 210×300 · ปุ่มเมนู ~44 → 256 คือเผื่อจอละเอียดสูงไว้เท่าตัวแล้ว
- ไม่ต้องแก้ .import — เปิด Godot แล้วมันนำเข้าใหม่เอง path เดิม ไม่ต้องชี้ไฟล์ใหม่

`fix_card_art.py` — ภาพการ์ด 2 ใบยังชื่อ `ChatGPT Image ....png` และใบหนึ่งวางหลุดอยู่นอกโฟลเดอร์ items
(เลยรอดการย่อมาตลอด) สคริปต์นี้ย้ายเข้า `Sprites/items/card_poring.png` · `card_fabre.png` แก้ path ใน .tres ให้เอง
แล้วเก็บไฟล์เดิมไว้ที่ `_to_delete/originals_icons/moved/`

**ผลรอบนี้ (บนเครื่องจริง):** ไอคอน 58 ไฟล์ · **77.9 MB → 2.4 MB** (ประหยัด 75 MB — ตอน push GitHub จะเบาลงมาก)

#### 3) เทสต์
`r60_runner.gd` 36 ข้อ — ข้อมูลไอเทม · พ่อค้าขายจริง · `Game.is_town` · จำเมืองล่าสุดข้ามแมพ/ข้ามเซฟ ·
กดใช้แล้วของหาย 1 ชิ้นและวาปจริง · ใช้ในเมืองไม่ได้และไม่เสียของ · วาปกลับ "เมืองล่าสุด" ไม่ใช่พรอนเทราเสมอ · ไม่มีไอคอนเกิน 300 px
ถดถอย: r59 102 · r44 59 · r45 59 · r30 66 · talk 50 · cardui 29 · iteminfo 27 ผ่านหมด
(r47 ยังแดง 1 ข้อเดิม — `rapier` ราคา 56,000 ไม่ใช่ ฐาน×5 เพราะผู้ใช้ตั้งเอง ไม่ใช่บั๊ก)

---

### 7.74 รอบ 61 — ตัดไฟล์เสียงพากย์ยาว ๆ ให้เป็นไฟล์ย่อยอัตโนมัติ

#### เสียงช่างตีเหล็กฮันส์ (ไฟล์แรกที่ใส่จริง)
ไฟล์เดียว 67.7 วินาที → ตัดเป็น 10 คลิป + ก็อป 1 = **11 ไฟล์** ใน `Sprites/voice/hans/`

| ไฟล์ | ช่วงในไฟล์ต้นฉบับ | ยาว |
|---|---|---|
| `greeting.ogg` | 0.1 – 2.1 | 2.0 |
| `dialog_1.ogg` | 2.6 – 4.9 | 2.4 |
| `dialog_2.ogg` | 5.6 – 18.9 | 13.3 |
| `hans_poring_offer_1.ogg` | 19.8 – 26.2 | 6.4 |
| `hans_poring_offer_ask.ogg` | 27.1 – 29.6 | 2.5 |
| `hans_poring_progress.ogg` | 30.3 – 33.6 | 3.3 |
| `hans_poring_complete.ogg` | 34.6 – 41.3 | 6.7 |
| `m5_old_mine_iron_offer_1.ogg` | 42.3 – 54.1 | 11.8 |
| `m5_old_mine_iron_offer_2.ogg` | 55.2 – 62.9 | 7.7 |
| `m5_old_mine_iron_offer_ask.ogg` | (ก็อปจาก hans_poring_offer_ask — ข้อความเดียวกันเป๊ะ) | 2.5 |
| `m5_old_mine_iron_progress.ogg` | 63.7 – 67.7 | 4.0 |

**ยังขาด 2 ประโยค** (ไม่มีในไฟล์เสียง — ไฟล์ถูกตัดจบก่อน):
- `m5_old_mine_iron_complete` — "พอถูไถไปได้อีกพัก... เจ้ารู้ไหม สมัยก่อนคนแคระใต้ภูเขาเหนือตีเหล็กให้ทั้งเก้าโลก ข้าเคยเป็นลูกศิษย์พวกเขา แต่สามเดือนมานี้... เงียบไปทั้งภูเขา"
- `hans_poring_offer_2` — "โพริงล้นออกมาจากป่าจนฮันส์ออกไปหาแร่ไม่ได้ ช่วยเคลียร์ทุ่งวิหารให้หน่อย" (บรรทัดบรรยายงาน — ไม่พากย์ก็ได้)

อัดเพิ่มแล้ววางไฟล์ชื่อนั้นในโฟลเดอร์เดิมได้เลย ไม่ต้องแก้โค้ด

#### `cut_voice.py` — ตัวตัดเสียงอัตโนมัติ (ใช้กับตัวละครอื่นต่อได้)
```
python3 cut_voice.py เสียงโทนี่.wav --voice tony            # ดูก่อนว่าจะตัดยังไง
python3 cut_voice.py เสียงโทนี่.wav --voice tony --apply    # ตัดจริง → Sprites/voice/tony/*.ogg
```
วิธีทำงาน: หาช่วงที่มีเสียง (เงียบเกิน 0.68 วิ = ขึ้นประโยคใหม่) → เอาบทพูดของ voice_id นั้นจาก
`_to_delete/npc_lines.csv` มาเรียงตามลำดับ → จับคู่ด้วย **จำนวนพยางค์** (ประโยคยาว = เสียงยาว)
แบบหาทางที่พอดีที่สุดทั้งไฟล์ → ตัด + เฟดหัวท้าย 15 ms + แปลงเป็น .ogg

- ถ้าไฟล์เสียง **ไม่ได้อ่านบางประโยค** ให้บอกด้วย `--skip` เช่น
  `--skip hans_poring_offer_2,m5_old_mine_iron_complete` (ของฮันส์ใช้แบบนี้)
- ดูคอลัมน์ **พยางค์/วินาที** ในผลลัพธ์ — ถ้าทุกบรรทัดใกล้เคียงกัน (เช่น 2.8-3.6) แปลว่าจับคู่ถูก
  ถ้ามีบรรทัดไหนกระโดด (เช่น 8.0 หรือ 1.2) แปลว่าเพี้ยน → ใช้ `--skip` เพิ่ม แล้วรันใหม่
- แก้เวลาเองได้: แผนการตัดเก็บที่ `_to_delete/voice_plan_<voice>.json` แก้เลขแล้วสั่ง
  `python3 cut_voice.py --from-plan _to_delete/voice_plan_hans.json --voice hans --apply`
- ตัวเลือกอื่น: `--gap 0.9` (ช่องว่างที่ถือว่าขึ้นประโยคใหม่) · `--db -35` (ระดับที่ถือว่าเงียบ)
- ลง `pip install pythainlp python-crfsuite` จะนับพยางค์แม่นขึ้น (ไม่มีก็ใช้จำนวนตัวอักษรประมาณให้)

#### `make_voice_check.py` — หน้าเว็บไว้ฟังตรวจ
```
python3 make_voice_check.py --voice hans
```
ได้ `_to_delete/ตรวจเสียง_hans.html` — ดับเบิลคลิกเปิดในเบราว์เซอร์ เห็นทุกประโยคเรียงกัน
พร้อมปุ่มเล่นและข้อความที่ควรตรงกัน (เสียงฝังในไฟล์ HTML แล้ว ส่งต่อได้เลย)
ถ้าเจอคู่ไหนสลับกัน บอกได้ ผมย้ายไฟล์ให้

#### เคล็ดตอนอัดเสียงรอบหน้า
- อัดทีละตัวละคร เรียงตามลำดับใน `_to_delete/npc_lines.md` แล้ว**เว้นเงียบ 1 วินาทีระหว่างประโยค** — ตัดง่ายและแม่นที่สุด
- ประโยค `<เควส>_offer_ask` ("เอาไงล่ะ รับงานนี้มั้ย") ของตัวละครเดียวกันใช้ไฟล์เดียวก็อปได้ทุกเควส
- บรรทัด `<เควส>_offer_2` เป็นคำบรรยายงาน (ไม่ใช่คำพูด NPC) — ข้ามได้ ระบบจะเงียบให้เอง
- ถ้า TTS มีจำกัดตัวอักษรต่อครั้ง ให้แบ่งอัดเป็นหลายไฟล์ แล้วรัน `cut_voice.py` ทีละไฟล์พร้อม `--skip` ของประโยคที่ไม่ได้อยู่ในไฟล์นั้น
  (ฮันส์รอบนี้โดนตัดท้ายเพราะเกินโควตา — ประโยคสุดท้ายหายไปทั้งประโยค)

เทสต์ `r61_runner.gd` 20 ข้อ (ไฟล์ครบ 11 · ประโยคที่ยังไม่มี = เงียบไม่ error · เล่นจริง · ฮันส์ในเมืองมี voice_id · เปิดกล่องคุยแล้วเสียงดังตามบรรทัด · ปิดกล่องเสียงหยุด)

---

### 7.75 รอบ 62 — กล่องสนทนา NPC หน้าตาใหม่ (แบบ RPG ยุคใหม่)

เปลี่ยนจาก "กล่องขาวมีขอบ" เป็น **แถบมืดโปร่งแสงพาดล่างจอ** — ใช้กับบทสนทนาทุกอันในเกม (NPC · เควส · ฉากแพนกล้อง · เสาวาป) เพราะทุกอย่างผ่าน `UI.talk()` ตัวเดียว
- แถบมืดไล่จางซ้าย-ขวา ยาวถึงขอบล่างจอ · ขอบบนมีเส้นแสงฟ้าบาง ๆ
- **ป้ายชื่อ** = แถบสีฟ้าโปร่งจางไปทางขวา วางบนแถบเหนือข้อความ (ไม่ใช่ป้ายลอยเหนือกล่องแล้ว)
- ตัวหนังสือ **ขาว มีเงา** 26 px · ข้อความเสริม (เงื่อนไข/รางวัล) สีทองอ่อน
- **รูปตัวละครใหญ่ขึ้น** = 60% ของความสูงจอ ยืนล้ำแถบขึ้นมา **ปลายล่างจางหายเนียน ๆ** (เชดเดอร์) · ข้อความเริ่มถัดจากรูปเสมอ รูปกว้างแค่ไหนก็ไม่ทับ
- มุมขวาล่างมี **▼ กระดึ๊บ** ตอนรอกดต่อ · ตอนมีตัวเลือกจะขึ้น "Esc = ยกเลิก" เล็ก ๆ แทน
- ฉากหลังมืดลง 30% ตอนคุย (เดิม 42%) — เห็นเมืองข้างหลังชัดขึ้น

ปรับได้ที่หัวไฟล์ `scripts/ui/dialogue_box.gd`:
| ค่า | คืออะไร |
|---|---|
| `BAND_COLOR` | สี/ความทึบแถบ (a = 0.62) |
| `LINE_GLOW` `NAME_BG` | สีเส้นแสง / สีป้ายชื่อ |
| `PORTRAIT_RATIO` `PORTRAIT_FADE` | รูปสูงกี่ % ของจอ · เริ่มจางที่กี่ % ของรูป |
| `DIM_ALPHA` | ฉากหลังมืดลงเท่าไหร่ |
| `FONT_TEXT` `BOX_HEIGHT` | ขนาดตัวหนังสือ · ความสูงแถบ |

ไม่ต้องแก้ NPC / เควส / รูปคุยอะไรเลย — ใช้รูปเดิม (`Sprites/portraits/`) ได้ทันที · ถ้าวาดรูปคุยใหม่ให้สูง ~500-600 px จะคมขึ้นเพราะรูปโชว์ใหญ่กว่าเดิม (ตอนนี้ 395×420 ถูกขยายนิดหน่อย)

โค้ด: `_band_bg` / `_band_line` (TextureRect + `GradientTexture2D` จาก `_h_gradient()`) · `_name_panel` เป็น Control มี `_name_bg` + `_name_label` · `_arrow` + `_set_arrow()` · รูปใช้ `ShaderMaterial` จาก `PORTRAIT_SHADER` · `_panel` ("Box") ยังเป็นพื้นที่ข้อความแต่โปร่งใส
เทสต์ `talk_test` แก้ตามดีไซน์ใหม่ 54 ข้อ · r30 66 · r45 59 · r59 102 · r61 20 ผ่าน

---

### 7.76 รอบ 63 — เสียงฮันส์ชุดใหม่ (พูดเร็วขึ้น) + ตัวเช็คว่าตัดตรงบทไหม

**เสียงฮันส์อัดใหม่** — เร็วขึ้นจาก 2.95 เป็น **3.60 พยางค์/วินาที** · ได้ครบ **12 ไฟล์** (เดิม 11)
มาเป็น 2 ไฟล์: ไฟล์แรก 57.0 วิ = 10 ประโยค · ไฟล์สอง 14.4 วิ = ประโยคปิดเควส M5 ที่รอบก่อนขาดไป

| ไฟล์ | ยาว (เดิม → ใหม่) |
|---|---|
| `greeting.ogg` | 2.00 → 1.37 |
| `dialog_1.ogg` | 2.36 → 1.77 |
| `dialog_2.ogg` | 13.33 → 10.52 |
| `hans_poring_offer_1.ogg` | 6.43 → 5.76 |
| `hans_poring_offer_ask.ogg` | 2.53 → 2.30 |
| `hans_poring_progress.ogg` | 3.27 → 2.88 |
| `hans_poring_complete.ogg` | 6.74 → 5.50 |
| `m5_old_mine_iron_offer_1.ogg` | 11.83 → 10.43 |
| `m5_old_mine_iron_offer_2.ogg` | 7.71 → 6.60 |
| `m5_old_mine_iron_offer_ask.ogg` | (ก็อปจาก hans_poring_offer_ask) |
| `m5_old_mine_iron_progress.ogg` | 3.95 → 3.93 |
| `m5_old_mine_iron_complete.ogg` | **ใหม่** 13.99 |

ยังขาดอันเดียว: `hans_poring_offer_2` (บรรทัดบรรยายงาน ไม่ใช่คำพูด NPC — ไม่พากย์ก็ได้ ระบบเงียบให้เอง)
ของเดิมสำรองไว้ที่ `_to_delete/voice_hans_รอบ61/`

#### ★ ของใหม่ใน `cut_voice.py` ★
**1. เทียบกับไฟล์เดิมอัตโนมัติ** — ถ้าในโฟลเดอร์มีไฟล์ชื่อเดียวกันอยู่แล้ว (เช่นอัดใหม่ทับของเก่า)
จะบอกให้ว่าคลิปใหม่กับคลิปเก่าเป็น "ประโยคเดียวกัน" หรือเปล่า:
```
เทียบกับไฟล์เดิมในโฟลเดอร์ (ยิ่งน้อยยิ่งเป็นประโยคเดียวกัน):
   greeting                       0.094  ตรง
   hans_poring_offer_ask          0.046  ตรง
```
ต่ำกว่า 0.10 = ตรงแน่นอน · 0.10-0.15 = น่าจะตรง · เกิน 0.15 = สงสัยจับคู่ผิด
(วิธีวัด: log-mel + DTW — คนละไฟล์ คนละความเร็ว ก็ยังจับได้ถ้าเป็นประโยคเดียวกัน · รอบนี้ทั้ง 10 ประโยคได้ 0.045-0.094 = ยืนยันว่าตัดถูกทุกอัน)

**2. `--only`** — ไฟล์ที่มีบางประโยค (TTS แบ่งไฟล์เพราะจำกัดตัวอักษร):
```
python3 cut_voice.py ไฟล์2.wav --voice hans --apply --only m5_old_mine_iron_complete
```

#### วิธีอัดรอบต่อไป (สรุป)
1. อัดตามลำดับใน `_to_delete/npc_lines.md` เว้นเงียบ ~1 วิ ระหว่างประโยค
2. `python3 cut_voice.py ไฟล์.wav --voice <id>` ดูก่อน → ถ้าคอลัมน์ "พยางค์/วิ" ใกล้กันทุกบรรทัด = ถูก
3. ใส่ `--apply` · แล้ว `python3 make_voice_check.py --voice <id>` เปิดฟังตรวจ

เทสต์ `r61_runner.gd` อัปเป็น 22 ข้อ (ครบ 12 ไฟล์ · บรรทัดบรรยายไม่มีไฟล์แล้วเงียบ · เล่นในกล่องคุยจริง) ผ่าน

---

### 7.77 รอบ 64 — สกิล "สายฟ้าคำราม" ของอสูรสายฟ้า (สายฟ้าฟาดเป็นแนว 5 เส้น)

อสูรสายฟ้าคำราม (ท่า `Skill` ที่วาดไว้) แล้วสายฟ้าฟาดลงพื้น **เรียงเป็นแนวออกไปข้างหน้า 5 เส้น**
แต่ละเส้นมี **วงเตือนบนพื้น** สว่างขึ้นก่อนฟาด → ผู้เล่นเห็นแล้ววิ่งหนีทัน

#### จังหวะ (ตั้งไว้ให้ตรงกับท่าคำราม 32 เฟรม 12fps = 2.67 วิ)
| เวลา | เกิดอะไร |
|---|---|
| 0.0 | เริ่มท่า Skill (ย่อตัว → เงยหน้า) |
| 0.9 | อ้าปากคำราม → วงเตือนเส้นแรกขึ้น (`Skill Bolt Delay`) |
| 1.22 | สายฟ้าเส้นที่ 1 ฟาด (ห่างตัว 150 px) |
| ทุก 0.46 | เส้นถัดไป (วงเตือน 0.32 + เว้น 0.14) ห่างกันเส้นละ 150 px |
| ~3.2 | เส้นที่ 5 ฟาด (ไกลสุด 750 px จากตัวบอส) |

- โดนได้สูงสุด **2 เส้น** ต่อการร่าย 1 ครั้ง (ยืนนิ่ง ๆ ไม่ตายคาที่)
- ดาเมจต่อเส้น = ATK บอส × 1.6 · กระเด็นออกจากจุดที่ฟ้าลง
- คูลดาวน์ 11 วิ · ร่ายเมื่อผู้เล่นอยู่ในระยะ 520 px (โอกาส 75%)
- ท่า `Skill` ปิด loop ให้แล้ว (เดิมเปิดไว้ พอเล่นจบจะวนกลับไปคำรามใหม่)

#### ★ ระบบใหม่ — ใช้กับมอนตัวอื่นได้ทุกตัว ★
`scripts/entities/lightning_strike.gd` + ช่องใหม่ใน MonsterData กลุ่ม **"สกิล — สายฟ้าฟาดเป็นแนว"**

| ช่อง | คืออะไร |
|---|---|
| `Skill Bolt Count` | จำนวนเส้น (**0 = ไม่ใช้ระบบนี้** — มอนตัวอื่นไม่กระทบ) |
| `Skill Bolt Start` / `Spacing` | เส้นแรกห่างตัวเท่าไหร่ / เว้นระยะเส้นละเท่าไหร่ |
| `Skill Bolt Interval` | เว้นเวลาระหว่างเส้น (ยิ่งน้อยยิ่งรัว) |
| `Skill Bolt Telegraph` | วงเตือนขึ้นก่อนฟาดกี่วินาที (0 = ฟาดทันที ไม่เตือน) |
| `Skill Bolt Delay` | หน่วงจากเริ่มท่าถึงเส้นแรก (ตั้งให้ตรงจังหวะท่า) |
| `Skill Bolt Hit Width/Height` | โซนที่โดนของแต่ละเส้น (ครึ่งความกว้าง/สูง) |
| `Skill Bolt Max Hits` | โดนได้สูงสุดกี่เส้นต่อการร่าย (0 = ไม่จำกัด) |
| `Skill Bolt Damage Mult` | ตัวคูณดาเมจต่อเส้น (0 = ใช้ Skill Damage Mult) |
| `Skill Bolt Height` | ความสูงภาพสายฟ้าบนจอ |
| `Skill Bolt Frames` | ภาพสายฟ้าของตัวเอง (ว่าง = ใช้ `data/sprites/fx_lightning.tres`) |
| `Skill Bolt Sfx` | ชื่อไฟล์เสียงตอนฟาด (ไม่มีไฟล์ = เงียบ) |

**สายฟ้าเกาะพื้นจริง** — ยิงเส้นหาพื้นตรงจุดที่จะฟาด แล้วเทียบกับพื้นใต้ตัวมอน
พื้นลาด/ต่างระดับก็ลงตรงพื้น ไม่ลอย (ต่างระดับเกิน Hit Height = คนละชั้น ไม่ไล่ตาม)

#### ภาพ + เสียง (สร้างไว้ให้แล้ว — เอาของจริงทับได้)
- `python3 make_lightning_fx.py` → `Sprites/effects/lightning_bolt.png` (10 เฟรม 256×640 · 30fps)
  + `data/sprites/fx_lightning.tres` · แก้สีที่ตัวแปร `CORE/GLOW/DEEP` ในสคริปต์
- `python3 make_thunder_sfx.py` → `Sprites/sfx/thunder_strike.ogg` (แครก + เปรี้ยง + ครืน ๆ)
- `python3 set_stormscar_skill.py [--apply]` — ตั้งค่าทั้งหมดของอสูรสายฟ้าในคำสั่งเดียว
  (แก้ตัวเลขในตาราง `VALUES` หัวสคริปต์แล้วรันใหม่ได้เลย · สำรองไฟล์เดิมที่ `_to_delete/originals_monsters_r64/`)

#### หมายเหตุ
- ดาเมจของสกิลนี้คิด "ทีละเส้น" แล้ว → `_skill_hit()` (ดาเมจรอบตัวแบบเดิม) จะไม่ทำงานเมื่อ `Skill Bolt Count > 0`
- ผู้ใช้ปรับบอสเองแล้ว: เลเวล 20→30 · HP 4500→6500 · ATK 85-125→95-155 · ความเร็ว 170→390 (เทสต์ r38 ปรับให้ไม่ล็อกเลเวลตายตัวแล้ว)

เทสต์ `r64_runner.gd` 30 ข้อ (ค่าที่ตั้ง · ท่า Skill มีจริง/ยาวพอ · เกิดโหนดในแมพไม่ใช่ลูกของมอน · วงเตือนขึ้นก่อนฟาดและอยู่ข้างหน้าตามที่มอนหัน · เกาะพื้นจริง · สายฟ้าโผล่แล้วลบตัวเอง · ยืนตรงจุดฟ้าลง=เจ็บ · ยืนหลังบอส/ยืนระหว่างเส้น=ไม่โดน · โดนไม่เกิน 2 เส้น · มอนตัวอื่นไม่กระทบ)
ถดถอย r44 59 · r45 59 · r59 102 · r38 41 ผ่าน

---

## 7.78 รอบ 65 — คูลดาวน์ยา + แก้บอสไม่ออกท่าคำราม

### 7.78.1 คูลดาวน์ยา (ยาฟื้นเยอะยิ่งรอนาน)

กินยาแล้วต้องรอถึงจะกินซ้ำได้ — **5 ถึง 10 วินาที ตามปริมาณที่ฟื้น**

| ยา | ฟื้น | คูลดาวน์ |
|---|---|---|
| ยาแดง | 45 | 5.0 วิ |
| ยาส้ม | 105 | ~5.8 วิ |
| เนื้อย่าง | 70 + 3% ของ HP สูงสุด | ~5.6-6 วิ |
| ยาขาว | 325 + 5% ของ HP สูงสุด | ~9-10 วิ |
| ยาน้ำเงิน | มานา 40 | 5.0 วิ |

**★ แยกเป็น 2 สาย ★** ยาเลือด (hp) กับ ยามานา (sp) นับคนละอัน
กินยาเลือดแล้วยัง **กินยามานาต่อได้ทันที** (เหมือน RO ที่แยกกลุ่มดีเลย์)
ยาที่ฟื้นทั้งเลือดและมานาจะติดคูลดาวน์ทั้งสองสาย

**สิ่งที่ไม่ติดคูลดาวน์** ของสวมใส่ · การ์ด · ปีกวาลคีรี · ยารีเซ็ตสกิล/สเตตัส
(ติดเฉพาะของที่ "ฟื้น HP หรือ SP" เท่านั้น)

**คูลดาวน์ถูกล้างเมื่อ** ฟื้นคืนชีพ · เริ่มเกมใหม่ · โหลดเซฟ
(ตายแล้วเกิดใหม่ไม่ต้องยืนรอคูลดาวน์ค้างจากตอนตาย)

#### เห็นที่ไหนบ้าง
- **ช่อง Q / R บนแถบล่าง** — ติดคูลดาวน์แล้วช่องจะหรี่ลง มีเลขวินาทีนับถอยหลังตรงกลาง
  (เหลือไม่ถึง 1 วิ จะโชว์ทศนิยม เช่น `0.4`)
- **เอาเมาส์ชี้ช่อง Q / R** — บอกคูลดาวน์ของยาที่ใส่ไว้
- **หน้าข้อมูลไอเทม** (คลิกยาในกระเป๋า) — บรรทัด "คูลดาวน์ x.x วินาที"
  ถ้ากำลังติดคูลดาวน์อยู่จะเป็นสีส้มพร้อมบอกว่ารออีกกี่วิ
- **กดกินตอนยังไม่พร้อม** — ขึ้นข้อความ "ยาแดง ยังไม่พร้อม — รออีก 3.2 วินาที"
  (ยาไม่หายจากกระเป๋า เลือดไม่เพิ่ม)

#### ★ อยากปรับความยาวคูลดาวน์ ★
แก้ 4 ค่าที่หัวไฟล์ `scripts/core/player_state.gd` แล้วรันใหม่ได้เลย ไม่ต้องแตะที่อื่น

```gdscript
const POTION_CD_MIN := 5.0        # คูลดาวน์สั้นสุด (ยาฟื้นน้อย ๆ)
const POTION_CD_MAX := 10.0       # คูลดาวน์ยาวสุด (ยาฟื้นเยอะ ๆ)
const POTION_HEAL_LOW := 50.0     # ฟื้นเท่านี้หรือน้อยกว่า = สั้นสุด
const POTION_HEAL_HIGH := 400.0   # ฟื้นเท่านี้หรือมากกว่า = ยาวสุด
```

ตัวอย่าง อยากให้ยาแรง ๆ รอนานถึง 15 วิ → เปลี่ยน `POTION_CD_MAX` เป็น `15.0`
อยากให้ยาแดงกินรัวกว่านี้ → ลด `POTION_CD_MIN` เหลือ `3.0`

ฟังก์ชันที่เพิ่มใน `PlayerState` (เอาไปเรียกจาก UI อื่นได้)

| ฟังก์ชัน | ได้อะไร |
|---|---|
| `potion_cooldown_for(amount)` | ยาที่ฟื้น `amount` หน่วย ติดคูลดาวน์กี่วิ |
| `potion_heal_amounts(data)` | ยาชิ้นนี้ฟื้นเลือด/มานาเท่าไหร่ `{hp, sp}` |
| `potion_cooldown_of(data)` | ยาชิ้นนี้จะติดคูลดาวน์กี่วิ |
| `potion_cooldown_left(&"hp")` | สายเลือดเหลืออีกกี่วิ (`&"sp"` = สายมานา) |
| `potion_cooldown_left_for(data)` | ยาชิ้นนี้ยังกินไม่ได้ เหลืออีกกี่วิ |
| `potion_cooldown_left_of_id(id)` | เหมือนบนแต่เรียกด้วย item_id |

---

### 7.78.2 ★ แก้บั๊ก: บอสไม่ออกท่าคำรามตอนร่ายสกิล ★

**อาการ** อสูรสายฟ้าร่าย "สายฟ้าคำราม" — ชื่อสกิลลอยขึ้น วงเตือนขึ้น สายฟ้าฟาดลงครบ
แต่ **ตัวบอสยืนแข็งค้างท่าเดิม ไม่คำราม**

**สาเหตุ — กับดัก 94** รอบ 64 ปิด loop ของท่า Skill ไว้ (ไม่ให้คำรามวนซ้ำไม่จบ)
ท่าที่ปิด loop พอเล่นจบจะ **ค้างที่เฟรมสุดท้าย** และ `is_playing()` = `false`

เงื่อนไขเดิมใน `_play()` สั่งเล่นเมื่อ "ชื่อท่าเปลี่ยน" **หรือ** "หยุดอยู่ + ท่านี้วนซ้ำ"

```gdscript
if sprite.animation != real or (not sprite.is_playing() and get_animation_loop(real)):
    sprite.play(real)
```

ท่า Skill ที่ค้างอยู่เข้าไม่ได้ทั้งสองเงื่อนไข (ชื่อยังเป็น "Skill" เหมือนเดิม · ไม่วนซ้ำ)
→ **ไม่ถูกสั่งเล่นเลย** ตัวบอสเลยค้างเฟรมสุดท้ายทั้งท่า

**วิธีแก้** เพิ่มพารามิเตอร์ `restart` ให้ `_play()` — ใส่ `true` ตอนที่ "เริ่มท่าใหม่จริง ๆ"

```gdscript
func _play(anim: String, restart: bool = false) -> String:
    ...
        if restart:
            sprite.play(real)
            sprite.set_frame_and_progress(0, 0.0)
        elif sprite.animation != real or (...):
            sprite.play(real)
```

ใส่ `true` ที่ 4 จุด — จุดที่ถูกเรียก **ครั้งเดียวต่อการกระทำ** (ไม่ได้เรียกทุกเฟรม)

| จุด | ทำไม |
|---|---|
| `_cast_skill()` | ร่ายสกิลซ้ำต้องคำรามใหม่ |
| `_attack()` | ตีซ้ำต้องเหวี่ยงใหม่ |
| `take_damage()` → `_play("Hit")` | โดนตีรัว ๆ ต้องสะดุ้งใหม่ทุกครั้ง |
| `_die()` → `_play("Death")` | เริ่มท่าตายที่เฟรม 0 เสมอ |

ส่วน `_play("Idle")` / `_play("Run")` ที่เรียกทุกเฟรม **ไม่ใส่** `restart`
(ใส่แล้วจะรีเซ็ตเป็นเฟรม 0 ทุกเฟรม = ภาพนิ่งสนิท)

**★ กับดัก 94 ★ ท่าที่ปิด loop เมื่อเล่นจบจะค้างเฟรมสุดท้ายและ `is_playing()` = false**
**เช็คแค่ "ชื่อท่าเปลี่ยนไหม" ไม่พอ — ต้องมีทางบังคับให้เริ่มใหม่ด้วย**
**อาการที่เห็น: ท่าเล่นครั้งแรกปกติ แต่ครั้งที่สองเป็นต้นไปตัวยืนแข็งค้าง**

---

### 7.78.3 เทสต์รอบ 65

`r65_test` — **ผ่าน 43 · ล้มเหลว 0**

1. สูตรคูลดาวน์ (ฟื้นน้อย=5 · ฟื้นเยอะ=10 · ยิ่งฟื้นเยอะยิ่งนาน ไม่มีช่วงไหนสวนทาง)
2. คูลดาวน์ของยาแต่ละชนิด + ของที่ไม่ใช่ยาต้องไม่ติด
3. กินจริง — กินซ้ำไม่ได้ · ยาไม่หาย · เลือดไม่เพิ่ม · สายเลือด/สายมานาแยกกัน · รอครบแล้วกินได้อีก
4. ฟื้นคืนชีพ / เริ่มเกมใหม่ / โหลดเซฟ = ไม่มีคูลดาวน์ค้าง
5. ปีกวาลคีรีไม่ติดคูลดาวน์ยา
6. ★ ท่าสกิลของบอส ★ ค้างเฟรมสุดท้ายแล้วร่ายซ้ำ → ต้องเริ่มเฟรม 0 และเฟรมเดินหน้าจริง

รีเกรสชัน 27 ชุด — ล้มเหลว 5 ข้อ ซึ่ง **มีอยู่ก่อนรอบ 65 ทั้งหมด** (ยืนยันด้วยการรันกับไฟล์เวอร์ชันก่อนรอบ 65 แล้วได้ผลเดียวกัน)

- r38 "M4 ต่อจาก M3" — เควส tony_fabre ไม่ได้ต้องการ hans_poring แล้ว (คุณแก้ข้อมูลเอง)
- r47 "rapier ซื้อ 56000 ควรเป็น 9000" — ราคาที่คุณตั้งเอง
- r39 หัวเรื่อง/ช่องไอเทมโปร่งแสง — ธีม UI เปลี่ยนไปจากรอบก่อน ๆ
- newfeat เมืองกว้าง 3580 (เทสต์คาดไว้ 3620) — ขนาดแมพที่คุณปรับเอง


---

## 7.79 รอบ 66 — มอนยิงไกลไม่ต้องเข้าประชิด + ลูกวิญญาณบาฟโฟเมทจูเนียร์ตรงจังหวะ

### 7.79.1 มอนโจมตีระยะไกลทุกตัว — ยิงได้ตั้งแต่เห็นตัว

**เดิม** มอนที่ใส่รูปกระสุน (Projectile Texture) ยังต้องเดินเข้ามาจนถึง **Attack Range** ก่อนถึงจะยิง
บาฟโฟเมทจูเนียร์ตั้ง Attack Range ไว้ 80 px = ต้องมายืนจ่อหน้าผู้เล่นก่อนถึงจะร่ายลูกไฟ ดูแปลกมาก

**ตอนนี้** พอผู้เล่นเข้า **ระยะมองเห็น (Detect Range)** มันจะ **ยืนอยู่กับที่แล้วยิงเลย**

| มอน | เดิมต้องเข้าใกล้ | ตอนนี้ยิงได้ที่ |
|---|---|---|
| บาฟโฟเมทจูเนียร์ | 80 px | **460 px** (ระยะมองเห็น) |
| ลูนาติก | 300 px | **420 px** (ระยะมองเห็น) |

**มอนตีติดตัวไม่กระทบเลย** — เงื่อนไขคือ "มี Projectile Texture" เท่านั้น
(คิงโพริงขว้างบอลโค้งด้วย *สกิล* ซึ่งมี Skill Range ของตัวเองอยู่แล้ว ไม่เกี่ยวกัน)

ช่องใหม่ใน MonsterData กลุ่ม **"โจมตีระยะไกล — ยิงกระสุน"**

| ช่อง | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| `ranged_attack` | เปิด | ยิงตั้งแต่เห็นตัว · **ปิด = กลับไปเดินเข้ามาเหมือนเดิม** |
| `ranged_attack_range` | 0 | ระยะที่ยิงได้ · **0 = ใช้ Detect Range** · ใส่เองถ้าอยากให้ยิงไกล/ใกล้กว่าที่มองเห็น |

ในโค้ด: `MonsterData.is_ranged()` และ `MonsterData.ranged_reach()`
`monster_base.attack_reach()` คืนระยะยิงให้มอนพวกนี้ · **`melee_reach()` (ใหม่)** ยังเป็นระยะประชิดเดิม
ใช้ตอนเช็คว่าดาบ/กรงเล็บฟันโดนไหม → มอนยิงไกลไม่ได้ตีติดตัวไกลตามไปด้วย

★ ล็อกเป้าแล้ว (โดนตี/เห็นผู้เล่น) ถ้าผู้เล่นวิ่งหนีเกินระยะยิง มันจะ **เดินตามจนกลับเข้าระยะ** แล้วยืนยิงต่อ
ไม่ได้ยืนแช่เฉย ๆ ★

### 7.79.2 ★ จับจังหวะกระสุนให้ตรงกับภาพ ★ (ช่องใหม่ใช้ได้กับมอนทุกตัว)

**ปัญหา** ระบบเดิมยิงกระสุนตาม **เวลา** (Attack Windup) ซึ่งไม่รู้ว่าภาพเล่นถึงเฟรมไหนแล้ว
ท่าโจมตีของบาฟโฟเมทจูเนียร์ยาว 17 เฟรม — ชาร์จลูกไฟในมือที่ยกขึ้น 11 เฟรม แล้วค่อยผลักมือออก
ตั้ง Windup เป็นวินาทีเมื่อไหร่ พอเปลี่ยน fps ของท่าก็เพี้ยนทันที

ช่องใหม่ใน MonsterData กลุ่ม **"Attack Timing"**

| ช่อง | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| `attack_hit_frame` | −1 | **เฟรมที่ปล่อยกระสุน/ทำดาเมจ** · −1 = ใช้ Attack Windup แบบเดิม |
| `attack_follow_anim` | ปิด | ให้ท่าโจมตีเล่นจนจบก่อนกลับไปยืน (แทน Attack Duration) |

ระบบคำนวณเวลาจาก **จำนวนเฟรม ÷ fps ของท่านั้นจริง ๆ** (`_anim_time_to_frame`)
→ **เปลี่ยน fps ของท่าเมื่อไหร่ จังหวะก็ยังตรงเป๊ะ ไม่ต้องมานั่งแก้ Windup ใหม่**

### 7.79.3 บาฟโฟเมทจูเนียร์ — ค่าที่ตั้งให้

รันด้วย `python3 set_ranged_monsters.py --apply` (ปิด Godot ก่อน · สำรองที่ `_to_delete/originals_monsters_r66/`)

**เฟรมที่ปล่อยลูกไฟ = 11** — วัดจากภาพจริง: เฟรม 0-10 ลูกไฟค้างอยู่ที่มือที่ยกขึ้น
พอถึงเฟรม 11 มันกระโดดไปอยู่ที่ปลายมือที่ยื่นออกไป (นั่นคือจังหวะปล่อย) แล้วเฟรม 12-16 ก็หายไป
→ ลูกไฟที่ระบบสร้างโผล่มาแทนที่ลูกที่วาดไว้พอดี ต่อกันเนียน

| ค่า | ตั้งเป็น | ที่มา |
|---|---|---|
| ท่า Attack fps | **14 (คงค่าที่คุณตั้งไว้)** | 17 เฟรม/14fps = **1.21 วิ · ปล่อยลูกไฟตอน 0.79 วิ** |
| ท่า Attack loop | เปิด → **ปิด** | ท่าโจมตีต้องเล่นรอบเดียว (กับดัก 94) |
| `attack_hit_frame` | **11** | เฟรมที่ผลักมือออก |
| `attack_follow_anim` | **เปิด** | ท่ายาว 2.1 วิ ถ้าไม่เปิดจะถูกตัดที่ Attack Duration |
| `attack_cooldown` | **1.0 (ของเดิม)** | รวมเป็นยิงทุก ~2.2 วิ |
| `projectile_offset` | (30, −130) → **(120, −110)** | ★ ของเดิมคือตำแหน่งมือ**ตอนชาร์จ** (ถูกแล้วสำหรับจังหวะเก่าที่ปล่อยตั้งแต่ต้นท่า) · ตอนนี้ปล่อยที่เฟรม 11 = มือยื่นออกไปแล้ว จุดปล่อยเลยขยับตาม (วัดจากพิกเซลจริง) ★ |
| `projectile_height` | **70** | ขนาดลูกวิญญาณบนจอ |
| `projectile_speed` | **820 (ของเดิม)** | |
| `projectile_range` | **820** | ไกลกว่าระยะยิง 460 พอสมควร |
| `projectile_hit_size` | **(56, 56)** | นับเฉพาะหัวลูกไฟ ไม่นับหางที่ลากยาว |
| `projectile_faces_left` | **เปิด** | `soul ball.png` หัวอยู่ซ้าย หางชี้ขวา — ยิงไปขวาระบบพลิกให้ |

**★ อยากให้ท่าเร็ว/ช้ากว่านี้ ★** แก้ `ATTACK_FPS` ที่หัวไฟล์ `set_ranged_monsters.py` แล้วรันใหม่
(5 = 3.4 วิ · 8 = 2.1 วิ · **14 = 1.2 วิ ที่ใช้อยู่**) — **จังหวะปล่อยลูกไฟจะเลื่อนตามให้เอง ไม่ต้องแก้อะไรอีก**

★ **ค่าที่คุณปรับเองไว้ถูกคืนให้แล้ว** (fps 14 · ความเร็วลูกไฟ 820 · คูลดาวน์ 1.0)
รอบแรกสคริปต์เขียนทับไปเพราะข้อมูลที่ผมดึงมาเก่ากว่าที่คุณเพิ่งแก้ · ไฟล์ก่อนแก้อยู่ที่
`_to_delete/originals_monsters_r66/baphomet_jr.tres` ★

### 7.79.4 แถม — ยืนรอคูลดาวน์แล้วกลับไปท่ายืน

`_try_attack()` เดิมตอนติดคูลดาวน์ return เฉย ๆ ไม่สั่งท่าอะไร → มอนค้างเฟรมสุดท้ายของท่าโจมตี
เมื่อก่อนไม่ค่อยเห็นเพราะมอนตีติดตัวจะขยับไปมาตลอด แต่มอนยิงไกลยืนนิ่งอยู่กับที่ทั้งช่วงคูลดาวน์ เลยเห็นชัดมาก
→ ตอนนี้สั่ง `_play("Idle")` ระหว่างรอ (มีผลกับมอนทุกตัว)

### 7.79.5 เทสต์รอบ 66

`r66_test` — **ผ่าน 33 · ล้มเหลว 0**

1. มอนยิงกระสุนทุกตัวได้ระยะยิง ≥ ระยะมองเห็น · กระสุนวิ่งไกลพอ · มอนตีติดตัวไม่กระทบ
2. บาฟโฟเมทจูเนียร์: เฟรมที่ปล่อยอยู่ในช่วงจริง · ท่าไม่วนซ้ำ · ท่าไม่ยาวเกิน 3 วิ
3. **ยิงจริงในแมพ** — ยืนห่าง 380 px มอนเริ่มท่าโจมตีเอง · ไม่ขยับเข้าหา · ระหว่างชาร์จยังไม่มีลูกไฟ ·
   **ลูกไฟออกตอนเฟรม 11 พอดี** · ออกจากมือสูง 110 px ด้านหน้า · บินไปโดนผู้เล่น · จบแล้วไม่ยืนแข็ง
4. ปิด `ranged_attack` แล้วกลับเป็นพฤติกรรมเดิม · ตั้ง `ranged_attack_range` เองได้

★ กับดักตอนเขียนเทสต์: ตั้งดาเมจมอนเป็น 1 แล้ว DEF ผู้เล่นหักจนเหลือ 0 เทสต์จะอ่านว่า "ไม่โดน"
ต้องตั้งให้แรงพอทะลุ DEF (ใช้ 40) ★


---

## 7.80 รอบ 67 — ลูนาติก: หลบไม่เว่อร์ · ดุร้าย · ยิงไกล

### 7.80.1 ★ "ป้องกันเยอะ" จริง ๆ คือ "หลบเก่ง" ไม่ใช่ DEF ★

ลูนาติกมี **DEF แค่ 4** = ลดดาเมจแค่ **4%** ซึ่งต่ำที่สุดในบรรดามอนเลเวลใกล้เคียง
(คิงโพริง DEF 12 · มูนัค DEF 14) — DEF ไม่ใช่ปัญหาเลย

ตัวที่ผิดปกติจริง ๆ คือ **ค่าหลบ (FLEE)**

```
ค่าหลบจริง = 100 + เลเวล + ค่า flee ใน .tres
โอกาสตีเข้า % = 85 + HIT ของผู้เล่น − ค่าหลบของมอน
```

ลูนาติกเลเวล 15 ตั้ง `flee = 28` → ค่าหลบจริง **143** ซึ่งสูงกว่า:

| มอน | Lv | ค่าหลบจริง |
|---|---|---|
| หมาป่า | 13 | 106 |
| ฮอร์เน็ต | 13 | 111 |
| **คิงโพริง (บอส!)** | 15 | 129 |
| **ลูนาติก (เดิม)** | **15** | **143** ← สูงกว่าบอสเลเวลเดียวกัน |
| มูนัค | 18 | 140 |
| พิตแมน | 19 | 137 |
| ด้วงเหล็ก | 22 | 134 |
| ออร์คนักรบ | 24 | 144 |

**มอนเลเวล 15 หลบเก่งพอ ๆ กับมอนเลเวล 24** — ผู้เล่นเลเวล 16 ที่ยังไม่ได้อัพ DEX
ตีเข้าแค่ **59%** (พลาด 4 ใน 10 ครั้ง) เลยรู้สึกว่า "ตีไม่เข้าเลย"

**★ อีกจุดที่ทำให้แย่กว่าเดิม ★** `combat.gd` มีกติกาลดค่าหลบให้มอน **เลเวลต่ำกว่า 15** ลง 20%
ลูนาติกอยู่ที่เลเวล **15 พอดี** = พลาดส่วนลดไปแบบเฉียดฉิว
(ถ้าเป็นเลเวล 14 จะได้ 114 แทนที่จะเป็น 143 — ต่างกัน 29!)

**★ ผู้ใช้แก้เองแล้ว — ลดเลเวล 15 → 13 ★** ซึ่งพอดีทำให้มันได้ส่วนลดค่าหลบของมอนเลเวลต่ำ

| | เดิม (Lv15) | ตอนนี้ (Lv13) |
|---|---|---|
| ค่าหลบจริง | 143 | **113** (เท่าฮอร์เน็ต 111) |
| ผู้เล่น Lv16 ตีเข้า | 59% | **89%** (วัดจริง 400 ครั้ง = 90%) |

**ปัญหาหายไปแล้วโดยไม่ต้องแตะ `flee` เลย** สคริปต์รอบนี้จึงไม่แก้ค่าหลบ
(อยากให้ตีเข้าง่ายกว่านี้อีก ใส่ `"flee": "5"` ใน `VALUES` → ได้ 94)

★ ลูนาติกเป็นมอน**ยิงไกล**ที่ยืนห่างอยู่แล้ว ไม่จำเป็นต้องหลบเก่งซ้ำอีก ★

### 7.80.2 ดุร้าย + ยิงไกล

ลูนาติกไม่เคยมีบรรทัด `ai_type` ในไฟล์เลย = ใช้ค่าเริ่มต้น **PASSIVE (ใจดี)**
คือยืนเฉย ๆ จนกว่าผู้เล่นจะตีก่อน ตอนนี้ตั้งเป็น **AGGRESSIVE (ดุร้าย)** — เห็นแล้วยิงเลย

| ค่า | เดิม | ใหม่ | เหตุผล |
|---|---|---|---|
| `ai_type` | (ไม่มี = ใจดี) | **1 = ดุร้าย** | เห็นผู้เล่นแล้วยิงทันที |
| `detect_range` | 320 | **520** | ★ ระบบรอบ 66 ใช้ Detect Range เป็น "ระยะยิง" ★ |
| `ranged_attack_range` | 300 | **0** | 0 = ใช้ Detect Range · ถ้าใส่เลขไว้มันจะชนะ Detect Range |
| `projectile_range` | 760 | **900** | ลูกบอลต้องวิ่งไกลกว่าระยะยิง |
| `leash_range` | 520 | **700** | เดิมสั้นกว่าระยะยิง มันจะถูกลากกลับบ้านกลางคัน |
| `attack_cooldown` | 1.6 | **2.4** | ★ ดาเมจเท่าเดิม แต่ยิงห่างขึ้น ★ (ดูหัวข้อถัดไป) |
| `ranged_attack` | — | **เปิด** | ยิงจากที่ยืน ไม่เดินเข้ามาประชิด |

`attack_range` เดิม (300) ไม่ต้องแตะ — ระบบรอบ 66 ใช้เฉพาะตอนเช็คตีติดตัวเท่านั้น

★ **`Ranged Attack Range` กับ `Detect Range` อย่าใส่ทั้งคู่** ★ ถ้าใส่ `Ranged Attack Range` เป็นเลข
มันจะชนะเสมอ ไม่สนใจ Detect Range · อยากให้ "เห็น = ยิงได้" ก็ปล่อยเป็น 0 แล้วคุม Detect Range ที่เดียว

รันด้วย `python3 set_lunatic.py --apply` (ปิด Godot ก่อน · สำรองที่ `_to_delete/originals_monsters_r67/`)
สคริปต์พิมพ์ **ตารางค่าหลบจริงของมอนทุกตัว** ออกมาด้วย ใช้เป็นแผ่นเทียบตอนปรับตัวอื่นได้

### 7.80.3 ★ เรื่องที่ต้องระวัง — ความยากกระโดดขึ้นเยอะ ★

ลูนาติกออกที่ **ป่ามืด (dark_forest)** และ MapSpawner ตั้ง `count_per_type` เริ่มต้น = **5 ตัว**
เดิมมันใจดี ยืนเฉย ๆ จนโดนตี · ตอนนี้ทั้ง 5 ตัวจะยิงใส่ทันทีที่เข้าระยะ 520 px

`atk_min/max = 55-65` ซึ่ง **สูงมากสำหรับเลเวล 13** (หมาป่า Lv13 = 42-58 · คิงโพริงบอส Lv15 = 44-66)
เดิมยิงทุก 1.6 วิ ถ้าเจอ 3 ตัวพร้อมกัน = ~110 ดาเมจ/วินาที

**★ ที่เลือกไว้: ไม่ลดดาเมจ แต่ยิงห่างขึ้น ★** `attack_cooldown` 1.6 → **2.4 วิ**
ลูกบอลยังเจ็บเท่าเดิม (มันน่ากลัวสมกับเป็นมอนดุ) แต่มีจังหวะให้วิ่งหลบ/เข้าไปฟันได้
ยิงถี่ 1.6 วิ × 3 ตัว = ลูกบอลลอยมาแทบไม่ขาดสาย · 2.4 วิ = พอหายใจได้

**ถ้ายังโหดเกินไปอีก** เลือกอย่างใดอย่างหนึ่ง (แก้ใน `set_lunatic.py` แล้วรันใหม่)

| อยากให้ | แก้ |
|---|---|
| ยิงเบาลง | `"atk_min": "38"`, `"atk_max": "48"` |
| ยิงห่างขึ้นอีก | `"attack_cooldown": "3.0"` |
| เจอน้อยตัวลง | ตั้ง `count_per_type` ของ MapSpawner ในป่ามืดเป็น 3 |
| เห็นใกล้ลง | `"detect_range": "440.0"` |

### 7.80.4 เทสต์รอบ 67

`r67_test` — **ผ่าน 20 · ล้มเหลว 0**

1. **ค่าหลบ** — < 130 · ไม่หลบเก่งกว่ามอนที่เลเวลสูงกว่า 4+ · ไม่แซงคิงโพริง ·
   ห่างจากฮอร์เน็ตไม่เกิน 20 · **ตีจริง 400 ครั้ง เข้า 90%** (ตรงกับที่คำนวณ) · จังหวะยิง ≥ 2 วิ
2. **ค่าข้อมูล** — ดุร้าย · เป็นมอนยิงไกล · ระยะยิง ≥ 500 · กระสุนไกลกว่าระยะยิง · leash ไม่สั้นกว่าระยะยิง
3. **ในแมพจริง** — ยืนเฉย ๆ ไม่ได้ตีมัน มันยิงก่อน · ยิงจากระยะ 470 px โดยไม่เดินเข้ามา ·
   ลูกบอลบินมาโดนจริง · **ยืนนอกระยะมองเห็น = ไม่โดน**
4. มอนตัวอื่นไม่โดนเปลี่ยนตาม (โพริงยังใจดี · ฮอร์เน็ตค่าหลบเท่าเดิม)

★ กับดักตอนเขียนเทสต์: `PlayerState.stats.add_exp(999999)` รัว ๆ พาไป **Lv99** ทันที
ผู้เล่น Lv99 มี FLEE สูงจนมอนยิงไม่โดนเลย เทสต์เลยอ่านว่า "ลูกบอลไม่โดน"
→ ตั้ง `stats.level` ตรง ๆ แล้ว `refresh(false)` ★

### 7.80.5 ★ บทเรียน: ผู้ใช้แก้ข้อมูลอยู่ตลอดเวลา ★

รอบนี้เจอซ้ำอีกครั้ง (เหมือนรอบ 66) — ระหว่างที่ผมทำงาน ผู้ใช้แก้ `lunatic.tres` ใน Godot ไป **2 ครั้ง**
(เลเวล 15→13 · DEF 4→3 · Detect 420→320 · ใส่ Ranged Attack Range = 300)
สคริปต์เลยเขียนทับค่าที่เพิ่งตั้งไป **ต้องคืนจากไฟล์สำรองแล้วถามก่อน**

**กติกาตั้งแต่นี้ไป**
1. ก่อนรันสคริปต์ปรับ `.tres` บนเครื่อง → **stage ไฟล์นั้นมาดูก่อนเสมอ** ไม่ใช้ข้อมูลจาก sync ตอนต้นรอบ
2. ดู diff ที่สคริปต์พิมพ์ออกมา — ถ้าเห็นค่าที่ **ไม่ได้ตั้งใจแตะ** เปลี่ยนด้วย = ผู้ใช้เพิ่งแก้ ต้องคืนค่า
3. ไฟล์สำรองอยู่ที่ `_to_delete/originals_monsters_rNN/` เสมอ — คืนได้ทันที

---

## 7.81 รอบ 68 — ฉากหลังรอยสายฟ้า: เห็นทั้งภาพ + พื้นตรงกับที่ยืน

### 7.81.1 เกิดอะไรขึ้น

**อาการที่ 1** ภาพจริง 1905×825 แต่ในเกมเห็นแค่บางส่วน — หายไป **ขวา 405 px** (หอคอย+โซ่) และ **ล่าง 175 px** (หน้าตัดใต้ดิน)

ฉากหลังวาดด้วย `Polygon2D` ซึ่งมี 2 ค่าคู่กัน

| ค่า | หมายถึง |
|---|---|
| `polygon` | รูปสี่เหลี่ยม **ในโลก** (พิกเซลของแมพ) |
| `uv` | ส่วนของ **ไฟล์ภาพ** ที่จะเอามาแปะ (พิกเซลของภาพ) |

`uv` ยังเป็น `1500 × 650` ซึ่งเป็นขนาดของภาพ**ใบเก่า** พอเปลี่ยนไฟล์เป็นใบใหม่ที่ใหญ่กว่า
มันเลยตัดเอาแค่มุมซ้ายบน 1500×650 มาใช้

**★ อาการที่ 2 — ตัวจริงที่ทำให้ฉากหลังหายไปเกือบทั้งแมพ ★**

ตอนลากแก้ใน Godot รูปทรงถูกเปลี่ยนเป็น **5 จุด** แต่ `uv` ยังมี **4 จุด**

```
polygon = PackedVector2Array(0, 0, 3000, 0, 3000, 1300, 1492.48, 1300, 0, 1300)   ← 5 จุด
uv      = PackedVector2Array(0, 0, 1500, 0, 1500, 650, 0, 650)                    ← 4 จุด
```

**Godot ต้องการให้จำนวนจุดเท่ากัน ถ้าไม่เท่า มันจะทิ้ง `uv` ทั้งชุด**
แล้วแปะภาพแบบ **1 พิกเซลภาพ = 1 พิกเซลโลก** แทน
→ ภาพ 1905×825 กองอยู่มุมซ้ายบนนิดเดียว ที่เหลือของแมพ **ดำสนิท**

ซ้ำร้ายโหนดยังถูกลากย่อไว้ `scale = 0.8038` ทำให้ภาพเล็กลงไปอีก
รวมแล้วภาพคลุมแค่ x −125..1406 · y −160..503 ทั้งที่แมพกว้าง −100..2900

★ **กับดัก 99: `polygon` กับ `uv` ของ Polygon2D ต้องมีจำนวนจุดเท่ากันเสมอ**
**ไม่เท่ากัน = Godot ทิ้ง uv เงียบ ๆ ไม่มี error ไม่มี warning** ★

### 7.81.2 ค่าที่ตั้งใหม่

```
position = Vector2(-100, -151)
polygon  = PackedVector2Array(0, 0, 3000, 0, 3000, 1299, 0, 1299)   ← 4 จุด
uv       = PackedVector2Array(0, 0, 1905, 0, 1905, 825, 0, 825)     ← 4 จุด ตรงกัน
scale    (ลบทิ้ง = 1)
map_bounds = Rect2(-100, -151, 3000, 1299)
```

**คิดมายังไง**

1. **ใช้ภาพครบทั้งใบ** — `uv` = ขนาดไฟล์จริงเป๊ะ (1905×825)
2. **ไม่ยืดภาพ** — ย่อขยายเท่ากันทั้งสองแกน
   กว้าง 3000/1905 = **1.5748** · สูง 1299/825 = **1.5745** (ต่างกัน 0.02% มองไม่ออก)
3. **พื้นในภาพตรงกับพื้นที่ยืนจริง** — วัดจากไฟล์ภาพได้ว่าผิวพื้นหินอยู่ที่ **แถว y = 580**
   พื้นชนในฉากอยู่ที่ y = **762** → วางขอบบนภาพที่ `762 − 580 × 1.5748 = −151`
4. **`map_bounds` เท่ากรอบภาพพอดี** — กล้องเลยแพนไปเจอที่ว่างดำ ๆ ไม่ได้
5. ยังเหลือหน้าตัดใต้ดินใต้พื้นอีก **386 px** (ภาพจบที่ y = 1148) เผื่อกล้องแพนลง

**แถม** กล่องชนพื้นถูกลากเลื่อนไปข้าง −6 px ทำให้พื้นจบก่อนขอบแมพ → ดันกลับเป็น 0

### 7.81.3 สคริปต์ `fix_map_bg.py`

```bash
python3 fix_map_bg.py            # ตรวจทุกแมพ ดูว่าจะตั้งอะไร (ยังไม่เขียน)
python3 fix_map_bg.py --apply    # เขียนจริง
```

- **ตรวจฉากหลังทุกแมพ** ว่า `uv` เท่ากับขนาดไฟล์ภาพไหม → เจอใบไหนโดนตัดจะบอกทันที
  (ตอนนี้ 9 แมพอื่นปกติหมด มีแต่ thunder_scar ที่โดน)
- **อ่านพื้นชนจากฉากเอง** — ปรับความสูงพื้นใน Godot แล้วรันสคริปต์ใหม่ ภาพจะขยับตามให้เอง
- ล้าง `scale` ที่ค้างอยู่ · เขียน `polygon`/`uv` เป็นสี่เหลี่ยม 4 จุดตรงกันเสมอ
- สำรองไฟล์เดิมที่ `_to_delete/originals_maps_r68/`

**★ เปลี่ยนภาพฉากหลังแมพไหนก็ตาม ★** ให้รัน `python3 fix_map_bg.py` ทุกครั้ง
แมพใหม่ที่ยังไม่ได้ตั้งค่าไว้ สคริปต์จะบอกให้เพิ่มเข้า `MAPS` พร้อมบอกว่าต้องใส่ `ground_row` (แถวในภาพที่เป็นผิวพื้น)

### 7.81.4 เทสต์รอบ 68

`r68_test` — **ผ่าน 25 · ล้มเหลว 0**

1. **ฉากหลังทุกแมพ (10 ชิ้น)** — `uv` ต้องเท่าขนาดภาพจริง ไม่ตัดทิ้ง
2. **รอยสายฟ้า** — ใช้ภาพครบทั้งใบ · โหนดไม่ถูกย่อ/ขยายค้าง · **`polygon` กับ `uv` จุดเท่ากัน** ·
   ภาพไม่ยืด (สองแกนต่างกัน < 1%) · **พื้นในภาพตรงกับพื้นที่ยืน (คลาด ≤ 12 px)** ·
   ภาพคลุมขอบแมพครบทั้ง 4 ด้าน (ไม่มีแถบดำ) · ยังเห็นใต้ดิน
3. **เดินในแมพจริง** — ยืนบนพื้นได้ทุกจุดตั้งแต่ซ้ายสุดถึงขวาสุด · จุดเกิด/จุดเกิดบอสอยู่ในขอบแมพ

ถ่ายภาพจริงยืนยันด้วย (`shot68_left/mid/right.png`) — เท้าอยู่บนพื้นหินพอดีทั้ง 3 จุด ไม่มีขอบดำ

---

## 7.82 รอบ 69 — อสูรสายฟ้าตะปบ 2 ที (ระบบ "ตีหลายทีในท่าเดียว")

### 7.82.1 ระบบใหม่ — Attack Hit Frames

รอบ 66 ทำช่อง `attack_hit_frame` ไว้ให้ใส่ **เฟรมเดียว** (จุดที่ดาเมจออก)
รอบนี้ขยายเป็นใส่ได้ **หลายเฟรม** = ดาเมจออกหลายครั้งในท่าโจมตีเดียว

ช่องใหม่ใน MonsterData กลุ่ม **"Attack Timing"**

| ช่อง | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| `attack_hit_frames` | ว่าง | **เฟรมที่ดาเมจออก ใส่กี่ตัวก็ได้ = ตีกี่ที** |
| `attack_hit_damage_mult` | 1.0 | **ดาเมจต่อที** — ใช้ตอนตีหลายที จะได้ไม่แรงเป็นเท่าตัวโดยไม่ตั้งใจ |

ลำดับความสำคัญ: `Attack Hit Frames` → `Attack Hit Frame` (เฟรมเดียว) → `Attack Windup` (เวลา)
**มอนที่ไม่ได้ตั้งอะไรเลยทำงานเหมือนเดิมทุกประการ**

`attack_hit_damage_mult` มีผล **เฉพาะตอนตีมากกว่า 1 ที** เท่านั้น
(ตี 1 ที ไม่โดนคูณ = ของเดิมไม่เปลี่ยน) และใช้ได้ทั้งตีติดตัวและยิงกระสุน
มอนที่ยิงกระสุนแล้วตั้งไว้ 2 เฟรม = **ยิง 2 นัดในท่าเดียว**

ในโค้ด: `MonsterData.attack_hit_frame_list()` · `attack_hit_count()` ·
`monster_base._attack_hit(hits)` (แยกดาเมจ 1 ทีออกมาเป็นฟังก์ชันของตัวเอง) ·
`MonsterProjectile.damage_mult`

### 7.82.2 อสูรสายฟ้า — หาเฟรมที่โดนยังไง

ท่า `Attack` ใหม่ (`attack v2.png` · 32 เฟรม **24 fps = 1.33 วิ**) มันตะปบ **2 ครั้ง** จริง ๆ
★ เฟรมที่โดนไม่ผูกกับ fps ★ ปรับ fps เมื่อไหร่จังหวะเลื่อนตามเอง (15 fps ตอนวัดครั้งแรก → 24 fps ตอนนี้)

วิธีวัด: ไล่ดูทุกเฟรมว่า **ขอบซ้ายสุดของเนื้อภาพ** (ตัวหันซ้าย = อุ้งเท้ายื่นไปทางซ้าย) อยู่ที่ไหน
เฟรมที่อุ้งเท้ายื่นสุดคือจังหวะฟาด — ได้ออกมาเป็น **2 ช่วงชัดเจน**

| เฟรม | อุ้งเท้ายื่นถึง | |
|---|---|---|
| 8-9 | 4-16 | ยกอุ้งเท้าขึ้น (เตรียมที่ 1) |
| 10-12 | 0 | ฟาดลง |
| **13** | **0** | ★ **โดนที่ 1** ★ (ปากอ้า อุ้งเท้าลงสุด) |
| 14-16 | 0→16 | ดึงกลับ ยกขึ้นใหม่ (เตรียมที่ 2) |
| 17-19 | 0 | ฟาดลงอีกครั้ง |
| **20** | **0** | ★ **โดนที่ 2** ★ |
| 21-31 | 0-4 | ลงท่า |

ค่าที่ตั้ง (รันด้วย `python3 set_stormscar_attack.py --apply`)

| ค่า | ตั้งเป็น | หมายเหตุ |
|---|---|---|
| `attack_hit_frames` | **PackedInt32Array(13, 20)** | ที่ 24 fps = โดนตอน 0.54 วิ และ 0.83 วิ (ห่างกัน 0.29 วิ) |
| `attack_hit_damage_mult` | **0.65** | รวม 2 ที = **1.30 เท่า** ของดาเมจเดิม |
| `attack_follow_anim` | **เปิด** | ท่ายาว 1.33 วิ ถ้าไม่เปิดจะถูกตัดที่ Attack Duration (0.5 วิ) แล้วเด้งกลับท่ายืนกลางท่า |
| `attack_windup` | 0.4 → **คิดให้เอง** | ไม่ได้ใช้แล้วเพราะมี hit frames · สคริปต์ตั้งเป็น เฟรมแรก ÷ fps จริง ให้อัตโนมัติ |

**★ อยากให้แต่ละทีเต็มดาเมจไปเลย (รวม 2 เท่า) ★**
แก้ `"attack_hit_damage_mult": "1.0"` ที่หัวไฟล์ `set_stormscar_attack.py` แล้วรันใหม่

**★ ถ้าเปลี่ยน fps ของท่า ★** จังหวะจะเลื่อนตามให้เอง ไม่ต้องแก้อะไร
(ระบบคิดเวลาจาก เฟรม ÷ fps จริงทุกครั้ง)

**★ เอาไปใช้กับมอนตัวอื่น ★** ก็แค่ใส่ `Attack Hit Frames` ใน Inspector
เช่น ท่าฟันซ้าย-ขวา 3 ที ใส่ `5, 12, 19` แล้วปรับ `Attack Hit Damage Mult` เป็น 0.45

### 7.82.3 เทสต์รอบ 69

`r69_test` — **ผ่าน 17 · ล้มเหลว 0**

1. **ค่าข้อมูล** — ตั้ง 2 เฟรม · ระบบนับว่าตี 2 ที · เฟรมเรียงถูก · อยู่ในช่วงของท่าจริง ·
   สองทีห่างกัน 0.47 วิ (อยู่ในช่วง 0.2-1.0 = เห็นเป็นคนละที) · ท่าเล่นจนจบ · ไม่วนซ้ำ ·
   **โพริง/บาฟโฟเมทจูเนียร์ยังตีทีเดียวเหมือนเดิม**
2. **ตีจริงในแมพ** — นับ "ครั้งที่เลือดลด" ในท่าโจมตี 1 ท่า → **ได้ 2 ครั้ง ตอนเฟรม 13 กับ 20 พอดี** ·
   ดาเมจสองทีใกล้เคียงกัน · ตีจบแล้วไม่ค้างท่า
3. **หมาป่า (ไม่ได้ตั้งเฟรม) ยังตีทีเดียว** — ของเดิมไม่พัง

★ กับดักตอนเขียนเทสต์: มอนตัวอื่นที่ยังอยู่ในฉากจะตีซ้อนเข้ามาระหว่างนับดาเมจ
ต้อง `queue_free()` ตัวก่อนหน้า และ `set_physics_process(false)` + ตั้ง `_player` เองก่อนเรียก `_attack()` ★

---

## 7.83 รอบ 70 — แมพใหม่ 3 แมพ ภาพโดนตัด (iron_road · ป่าเงาลึกชั้นใน · นิดาเวลลิร์)

### 7.83.1 อาการเดียวกับ thunder_scar

เปลี่ยนไฟล์ภาพฉากหลังเป็นใบใหม่ที่ใหญ่กว่าเดิม แต่ `uv` ของ Polygon2D ยังเป็นเลขของภาพใบเก่า
→ เห็นแค่มุมซ้ายบน

| แมพ | ภาพจริง | uv เดิม | หายไป |
|---|---|---|---|
| ทางเหล็ก (iron_road) | 5600 × 1300 | 2500 × 650 | **3100 × 650** |
| ป่าเงาลึกชั้นใน (dark_forest_2) | 5600 × 1300 | 2100 × 650 | **3500 × 650** |
| นิดาเวลลิร์ (nidavellir_town) | 3600 × 1200 | 1800 × 650 | **1800 × 550** |

### 7.83.2 หา "แถวพื้น" ในภาพยังไง

ต้องรู้ว่าในไฟล์ภาพ **ผิวพื้นที่ตัวละครยืนอยู่ที่แถวไหน** ถึงจะวางภาพให้ตรงกับพื้นชนได้
วิธี: ครอปแถบแนวนอนแถว ๆ กลางภาพ เร่งความสว่าง แล้วดูว่า "ขอบบนของพื้นหิน" อยู่แถวไหน

| แมพ | แถวพื้นในภาพ | สิ่งที่เห็น |
|---|---|---|
| ทางเหล็ก | **880** | ขอบบนของถนนหิน (เหนือขึ้นไปเป็นกำแพง/เตาหลอม) |
| ป่าเงาลึกชั้นใน | **884** | ขอบบนของทางเดินหินมอส (เหนือขึ้นไปเป็นพุ่มเห็ดเรืองแสง) |
| นิดาเวลลิร์ | **893** | ขอบบนของถนนหินในเมือง (เหนือขึ้นไปเป็นทั่ง/ร้านช่าง) |

พื้นชนของทั้ง 3 แมพอยู่ที่ **y = 880** เท่ากันหมด (สคริปต์อ่านจากฉากเอง)

### 7.83.3 ค่าที่ตั้ง

เลือกแบบ **"ย่อภาพให้พอดีแมพ"** — ขนาดแมพ / พื้น / ประตู / จุดเกิด / มอน **ไม่เปลี่ยนเลย**
มีแค่ภาพฉากหลังที่ถูกจัดใหม่ (เสี่ยงน้อยสุด)

| แมพ | ย่อขยาย | กรอบภาพในแมพ | map_bounds ใหม่ |
|---|---|---|---|
| ทางเหล็ก | ×0.8929 | x −100..4900 · y 94..1255 | Rect2(−100, 94, 5000, 1161) |
| ป่าเงาลึกชั้นใน | ×0.7857 | x −100..4300 · y 185..1206 | Rect2(−100, 185, 4400, 1021) |
| นิดาเวลลิร์ | **×1.0000** | x −100..3500 · y −13..1187 | Rect2(−100, −13, 3600, 1200) |

★ นิดาเวลลิร์ภาพ 3600 × 1200 = เท่าขนาดแมพพอดี → ได้ **1 พิกเซลภาพ = 1 พิกเซลโลก** ภาพคมสุด ★

`map_bounds` ตั้งให้เท่ากรอบภาพเสมอ กล้องจะได้ไม่แพนไปเจอที่ว่างดำ ๆ

**★ ถ้าวันหลังอยากได้ภาพคม 1:1 ของอีก 2 แมพด้วย ★**
เปลี่ยน `"fit": "map"` เป็น `"fit": "art"` ใน `fix_map_bg.py` แล้วรันใหม่
→ แมพจะกว้างขึ้นเป็น 5600 (ทางเหล็ก +600 · ป่าเงาลึกชั้นใน +1200) และสคริปต์**ยืดกล่องชนพื้น + ย้ายกึ่งกลางให้เอง**
(ประตู/จุดเกิด/มอน ยังอยู่ที่เดิม จะมีพื้นที่ว่างเพิ่มทางขวา)

### 7.83.4 สคริปต์ `fix_map_bg.py` ของใหม่

- เพิ่ม 3 แมพเข้า `MAPS` แล้ว — แก้ `ground_row` ได้ถ้าเห็นตัวละครลอย/จม
- เพิ่มโหมด `"fit"`:
  - `"map"` = ย่อภาพให้พอดีแมพ (ค่าเริ่มต้น · ไม่แตะขนาดแมพ)
  - `"art"` = ขยายแมพให้เท่าภาพ (คม 1:1 · ยืดกล่องชนพื้นให้เอง)
- ยังตรวจ **ทุกแมพ** ให้อัตโนมัติว่า `uv` เท่าขนาดไฟล์ภาพไหม
  เจอแมพที่โดนตัดแต่ยังไม่ได้ตั้งค่าไว้ จะบอกให้เพิ่มเข้า `MAPS`

**★ เปลี่ยนภาพฉากหลังแมพไหนก็ตาม รัน `python3 fix_map_bg.py` ทุกครั้ง ★**

### 7.83.5 ตรวจแล้ว

- `r68_test` **ผ่าน 25 · ล้มเหลว 0** — ข้อ 1 ไล่เช็ค **ฉากหลังทุกแมพ (10 ชิ้น)** ว่า `uv` เท่าขนาดภาพจริง
- เรนเดอร์ภาพจริง 6 ใบ (ซ้ายสุด/ขวาสุดของทั้ง 3 แมพ) — **เท้าอยู่ที่ y = 880 ตรงขอบพื้นที่วาดไว้ทุกจุด ไม่มีขอบดำ**
  (นิดาเวลลิร์เห็นตัวละครจริงยืนบนถนนหินพอดีเป๊ะ)
- ถดถอย 11 ชุด — แดง 17 ข้อ **มีอยู่ก่อนรอบนี้ทั้งหมด** (ยืนยันด้วยการรันกับไฟล์ฉากเวอร์ชันก่อนแก้แล้วได้ผลเดียวกัน):
  r38 M4/M3 · quest_test 15 ข้อ (จำนวนล่า/รางวัลเควสที่คุณปรับเอง) · portal_test 1 · newfeat เมืองกว้าง

★ กับดักตอนถ่ายภาพทดสอบ: **วิดีโอคัทซีน/วิดีโอเปิดตัวบอส pause ทั้งเกม** ตัวละครเลยไม่ตกลงพื้น
ต้องตั้งธง `seen_map_<id>` **และ** `seen_intro_<boss_id>` ก่อนเข้าแมพ ★

---

## 7.84 รอบ 71 — กล้องเห็นฉากสูงเต็มจอ (ขอบบนสุดถึงล่างสุดของแมพ)

### 7.84.1 ปัญหา

ภาพฉากหลังวาดสูง 1200-1300 px แต่จอเกมสูงแค่ **720** (ตั้งใน project.godot)
กล้องเลยเห็นแค่แถบกลาง ๆ ประมาณ 55-60% ของภาพ — ยอดเขาด้านบนกับกำแพงด้านล่างหายไป
(ขยายหน้าต่างให้ใหญ่ขึ้นก็ไม่ช่วย เพราะ `stretch/aspect = expand` จะขยายแค่ **ด้านข้าง** ความสูงยังเป็น 720 เท่าเดิม)

### 7.84.2 วิธีแก้ — กล้องซูมออกอัตโนมัติตามความสูงแมพ

MapBase กลุ่มใหม่ **"กล้องเห็นฉากสูงเต็มจอ"**

| ช่อง | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| `camera_fit_height` | **เปิด** | ซูมออกให้ความสูงของ `map_bounds` พอดีกับความสูงจอ · ปิด = ใช้ `Camera Zoom` ตามเดิม |
| `camera_min_zoom` | 0.5 | ซูมออกได้ต่ำสุดแค่ไหน (กันตัวละครเล็กจนมองไม่เห็น) |

สูตร: `zoom = ความสูงจอ ÷ ความสูงแมพ` (Godot: zoom < 1 = ซูมออก)
ไม่ซูม "เข้า" เกิน 1 — แมพที่เตี้ยกว่าจอจะเห็นปกติ

| แมพ | แมพสูง | zoom | ตัวละครเหลือ |
|---|---|---|---|
| prontera_field | 950 | 0.758 | 76% |
| nidavellir_town | 1200 | 0.600 | 60% |
| iron_road | 1161 | 0.620 | 62% |
| thunder_scar / dark_forest | 1300 | 0.554 | 55% |

- แนวนอนกล้องยังเลื่อนตามตัวละครเหมือนเดิม · แนวตั้งนิ่ง (เห็นครบอยู่แล้ว)
- **ปรับขนาดหน้าต่างเกมเมื่อไหร่ก็คำนวณใหม่ให้เอง** (เชื่อม `viewport.size_changed`)
- ระบบที่พึ่งกล้องอยู่แล้ว (มอนเกิดนอกจอ · มินิแมพ · คลิกเมาส์) คิด zoom อยู่แล้ว ไม่ต้องแก้

**★ แลกกับอะไร ★** ตัวละคร/มอน/ตัวเลขดาเมจ **เล็กลงตามสัดส่วน** — แมพสูง 1300 = เหลือ 55%
ถ้าเล็กเกินไปมี 3 ทาง:
1. `camera_min_zoom` สูงขึ้น (เช่น 0.7) → จะเห็นไม่ครบทั้งสูง แต่ตัวใหญ่ขึ้น
2. ปิด `camera_fit_height` เฉพาะแมพนั้น (Inspector ของโหนด Map)
3. **วาดฉากให้เตี้ยลง** — ความสูงภาพ ≈ 900-1000 จะได้ zoom ~0.75 ตัวยังใหญ่ เห็นครบ

★ **ทิศทางที่ผู้ใช้บอก: ฉากต่อ ๆ ไปจะยิ่งสูงขึ้นและขอบล่างต่ำลง** → เปิดตัวนี้ไว้เป็นค่าเริ่มต้นทุกแมพ
ภาพสูงเท่าไหร่ก็เห็นครบ ไม่ต้องมาตั้ง zoom ทีละแมพ ★

### 7.84.3 กับดัก 102 — Camera Offset ทะลุ limit

`camera_offset = (0, −60)` (กล้องเงยขึ้นนิดหน่อยให้เห็นข้างหน้า) — Godot **ไม่เอา offset ไปคิดกับ limit_top/bottom**
พอเห็นครบทั้งสูงแล้วยังมี offset −60 อยู่ กล้องเลยเลยขอบบนแมพไป 60 px → **แถบเปล่า ๆ ด้านบนจอ 36 px**
(เจอตอนวัดสี 40 แถวบนของภาพเรนเดอร์ สว่างสม่ำเสมอผิดปกติ)
→ ตอนที่เห็นครบทั้งสูง `_apply_fit_zoom()` ตั้ง `offset.y = 0` ให้เอง

### 7.84.4 เทสต์รอบ 71

`r71_test` — **ผ่าน 29 · ล้มเหลว 0** ★ ต้องรันแบบมีหน้าต่างจริง (xvfb + opengl3) ไม่ใช่ `--headless` ★
(headless ไม่มีหน้าต่าง `get_visible_rect()` คืน 1280 สูง ทำให้ zoom = 1 ตลอด — เทสต์เช็คข้อนี้ไว้ข้อแรก)

1. 4 แมพ — เปิดโหมด · **กล้องเห็นสูงเท่าแมพ (คลาด ≤ 2 px)** · zoom < 1 · ซูมเท่ากันสองแกน · limit บน/ล่าง = ขอบแมพ
2. เลื่อนแนวนอนยังทำงาน · แนวตั้งนิ่ง · **ขอบบน/ล่างที่เห็น = ขอบบน/ล่างแมพพอดี** (ไม่มีแถบเปล่า)
3. ปิดสวิตช์ → zoom กลับเป็น 1 · `camera_min_zoom` กันซูมออกเกิน
4. มอนยังเกิดในแมพ

ถดถอย 8 ชุด (r68 · r55 ขอบแมพ · touch · mouse · spread · r44 · r53 · r47) ผ่านหมด ยกเว้น r47 rapier ราคาเดิม
เรนเดอร์จริง 3 แมพ (`shot71_*.png`) เห็นตั้งแต่ยอดเขาถึงกำแพงล่าง ไม่มีแถบเปล่า

★ กับดัก 103: เทสต์ที่วัดกล้อง/ซูม/ขนาดจอ ต้องรัน **มีหน้าต่างจริง** (`xvfb-run -s "-screen 0 1280x720x24" ... --rendering-driver opengl3`)
`--headless` คืนขนาดจอผิด ★

---

## 7.85 รอบ 72 — ที่ว่างเทา ๆ ใต้ภาพฉาก (ขอบแมพบน-ล่างตามภาพเสมอ)

### 7.85.1 อาการ

หลังรอบ 71 (กล้องเห็นสูงเต็มจอ) พอ**ลากภาพฉากหลังใน Godot ขึ้น/ลง หรือย่อ** ให้เตี้ยกว่า `map_bounds`
กล้องซูมตาม `map_bounds` (ที่ยังสูงเท่าเดิม) เลยเห็น "ที่ว่างเทา ๆ" ใต้ภาพเป็นแถบใหญ่

ตัวอย่างที่เจอ:
- **ป่าเงาลึกชั้นใน** — ภาพถูกลากขึ้น 289 px (y −104..917) แต่ `map_bounds` ยัง 185..1206 → ว่างใต้ภาพ 289 px
- **ป่าเงาลึก** — ภาพถูกย่อ 0.715 (y −66..863) แต่ `map_bounds` 0..1300 → ว่างใต้ภาพ 437 px

### 7.85.2 แก้ที่ต้นทาง — `map_bounds` บน-ล่าง = ขอบภาพเสมอ

รอบ 55 มี `_clamp_bounds_to_art()` หุบขอบ **ซ้าย-ขวา** ตามภาพอยู่แล้ว
รอบนี้เพิ่ม **บน-ล่าง** ด้วย (`art_span_y()` วัดเกณฑ์เดียวกับ `art_span()`)

| แกน | กติกา | เหตุผล |
|---|---|---|
| ซ้าย-ขวา | **หุบเข้าอย่างเดียว** ไม่ขยาย | ขยาย = เปิดที่ว่างให้เดินไปตกได้ |
| บน-ล่าง | **= ขอบภาพเป๊ะ** (หุบหรือขยายก็ได้) | แค่เปลี่ยนช่วงที่กล้องมอง ไม่ได้เปิดที่ให้เดิน |

ทั้งสองแกนยังมีกันไม่ให้แคบจนประตู/จุดเกิดหลุดออกนอกขอบ (`KEEP_INSIDE_MARGIN`)

**ผลคือ ลากภาพ/ย่อภาพใน Godot ยังไง กล้องก็เห็นครบทั้งภาพพอดี ไม่มีที่ว่าง** — ไม่ต้องรันสคริปต์ ไม่ต้องแก้ `map_bounds` เอง

| แมพ | ภาพ y | zoom (จอ 720) | ตัวละครเหลือ |
|---|---|---|---|
| ป่าเงาลึกชั้นใน | −104..917 (1021) | 0.705 | 71% |
| ป่าเงาลึก | −66..863 (929) | 0.775 | 78% |

Output ตอนเข้าแมพจะพิมพ์: `[Map] <id> หุบขอบตามภาพฉาก: x … · y 185..1206 → -104..917`

★ **เทคนิคที่คุณใช้ (ย่อภาพให้เตี้ยลง = ตัวละครใหญ่ขึ้น) ใช้ได้เลย** ★
กล้องซูมตามความสูงภาพ → ภาพเตี้ย = ซูมออกน้อย = ตัวใหญ่ · ภาพสูง = เห็นเยอะแต่ตัวเล็ก
ปรับได้ที่ scale ของโหนด Sky ใน Inspector แล้วรันเกมดูได้ทันที

### 7.85.3 เทสต์

`r71_test` ขยายเป็น **45 ข้อ ผ่านหมด** (เพิ่ม 6 แมพ × "ขอบบน-ล่างแมพ = ขอบภาพฉาก")
ถดถอย r55 (หุบขอบซ้าย-ขวา) · r68 · r44 · r30 ผ่าน · แดง 3 ข้อเดิม (r47 rapier · newfeat · portal)
เรนเดอร์จริงที่ **ขนาดหน้าต่างเท่าของคุณ 1906×931** (`--resolution 1906x931`) — ภาพเต็มจอบนถึงล่าง ไม่มีแถบเทา

---

## 7.86 รอบ 73 — พรอนเทรา: แถบน้ำตาลใต้ภาพเมือง (ไม่ต้องวาดภาพใหม่)

### 7.86.1 คำตอบสั้น ๆ

**ไม่ต้องสร้างภาพใหม่** — ภาพ `Map_Asgard.png` (3212 × 941) ใช้ได้ครบทั้งใบอยู่แล้ว
สิ่งที่เห็นเป็น "แถบน้ำตาล" ใต้ภาพ **ไม่ใช่ส่วนที่ภาพหาย** แต่เป็น **แผ่นสีน้ำตาลสมัย placeholder**
(`Terrain/Ground0/Visual`) ที่ยังเปิดอยู่ในฉากนี้ฉากเดียว

| ชิ้น | ช่วง y ในโลก | หมายเหตุ |
|---|---|---|
| ภาพเมือง `Background/MapAsgard` | **−73 .. 987** | ภาพจริง (ย่อขยาย 1.127) |
| แผ่นน้ำตาล `Terrain/Ground0/Visual` | 929 .. **1089** | ยื่นเลยขอบล่างภาพ 102 px และวาด**ทับ**ขอบล่างภาพ 58 px |
| แผ่นฟ้า `Sky` · `FarLayer` | 0..901 · 520..916 | อยู่หลังภาพ ไม่เห็น แต่ถูกนับเป็น "ภาพฉาก" |

รอบ 72 ทำให้ `map_bounds` บน-ล่าง = ขอบ "ภาพฉาก" — แต่ตอนนั้นนับแผ่นสีล้วนเป็นภาพฉากด้วย
→ ขอบล่างแมพเลยไปอยู่ที่ 1089 (ขอบแผ่นน้ำตาล) แทนที่จะเป็น 987 (ขอบภาพเมือง) → กล้องซูมออกให้เห็นแถบน้ำตาลนั้นด้วย

แมพอื่น ๆ ทุกแมพซ่อนแผ่นสีพวกนี้ไปแล้ว (`visible = false`) — พรอนเทราเป็นแมพแรกสุดเลยตกหล่น

### 7.86.2 แก้ 2 ชั้น

**ชั้นที่ 1 — `map_base.gd`: แผ่นสีล้วนไม่นับเป็นภาพฉากอีกต่อไป**

`art_span()` / `art_span_y()` ใช้ตัวช่วยใหม่ `_art_rects()`:
- ถ้าแมพมี **ภาพจริง** (Sprite2D / TextureRect / Polygon2D ที่มี texture) แม้แต่ชิ้นเดียว → นับ**เฉพาะภาพจริง**
- แมพที่ยังเป็น placeholder ล้วน (ไม่มีภาพเลย) → ยังนับแผ่นสีเหมือนเดิม
- เกณฑ์อื่นเท่าเดิม: กว้าง ≥ 25% ของแมพ · มองเห็นอยู่ · ไม่ใช่ตัวละคร/มอน/NPC

ผลข้างเคียงเล็กน้อยที่พรอนเทรา: ขอบซ้ายหุบเข้าตามภาพ + `art_edge_margin` 40 → `x −996..2544` (เดิม −1036) เหมือนกติกาแมพอื่น

**ชั้นที่ 2 — `hide_placeholder_layers.py`: ซ่อนแผ่นสีที่ยังค้างอยู่**

```
python3 hide_placeholder_layers.py          # ดูก่อนว่าจะแก้อะไร
python3 hide_placeholder_layers.py --apply  # แก้จริง (สำรองที่ _to_delete/originals_maps_r73/)
```

กติกา: แมพที่มีภาพจริงแล้ว → Polygon2D **สีล้วน** ชื่อ `Sky` / `FarLayer` / `FarTrees` / `Visual`
ที่**กว้าง ≥ 25% ของแมพ** และยังเปิดอยู่ → ตั้ง `visible = false`
(แผ่นเล็ก ๆ เช่น พื้นลอย `Plat0..N/Visual` ใน ember_mine / hall_of_silence / thunder_scar **ไม่แตะ** — ยังเป็นภาพพื้นลอยที่ใช้อยู่)

รอบนี้แก้แค่ **พรอนเทรา** (Sky · FarLayer · Ground0/Visual) — อีก 10 แมพเรียบร้อยอยู่แล้ว

### 7.86.3 ผลหลังแก้

| | ก่อน | หลัง |
|---|---|---|
| ขอบแมพ y | −73 .. 1089 (1162) | **−73 .. 987 (1060)** = ภาพเมืองพอดี |
| zoom (จอ 720) | 0.620 | **0.679** (ตัวละครใหญ่ขึ้น ~10%) |
| แถบน้ำตาลใต้ภาพ | มี ~80 px | **ไม่มี** |
| ขอบล่างภาพเมือง (กำแพงหินล่างสุด) | โดนแผ่นน้ำตาลบัง 58 px | **เห็นครบ** |

พื้นชน (เท้า y 711) · จุดเกิด · ประตูไปทุ่งวิหาร — **ไม่เปลี่ยน**

**★ ถ้าวันหลังอยากวาดภาพพรอนเทราใหม่ ★** ขนาดแนะนำ **3620 × 1060** (= ขนาดแมพพอดี จะได้ 1 พิกเซลภาพ = 1 พิกเซลโลก คมสุด)
หรือสัดส่วนเดิม 3212 × 941 ก็ใช้ได้ (Sprite ย่อขยาย 1.127 ให้เอง) · ภาพสูงกว่านี้ = เห็นเยอะแต่ตัวเล็กลง (สูตรรอบ 71)
พื้นที่ตัวละครยืนอยู่แถว **696** ของภาพ 941 (≈ 74% จากบน) — วาดผิวถนนไว้แถวนั้น

### 7.86.4 เทสต์

`r73_test` **ผ่าน 19 · ล้มเหลว 0** (รันมีหน้าต่างจริง)
1. หน่วยย่อย: แมพ placeholder ล้วนยังนับแผ่นสี · มีภาพจริงแล้วนับเฉพาะภาพ (ทั้งแกน x และ y) · ซ่อนภาพจริง → กลับไปนับแผ่นสี
2. พรอนเทราจริง: แผ่นสี 3 ชิ้นซ่อน · ขอบบน-ล่างแมพ = ขอบภาพเมือง · กล้องเห็นสูง 1060 พอดี · zoom ≈ 0.68 · เท้า 711 · จุดเกิด/ประตูยังอยู่ในขอบ
3. เรนเดอร์จริง: 60 แถวล่างของจอไม่มีแถบน้ำตาลล้วน · แถวบนสุดเป็นภาพ

ถดถอย: r71 (45) · r55 (59) · r44 (59) · r30 (66) · r64 (31 — แก้เทสต์ให้รอบอสตกถึงพื้นก่อนร่าย) ผ่านหมด
แดงเดิม: newfeat เมืองกว้าง · portal 1 ข้อ
**★ r68 ข้อ "พื้นในภาพตรงกับพื้นที่ยืน" แดงใหม่ที่ thunder_scar (−24 px)** — มาจากที่คุณย้าย Ground/จุดเกิด/ประตูของ thunder_scar เอง (พื้นชนย้ายไป y 800 แต่แถวพื้นในภาพตกที่ 776)
ถ้าเห็นตัวละครจมพื้นนิดหน่อยในแมพนั้น ลาก `Terrain/Ground` ขึ้น 24 px หรือรัน `fix_map_bg.py` ใหม่ ★

เรนเดอร์จริงที่ 1906×931 (`shot73_town_1906.png`) — เห็นตั้งแต่ท้องฟ้าถึงกำแพงหินล่างสุด ไม่มีแถบน้ำตาล

---

## 7.87 รอบ 74 — เปลี่ยนภาพฉากเป็นใบกว้างขึ้น = เดินได้เต็มภาพทันที

### 7.87.1 อาการ

คุณเปลี่ยน `Sprites/Map_Asgard.png` เป็นใบใหม่ **6424 × 941** (เดิม 3212 × 941) และยืดกล่องชนพื้นเป็น **7234 px** แล้ว
แต่เดินไปสุดซ้าย/ขวาไม่ได้ — ติดกำแพงล่องหนตั้งแต่กลางภาพ

| | ค่า |
|---|---|
| ภาพในโลก (scale 1.1269) | x **−2846 .. 4394** (กว้าง 7239) |
| กล่องชนพื้นที่คุณยืดแล้ว | x −2845 .. 4389 (กว้าง 7234) |
| `map_bounds` ในฉาก | x −1036 .. 2584 (**กว้างแค่ 3620** — ของภาพใบเก่า) |

**สาเหตุ:** กติกาตั้งแต่รอบ 55 คือ "ขอบแมพ **หุบเข้า** ตามภาพได้อย่างเดียว ห้ามขยายออก"
(กันไม่ให้เดินออกไปยืนบนที่ว่างดำ ๆ นอกภาพ) — พอภาพใหญ่ขึ้น ขอบแมพเลยไม่ยอมขยายตาม
ที่เดินไปทางขวาได้ถึง 4297 เพราะมีกฎ "ห้ามหุบจนประตูอยู่นอกขอบ" ดันขอบตามประตูไปให้เท่านั้น

### 7.87.2 กติกาใหม่ — ขยายได้ แต่ไม่เกิน "พื้นที่ยืนได้จริง"

| แกน | เดิม | รอบ 74 |
|---|---|---|
| ซ้าย-ขวา | หุบเข้าอย่างเดียว | **= ขอบภาพ (ขยายได้) แต่ไม่เกินขอบกล่องชนพื้นหลัก** |
| บน-ล่าง | = ขอบภาพ (รอบ 72) | เหมือนเดิม |

ฟังก์ชันใหม่ `floor_span_x()` วัดขอบซ้าย-ขวาของ **กล่องชนของ StaticBody2D ที่กว้าง ≥ 25% ของแมพ**
(= พื้นหลักของแมพ · พื้นลอยเล็ก ๆ `Plat0..N` และกำแพงขอบแมพไม่นับ)

- **มีพื้นรองรับแค่ไหน ขยายได้แค่นั้น** — ไม่มีวันเดินออกไปตกในที่ว่าง
- แมพที่ไม่มีพื้นหลัก (ใช้ TileMap เช่น อัสการ์ดชั้นใน) → ตกกลับไปใช้กติกาเดิม (หุบเข้าอย่างเดียว)
- ปิดได้รายแมพที่ช่อง **"ขยายขอบซ้าย-ขวาตามภาพ"** (`bounds_follow_art_x`) ใน Inspector ของโหนด Map

**★ ต่อไปนี้เปลี่ยนภาพฉากเป็นใบกว้างขึ้นเมื่อไหร่ ให้ยืด "กล่องชนพื้น" ตามอย่างเดียวพอ — ขอบแมพ · กำแพงล่องหน · ขอบกล้อง ขยับตามให้เอง ★**
(ถ้าลืมยืดพื้น เกมจะไม่พังแต่จะเดินได้แค่ปลายพื้นเดิม = เบาะแสว่ายังไม่ได้ยืด)

### 7.87.3 ผลกับแมพทั้งหมด

| แมพ | ขอบแมพเดิม | ขอบแมพใหม่ |
|---|---|---|
| **พรอนเทรา** | −1036 .. 4297 (5333) | **−2806 .. 4354 (7159)** = เต็มภาพ |
| ทุ่งวิหาร | 120 .. 5608 | **−440** .. 5608 (เดินถึงหน้าผาซ้ายที่วาดไว้แล้ว) |
| ป่าเงาลึก | 0 .. 3876 | −48 .. 3876 |
| อีก 8 แมพ | — | **ไม่เปลี่ยน** (ขอบเท่าเดิมทุกค่า) |

พรอนเทรา: พื้นชนที่คุณยืดไว้ผิวอยู่ที่ **y 720** — ยืนได้ทั้งซ้ายสุด กลาง ขวาสุด (เทสต์เดินชนกำแพงซ้ายแล้วหยุดที่ −2785 พอดี)

### 7.87.4 เทสต์

`r74_test` **ผ่าน 31 · ล้มเหลว 0**
1. พรอนเทรา: ขอบแมพ = ขอบภาพ ± 40 · กว้าง 7159 · กำแพงล่องหน/ขอบกล้องย้ายตาม · ยืนได้ 3 จุด (ซ้ายสุด/กลาง/ขวาสุด) เท้า y 720 · เดินไปสุดซ้ายได้และไม่หลุดแมพ · ประตู/จุดเกิดยังอยู่ในขอบ
2. หน่วยย่อย: พื้นลอยแคบไม่ถูกนับ · ภาพกว้าง 4000 แต่พื้น 2600 → ขยายได้แค่ 2600 · ปิดสวิตช์แล้วกลับเป็นกติกาเดิม · แมพไม่มีพื้นหลักไม่ขยาย
3. ทางเหล็ก / เหมืองถ่านไฟ: ขอบเท่าเดิมเป๊ะ

ถดถอย: r71 45 · r73 19 · r55 62 (แก้ข้อ "ห้ามขยายซ้าย" เป็นกติกาใหม่ + เพิ่มข้อ "ไม่เกินพื้นที่ยืนได้") · r44 59 · r30 66 · r64 31 · newfeat 42 (ข้อ "เมืองกว้างเท่าภาพ" คำนวณจากภาพจริงแล้ว ไม่ฝังเลข 3620) · แดงเดิม portal 1
เรนเดอร์จริง 1906×931 ที่ซ้ายสุด/กลาง/ขวาสุดของเมือง — ภาพเต็มจอทั้ง 3 จุด ตัวละครยืนบนถนน

★ หมายเหตุ: คุณเปลี่ยนชื่อโหนดรากของฉากเป็น **`Map2`** — ระบบใช้กลุ่ม `map` ไม่ได้ใช้ชื่อโหนด จึงไม่มีผลอะไร ★

---

## 7.88 รอบ 75 — คลิปใหม่ 4 ไฟล์เข้าเกม + ช่อง "วิดีโอตอนตาย"

### 7.88.1 คลิปที่ใส่ให้แล้ว

แปลงเป็น `.ogv` (Ogg Theora — Godot เล่นได้แค่สกุลนี้) วางไว้ที่ `Sprites/video/` ไฟล์ mp4 เดิมยังอยู่ครบ

| ไฟล์ต้นฉบับ | → ไฟล์ในเกม | ใส่ที่ | เล่นตอนไหน |
|---|---|---|---|
| opening boss fight stormscar.mp4 | `boss_intro_stormscar.ogv` | มอน `stormscar` → **Intro Video** | เดินเข้าใกล้อสูรสายฟ้าครั้งแรก (ระยะ 700) |
| stormscar die.mp4 | `boss_death_stormscar.ogv` | มอน `stormscar` → **Death Video** ★ช่องใหม่★ | อสูรสายฟ้าตาย — หลังท่าตายเล่นจบ |
| kingporing die.mp4 | `boss_death_king_poring.ogv` | มอน `king_poring` → **Death Video** | คิงโพริงตาย |
| m6_ceremony.mp4 | `m6_ceremony.ogv` | เควส `m6_ceremony` → **Video On Complete** | ส่งเควส M6 → เล่นคลิป → แล้วกล้องแพนไปหยุดที่กุนนาร์ |

ทั้ง 4 คลิป 1920×1080 24fps 8 วินาที → ย่อเป็น 1280 กว้างตอนแปลง (คมพอ ไฟล์เล็กลงครึ่ง)

```
ffmpeg -i "clip.mp4" -vf scale=1280:-2 -c:v libtheora -q:v 7 -c:a libvorbis -q:a 4 clip.ogv
```

★ M6 ต่อกันพอดีตามแผนรอบ 59: คลิป = ภาพรวมทั้งเมืองสวด → กล้องในเกมแพนใกล้ ๆ ไปหยุดที่กุนนาร์ที่ไม่ยกมือ ★

### 7.88.2 ช่องใหม่ — "วิดีโอตอนตาย" (Death Video)

`MonsterData` เพิ่ม 2 ช่องในกลุ่มเดียวกับ Intro Video:

| ช่อง | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| `Death Video` | ว่าง | คลิป .ogv ที่เล่นตอนมอน/บอสตัวนี้ตาย · ว่าง = ไม่มี (มอนทั่วไปไม่กระทบ) |
| `Death Video Once` | เปิด | เล่นครั้งเดียวต่อเซฟ (จำด้วยธง `seen_death_<id>`) · ปิด = ตายกี่ครั้งก็เล่น |

**จังหวะ:** ตาย → ได้ EXP/ของดรอป/ป้าย MVP → **เล่นท่าตายจนจบ** → คลิป → แล้วค่อยกลายเป็นศพ (บอสที่มีคูลดาวน์) หรือจางหาย
ผู้เล่นจึงเห็นบอสล้มด้วยตาก่อน แล้วค่อยตัดเข้าคลิป — ไม่ใช่คลิปเด้งขึ้นมาทับตอนบอสยังยืนอยู่

เหมือนคลิปอื่น: เกมหยุดระหว่างเล่น · กด Enter / Esc / คลิก ข้ามได้ · เข้าแมพใหม่ตอนบอสยังตาย (ศพ) ไม่เล่นซ้ำ

### 7.88.3 สคริปต์ `set_cutscene_videos.py`

ต่อสายให้ทั้ง 4 จุดในทีเดียว (แก้เฉพาะบรรทัดวิดีโอ ค่าอื่นไม่แตะ · สำรองที่ `_to_delete/originals_video_r75/`)

```
python3 set_cutscene_videos.py          # ดูก่อน
python3 set_cutscene_videos.py --apply  # แก้จริง
```

มีคลิปใหม่อีกก็เพิ่มใน `JOBS` ข้างบนไฟล์ได้เลย หรือใส่มือใน Godot ก็ได้ (`data/monsters/<id>.tres` → ช่อง Death Video)

### 7.88.4 คลิปที่ยังขาดตามแผนรอบ 59

`map_thunder_scar` · `c2_8_hammer_truth` · `m10_hunter_journal` · `m12_valder_shard` · `map_iron_road` · `map_nidavellir` · `boss_intro_forge_guardian` · `m2_oath_pillar` · `m7_silent_forest` · `c2_4_nameless_grave` · `map_hall_of_silence`
(พรอมพ์ทุกคลิปอยู่ในเอกสาร «แผนวิดีโอคัทซีน + เสียงพากย์ NPC (รอบ 59)»)

★ หมายเหตุ: แผนรอบ 59 ให้คลิป "อสูรสายฟ้าตาย" ไปอยู่ที่ **M13 Video On Complete** (ตอนส่งเควส)
รอบนี้ย้ายไปไว้ที่ **ตอนบอสตายจริง** แทน — เห็นทันทีที่ล้มบอส ไม่ต้องเดินกลับเมืองก่อน
ถ้าอยากได้ทั้งสองจังหวะ ใส่ `video_on_complete` ของ M13 เพิ่มได้ (คนละธง เล่นทั้งคู่) ★

### 7.88.5 เทสต์

`r75_test` **ผ่าน 26 · ล้มเหลว 0**
1. ไฟล์ .ogv ทั้ง 4 โหลดเป็น VideoStream ได้ · ต่อเข้าช่องถูกทุกจุด · คิงโพริง Intro เดิมยังอยู่ · M6 ยังแพนกล้องต่อ · มอนทั่วไป Death Video ว่าง
2. ฆ่าอสูรสายฟ้าจริงในเกม → เล่นคลิป · ตั้งธง `seen_death_stormscar` · เกม paused ระหว่างเล่น · กดข้ามแล้วเล่นต่อได้
3. ตายรอบสอง = ไม่เล่นซ้ำ ไม่ค้าง paused · ปิดสวิตช์ Once = เล่นทุกครั้ง
4. มอนธรรมดาตาย = ไม่มีคลิป ไม่ค้างจอ หายไปตามปกติ

ถดถอย: r74 31 · r55 62 · r44 59 · r30 66 · newfeat 42 · r64 31 ผ่านหมด
แดงเดิม: quest_test 15 (จำนวนล่า/รางวัลที่คุณปรับเอง) · r38 M4 · portal 1 · r68 พื้น thunder_scar (คุณย้าย Ground เอง)

★ กับดัก 107: เทสต์ที่ "กดข้ามคลิป" ด้วย `Input.parse_input_event(pressed = true)` ต้องส่ง `pressed = false` ปล่อยปุ่มด้วย
ไม่งั้นปุ่มค้าง คลิปถัดไปโดนข้ามทันที (อ่านผลว่า "ไม่เล่น") ★

---

## 7.89 รอบ 76 — เควส M2 ค้าง (เสาหินเปลี่ยนชื่อ) + ปุ่ม "ทำพิธี"

### 7.89.1 ทำไมทำต่อไม่ได้

เควส **M2 คำสาบานใต้ค้อน** มีเงื่อนไขชนิด "คุยด้วย" เป้าหมาย = **«ศิลาสลักแห่งธอร์»**
แต่เสาหินกลางเมือง **รอบ 57 ถูกเปลี่ยนชื่อเป็น «เสาวาปแห่งธอร์»** ตอนที่ศิลาเซฟกลายเป็นเสาวาป

→ ในเมืองไม่มีของชิ้นไหนชื่อ «ศิลาสลักแห่งธอร์» อีกแล้ว · กดคุยกี่ครั้งเควสก็ไม่ขยับ · เส้นเนื้อเรื่องหลักตันตั้งแต่ M2

### 7.89.2 แก้ยังไง

**1. ช่องใหม่ `Quest Talk Names` ในโหนด NPC** — "ชื่ออื่นที่ของชิ้นนี้รับเงื่อนไขเควสด้วย"
ใส่ `ศิลาสลักแห่งธอร์` ไว้ที่เสาวาปของพรอนเทราแล้ว → เสาต้นเดิมรับพิธีของ M2 ได้ ทั้งที่ชื่อบนหัวยังเป็น «เสาวาปแห่งธอร์»

**2. ปุ่มทำพิธีในเมนู** (ตามที่คุณเดาไว้เป๊ะ)
ชื่อจริงของ NPC ยังนับให้อัตโนมัติตอนกดคุยเหมือนเดิม แต่ **ชื่อสำรองจะไม่แอบนับ** — จะโผล่เป็นปุ่มบนสุดของเมนูแทน:

```
เสาวาปโบราณ · เลือกปลายทางแล้วก้าวเข้าไปได้เลย
  ► ทำพิธีที่ศิลาสลักแห่งธอร์      ← ปุ่มใหม่ (โผล่เฉพาะตอนมีเควสที่ต้องใช้)
    ไป ป่าแอสการ์ด 2
    บันทึกเกม
    ไม่คุย
```

- ข้อความบนปุ่ม = **ข้อความของเงื่อนไขในสมุดเควส** เป๊ะ ๆ (ไม่ต้องตั้งซ้ำ) · เควสไหนก็ใช้ได้
- ทำเสร็จแล้วปุ่มหายไปเอง · ยังไม่รับเควส = ไม่มีปุ่ม
- **มีเครื่องหมาย ! สีฟ้าลอยเหนือเสา** ตอนที่ทำพิธีได้ (คนละสีกับ ! เหลือง = รับเควส · ? เขียว = ส่งเควส) จะได้หาเจอว่าต้องไปที่ไหน
- ใช้ได้กับ NPC ทุกแบบ ไม่ใช่แค่เสาวาป

### 7.89.3 สคริปต์ตรวจ `check_quest_targets.py` (กันพลาดแบบนี้อีก)

```
python3 check_quest_targets.py
```

ไล่ทุกเควสว่าเงื่อนไขทุกข้อ **มีเป้าหมายอยู่จริงไหม**:

| ชนิด | ตรวจว่า |
|---|---|
| คุยด้วย (TALK) | มี NPC ชื่อนี้ หรือมีใครใส่ `Quest Talk Names` ไว้ |
| ฆ่ามอน (KILL) | มี `data/monsters/<id>.tres` |
| เก็บของ (COLLECT) | มี `data/items/<id>.tres` |
| ไปแมพ (VISIT) | มี `scenes/maps/<id>.tscn` |
| อ่าน/ตรวจ (READ) | มีจุดอ่าน `lore_id` นี้ในฉากไหนสักฉาก |

ตอนนี้ผลคือ **✓ ครบทุกข้อ** (ก่อนแก้จะฟ้อง m2_oath 1 ข้อ)
★ เปลี่ยนชื่อ NPC / ย้ายของในฉากเมื่อไหร่ รันตัวนี้สักที ★

### 7.89.4 เทสต์

`r76_test` **ผ่าน 20 · ล้มเหลว 0**
1. เจอเสาวาปในเมือง · รับชื่อเก่าไว้แล้ว · ยังไม่รับเควส = ไม่มีปุ่ม
2. รับ M2 → ปุ่มโผล่ ข้อความตรงสมุดเควส · เครื่องหมายเหนือเสาโชว์ · **กดคุยเฉย ๆ เควสยังไม่ขยับ**
3. กดปุ่มพิธี → M2 ครบ 1/1 พร้อมส่งให้วาลเดอร์ · ปุ่มกับเครื่องหมายหายไป
4. เมนูเสาวาปยังครบ — ปุ่มพิธีอยู่บนสุด แต่ "ไป ป่าแอสการ์ด 2" / "บันทึกเกม" ยังทำงานถูกตัว (ลำดับไม่เลื่อน)
5. NPC ทั่วไปไม่มีปุ่มพิธี เครื่องหมายเควสปกติ

ถดถอย r55 62 · r44 59 · r30 66 · r45 59 · newfeat 42 ผ่าน · แดงเดิม quest_test 15 · r38 M4 · portal 1

★ กับดัก 108: ตัวแปรชื่อ `log` ใน npc.gd ทับฟังก์ชัน `log()` ของ Godot — รอบนี้เปลี่ยนเป็น `qlog` ทั้งไฟล์แล้ว ★
